--[=[
 d888b  db    db d888888b      .d888b.      db      db    db  .d8b.  
88' Y8b 88    88   `88'        VP  `8D      88      88    88 d8' `8b 
88      88    88    88            odD'      88      88    88 88ooo88 
88  ooo 88    88    88          .88'        88      88    88 88~~~88 
88. ~8~ 88b  d88   .88.        j88.         88booo. 88b  d88 88   88    @uniquadev
 Y888P  ~Y8888P' Y888888P      888888D      Y88888P ~Y8888P' YP   YP  CONVERTER 
]=]

-- Instances: 551 | Scripts: 123 | Modules: 0 | Tags: 0
local G2L = {};

-- StarterGui.ScreenGui
G2L["1"] = Instance.new("ScreenGui", game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui"));
G2L["1"]["DisplayOrder"] = 999999999;
G2L["1"]["ZIndexBehavior"] = Enum.ZIndexBehavior.Sibling;
G2L["1"]["ResetOnSpawn"] = false;


-- StarterGui.ScreenGui.MoveToCorGui
G2L["2"] = Instance.new("LocalScript", G2L["1"]);
G2L["2"]["Name"] = [[MoveToCorGui]];


-- StarterGui.ScreenGui.ChatQueue
G2L["3"] = Instance.new("LocalScript", G2L["1"]);
G2L["3"]["Name"] = [[ChatQueue]];


-- StarterGui.ScreenGui.WelcomeMessage
G2L["4"] = Instance.new("LocalScript", G2L["1"]);
G2L["4"]["Name"] = [[WelcomeMessage]];


-- StarterGui.ScreenGui.UnloadBLACKLIST
G2L["5"] = Instance.new("LocalScript", G2L["1"]);
G2L["5"]["Name"] = [[UnloadBLACKLIST]];


-- StarterGui.ScreenGui.AGARVOWNERLABEL
G2L["6"] = Instance.new("LocalScript", G2L["1"]);
G2L["6"]["Name"] = [[AGARVOWNERLABEL]];


-- StarterGui.ScreenGui.GriefDetectorConsole
G2L["7"] = Instance.new("LocalScript", G2L["1"]);
G2L["7"]["Name"] = [[GriefDetectorConsole]];


-- StarterGui.ScreenGui.MainFrame
G2L["8"] = Instance.new("Frame", G2L["1"]);
G2L["8"]["Active"] = true;
G2L["8"]["ZIndex"] = 10;
G2L["8"]["BackgroundColor3"] = Color3.fromRGB(23, 23, 23);
G2L["8"]["Selectable"] = true;
G2L["8"]["Size"] = UDim2.new(0, 450, 0, 300);
G2L["8"]["Position"] = UDim2.new(0.07755, -60, 0.35676, 78);
G2L["8"]["BorderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["8"]["Name"] = [[MainFrame]];


-- StarterGui.ScreenGui.MainFrame.UIPadding
G2L["9"] = Instance.new("UIPadding", G2L["8"]);
G2L["9"]["PaddingLeft"] = UDim.new(0, 8);


-- StarterGui.ScreenGui.MainFrame.SideBar
G2L["a"] = Instance.new("ScrollingFrame", G2L["8"]);
G2L["a"]["ScrollingDirection"] = Enum.ScrollingDirection.Y;
G2L["a"]["CanvasSize"] = UDim2.new(0, 0, 1, 0);
G2L["a"]["ScrollingEnabled"] = false;
G2L["a"]["Name"] = [[SideBar]];
G2L["a"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a"]["AutomaticCanvasSize"] = Enum.AutomaticSize.Y;
G2L["a"]["Size"] = UDim2.new(0, 60, 0, 266);
G2L["a"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a"]["Position"] = UDim2.new(0, 0, 0.08667, 0);
G2L["a"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["a"]["ScrollBarThickness"] = 0;


-- StarterGui.ScreenGui.MainFrame.SideBar.PageHandler
G2L["b"] = Instance.new("LocalScript", G2L["a"]);
G2L["b"]["Name"] = [[PageHandler]];


-- StarterGui.ScreenGui.MainFrame.SideBar.UIListLayout
G2L["c"] = Instance.new("UIListLayout", G2L["a"]);
G2L["c"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.SideBar.UIPadding
G2L["d"] = Instance.new("UIPadding", G2L["a"]);



-- StarterGui.ScreenGui.MainFrame.SideBar.MainPG
G2L["e"] = Instance.new("TextButton", G2L["a"]);
G2L["e"]["TextWrapped"] = true;
G2L["e"]["TextSize"] = 12;
G2L["e"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["e"]["Size"] = UDim2.new(0, 60, 0, 25);
G2L["e"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["e"]["Text"] = [[Main]];
G2L["e"]["Name"] = [[MainPG]];


-- StarterGui.ScreenGui.MainFrame.SideBar.SettingsPG
G2L["f"] = Instance.new("TextButton", G2L["a"]);
G2L["f"]["TextWrapped"] = true;
G2L["f"]["TextSize"] = 12;
G2L["f"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["f"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["f"]["Size"] = UDim2.new(0, 60, 0, 25);
G2L["f"]["LayoutOrder"] = 1;
G2L["f"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["f"]["Text"] = [[Settings]];
G2L["f"]["Name"] = [[SettingsPG]];


-- StarterGui.ScreenGui.MainFrame.SideBar.VisualPG
G2L["10"] = Instance.new("TextButton", G2L["a"]);
G2L["10"]["TextSize"] = 12;
G2L["10"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["10"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["10"]["Size"] = UDim2.new(0, 60, 0, 25);
G2L["10"]["LayoutOrder"] = 5;
G2L["10"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["10"]["Text"] = [[Visual]];
G2L["10"]["Name"] = [[VisualPG]];


-- StarterGui.ScreenGui.MainFrame.SideBar.TCOPG
G2L["11"] = Instance.new("TextButton", G2L["a"]);
G2L["11"]["TextWrapped"] = true;
G2L["11"]["TextSize"] = 12;
G2L["11"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["11"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["11"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["11"]["Size"] = UDim2.new(0, 60, 0, 25);
G2L["11"]["LayoutOrder"] = 1;
G2L["11"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["11"]["Text"] = [[TCO]];
G2L["11"]["Name"] = [[TCOPG]];


-- StarterGui.ScreenGui.MainFrame.SideBar.TCOPG.TCOIngame
G2L["12"] = Instance.new("LocalScript", G2L["11"]);
G2L["12"]["Name"] = [[TCOIngame]];


-- StarterGui.ScreenGui.MainFrame.SideBar.MiscPG
G2L["13"] = Instance.new("TextButton", G2L["a"]);
G2L["13"]["TextSize"] = 12;
G2L["13"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["13"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["13"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["13"]["Size"] = UDim2.new(0, 60, 0, 25);
G2L["13"]["LayoutOrder"] = 5;
G2L["13"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["13"]["Text"] = [[Misc]];
G2L["13"]["Name"] = [[MiscPG]];


-- StarterGui.ScreenGui.MainFrame.ContentArea
G2L["14"] = Instance.new("Frame", G2L["8"]);
G2L["14"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["14"]["Size"] = UDim2.new(0, 365, 0, 266);
G2L["14"]["Position"] = UDim2.new(0.15385, 0, 0.08667, 0);
G2L["14"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["14"]["Name"] = [[ContentArea]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage
G2L["15"] = Instance.new("ScrollingFrame", G2L["14"]);
G2L["15"]["Visible"] = false;
G2L["15"]["Active"] = true;
G2L["15"]["BorderSizePixel"] = 0;
G2L["15"]["CanvasSize"] = UDim2.new(0, 0, 1.5, 0);
G2L["15"]["Name"] = [[MainPage]];
G2L["15"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["15"]["Size"] = UDim2.new(0, 349, 0, 250);
G2L["15"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["15"]["Position"] = UDim2.new(0.02329, 0, 0.03008, 0);
G2L["15"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["15"]["ScrollBarThickness"] = 0;
G2L["15"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide
G2L["16"] = Instance.new("Frame", G2L["15"]);
G2L["16"]["BorderSizePixel"] = 0;
G2L["16"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["16"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["16"]["Position"] = UDim2.new(0, 0, 0.00439, 0);
G2L["16"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["16"]["Name"] = [[MainLeftSide]];
G2L["16"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.UIListLayout
G2L["17"] = Instance.new("UIListLayout", G2L["16"]);
G2L["17"]["Padding"] = UDim.new(0, 7);
G2L["17"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup
G2L["18"] = Instance.new("Frame", G2L["16"]);
G2L["18"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["18"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["18"]["Size"] = UDim2.new(0, 170, 0, 120);
G2L["18"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["18"]["Name"] = [[MovementGroup]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.MovementScript
G2L["19"] = Instance.new("LocalScript", G2L["18"]);
G2L["19"]["Name"] = [[MovementScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.UIListLayout
G2L["1a"] = Instance.new("UIListLayout", G2L["18"]);
G2L["1a"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.MovementAccentColor
G2L["1b"] = Instance.new("Frame", G2L["18"]);
G2L["1b"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1b"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["1b"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["1b"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["1b"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["1b"]["Name"] = [[MovementAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.MovementTitle
G2L["1c"] = Instance.new("TextLabel", G2L["18"]);
G2L["1c"]["TextWrapped"] = true;
G2L["1c"]["BorderSizePixel"] = 0;
G2L["1c"]["TextSize"] = 14;
G2L["1c"]["TextScaled"] = true;
G2L["1c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1c"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["1c"]["BackgroundTransparency"] = 0.95;
G2L["1c"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["1c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1c"]["Text"] = [[Movement]];
G2L["1c"]["LayoutOrder"] = 1;
G2L["1c"]["Name"] = [[MovementTitle]];
G2L["1c"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.MovementTitle.MovementMinimize
G2L["1d"] = Instance.new("TextButton", G2L["1c"]);
G2L["1d"]["BorderSizePixel"] = 0;
G2L["1d"]["TextSize"] = 14;
G2L["1d"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1d"]["BackgroundTransparency"] = 1;
G2L["1d"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["1d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1d"]["Text"] = [[-]];
G2L["1d"]["Name"] = [[MovementMinimize]];
G2L["1d"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.WalkspeedRow
G2L["1e"] = Instance.new("Frame", G2L["18"]);
G2L["1e"]["BorderSizePixel"] = 0;
G2L["1e"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1e"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1e"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1e"]["Name"] = [[WalkspeedRow]];
G2L["1e"]["LayoutOrder"] = 3;
G2L["1e"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.WalkspeedRow.WalkspeedScript
G2L["1f"] = Instance.new("LocalScript", G2L["1e"]);
G2L["1f"]["Name"] = [[WalkspeedScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.WalkspeedRow.WalkspeedTickBox
G2L["20"] = Instance.new("TextButton", G2L["1e"]);
G2L["20"]["TextSize"] = 14;
G2L["20"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["20"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["20"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["20"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["20"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["20"]["Text"] = [[]];
G2L["20"]["Name"] = [[WalkspeedTickBox]];
G2L["20"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.WalkspeedRow.WalkspeedText
G2L["21"] = Instance.new("TextLabel", G2L["1e"]);
G2L["21"]["BorderSizePixel"] = 0;
G2L["21"]["TextSize"] = 14;
G2L["21"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["21"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["21"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["21"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["21"]["BackgroundTransparency"] = 1;
G2L["21"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["21"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["21"]["Text"] = [[Walk Speed]];
G2L["21"]["Name"] = [[WalkspeedText]];
G2L["21"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.WalkspeedRow.WalkspeedSpeedChanger
G2L["22"] = Instance.new("TextBox", G2L["1e"]);
G2L["22"]["Name"] = [[WalkspeedSpeedChanger]];
G2L["22"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["22"]["TextWrapped"] = true;
G2L["22"]["TextSize"] = 14;
G2L["22"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["22"]["TextScaled"] = true;
G2L["22"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["22"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["22"]["PlaceholderText"] = [[(16)]];
G2L["22"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["22"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["22"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["22"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.JumppowerRow
G2L["23"] = Instance.new("Frame", G2L["18"]);
G2L["23"]["BorderSizePixel"] = 0;
G2L["23"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["23"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["23"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["23"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["23"]["Name"] = [[JumppowerRow]];
G2L["23"]["LayoutOrder"] = 3;
G2L["23"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.JumppowerRow.JumppowerScript
G2L["24"] = Instance.new("LocalScript", G2L["23"]);
G2L["24"]["Name"] = [[JumppowerScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.JumppowerRow.JumppowerTickBox
G2L["25"] = Instance.new("TextButton", G2L["23"]);
G2L["25"]["TextSize"] = 14;
G2L["25"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["25"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["25"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["25"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["25"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["25"]["Text"] = [[]];
G2L["25"]["Name"] = [[JumppowerTickBox]];
G2L["25"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.JumppowerRow.JumppowerText
G2L["26"] = Instance.new("TextLabel", G2L["23"]);
G2L["26"]["BorderSizePixel"] = 0;
G2L["26"]["TextSize"] = 14;
G2L["26"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["26"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["26"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["26"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["26"]["BackgroundTransparency"] = 1;
G2L["26"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["26"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["26"]["Text"] = [[Jump Power]];
G2L["26"]["Name"] = [[JumppowerText]];
G2L["26"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.JumppowerRow.JumppowerJumpChanger
G2L["27"] = Instance.new("TextBox", G2L["23"]);
G2L["27"]["Name"] = [[JumppowerJumpChanger]];
G2L["27"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["27"]["TextWrapped"] = true;
G2L["27"]["TextSize"] = 14;
G2L["27"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["27"]["TextScaled"] = true;
G2L["27"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["27"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["27"]["PlaceholderText"] = [[(50)]];
G2L["27"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["27"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["27"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["27"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.FlyRow
G2L["28"] = Instance.new("Frame", G2L["18"]);
G2L["28"]["BorderSizePixel"] = 0;
G2L["28"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["28"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["28"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["28"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["28"]["Name"] = [[FlyRow]];
G2L["28"]["LayoutOrder"] = 3;
G2L["28"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.FlyRow.FlyScript
G2L["29"] = Instance.new("LocalScript", G2L["28"]);
G2L["29"]["Name"] = [[FlyScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.FlyRow.FlyTickBox
G2L["2a"] = Instance.new("TextButton", G2L["28"]);
G2L["2a"]["TextSize"] = 14;
G2L["2a"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["2a"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["2a"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["2a"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["2a"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["2a"]["Text"] = [[]];
G2L["2a"]["Name"] = [[FlyTickBox]];
G2L["2a"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.FlyRow.FlyText
G2L["2b"] = Instance.new("TextLabel", G2L["28"]);
G2L["2b"]["BorderSizePixel"] = 0;
G2L["2b"]["TextSize"] = 14;
G2L["2b"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["2b"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["2b"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["2b"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["2b"]["BackgroundTransparency"] = 1;
G2L["2b"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["2b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["2b"]["Text"] = [[Fly]];
G2L["2b"]["Name"] = [[FlyText]];
G2L["2b"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.FlyRow.FlySpeedChanger
G2L["2c"] = Instance.new("TextBox", G2L["28"]);
G2L["2c"]["Name"] = [[FlySpeedChanger]];
G2L["2c"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["2c"]["TextWrapped"] = true;
G2L["2c"]["TextSize"] = 14;
G2L["2c"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["2c"]["TextScaled"] = true;
G2L["2c"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["2c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["2c"]["PlaceholderText"] = [[(50)]];
G2L["2c"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["2c"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["2c"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["2c"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.InfJumpRow
G2L["2d"] = Instance.new("Frame", G2L["18"]);
G2L["2d"]["BorderSizePixel"] = 0;
G2L["2d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["2d"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["2d"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["2d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["2d"]["Name"] = [[InfJumpRow]];
G2L["2d"]["LayoutOrder"] = 3;
G2L["2d"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.InfJumpRow.InfJumpScript
G2L["2e"] = Instance.new("LocalScript", G2L["2d"]);
G2L["2e"]["Name"] = [[InfJumpScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.InfJumpRow.InfJumpTickBox
G2L["2f"] = Instance.new("TextButton", G2L["2d"]);
G2L["2f"]["TextSize"] = 14;
G2L["2f"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["2f"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["2f"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["2f"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["2f"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["2f"]["Text"] = [[]];
G2L["2f"]["Name"] = [[InfJumpTickBox]];
G2L["2f"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.InfJumpRow.InfJumpText
G2L["30"] = Instance.new("TextLabel", G2L["2d"]);
G2L["30"]["BorderSizePixel"] = 0;
G2L["30"]["TextSize"] = 14;
G2L["30"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["30"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["30"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["30"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["30"]["BackgroundTransparency"] = 1;
G2L["30"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["30"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["30"]["Text"] = [[Infinite Jump]];
G2L["30"]["Name"] = [[InfJumpText]];
G2L["30"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.ShiftLockRow
G2L["31"] = Instance.new("Frame", G2L["18"]);
G2L["31"]["BorderSizePixel"] = 0;
G2L["31"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["31"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["31"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["31"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["31"]["Name"] = [[ShiftLockRow]];
G2L["31"]["LayoutOrder"] = 3;
G2L["31"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.ShiftLockRow.ShiftLockScript
G2L["32"] = Instance.new("LocalScript", G2L["31"]);
G2L["32"]["Name"] = [[ShiftLockScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.ShiftLockRow.ShiftLockTickBox
G2L["33"] = Instance.new("TextButton", G2L["31"]);
G2L["33"]["TextSize"] = 14;
G2L["33"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["33"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["33"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["33"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["33"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["33"]["Text"] = [[]];
G2L["33"]["Name"] = [[ShiftLockTickBox]];
G2L["33"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.ShiftLockRow.ShiftLockText
G2L["34"] = Instance.new("TextLabel", G2L["31"]);
G2L["34"]["BorderSizePixel"] = 0;
G2L["34"]["TextSize"] = 14;
G2L["34"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["34"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["34"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["34"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["34"]["BackgroundTransparency"] = 1;
G2L["34"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["34"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["34"]["Text"] = [[Mobile Shiftlock]];
G2L["34"]["Name"] = [[ShiftLockText]];
G2L["34"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup
G2L["35"] = Instance.new("Frame", G2L["16"]);
G2L["35"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["35"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["35"]["Size"] = UDim2.new(0, 170, 0, 85);
G2L["35"]["Position"] = UDim2.new(-0.00588, 0, 0.71111, 0);
G2L["35"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["35"]["Name"] = [[MiscGroup]];
G2L["35"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.MiscScript
G2L["36"] = Instance.new("LocalScript", G2L["35"]);
G2L["36"]["Name"] = [[MiscScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.UIListLayout
G2L["37"] = Instance.new("UIListLayout", G2L["35"]);
G2L["37"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.MiscAccentColor
G2L["38"] = Instance.new("Frame", G2L["35"]);
G2L["38"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["38"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["38"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["38"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["38"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["38"]["Name"] = [[MiscAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.MiscTitle
G2L["39"] = Instance.new("TextLabel", G2L["35"]);
G2L["39"]["TextWrapped"] = true;
G2L["39"]["BorderSizePixel"] = 0;
G2L["39"]["TextSize"] = 14;
G2L["39"]["TextScaled"] = true;
G2L["39"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["39"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["39"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["39"]["BackgroundTransparency"] = 0.95;
G2L["39"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["39"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["39"]["Text"] = [[Misc]];
G2L["39"]["LayoutOrder"] = 1;
G2L["39"]["Name"] = [[MiscTitle]];
G2L["39"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.MiscTitle.MiscMinimize
G2L["3a"] = Instance.new("TextButton", G2L["39"]);
G2L["3a"]["BorderSizePixel"] = 0;
G2L["3a"]["TextSize"] = 14;
G2L["3a"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["3a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["3a"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["3a"]["BackgroundTransparency"] = 1;
G2L["3a"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["3a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["3a"]["Text"] = [[-]];
G2L["3a"]["Name"] = [[MiscMinimize]];
G2L["3a"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.RejoinRow
G2L["3b"] = Instance.new("Frame", G2L["35"]);
G2L["3b"]["BorderSizePixel"] = 0;
G2L["3b"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["3b"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["3b"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["3b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["3b"]["Name"] = [[RejoinRow]];
G2L["3b"]["LayoutOrder"] = 10;
G2L["3b"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.RejoinRow.RejoinScript
G2L["3c"] = Instance.new("LocalScript", G2L["3b"]);
G2L["3c"]["Name"] = [[RejoinScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.RejoinRow.RejoinButton
G2L["3d"] = Instance.new("TextButton", G2L["3b"]);
G2L["3d"]["TextSize"] = 14;
G2L["3d"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["3d"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["3d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["3d"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["3d"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["3d"]["Text"] = [[Rejoin Server]];
G2L["3d"]["Name"] = [[RejoinButton]];
G2L["3d"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.GravityRow
G2L["3e"] = Instance.new("Frame", G2L["35"]);
G2L["3e"]["BorderSizePixel"] = 0;
G2L["3e"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["3e"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["3e"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["3e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["3e"]["Name"] = [[GravityRow]];
G2L["3e"]["LayoutOrder"] = 2;
G2L["3e"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.GravityRow.GravityScript
G2L["3f"] = Instance.new("LocalScript", G2L["3e"]);
G2L["3f"]["Name"] = [[GravityScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.GravityRow.GravityTickBox
G2L["40"] = Instance.new("TextButton", G2L["3e"]);
G2L["40"]["TextSize"] = 14;
G2L["40"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["40"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["40"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["40"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["40"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["40"]["Text"] = [[]];
G2L["40"]["Name"] = [[GravityTickBox]];
G2L["40"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.GravityRow.GravityText
G2L["41"] = Instance.new("TextLabel", G2L["3e"]);
G2L["41"]["BorderSizePixel"] = 0;
G2L["41"]["TextSize"] = 14;
G2L["41"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["41"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["41"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["41"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["41"]["BackgroundTransparency"] = 1;
G2L["41"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["41"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["41"]["Text"] = [[Set Gravity]];
G2L["41"]["Name"] = [[GravityText]];
G2L["41"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.GravityRow.GravityChanger
G2L["42"] = Instance.new("TextBox", G2L["3e"]);
G2L["42"]["Name"] = [[GravityChanger]];
G2L["42"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["42"]["TextWrapped"] = true;
G2L["42"]["TextSize"] = 14;
G2L["42"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["42"]["TextScaled"] = true;
G2L["42"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["42"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["42"]["PlaceholderText"] = [[(196.2)]];
G2L["42"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["42"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["42"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["42"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.BoostFPSRow
G2L["43"] = Instance.new("Frame", G2L["35"]);
G2L["43"]["BorderSizePixel"] = 0;
G2L["43"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["43"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["43"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["43"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["43"]["Name"] = [[BoostFPSRow]];
G2L["43"]["LayoutOrder"] = 2;
G2L["43"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.BoostFPSRow.BoostFPSScript
G2L["44"] = Instance.new("LocalScript", G2L["43"]);
G2L["44"]["Name"] = [[BoostFPSScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.BoostFPSRow.BoostFPSTickBox
G2L["45"] = Instance.new("TextButton", G2L["43"]);
G2L["45"]["TextSize"] = 14;
G2L["45"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["45"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["45"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["45"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["45"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["45"]["Text"] = [[]];
G2L["45"]["Name"] = [[BoostFPSTickBox]];
G2L["45"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.BoostFPSRow.BoostFPSText
G2L["46"] = Instance.new("TextLabel", G2L["43"]);
G2L["46"]["BorderSizePixel"] = 0;
G2L["46"]["TextSize"] = 14;
G2L["46"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["46"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["46"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["46"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["46"]["BackgroundTransparency"] = 1;
G2L["46"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["46"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["46"]["Text"] = [[Boost FPS]];
G2L["46"]["Name"] = [[BoostFPSText]];
G2L["46"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup
G2L["47"] = Instance.new("Frame", G2L["16"]);
G2L["47"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["47"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["47"]["Size"] = UDim2.new(0, 170, 0, 45);
G2L["47"]["Position"] = UDim2.new(0, 0, 0.35278, 0);
G2L["47"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["47"]["Name"] = [[TrollGroup]];
G2L["47"]["LayoutOrder"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TrollScript
G2L["48"] = Instance.new("LocalScript", G2L["47"]);
G2L["48"]["Name"] = [[TrollScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.UIListLayout
G2L["49"] = Instance.new("UIListLayout", G2L["47"]);
G2L["49"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TrollAccentColor
G2L["4a"] = Instance.new("Frame", G2L["47"]);
G2L["4a"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["4a"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["4a"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["4a"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["4a"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["4a"]["Name"] = [[TrollAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TrollTitle
G2L["4b"] = Instance.new("TextLabel", G2L["47"]);
G2L["4b"]["TextWrapped"] = true;
G2L["4b"]["BorderSizePixel"] = 0;
G2L["4b"]["TextSize"] = 14;
G2L["4b"]["TextScaled"] = true;
G2L["4b"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["4b"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["4b"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["4b"]["BackgroundTransparency"] = 0.95;
G2L["4b"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["4b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["4b"]["Text"] = [[Troll]];
G2L["4b"]["LayoutOrder"] = 1;
G2L["4b"]["Name"] = [[TrollTitle]];
G2L["4b"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TrollTitle.TrollMinimize
G2L["4c"] = Instance.new("TextButton", G2L["4b"]);
G2L["4c"]["BorderSizePixel"] = 0;
G2L["4c"]["TextSize"] = 14;
G2L["4c"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["4c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["4c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["4c"]["BackgroundTransparency"] = 1;
G2L["4c"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["4c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["4c"]["Text"] = [[-]];
G2L["4c"]["Name"] = [[TrollMinimize]];
G2L["4c"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TouchFlingRow
G2L["4d"] = Instance.new("Frame", G2L["47"]);
G2L["4d"]["BorderSizePixel"] = 0;
G2L["4d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["4d"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["4d"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["4d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["4d"]["Name"] = [[TouchFlingRow]];
G2L["4d"]["LayoutOrder"] = 3;
G2L["4d"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TouchFlingRow.TouchFlingScript
G2L["4e"] = Instance.new("LocalScript", G2L["4d"]);
G2L["4e"]["Name"] = [[TouchFlingScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TouchFlingRow.TouchFlingTickBox
G2L["4f"] = Instance.new("TextButton", G2L["4d"]);
G2L["4f"]["TextSize"] = 14;
G2L["4f"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["4f"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["4f"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["4f"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["4f"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["4f"]["Text"] = [[]];
G2L["4f"]["Name"] = [[TouchFlingTickBox]];
G2L["4f"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TouchFlingRow.TouchFlingText
G2L["50"] = Instance.new("TextLabel", G2L["4d"]);
G2L["50"]["BorderSizePixel"] = 0;
G2L["50"]["TextSize"] = 14;
G2L["50"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["50"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["50"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["50"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["50"]["BackgroundTransparency"] = 1;
G2L["50"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["50"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["50"]["Text"] = [[Touch Fling]];
G2L["50"]["Name"] = [[TouchFlingText]];
G2L["50"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide
G2L["51"] = Instance.new("Frame", G2L["15"]);
G2L["51"]["BorderSizePixel"] = 0;
G2L["51"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["51"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["51"]["Position"] = UDim2.new(0.51003, 0, 0.00439, 0);
G2L["51"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["51"]["Name"] = [[MainRightSide]];
G2L["51"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.UIListLayout
G2L["52"] = Instance.new("UIListLayout", G2L["51"]);
G2L["52"]["Padding"] = UDim.new(0, 7);
G2L["52"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup
G2L["53"] = Instance.new("Frame", G2L["51"]);
G2L["53"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["53"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["53"]["Size"] = UDim2.new(0, 170, 0, 147);
G2L["53"]["Position"] = UDim2.new(0.01176, 0, 0.40278, 0);
G2L["53"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["53"]["Name"] = [[AntiGroup]];
G2L["53"]["LayoutOrder"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiScript
G2L["54"] = Instance.new("LocalScript", G2L["53"]);
G2L["54"]["Name"] = [[AntiScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.UIListLayout
G2L["55"] = Instance.new("UIListLayout", G2L["53"]);
G2L["55"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiAccentColor
G2L["56"] = Instance.new("Frame", G2L["53"]);
G2L["56"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["56"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["56"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["56"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["56"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["56"]["Name"] = [[AntiAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiTitle
G2L["57"] = Instance.new("TextLabel", G2L["53"]);
G2L["57"]["TextWrapped"] = true;
G2L["57"]["BorderSizePixel"] = 0;
G2L["57"]["TextSize"] = 14;
G2L["57"]["TextScaled"] = true;
G2L["57"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["57"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["57"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["57"]["BackgroundTransparency"] = 0.95;
G2L["57"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["57"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["57"]["Text"] = [[Anti]];
G2L["57"]["LayoutOrder"] = 1;
G2L["57"]["Name"] = [[AntiTitle]];
G2L["57"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiTitle.AntiMinimize
G2L["58"] = Instance.new("TextButton", G2L["57"]);
G2L["58"]["BorderSizePixel"] = 0;
G2L["58"]["TextSize"] = 14;
G2L["58"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["58"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["58"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["58"]["BackgroundTransparency"] = 1;
G2L["58"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["58"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["58"]["Text"] = [[-]];
G2L["58"]["Name"] = [[AntiMinimize]];
G2L["58"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiFlingRow
G2L["59"] = Instance.new("Frame", G2L["53"]);
G2L["59"]["BorderSizePixel"] = 0;
G2L["59"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["59"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["59"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["59"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["59"]["Name"] = [[AntiFlingRow]];
G2L["59"]["LayoutOrder"] = 3;
G2L["59"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiFlingRow.AntiFlingScript
G2L["5a"] = Instance.new("LocalScript", G2L["59"]);
G2L["5a"]["Name"] = [[AntiFlingScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiFlingRow.AntiFlingTickBox
G2L["5b"] = Instance.new("TextButton", G2L["59"]);
G2L["5b"]["TextSize"] = 14;
G2L["5b"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5b"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5b"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["5b"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["5b"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["5b"]["Text"] = [[]];
G2L["5b"]["Name"] = [[AntiFlingTickBox]];
G2L["5b"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiFlingRow.AntiFlingText
G2L["5c"] = Instance.new("TextLabel", G2L["59"]);
G2L["5c"]["BorderSizePixel"] = 0;
G2L["5c"]["TextSize"] = 14;
G2L["5c"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["5c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["5c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["5c"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["5c"]["BackgroundTransparency"] = 1;
G2L["5c"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["5c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5c"]["Text"] = [[Anti Fling]];
G2L["5c"]["Name"] = [[AntiFlingText]];
G2L["5c"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiAfkRow
G2L["5d"] = Instance.new("Frame", G2L["53"]);
G2L["5d"]["BorderSizePixel"] = 0;
G2L["5d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["5d"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["5d"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["5d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5d"]["Name"] = [[AntiAfkRow]];
G2L["5d"]["LayoutOrder"] = 3;
G2L["5d"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiAfkRow.AntiAfkScript
G2L["5e"] = Instance.new("LocalScript", G2L["5d"]);
G2L["5e"]["Name"] = [[AntiAfkScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiAfkRow.AntiAfkTickBox
G2L["5f"] = Instance.new("TextButton", G2L["5d"]);
G2L["5f"]["TextSize"] = 14;
G2L["5f"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5f"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5f"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["5f"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["5f"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["5f"]["Text"] = [[]];
G2L["5f"]["Name"] = [[AntiAfkTickBox]];
G2L["5f"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiAfkRow.AntiAfkText
G2L["60"] = Instance.new("TextLabel", G2L["5d"]);
G2L["60"]["BorderSizePixel"] = 0;
G2L["60"]["TextSize"] = 14;
G2L["60"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["60"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["60"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["60"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["60"]["BackgroundTransparency"] = 1;
G2L["60"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["60"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["60"]["Text"] = [[Anti AFK]];
G2L["60"]["Name"] = [[AntiAfkText]];
G2L["60"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiRagdollRow
G2L["61"] = Instance.new("Frame", G2L["53"]);
G2L["61"]["BorderSizePixel"] = 0;
G2L["61"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["61"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["61"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["61"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["61"]["Name"] = [[AntiRagdollRow]];
G2L["61"]["LayoutOrder"] = 3;
G2L["61"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiRagdollRow.AntiRagdollScript
G2L["62"] = Instance.new("LocalScript", G2L["61"]);
G2L["62"]["Name"] = [[AntiRagdollScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiRagdollRow.AntiRagdollTickBox
G2L["63"] = Instance.new("TextButton", G2L["61"]);
G2L["63"]["TextSize"] = 14;
G2L["63"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["63"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["63"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["63"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["63"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["63"]["Text"] = [[]];
G2L["63"]["Name"] = [[AntiRagdollTickBox]];
G2L["63"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiRagdollRow.AntiRagdollText
G2L["64"] = Instance.new("TextLabel", G2L["61"]);
G2L["64"]["BorderSizePixel"] = 0;
G2L["64"]["TextSize"] = 14;
G2L["64"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["64"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["64"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["64"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["64"]["BackgroundTransparency"] = 1;
G2L["64"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["64"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["64"]["Text"] = [[Anti Ragdoll]];
G2L["64"]["Name"] = [[AntiRagdollText]];
G2L["64"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiVoidRow
G2L["65"] = Instance.new("Frame", G2L["53"]);
G2L["65"]["BorderSizePixel"] = 0;
G2L["65"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["65"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["65"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["65"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["65"]["Name"] = [[AntiVoidRow]];
G2L["65"]["LayoutOrder"] = 3;
G2L["65"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiVoidRow.AntiVoidScript
G2L["66"] = Instance.new("LocalScript", G2L["65"]);
G2L["66"]["Name"] = [[AntiVoidScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiVoidRow.AntiVoidTickBox
G2L["67"] = Instance.new("TextButton", G2L["65"]);
G2L["67"]["TextSize"] = 14;
G2L["67"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["67"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["67"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["67"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["67"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["67"]["Text"] = [[]];
G2L["67"]["Name"] = [[AntiVoidTickBox]];
G2L["67"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiVoidRow.AntiVoidText
G2L["68"] = Instance.new("TextLabel", G2L["65"]);
G2L["68"]["BorderSizePixel"] = 0;
G2L["68"]["TextSize"] = 14;
G2L["68"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["68"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["68"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["68"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["68"]["BackgroundTransparency"] = 1;
G2L["68"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["68"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["68"]["Text"] = [[Anti Void]];
G2L["68"]["Name"] = [[AntiVoidText]];
G2L["68"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKBRow
G2L["69"] = Instance.new("Frame", G2L["53"]);
G2L["69"]["BorderSizePixel"] = 0;
G2L["69"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["69"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["69"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["69"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["69"]["Name"] = [[AntiKBRow]];
G2L["69"]["LayoutOrder"] = 3;
G2L["69"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKBRow.AntiKBScript
G2L["6a"] = Instance.new("LocalScript", G2L["69"]);
G2L["6a"]["Name"] = [[AntiKBScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKBRow.AntiKBTickBox
G2L["6b"] = Instance.new("TextButton", G2L["69"]);
G2L["6b"]["TextSize"] = 14;
G2L["6b"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6b"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6b"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["6b"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["6b"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["6b"]["Text"] = [[]];
G2L["6b"]["Name"] = [[AntiKBTickBox]];
G2L["6b"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKBRow.AntiKBText
G2L["6c"] = Instance.new("TextLabel", G2L["69"]);
G2L["6c"]["BorderSizePixel"] = 0;
G2L["6c"]["TextSize"] = 14;
G2L["6c"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["6c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["6c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["6c"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["6c"]["BackgroundTransparency"] = 1;
G2L["6c"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["6c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6c"]["Text"] = [[Anti Knockback]];
G2L["6c"]["Name"] = [[AntiKBText]];
G2L["6c"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKickRow
G2L["6d"] = Instance.new("Frame", G2L["53"]);
G2L["6d"]["BorderSizePixel"] = 0;
G2L["6d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["6d"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["6d"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["6d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6d"]["Name"] = [[AntiKickRow]];
G2L["6d"]["LayoutOrder"] = 2;
G2L["6d"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKickRow.AntiKickScript
G2L["6e"] = Instance.new("LocalScript", G2L["6d"]);
G2L["6e"]["Name"] = [[AntiKickScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKickRow.AntiKickTickBox
G2L["6f"] = Instance.new("TextButton", G2L["6d"]);
G2L["6f"]["TextSize"] = 14;
G2L["6f"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6f"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6f"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["6f"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["6f"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["6f"]["Text"] = [[]];
G2L["6f"]["Name"] = [[AntiKickTickBox]];
G2L["6f"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKickRow.AntiKickText
G2L["70"] = Instance.new("TextLabel", G2L["6d"]);
G2L["70"]["BorderSizePixel"] = 0;
G2L["70"]["TextSize"] = 14;
G2L["70"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["70"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["70"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["70"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["70"]["BackgroundTransparency"] = 1;
G2L["70"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["70"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["70"]["Text"] = [[Anti Kick (Client)]];
G2L["70"]["Name"] = [[AntiKickText]];
G2L["70"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup
G2L["71"] = Instance.new("Frame", G2L["51"]);
G2L["71"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["71"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["71"]["Size"] = UDim2.new(0, 170, 0, 164);
G2L["71"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["71"]["Name"] = [[CharacterGroup]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.CharacterScript
G2L["72"] = Instance.new("LocalScript", G2L["71"]);
G2L["72"]["Name"] = [[CharacterScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.UIListLayout
G2L["73"] = Instance.new("UIListLayout", G2L["71"]);
G2L["73"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.CharacterAccentColor
G2L["74"] = Instance.new("Frame", G2L["71"]);
G2L["74"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["74"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["74"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["74"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["74"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["74"]["Name"] = [[CharacterAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.CharacterTitle
G2L["75"] = Instance.new("TextLabel", G2L["71"]);
G2L["75"]["TextWrapped"] = true;
G2L["75"]["BorderSizePixel"] = 0;
G2L["75"]["TextSize"] = 14;
G2L["75"]["TextScaled"] = true;
G2L["75"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["75"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["75"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["75"]["BackgroundTransparency"] = 0.95;
G2L["75"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["75"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["75"]["Text"] = [[Character]];
G2L["75"]["LayoutOrder"] = 1;
G2L["75"]["Name"] = [[CharacterTitle]];
G2L["75"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.CharacterTitle.CharacterMinimize
G2L["76"] = Instance.new("TextButton", G2L["75"]);
G2L["76"]["BorderSizePixel"] = 0;
G2L["76"]["TextSize"] = 14;
G2L["76"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["76"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["76"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["76"]["BackgroundTransparency"] = 1;
G2L["76"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["76"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["76"]["Text"] = [[-]];
G2L["76"]["Name"] = [[CharacterMinimize]];
G2L["76"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.NoclipRow
G2L["77"] = Instance.new("Frame", G2L["71"]);
G2L["77"]["BorderSizePixel"] = 0;
G2L["77"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["77"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["77"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["77"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["77"]["Name"] = [[NoclipRow]];
G2L["77"]["LayoutOrder"] = 3;
G2L["77"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.NoclipRow.NoclipScript
G2L["78"] = Instance.new("LocalScript", G2L["77"]);
G2L["78"]["Name"] = [[NoclipScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.NoclipRow.NoclipTickBox
G2L["79"] = Instance.new("TextButton", G2L["77"]);
G2L["79"]["TextSize"] = 14;
G2L["79"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["79"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["79"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["79"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["79"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["79"]["Text"] = [[]];
G2L["79"]["Name"] = [[NoclipTickBox]];
G2L["79"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.NoclipRow.NoclipText
G2L["7a"] = Instance.new("TextLabel", G2L["77"]);
G2L["7a"]["BorderSizePixel"] = 0;
G2L["7a"]["TextSize"] = 14;
G2L["7a"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["7a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7a"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["7a"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7a"]["BackgroundTransparency"] = 1;
G2L["7a"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["7a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7a"]["Text"] = [[Noclip]];
G2L["7a"]["Name"] = [[NoclipText]];
G2L["7a"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.FreezeAnimRow
G2L["7b"] = Instance.new("Frame", G2L["71"]);
G2L["7b"]["BorderSizePixel"] = 0;
G2L["7b"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7b"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["7b"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["7b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7b"]["Name"] = [[FreezeAnimRow]];
G2L["7b"]["LayoutOrder"] = 3;
G2L["7b"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.FreezeAnimRow.FreezeAnimScript
G2L["7c"] = Instance.new("LocalScript", G2L["7b"]);
G2L["7c"]["Name"] = [[FreezeAnimScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.FreezeAnimRow.FreezeAnimTickBox
G2L["7d"] = Instance.new("TextButton", G2L["7b"]);
G2L["7d"]["TextSize"] = 14;
G2L["7d"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7d"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7d"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["7d"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["7d"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["7d"]["Text"] = [[]];
G2L["7d"]["Name"] = [[FreezeAnimTickBox]];
G2L["7d"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.FreezeAnimRow.FreezeAnimText
G2L["7e"] = Instance.new("TextLabel", G2L["7b"]);
G2L["7e"]["BorderSizePixel"] = 0;
G2L["7e"]["TextSize"] = 14;
G2L["7e"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["7e"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7e"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["7e"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7e"]["BackgroundTransparency"] = 1;
G2L["7e"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["7e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7e"]["Text"] = [[Freeze Animation]];
G2L["7e"]["Name"] = [[FreezeAnimText]];
G2L["7e"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpSpawnRow
G2L["7f"] = Instance.new("Frame", G2L["71"]);
G2L["7f"]["BorderSizePixel"] = 0;
G2L["7f"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7f"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["7f"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["7f"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7f"]["Name"] = [[TpSpawnRow]];
G2L["7f"]["LayoutOrder"] = 5;
G2L["7f"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpSpawnRow.TpSpawnScript
G2L["80"] = Instance.new("LocalScript", G2L["7f"]);
G2L["80"]["Name"] = [[TpSpawnScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpSpawnRow.TpSpawnButton
G2L["81"] = Instance.new("TextButton", G2L["7f"]);
G2L["81"]["TextSize"] = 14;
G2L["81"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["81"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["81"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["81"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["81"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["81"]["Text"] = [[TP To Spawn]];
G2L["81"]["Name"] = [[TpSpawnButton]];
G2L["81"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.GodModeRow
G2L["82"] = Instance.new("Frame", G2L["71"]);
G2L["82"]["BorderSizePixel"] = 0;
G2L["82"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["82"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["82"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["82"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["82"]["Name"] = [[GodModeRow]];
G2L["82"]["LayoutOrder"] = 3;
G2L["82"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.GodModeRow.GodModeScript
G2L["83"] = Instance.new("LocalScript", G2L["82"]);
G2L["83"]["Name"] = [[GodModeScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.GodModeRow.GodModeTickBox
G2L["84"] = Instance.new("TextButton", G2L["82"]);
G2L["84"]["TextSize"] = 14;
G2L["84"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["84"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["84"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["84"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["84"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["84"]["Text"] = [[]];
G2L["84"]["Name"] = [[GodModeTickBox]];
G2L["84"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.GodModeRow.GodModeText
G2L["85"] = Instance.new("TextLabel", G2L["82"]);
G2L["85"]["BorderSizePixel"] = 0;
G2L["85"]["TextSize"] = 14;
G2L["85"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["85"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["85"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["85"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["85"]["BackgroundTransparency"] = 1;
G2L["85"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["85"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["85"]["Text"] = [[God Mode]];
G2L["85"]["Name"] = [[GodModeText]];
G2L["85"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow2
G2L["86"] = Instance.new("Frame", G2L["71"]);
G2L["86"]["BorderSizePixel"] = 0;
G2L["86"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["86"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["86"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["86"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["86"]["Name"] = [[TpPlayerRow2]];
G2L["86"]["LayoutOrder"] = 4;
G2L["86"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow2.TpPlayerScript2
G2L["87"] = Instance.new("LocalScript", G2L["86"]);
G2L["87"]["Name"] = [[TpPlayerScript2]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow2.TpPlayerButton
G2L["88"] = Instance.new("TextButton", G2L["86"]);
G2L["88"]["TextSize"] = 14;
G2L["88"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["88"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["88"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["88"]["Size"] = UDim2.new(0, 40, 0, 16);
G2L["88"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["88"]["Text"] = [[TP]];
G2L["88"]["Name"] = [[TpPlayerButton]];
G2L["88"]["Position"] = UDim2.new(0.73964, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow2.TpPlayerText
G2L["89"] = Instance.new("TextLabel", G2L["86"]);
G2L["89"]["BorderSizePixel"] = 0;
G2L["89"]["TextSize"] = 14;
G2L["89"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["89"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["89"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["89"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["89"]["BackgroundTransparency"] = 1;
G2L["89"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["89"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["89"]["Text"] = [[TP To Player:]];
G2L["89"]["Name"] = [[TpPlayerText]];
G2L["89"]["Position"] = UDim2.new(0.02367, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow1
G2L["8a"] = Instance.new("Frame", G2L["71"]);
G2L["8a"]["BorderSizePixel"] = 0;
G2L["8a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["8a"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["8a"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["8a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["8a"]["Name"] = [[TpPlayerRow1]];
G2L["8a"]["LayoutOrder"] = 4;
G2L["8a"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow1.TpPlayerScript
G2L["8b"] = Instance.new("LocalScript", G2L["8a"]);
G2L["8b"]["Name"] = [[TpPlayerScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow1.TpPlayerDropdown
G2L["8c"] = Instance.new("TextButton", G2L["8a"]);
G2L["8c"]["TextTransparency"] = 1;
G2L["8c"]["TextSize"] = 14;
G2L["8c"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["8c"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["8c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["8c"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["8c"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["8c"]["Text"] = [[Dropdown]];
G2L["8c"]["Name"] = [[TpPlayerDropdown]];
G2L["8c"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.ResetRow
G2L["8d"] = Instance.new("Frame", G2L["71"]);
G2L["8d"]["BorderSizePixel"] = 0;
G2L["8d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["8d"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["8d"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["8d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["8d"]["Name"] = [[ResetRow]];
G2L["8d"]["LayoutOrder"] = 5;
G2L["8d"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.ResetRow.ResetScript
G2L["8e"] = Instance.new("LocalScript", G2L["8d"]);
G2L["8e"]["Name"] = [[ResetScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.ResetRow.ResetButton
G2L["8f"] = Instance.new("TextButton", G2L["8d"]);
G2L["8f"]["TextSize"] = 14;
G2L["8f"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["8f"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["8f"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["8f"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["8f"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["8f"]["Text"] = [[Reset Character]];
G2L["8f"]["Name"] = [[ResetButton]];
G2L["8f"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage
G2L["90"] = Instance.new("ScrollingFrame", G2L["14"]);
G2L["90"]["Visible"] = false;
G2L["90"]["Active"] = true;
G2L["90"]["BorderSizePixel"] = 0;
G2L["90"]["CanvasSize"] = UDim2.new(0, 0, 1.5, 0);
G2L["90"]["Name"] = [[VisualPage]];
G2L["90"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["90"]["Size"] = UDim2.new(0, 349, 0, 250);
G2L["90"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["90"]["Position"] = UDim2.new(0.02329, 0, 0.03008, 0);
G2L["90"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["90"]["ScrollBarThickness"] = 0;
G2L["90"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide
G2L["91"] = Instance.new("Frame", G2L["90"]);
G2L["91"]["BorderSizePixel"] = 0;
G2L["91"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["91"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["91"]["Position"] = UDim2.new(0, 0, 0.00439, 0);
G2L["91"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["91"]["Name"] = [[VisualLeftSide]];
G2L["91"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.UIListLayout
G2L["92"] = Instance.new("UIListLayout", G2L["91"]);
G2L["92"]["Padding"] = UDim.new(0, 7);
G2L["92"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup
G2L["93"] = Instance.new("Frame", G2L["91"]);
G2L["93"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["93"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["93"]["Size"] = UDim2.new(0, 170, 0, 103);
G2L["93"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["93"]["Name"] = [[ESPGroup]];
G2L["93"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPScript
G2L["94"] = Instance.new("LocalScript", G2L["93"]);
G2L["94"]["Name"] = [[ESPScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.UIListLayout
G2L["95"] = Instance.new("UIListLayout", G2L["93"]);
G2L["95"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPAccentColor
G2L["96"] = Instance.new("Frame", G2L["93"]);
G2L["96"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["96"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["96"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["96"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["96"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["96"]["Name"] = [[ESPAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPTitle
G2L["97"] = Instance.new("TextLabel", G2L["93"]);
G2L["97"]["TextWrapped"] = true;
G2L["97"]["BorderSizePixel"] = 0;
G2L["97"]["TextSize"] = 14;
G2L["97"]["TextScaled"] = true;
G2L["97"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["97"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["97"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["97"]["BackgroundTransparency"] = 0.95;
G2L["97"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["97"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["97"]["Text"] = [[ESP]];
G2L["97"]["LayoutOrder"] = 1;
G2L["97"]["Name"] = [[ESPTitle]];
G2L["97"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPTitle.ESPMinimize
G2L["98"] = Instance.new("TextButton", G2L["97"]);
G2L["98"]["BorderSizePixel"] = 0;
G2L["98"]["TextSize"] = 14;
G2L["98"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["98"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["98"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["98"]["BackgroundTransparency"] = 1;
G2L["98"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["98"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["98"]["Text"] = [[-]];
G2L["98"]["Name"] = [[ESPMinimize]];
G2L["98"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPNameRow
G2L["99"] = Instance.new("Frame", G2L["93"]);
G2L["99"]["BorderSizePixel"] = 0;
G2L["99"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["99"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["99"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["99"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["99"]["Name"] = [[ESPNameRow]];
G2L["99"]["LayoutOrder"] = 2;
G2L["99"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPNameRow.ESPNameScript
G2L["9a"] = Instance.new("LocalScript", G2L["99"]);
G2L["9a"]["Name"] = [[ESPNameScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPNameRow.ESPNameTickBox
G2L["9b"] = Instance.new("TextButton", G2L["99"]);
G2L["9b"]["TextSize"] = 14;
G2L["9b"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["9b"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["9b"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["9b"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["9b"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["9b"]["Text"] = [[]];
G2L["9b"]["Name"] = [[ESPNameTickBox]];
G2L["9b"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPNameRow.ESPNameText
G2L["9c"] = Instance.new("TextLabel", G2L["99"]);
G2L["9c"]["BorderSizePixel"] = 0;
G2L["9c"]["TextSize"] = 14;
G2L["9c"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["9c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["9c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["9c"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["9c"]["BackgroundTransparency"] = 1;
G2L["9c"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["9c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["9c"]["Text"] = [[Name ESP]];
G2L["9c"]["Name"] = [[ESPNameText]];
G2L["9c"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPMaxDistRow
G2L["9d"] = Instance.new("Frame", G2L["93"]);
G2L["9d"]["BorderSizePixel"] = 0;
G2L["9d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["9d"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["9d"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["9d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["9d"]["Name"] = [[ESPMaxDistRow]];
G2L["9d"]["LayoutOrder"] = 2;
G2L["9d"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPMaxDistRow.ESPMaxDistScript
G2L["9e"] = Instance.new("LocalScript", G2L["9d"]);
G2L["9e"]["Name"] = [[ESPMaxDistScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPMaxDistRow.ESPMaxDistTickBox
G2L["9f"] = Instance.new("TextButton", G2L["9d"]);
G2L["9f"]["TextSize"] = 14;
G2L["9f"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["9f"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["9f"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["9f"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["9f"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["9f"]["Text"] = [[?]];
G2L["9f"]["Name"] = [[ESPMaxDistTickBox]];
G2L["9f"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPMaxDistRow.ESPMaxDIstText
G2L["a0"] = Instance.new("TextLabel", G2L["9d"]);
G2L["a0"]["BorderSizePixel"] = 0;
G2L["a0"]["TextSize"] = 14;
G2L["a0"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["a0"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a0"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["a0"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a0"]["BackgroundTransparency"] = 1;
G2L["a0"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["a0"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a0"]["Text"] = [[Max Dist]];
G2L["a0"]["Name"] = [[ESPMaxDIstText]];
G2L["a0"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPMaxDistRow.ESPMaxDistChanger
G2L["a1"] = Instance.new("TextBox", G2L["9d"]);
G2L["a1"]["Name"] = [[ESPMaxDistChanger]];
G2L["a1"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["a1"]["TextWrapped"] = true;
G2L["a1"]["TextSize"] = 14;
G2L["a1"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["a1"]["TextScaled"] = true;
G2L["a1"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a1"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["a1"]["PlaceholderText"] = [[(1000)]];
G2L["a1"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["a1"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["a1"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["a1"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPDistanceRow
G2L["a2"] = Instance.new("Frame", G2L["93"]);
G2L["a2"]["BorderSizePixel"] = 0;
G2L["a2"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a2"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["a2"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["a2"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a2"]["Name"] = [[ESPDistanceRow]];
G2L["a2"]["LayoutOrder"] = 2;
G2L["a2"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPDistanceRow.ESPDistanceScript
G2L["a3"] = Instance.new("LocalScript", G2L["a2"]);
G2L["a3"]["Name"] = [[ESPDistanceScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPDistanceRow.ESPDistanceTickBox
G2L["a4"] = Instance.new("TextButton", G2L["a2"]);
G2L["a4"]["TextSize"] = 14;
G2L["a4"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a4"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a4"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["a4"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["a4"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["a4"]["Text"] = [[]];
G2L["a4"]["Name"] = [[ESPDistanceTickBox]];
G2L["a4"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPDistanceRow.ESPDistanceText
G2L["a5"] = Instance.new("TextLabel", G2L["a2"]);
G2L["a5"]["BorderSizePixel"] = 0;
G2L["a5"]["TextSize"] = 14;
G2L["a5"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["a5"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a5"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["a5"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a5"]["BackgroundTransparency"] = 1;
G2L["a5"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["a5"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a5"]["Text"] = [[Distance Label]];
G2L["a5"]["Name"] = [[ESPDistanceText]];
G2L["a5"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPOutlineRow
G2L["a6"] = Instance.new("Frame", G2L["93"]);
G2L["a6"]["BorderSizePixel"] = 0;
G2L["a6"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a6"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["a6"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["a6"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a6"]["Name"] = [[ESPOutlineRow]];
G2L["a6"]["LayoutOrder"] = 2;
G2L["a6"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPOutlineRow.ESPOutlineScript
G2L["a7"] = Instance.new("LocalScript", G2L["a6"]);
G2L["a7"]["Name"] = [[ESPOutlineScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPOutlineRow.ESPOutlineTickBox
G2L["a8"] = Instance.new("TextButton", G2L["a6"]);
G2L["a8"]["TextSize"] = 14;
G2L["a8"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a8"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a8"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["a8"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["a8"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["a8"]["Text"] = [[]];
G2L["a8"]["Name"] = [[ESPOutlineTickBox]];
G2L["a8"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPOutlineRow.ESPOutlineText
G2L["a9"] = Instance.new("TextLabel", G2L["a6"]);
G2L["a9"]["BorderSizePixel"] = 0;
G2L["a9"]["TextSize"] = 14;
G2L["a9"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["a9"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a9"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["a9"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a9"]["BackgroundTransparency"] = 1;
G2L["a9"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["a9"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a9"]["Text"] = [[Player Outline]];
G2L["a9"]["Name"] = [[ESPOutlineText]];
G2L["a9"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide
G2L["aa"] = Instance.new("Frame", G2L["90"]);
G2L["aa"]["BorderSizePixel"] = 0;
G2L["aa"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["aa"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["aa"]["Position"] = UDim2.new(0.51003, 0, 0.00439, 0);
G2L["aa"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["aa"]["Name"] = [[VisualRightSide]];
G2L["aa"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.UIListLayout
G2L["ab"] = Instance.new("UIListLayout", G2L["aa"]);
G2L["ab"]["Padding"] = UDim.new(0, 7);
G2L["ab"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup
G2L["ac"] = Instance.new("Frame", G2L["aa"]);
G2L["ac"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["ac"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["ac"]["Size"] = UDim2.new(0, 170, 0, 65);
G2L["ac"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["ac"]["Name"] = [[ESPSGroup]];
G2L["ac"]["LayoutOrder"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPSScript
G2L["ad"] = Instance.new("LocalScript", G2L["ac"]);
G2L["ad"]["Name"] = [[ESPSScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.UIListLayout
G2L["ae"] = Instance.new("UIListLayout", G2L["ac"]);
G2L["ae"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPSAccentColor
G2L["af"] = Instance.new("Frame", G2L["ac"]);
G2L["af"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["af"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["af"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["af"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["af"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["af"]["Name"] = [[ESPSAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPSTitle
G2L["b0"] = Instance.new("TextLabel", G2L["ac"]);
G2L["b0"]["TextWrapped"] = true;
G2L["b0"]["BorderSizePixel"] = 0;
G2L["b0"]["TextSize"] = 14;
G2L["b0"]["TextScaled"] = true;
G2L["b0"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b0"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["b0"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["b0"]["BackgroundTransparency"] = 0.95;
G2L["b0"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["b0"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b0"]["Text"] = [[ESP Settings]];
G2L["b0"]["LayoutOrder"] = 1;
G2L["b0"]["Name"] = [[ESPSTitle]];
G2L["b0"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPSTitle.ESPSMinimize
G2L["b1"] = Instance.new("TextButton", G2L["b0"]);
G2L["b1"]["BorderSizePixel"] = 0;
G2L["b1"]["TextSize"] = 14;
G2L["b1"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b1"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b1"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["b1"]["BackgroundTransparency"] = 1;
G2L["b1"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["b1"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b1"]["Text"] = [[-]];
G2L["b1"]["Name"] = [[ESPSMinimize]];
G2L["b1"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPEnableRow
G2L["b2"] = Instance.new("Frame", G2L["ac"]);
G2L["b2"]["BorderSizePixel"] = 0;
G2L["b2"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b2"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["b2"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["b2"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b2"]["Name"] = [[ESPEnableRow]];
G2L["b2"]["LayoutOrder"] = 3;
G2L["b2"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPEnableRow.ESPEnableScript
G2L["b3"] = Instance.new("LocalScript", G2L["b2"]);
G2L["b3"]["Name"] = [[ESPEnableScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPEnableRow.ESPEnableTickBox
G2L["b4"] = Instance.new("TextButton", G2L["b2"]);
G2L["b4"]["TextSize"] = 14;
G2L["b4"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b4"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b4"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["b4"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["b4"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["b4"]["Text"] = [[]];
G2L["b4"]["Name"] = [[ESPEnableTickBox]];
G2L["b4"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPEnableRow.ESPEnableText
G2L["b5"] = Instance.new("TextLabel", G2L["b2"]);
G2L["b5"]["BorderSizePixel"] = 0;
G2L["b5"]["TextSize"] = 14;
G2L["b5"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["b5"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b5"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["b5"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b5"]["BackgroundTransparency"] = 1;
G2L["b5"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["b5"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b5"]["Text"] = [[Enable ESP]];
G2L["b5"]["Name"] = [[ESPEnableText]];
G2L["b5"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPUserRow
G2L["b6"] = Instance.new("Frame", G2L["ac"]);
G2L["b6"]["BorderSizePixel"] = 0;
G2L["b6"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b6"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["b6"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["b6"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b6"]["Name"] = [[ESPUserRow]];
G2L["b6"]["LayoutOrder"] = 3;
G2L["b6"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPUserRow.ESPUserScript
G2L["b7"] = Instance.new("LocalScript", G2L["b6"]);
G2L["b7"]["Name"] = [[ESPUserScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPUserRow.ESPUserTickBox
G2L["b8"] = Instance.new("TextButton", G2L["b6"]);
G2L["b8"]["TextSize"] = 14;
G2L["b8"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b8"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b8"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["b8"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["b8"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["b8"]["Text"] = [[]];
G2L["b8"]["Name"] = [[ESPUserTickBox]];
G2L["b8"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPUserRow.ESPUserText
G2L["b9"] = Instance.new("TextLabel", G2L["b6"]);
G2L["b9"]["BorderSizePixel"] = 0;
G2L["b9"]["TextSize"] = 14;
G2L["b9"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["b9"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b9"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["b9"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b9"]["BackgroundTransparency"] = 1;
G2L["b9"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["b9"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b9"]["Text"] = [[Show Display Names]];
G2L["b9"]["Name"] = [[ESPUserText]];
G2L["b9"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage
G2L["ba"] = Instance.new("ScrollingFrame", G2L["14"]);
G2L["ba"]["Visible"] = false;
G2L["ba"]["Active"] = true;
G2L["ba"]["BorderSizePixel"] = 0;
G2L["ba"]["CanvasSize"] = UDim2.new(0, 0, 1.5, 0);
G2L["ba"]["Name"] = [[SettingsPage]];
G2L["ba"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["ba"]["Size"] = UDim2.new(0, 349, 0, 250);
G2L["ba"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["ba"]["Position"] = UDim2.new(0.02329, 0, 0.03008, 0);
G2L["ba"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["ba"]["ScrollBarThickness"] = 0;
G2L["ba"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide
G2L["bb"] = Instance.new("Frame", G2L["ba"]);
G2L["bb"]["BorderSizePixel"] = 0;
G2L["bb"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["bb"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["bb"]["Position"] = UDim2.new(0, 0, 0.00439, 0);
G2L["bb"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["bb"]["Name"] = [[SettingsLeftSide]];
G2L["bb"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide.UIListLayout
G2L["bc"] = Instance.new("UIListLayout", G2L["bb"]);
G2L["bc"]["Padding"] = UDim.new(0, 7);
G2L["bc"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide.MainSettingsGroup
G2L["bd"] = Instance.new("Frame", G2L["bb"]);
G2L["bd"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["bd"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["bd"]["Size"] = UDim2.new(0, 170, 0, 27);
G2L["bd"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["bd"]["Name"] = [[MainSettingsGroup]];
G2L["bd"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide.MainSettingsGroup.MainSettingsScript
G2L["be"] = Instance.new("LocalScript", G2L["bd"]);
G2L["be"]["Name"] = [[MainSettingsScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide.MainSettingsGroup.UIListLayout
G2L["bf"] = Instance.new("UIListLayout", G2L["bd"]);
G2L["bf"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide.MainSettingsGroup.MainSettingsAccentColor
G2L["c0"] = Instance.new("Frame", G2L["bd"]);
G2L["c0"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["c0"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["c0"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["c0"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["c0"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["c0"]["Name"] = [[MainSettingsAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide.MainSettingsGroup.MainSettingsTitle
G2L["c1"] = Instance.new("TextLabel", G2L["bd"]);
G2L["c1"]["TextWrapped"] = true;
G2L["c1"]["BorderSizePixel"] = 0;
G2L["c1"]["TextSize"] = 14;
G2L["c1"]["TextScaled"] = true;
G2L["c1"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["c1"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["c1"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["c1"]["BackgroundTransparency"] = 0.95;
G2L["c1"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["c1"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["c1"]["Text"] = [[Main Settings]];
G2L["c1"]["LayoutOrder"] = 1;
G2L["c1"]["Name"] = [[MainSettingsTitle]];
G2L["c1"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide.MainSettingsGroup.MainSettingsTitle.MainSettingsMinimize
G2L["c2"] = Instance.new("TextButton", G2L["c1"]);
G2L["c2"]["BorderSizePixel"] = 0;
G2L["c2"]["TextSize"] = 14;
G2L["c2"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["c2"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["c2"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["c2"]["BackgroundTransparency"] = 1;
G2L["c2"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["c2"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["c2"]["Text"] = [[-]];
G2L["c2"]["Name"] = [[MainSettingsMinimize]];
G2L["c2"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide
G2L["c3"] = Instance.new("Frame", G2L["ba"]);
G2L["c3"]["BorderSizePixel"] = 0;
G2L["c3"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["c3"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["c3"]["Position"] = UDim2.new(0.51003, 0, 0.00439, 0);
G2L["c3"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["c3"]["Name"] = [[SettingsRightSide]];
G2L["c3"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.UIListLayout
G2L["c4"] = Instance.new("UIListLayout", G2L["c3"]);
G2L["c4"]["Padding"] = UDim.new(0, 7);
G2L["c4"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup
G2L["c5"] = Instance.new("Frame", G2L["c3"]);
G2L["c5"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["c5"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["c5"]["Size"] = UDim2.new(0, 170, 0, 80);
G2L["c5"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["c5"]["Name"] = [[ExploitsGroup]];
G2L["c5"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.ExploitsScript
G2L["c6"] = Instance.new("LocalScript", G2L["c5"]);
G2L["c6"]["Name"] = [[ExploitsScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.UIListLayout
G2L["c7"] = Instance.new("UIListLayout", G2L["c5"]);
G2L["c7"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.ExploitsAccentColor
G2L["c8"] = Instance.new("Frame", G2L["c5"]);
G2L["c8"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["c8"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["c8"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["c8"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["c8"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["c8"]["Name"] = [[ExploitsAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.ExploitsTitle
G2L["c9"] = Instance.new("TextLabel", G2L["c5"]);
G2L["c9"]["TextWrapped"] = true;
G2L["c9"]["BorderSizePixel"] = 0;
G2L["c9"]["TextSize"] = 14;
G2L["c9"]["TextScaled"] = true;
G2L["c9"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["c9"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["c9"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["c9"]["BackgroundTransparency"] = 0.95;
G2L["c9"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["c9"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["c9"]["Text"] = [[Exploits]];
G2L["c9"]["LayoutOrder"] = 1;
G2L["c9"]["Name"] = [[ExploitsTitle]];
G2L["c9"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.ExploitsTitle.ExploitsMinimize
G2L["ca"] = Instance.new("TextButton", G2L["c9"]);
G2L["ca"]["BorderSizePixel"] = 0;
G2L["ca"]["TextSize"] = 14;
G2L["ca"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["ca"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["ca"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["ca"]["BackgroundTransparency"] = 1;
G2L["ca"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["ca"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["ca"]["Text"] = [[-]];
G2L["ca"]["Name"] = [[ExploitsMinimize]];
G2L["ca"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.IYRow
G2L["cb"] = Instance.new("Frame", G2L["c5"]);
G2L["cb"]["BorderSizePixel"] = 0;
G2L["cb"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["cb"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["cb"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["cb"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["cb"]["Name"] = [[IYRow]];
G2L["cb"]["LayoutOrder"] = 3;
G2L["cb"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.IYRow.IYScript
G2L["cc"] = Instance.new("LocalScript", G2L["cb"]);
G2L["cc"]["Name"] = [[IYScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.IYRow.IYButton
G2L["cd"] = Instance.new("TextButton", G2L["cb"]);
G2L["cd"]["TextSize"] = 14;
G2L["cd"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["cd"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["cd"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["cd"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["cd"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["cd"]["Text"] = [[Infinite Yield]];
G2L["cd"]["Name"] = [[IYButton]];
G2L["cd"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.SSRow
G2L["ce"] = Instance.new("Frame", G2L["c5"]);
G2L["ce"]["BorderSizePixel"] = 0;
G2L["ce"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["ce"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["ce"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["ce"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["ce"]["Name"] = [[SSRow]];
G2L["ce"]["LayoutOrder"] = 4;
G2L["ce"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.SSRow.SSScript
G2L["cf"] = Instance.new("LocalScript", G2L["ce"]);
G2L["cf"]["Name"] = [[SSScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.SSRow.SSButton
G2L["d0"] = Instance.new("TextButton", G2L["ce"]);
G2L["d0"]["TextSize"] = 14;
G2L["d0"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["d0"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["d0"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["d0"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["d0"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["d0"]["Text"] = [[Simple Spy V3]];
G2L["d0"]["Name"] = [[SSButton]];
G2L["d0"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.FlingScriptRow
G2L["d1"] = Instance.new("Frame", G2L["c5"]);
G2L["d1"]["BorderSizePixel"] = 0;
G2L["d1"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["d1"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["d1"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["d1"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["d1"]["Name"] = [[FlingScriptRow]];
G2L["d1"]["LayoutOrder"] = 4;
G2L["d1"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.FlingScriptRow.FlingScriptScript
G2L["d2"] = Instance.new("LocalScript", G2L["d1"]);
G2L["d2"]["Name"] = [[FlingScriptScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.FlingScriptRow.FlingScriptButton
G2L["d3"] = Instance.new("TextButton", G2L["d1"]);
G2L["d3"]["TextSize"] = 14;
G2L["d3"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["d3"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["d3"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["d3"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["d3"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["d3"]["Text"] = [[Fling GUI (for bedw)]];
G2L["d3"]["Name"] = [[FlingScriptButton]];
G2L["d3"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage
G2L["d4"] = Instance.new("ScrollingFrame", G2L["14"]);
G2L["d4"]["Visible"] = false;
G2L["d4"]["Active"] = true;
G2L["d4"]["BorderSizePixel"] = 0;
G2L["d4"]["Name"] = [[TCOMainPage]];
G2L["d4"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["d4"]["Size"] = UDim2.new(0, 349, 0, 233);
G2L["d4"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["d4"]["Position"] = UDim2.new(0.02329, 0, 0.09398, 0);
G2L["d4"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["d4"]["ScrollBarThickness"] = 0;
G2L["d4"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide
G2L["d5"] = Instance.new("Frame", G2L["d4"]);
G2L["d5"]["BorderSizePixel"] = 0;
G2L["d5"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["d5"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["d5"]["Position"] = UDim2.new(0, 0, 0.00439, 0);
G2L["d5"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["d5"]["Name"] = [[TCOMainLeftSide]];
G2L["d5"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.UIListLayout
G2L["d6"] = Instance.new("UIListLayout", G2L["d5"]);
G2L["d6"]["Padding"] = UDim.new(0, 7);
G2L["d6"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup
G2L["d7"] = Instance.new("Frame", G2L["d5"]);
G2L["d7"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["d7"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["d7"]["Size"] = UDim2.new(0, 170, 0, 100);
G2L["d7"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["d7"]["Name"] = [[MainGroup]];
G2L["d7"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.MainScript
G2L["d8"] = Instance.new("LocalScript", G2L["d7"]);
G2L["d8"]["Name"] = [[MainScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.UIListLayout
G2L["d9"] = Instance.new("UIListLayout", G2L["d7"]);
G2L["d9"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.MainAccentColor
G2L["da"] = Instance.new("Frame", G2L["d7"]);
G2L["da"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["da"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["da"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["da"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["da"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["da"]["Name"] = [[MainAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.MainTitle
G2L["db"] = Instance.new("TextLabel", G2L["d7"]);
G2L["db"]["TextWrapped"] = true;
G2L["db"]["BorderSizePixel"] = 0;
G2L["db"]["TextSize"] = 14;
G2L["db"]["TextScaled"] = true;
G2L["db"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["db"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["db"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["db"]["BackgroundTransparency"] = 0.95;
G2L["db"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["db"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["db"]["Text"] = [[Main]];
G2L["db"]["LayoutOrder"] = 1;
G2L["db"]["Name"] = [[MainTitle]];
G2L["db"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.MainTitle.MainMinimize
G2L["dc"] = Instance.new("TextButton", G2L["db"]);
G2L["dc"]["BorderSizePixel"] = 0;
G2L["dc"]["TextSize"] = 14;
G2L["dc"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["dc"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["dc"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["dc"]["BackgroundTransparency"] = 1;
G2L["dc"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["dc"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["dc"]["Text"] = [[-]];
G2L["dc"]["Name"] = [[MainMinimize]];
G2L["dc"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ChatSpyRow
G2L["dd"] = Instance.new("Frame", G2L["d7"]);
G2L["dd"]["BorderSizePixel"] = 0;
G2L["dd"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["dd"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["dd"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["dd"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["dd"]["Name"] = [[ChatSpyRow]];
G2L["dd"]["LayoutOrder"] = 2;
G2L["dd"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ChatSpyRow.ChatSpyScript
G2L["de"] = Instance.new("LocalScript", G2L["dd"]);
G2L["de"]["Name"] = [[ChatSpyScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ChatSpyRow.ChatSpyTickBox
G2L["df"] = Instance.new("TextButton", G2L["dd"]);
G2L["df"]["TextSize"] = 14;
G2L["df"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["df"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["df"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["df"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["df"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["df"]["Text"] = [[]];
G2L["df"]["Name"] = [[ChatSpyTickBox]];
G2L["df"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ChatSpyRow.ChatSpyText
G2L["e0"] = Instance.new("TextLabel", G2L["dd"]);
G2L["e0"]["BorderSizePixel"] = 0;
G2L["e0"]["TextSize"] = 14;
G2L["e0"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["e0"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e0"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["e0"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e0"]["BackgroundTransparency"] = 1;
G2L["e0"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["e0"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e0"]["Text"] = [[Spy Chat]];
G2L["e0"]["Name"] = [[ChatSpyText]];
G2L["e0"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ADODeathRow
G2L["e1"] = Instance.new("Frame", G2L["d7"]);
G2L["e1"]["BorderSizePixel"] = 0;
G2L["e1"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e1"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["e1"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["e1"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e1"]["Name"] = [[ADODeathRow]];
G2L["e1"]["LayoutOrder"] = 2;
G2L["e1"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ADODeathRow.ADODeathScript
G2L["e2"] = Instance.new("LocalScript", G2L["e1"]);
G2L["e2"]["Name"] = [[ADODeathScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ADODeathRow.ADODeathTickBox
G2L["e3"] = Instance.new("TextButton", G2L["e1"]);
G2L["e3"]["TextSize"] = 14;
G2L["e3"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e3"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e3"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["e3"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["e3"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["e3"]["Text"] = [[]];
G2L["e3"]["Name"] = [[ADODeathTickBox]];
G2L["e3"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ADODeathRow.ADODeathText
G2L["e4"] = Instance.new("TextLabel", G2L["e1"]);
G2L["e4"]["BorderSizePixel"] = 0;
G2L["e4"]["TextSize"] = 14;
G2L["e4"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["e4"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e4"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["e4"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e4"]["BackgroundTransparency"] = 1;
G2L["e4"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["e4"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e4"]["Text"] = [[Auto Drop on Death]];
G2L["e4"]["Name"] = [[ADODeathText]];
G2L["e4"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.APickupRow
G2L["e5"] = Instance.new("Frame", G2L["d7"]);
G2L["e5"]["BorderSizePixel"] = 0;
G2L["e5"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e5"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["e5"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["e5"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e5"]["Name"] = [[APickupRow]];
G2L["e5"]["LayoutOrder"] = 2;
G2L["e5"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.APickupRow.APickupScript
G2L["e6"] = Instance.new("LocalScript", G2L["e5"]);
G2L["e6"]["Name"] = [[APickupScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.APickupRow.APickupTickBox
G2L["e7"] = Instance.new("TextButton", G2L["e5"]);
G2L["e7"]["TextSize"] = 14;
G2L["e7"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e7"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e7"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["e7"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["e7"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["e7"]["Text"] = [[]];
G2L["e7"]["Name"] = [[APickupTickBox]];
G2L["e7"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.APickupRow.APickupText
G2L["e8"] = Instance.new("TextLabel", G2L["e5"]);
G2L["e8"]["BorderSizePixel"] = 0;
G2L["e8"]["TextSize"] = 14;
G2L["e8"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["e8"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e8"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["e8"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e8"]["BackgroundTransparency"] = 1;
G2L["e8"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["e8"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e8"]["Text"] = [[Auto Pickup Items]];
G2L["e8"]["Name"] = [[APickupText]];
G2L["e8"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.AntiEnliRow
G2L["e9"] = Instance.new("Frame", G2L["d7"]);
G2L["e9"]["BorderSizePixel"] = 0;
G2L["e9"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["e9"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["e9"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["e9"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e9"]["Name"] = [[AntiEnliRow]];
G2L["e9"]["LayoutOrder"] = 2;
G2L["e9"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.AntiEnliRow.AntiEnliScript
G2L["ea"] = Instance.new("LocalScript", G2L["e9"]);
G2L["ea"]["Name"] = [[AntiEnliScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.AntiEnliRow.AntiEnliTickBox
G2L["eb"] = Instance.new("TextButton", G2L["e9"]);
G2L["eb"]["TextSize"] = 14;
G2L["eb"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["eb"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["eb"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["eb"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["eb"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["eb"]["Text"] = [[]];
G2L["eb"]["Name"] = [[AntiEnliTickBox]];
G2L["eb"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.AntiEnliRow.AntiEnliText
G2L["ec"] = Instance.new("TextLabel", G2L["e9"]);
G2L["ec"]["BorderSizePixel"] = 0;
G2L["ec"]["TextSize"] = 14;
G2L["ec"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["ec"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["ec"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["ec"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["ec"]["BackgroundTransparency"] = 1;
G2L["ec"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["ec"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["ec"]["Text"] = [[Prevent Enli Drop]];
G2L["ec"]["Name"] = [[AntiEnliText]];
G2L["ec"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup
G2L["ed"] = Instance.new("Frame", G2L["d5"]);
G2L["ed"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["ed"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["ed"]["Size"] = UDim2.new(0, 170, 0, 65);
G2L["ed"]["Position"] = UDim2.new(0, 0, 0.25278, 0);
G2L["ed"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["ed"]["Name"] = [[RepGroup]];
G2L["ed"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.RepScript
G2L["ee"] = Instance.new("LocalScript", G2L["ed"]);
G2L["ee"]["Name"] = [[RepScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.UIListLayout
G2L["ef"] = Instance.new("UIListLayout", G2L["ed"]);
G2L["ef"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.RepAccentColor
G2L["f0"] = Instance.new("Frame", G2L["ed"]);
G2L["f0"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["f0"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["f0"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["f0"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["f0"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["f0"]["Name"] = [[RepAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.RepTitle
G2L["f1"] = Instance.new("TextLabel", G2L["ed"]);
G2L["f1"]["TextWrapped"] = true;
G2L["f1"]["BorderSizePixel"] = 0;
G2L["f1"]["TextSize"] = 14;
G2L["f1"]["TextScaled"] = true;
G2L["f1"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f1"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["f1"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["f1"]["BackgroundTransparency"] = 0.95;
G2L["f1"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["f1"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["f1"]["Text"] = [[RepStorage Brick]];
G2L["f1"]["LayoutOrder"] = 1;
G2L["f1"]["Name"] = [[RepTitle]];
G2L["f1"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.RepTitle.RepMinimize
G2L["f2"] = Instance.new("TextButton", G2L["f1"]);
G2L["f2"]["BorderSizePixel"] = 0;
G2L["f2"]["TextSize"] = 14;
G2L["f2"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f2"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f2"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["f2"]["BackgroundTransparency"] = 1;
G2L["f2"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["f2"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["f2"]["Text"] = [[-]];
G2L["f2"]["Name"] = [[RepMinimize]];
G2L["f2"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitFixRow
G2L["f3"] = Instance.new("Frame", G2L["ed"]);
G2L["f3"]["BorderSizePixel"] = 0;
G2L["f3"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f3"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["f3"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["f3"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["f3"]["Name"] = [[BKitFixRow]];
G2L["f3"]["LayoutOrder"] = 4;
G2L["f3"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitFixRow.BKitFixScript
G2L["f4"] = Instance.new("LocalScript", G2L["f3"]);
G2L["f4"]["Name"] = [[BKitFixScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitFixRow.BKitFixButton
G2L["f5"] = Instance.new("TextButton", G2L["f3"]);
G2L["f5"]["TextSize"] = 14;
G2L["f5"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f5"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["f5"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["f5"]["Size"] = UDim2.new(0, 135, 0, 16);
G2L["f5"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["f5"]["Text"] = [[Fix Bkit]];
G2L["f5"]["Name"] = [[BKitFixButton]];
G2L["f5"]["Position"] = UDim2.new(0.17751, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitFixRow.BKitFixHelp
G2L["f6"] = Instance.new("TextButton", G2L["f3"]);
G2L["f6"]["TextSize"] = 14;
G2L["f6"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["f6"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["f6"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["f6"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["f6"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["f6"]["Text"] = [[?]];
G2L["f6"]["Name"] = [[BKitFixHelp]];
G2L["f6"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitBreakRow
G2L["f7"] = Instance.new("Frame", G2L["ed"]);
G2L["f7"]["BorderSizePixel"] = 0;
G2L["f7"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f7"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["f7"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["f7"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["f7"]["Name"] = [[BKitBreakRow]];
G2L["f7"]["LayoutOrder"] = 6;
G2L["f7"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitBreakRow.BKitBreakScript
G2L["f8"] = Instance.new("LocalScript", G2L["f7"]);
G2L["f8"]["Name"] = [[BKitBreakScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitBreakRow.BKitBreakButton
G2L["f9"] = Instance.new("TextButton", G2L["f7"]);
G2L["f9"]["TextSize"] = 14;
G2L["f9"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f9"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["f9"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["f9"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["f9"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["f9"]["Text"] = [[Break Bkit]];
G2L["f9"]["Name"] = [[BKitBreakButton]];
G2L["f9"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide
G2L["fa"] = Instance.new("Frame", G2L["d4"]);
G2L["fa"]["BorderSizePixel"] = 0;
G2L["fa"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["fa"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["fa"]["Position"] = UDim2.new(0.51003, 0, 0.00439, 0);
G2L["fa"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["fa"]["Name"] = [[TCOMainRightSide]];
G2L["fa"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.UIListLayout
G2L["fb"] = Instance.new("UIListLayout", G2L["fa"]);
G2L["fb"]["Padding"] = UDim.new(0, 7);
G2L["fb"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup
G2L["fc"] = Instance.new("Frame", G2L["fa"]);
G2L["fc"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["fc"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["fc"]["Size"] = UDim2.new(0, 170, 0, 100);
G2L["fc"]["Position"] = UDim2.new(0, 0, -0, 0);
G2L["fc"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["fc"]["Name"] = [[AntiGroup]];
G2L["fc"]["LayoutOrder"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiScript
G2L["fd"] = Instance.new("LocalScript", G2L["fc"]);
G2L["fd"]["Name"] = [[AntiScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.UIListLayout
G2L["fe"] = Instance.new("UIListLayout", G2L["fc"]);
G2L["fe"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiTitle
G2L["ff"] = Instance.new("TextLabel", G2L["fc"]);
G2L["ff"]["TextWrapped"] = true;
G2L["ff"]["BorderSizePixel"] = 0;
G2L["ff"]["TextSize"] = 14;
G2L["ff"]["TextScaled"] = true;
G2L["ff"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["ff"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["ff"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["ff"]["BackgroundTransparency"] = 0.95;
G2L["ff"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["ff"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["ff"]["Text"] = [[Anti]];
G2L["ff"]["LayoutOrder"] = 1;
G2L["ff"]["Name"] = [[AntiTitle]];
G2L["ff"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiTitle.AntiMinimize
G2L["100"] = Instance.new("TextButton", G2L["ff"]);
G2L["100"]["BorderSizePixel"] = 0;
G2L["100"]["TextSize"] = 14;
G2L["100"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["100"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["100"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["100"]["BackgroundTransparency"] = 1;
G2L["100"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["100"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["100"]["Text"] = [[-]];
G2L["100"]["Name"] = [[AntiMinimize]];
G2L["100"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiNanRow
G2L["101"] = Instance.new("Frame", G2L["fc"]);
G2L["101"]["BorderSizePixel"] = 0;
G2L["101"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["101"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["101"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["101"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["101"]["Name"] = [[AntiNanRow]];
G2L["101"]["LayoutOrder"] = 3;
G2L["101"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiNanRow.AntiNanScript
G2L["102"] = Instance.new("LocalScript", G2L["101"]);
G2L["102"]["Name"] = [[AntiNanScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiNanRow.AntiNanTickBox
G2L["103"] = Instance.new("TextButton", G2L["101"]);
G2L["103"]["TextSize"] = 14;
G2L["103"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["103"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["103"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["103"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["103"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["103"]["Text"] = [[]];
G2L["103"]["Name"] = [[AntiNanTickBox]];
G2L["103"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiNanRow.AntiNanText
G2L["104"] = Instance.new("TextLabel", G2L["101"]);
G2L["104"]["BorderSizePixel"] = 0;
G2L["104"]["TextSize"] = 14;
G2L["104"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["104"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["104"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["104"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["104"]["BackgroundTransparency"] = 1;
G2L["104"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["104"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["104"]["Text"] = [[Anti Maptide Nan]];
G2L["104"]["Name"] = [[AntiNanText]];
G2L["104"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiAccentColor
G2L["105"] = Instance.new("Frame", G2L["fc"]);
G2L["105"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["105"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["105"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["105"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["105"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["105"]["Name"] = [[AntiAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiVisualRow
G2L["106"] = Instance.new("Frame", G2L["fc"]);
G2L["106"]["BorderSizePixel"] = 0;
G2L["106"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["106"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["106"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["106"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["106"]["Name"] = [[AntiVisualRow]];
G2L["106"]["LayoutOrder"] = 3;
G2L["106"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiVisualRow.AntiVisualScript
G2L["107"] = Instance.new("LocalScript", G2L["106"]);
G2L["107"]["Name"] = [[AntiVisualScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiVisualRow.AntiVisualTickBox
G2L["108"] = Instance.new("TextButton", G2L["106"]);
G2L["108"]["TextSize"] = 14;
G2L["108"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["108"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["108"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["108"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["108"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["108"]["Text"] = [[]];
G2L["108"]["Name"] = [[AntiVisualTickBox]];
G2L["108"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiVisualRow.AntiVisualText
G2L["109"] = Instance.new("TextLabel", G2L["106"]);
G2L["109"]["BorderSizePixel"] = 0;
G2L["109"]["TextSize"] = 14;
G2L["109"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["109"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["109"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["109"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["109"]["BackgroundTransparency"] = 1;
G2L["109"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["109"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["109"]["Text"] = [[Anti Visual]];
G2L["109"]["Name"] = [[AntiVisualText]];
G2L["109"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiJailRow
G2L["10a"] = Instance.new("Frame", G2L["fc"]);
G2L["10a"]["BorderSizePixel"] = 0;
G2L["10a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["10a"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["10a"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["10a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10a"]["Name"] = [[AntiJailRow]];
G2L["10a"]["LayoutOrder"] = 3;
G2L["10a"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiJailRow.AntiJailScript
G2L["10b"] = Instance.new("LocalScript", G2L["10a"]);
G2L["10b"]["Name"] = [[AntiJailScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiJailRow.AntiJailTickBox
G2L["10c"] = Instance.new("TextButton", G2L["10a"]);
G2L["10c"]["TextSize"] = 14;
G2L["10c"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10c"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10c"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["10c"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["10c"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["10c"]["Text"] = [[]];
G2L["10c"]["Name"] = [[AntiJailTickBox]];
G2L["10c"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiJailRow.AntiJailText
G2L["10d"] = Instance.new("TextLabel", G2L["10a"]);
G2L["10d"]["BorderSizePixel"] = 0;
G2L["10d"]["TextSize"] = 14;
G2L["10d"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["10d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["10d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["10d"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["10d"]["BackgroundTransparency"] = 1;
G2L["10d"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["10d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10d"]["Text"] = [[Anti Jail]];
G2L["10d"]["Name"] = [[AntiJailText]];
G2L["10d"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiFreezeRow
G2L["10e"] = Instance.new("Frame", G2L["fc"]);
G2L["10e"]["BorderSizePixel"] = 0;
G2L["10e"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["10e"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["10e"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["10e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10e"]["Name"] = [[AntiFreezeRow]];
G2L["10e"]["LayoutOrder"] = 3;
G2L["10e"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiFreezeRow.AntiFreezeScript
G2L["10f"] = Instance.new("LocalScript", G2L["10e"]);
G2L["10f"]["Name"] = [[AntiFreezeScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiFreezeRow.AntiFreezeTickBox
G2L["110"] = Instance.new("TextButton", G2L["10e"]);
G2L["110"]["TextSize"] = 14;
G2L["110"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["110"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["110"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["110"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["110"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["110"]["Text"] = [[]];
G2L["110"]["Name"] = [[AntiFreezeTickBox]];
G2L["110"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiFreezeRow.AntiFreezeText
G2L["111"] = Instance.new("TextLabel", G2L["10e"]);
G2L["111"]["BorderSizePixel"] = 0;
G2L["111"]["TextSize"] = 14;
G2L["111"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["111"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["111"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["111"]["TextColor3"] = Color3.fromRGB(255, 0, 0);
G2L["111"]["BackgroundTransparency"] = 1;
G2L["111"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["111"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["111"]["Text"] = [[Anti Freeze]];
G2L["111"]["Name"] = [[AntiFreezeText]];
G2L["111"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage
G2L["112"] = Instance.new("ScrollingFrame", G2L["14"]);
G2L["112"]["Visible"] = false;
G2L["112"]["Active"] = true;
G2L["112"]["BorderSizePixel"] = 0;
G2L["112"]["CanvasSize"] = UDim2.new(0, 0, 1.5, 0);
G2L["112"]["Name"] = [[MiscPage]];
G2L["112"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["112"]["Size"] = UDim2.new(0, 349, 0, 250);
G2L["112"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["112"]["Position"] = UDim2.new(0.02329, 0, 0.03008, 0);
G2L["112"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["112"]["ScrollBarThickness"] = 0;
G2L["112"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide
G2L["113"] = Instance.new("Frame", G2L["112"]);
G2L["113"]["BorderSizePixel"] = 0;
G2L["113"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["113"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["113"]["Position"] = UDim2.new(0, 0, 0.00439, 0);
G2L["113"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["113"]["Name"] = [[MiscLeftSide]];
G2L["113"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.UIListLayout
G2L["114"] = Instance.new("UIListLayout", G2L["113"]);
G2L["114"]["Padding"] = UDim.new(0, 7);
G2L["114"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group
G2L["115"] = Instance.new("Frame", G2L["113"]);
G2L["115"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["115"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["115"]["Size"] = UDim2.new(0, 170, 0, 103);
G2L["115"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["115"]["Name"] = [[Misc1Group]];
G2L["115"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.Misc1Script
G2L["116"] = Instance.new("LocalScript", G2L["115"]);
G2L["116"]["Name"] = [[Misc1Script]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.UIListLayout
G2L["117"] = Instance.new("UIListLayout", G2L["115"]);
G2L["117"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.MiscAccentColor
G2L["118"] = Instance.new("Frame", G2L["115"]);
G2L["118"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["118"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["118"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["118"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["118"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["118"]["Name"] = [[MiscAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.Misc1Title
G2L["119"] = Instance.new("TextLabel", G2L["115"]);
G2L["119"]["TextWrapped"] = true;
G2L["119"]["BorderSizePixel"] = 0;
G2L["119"]["TextSize"] = 14;
G2L["119"]["TextScaled"] = true;
G2L["119"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["119"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["119"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["119"]["BackgroundTransparency"] = 0.95;
G2L["119"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["119"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["119"]["Text"] = [[Misc]];
G2L["119"]["LayoutOrder"] = 1;
G2L["119"]["Name"] = [[Misc1Title]];
G2L["119"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.Misc1Title.Misc1Minimize
G2L["11a"] = Instance.new("TextButton", G2L["119"]);
G2L["11a"]["BorderSizePixel"] = 0;
G2L["11a"]["TextSize"] = 14;
G2L["11a"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["11a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["11a"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["11a"]["BackgroundTransparency"] = 1;
G2L["11a"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["11a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["11a"]["Text"] = [[-]];
G2L["11a"]["Name"] = [[Misc1Minimize]];
G2L["11a"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.WWalkRow
G2L["11b"] = Instance.new("Frame", G2L["115"]);
G2L["11b"]["BorderSizePixel"] = 0;
G2L["11b"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["11b"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["11b"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["11b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["11b"]["Name"] = [[WWalkRow]];
G2L["11b"]["LayoutOrder"] = 2;
G2L["11b"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.WWalkRow.WWalkScript
G2L["11c"] = Instance.new("LocalScript", G2L["11b"]);
G2L["11c"]["Name"] = [[WWalkScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.WWalkRow.WWalkButton
G2L["11d"] = Instance.new("TextButton", G2L["11b"]);
G2L["11d"]["TextSize"] = 14;
G2L["11d"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["11d"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["11d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["11d"]["Size"] = UDim2.new(0, 135, 0, 16);
G2L["11d"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["11d"]["Text"] = [[Execute Wall Walk]];
G2L["11d"]["Name"] = [[WWalkButton]];
G2L["11d"]["Position"] = UDim2.new(0.17751, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.WWalkRow.WWalkHelp
G2L["11e"] = Instance.new("TextButton", G2L["11b"]);
G2L["11e"]["TextSize"] = 14;
G2L["11e"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["11e"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["11e"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["11e"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["11e"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["11e"]["Text"] = [[?]];
G2L["11e"]["Name"] = [[WWalkHelp]];
G2L["11e"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.SpinRow
G2L["11f"] = Instance.new("Frame", G2L["115"]);
G2L["11f"]["BorderSizePixel"] = 0;
G2L["11f"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["11f"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["11f"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["11f"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["11f"]["Name"] = [[SpinRow]];
G2L["11f"]["LayoutOrder"] = 2;
G2L["11f"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.SpinRow.SpinScript
G2L["120"] = Instance.new("LocalScript", G2L["11f"]);
G2L["120"]["Name"] = [[SpinScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.SpinRow.SpinTickBox
G2L["121"] = Instance.new("TextButton", G2L["11f"]);
G2L["121"]["TextSize"] = 14;
G2L["121"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["121"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["121"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["121"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["121"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["121"]["Text"] = [[]];
G2L["121"]["Name"] = [[SpinTickBox]];
G2L["121"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.SpinRow.SpinText
G2L["122"] = Instance.new("TextLabel", G2L["11f"]);
G2L["122"]["BorderSizePixel"] = 0;
G2L["122"]["TextSize"] = 14;
G2L["122"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["122"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["122"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["122"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["122"]["BackgroundTransparency"] = 1;
G2L["122"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["122"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["122"]["Text"] = [[Spin]];
G2L["122"]["Name"] = [[SpinText]];
G2L["122"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.SpinRow.SpinChanger
G2L["123"] = Instance.new("TextBox", G2L["11f"]);
G2L["123"]["Name"] = [[SpinChanger]];
G2L["123"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["123"]["TextWrapped"] = true;
G2L["123"]["TextSize"] = 14;
G2L["123"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["123"]["TextScaled"] = true;
G2L["123"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["123"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["123"]["PlaceholderText"] = [[(1)]];
G2L["123"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["123"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["123"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["123"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.FakeLagRow
G2L["124"] = Instance.new("Frame", G2L["115"]);
G2L["124"]["BorderSizePixel"] = 0;
G2L["124"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["124"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["124"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["124"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["124"]["Name"] = [[FakeLagRow]];
G2L["124"]["LayoutOrder"] = 2;
G2L["124"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.FakeLagRow.FakeLagScript
G2L["125"] = Instance.new("LocalScript", G2L["124"]);
G2L["125"]["Name"] = [[FakeLagScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.FakeLagRow.FakeLagTickBox
G2L["126"] = Instance.new("TextButton", G2L["124"]);
G2L["126"]["TextSize"] = 14;
G2L["126"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["126"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["126"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["126"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["126"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["126"]["Text"] = [[]];
G2L["126"]["Name"] = [[FakeLagTickBox]];
G2L["126"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.FakeLagRow.FakeLagText
G2L["127"] = Instance.new("TextLabel", G2L["124"]);
G2L["127"]["BorderSizePixel"] = 0;
G2L["127"]["TextSize"] = 14;
G2L["127"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["127"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["127"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["127"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["127"]["BackgroundTransparency"] = 1;
G2L["127"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["127"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["127"]["Text"] = [[Fake Lag]];
G2L["127"]["Name"] = [[FakeLagText]];
G2L["127"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.FakeLagRow.FakeLagChanger
G2L["128"] = Instance.new("TextBox", G2L["124"]);
G2L["128"]["Name"] = [[FakeLagChanger]];
G2L["128"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["128"]["TextWrapped"] = true;
G2L["128"]["TextSize"] = 14;
G2L["128"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["128"]["TextScaled"] = true;
G2L["128"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["128"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["128"]["PlaceholderText"] = [[(1)]];
G2L["128"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["128"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["128"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["128"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.AnimSpeedRow
G2L["129"] = Instance.new("Frame", G2L["115"]);
G2L["129"]["BorderSizePixel"] = 0;
G2L["129"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["129"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["129"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["129"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["129"]["Name"] = [[AnimSpeedRow]];
G2L["129"]["LayoutOrder"] = 2;
G2L["129"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.AnimSpeedRow.AnimSpeedScript
G2L["12a"] = Instance.new("LocalScript", G2L["129"]);
G2L["12a"]["Name"] = [[AnimSpeedScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.AnimSpeedRow.AnimSpeedTickBox
G2L["12b"] = Instance.new("TextButton", G2L["129"]);
G2L["12b"]["TextSize"] = 14;
G2L["12b"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["12b"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["12b"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["12b"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["12b"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["12b"]["Text"] = [[]];
G2L["12b"]["Name"] = [[AnimSpeedTickBox]];
G2L["12b"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.AnimSpeedRow.AnimSpeedText
G2L["12c"] = Instance.new("TextLabel", G2L["129"]);
G2L["12c"]["BorderSizePixel"] = 0;
G2L["12c"]["TextSize"] = 14;
G2L["12c"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["12c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["12c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["12c"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["12c"]["BackgroundTransparency"] = 1;
G2L["12c"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["12c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["12c"]["Text"] = [[Anim Speed]];
G2L["12c"]["Name"] = [[AnimSpeedText]];
G2L["12c"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.AnimSpeedRow.AnimSpeedChanger
G2L["12d"] = Instance.new("TextBox", G2L["129"]);
G2L["12d"]["Name"] = [[AnimSpeedChanger]];
G2L["12d"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["12d"]["TextWrapped"] = true;
G2L["12d"]["TextSize"] = 14;
G2L["12d"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["12d"]["TextScaled"] = true;
G2L["12d"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["12d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["12d"]["PlaceholderText"] = [[(1)]];
G2L["12d"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["12d"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["12d"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["12d"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscRightSide
G2L["12e"] = Instance.new("Frame", G2L["112"]);
G2L["12e"]["BorderSizePixel"] = 0;
G2L["12e"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["12e"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["12e"]["Position"] = UDim2.new(0.51003, 0, 0.00439, 0);
G2L["12e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["12e"]["Name"] = [[MiscRightSide]];
G2L["12e"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscRightSide.UIListLayout
G2L["12f"] = Instance.new("UIListLayout", G2L["12e"]);
G2L["12f"]["Padding"] = UDim.new(0, 7);
G2L["12f"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOSideBar
G2L["130"] = Instance.new("Frame", G2L["14"]);
G2L["130"]["Visible"] = false;
G2L["130"]["BorderSizePixel"] = 0;
G2L["130"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["130"]["Size"] = UDim2.new(0, 365, 0, 25);
G2L["130"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["130"]["Name"] = [[TCOSideBar]];
G2L["130"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOSideBar.MainTCOPG
G2L["131"] = Instance.new("TextButton", G2L["130"]);
G2L["131"]["TextWrapped"] = true;
G2L["131"]["TextSize"] = 12;
G2L["131"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["131"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["131"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["131"]["Size"] = UDim2.new(0, 80, 0, 25);
G2L["131"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["131"]["Text"] = [[Main]];
G2L["131"]["Name"] = [[MainTCOPG]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOSideBar.UIListLayout
G2L["132"] = Instance.new("UIListLayout", G2L["130"]);
G2L["132"]["SortOrder"] = Enum.SortOrder.LayoutOrder;
G2L["132"]["FillDirection"] = Enum.FillDirection.Horizontal;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOSideBar.MiscTCOPG
G2L["133"] = Instance.new("TextButton", G2L["130"]);
G2L["133"]["TextWrapped"] = true;
G2L["133"]["TextSize"] = 12;
G2L["133"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["133"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["133"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["133"]["Size"] = UDim2.new(0, 80, 0, 25);
G2L["133"]["LayoutOrder"] = 10;
G2L["133"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["133"]["Text"] = [[Misc]];
G2L["133"]["Name"] = [[MiscTCOPG]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOSideBar.TrollTCOPG
G2L["134"] = Instance.new("TextButton", G2L["130"]);
G2L["134"]["TextWrapped"] = true;
G2L["134"]["TextSize"] = 12;
G2L["134"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["134"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["134"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["134"]["Size"] = UDim2.new(0, 80, 0, 25);
G2L["134"]["LayoutOrder"] = 1;
G2L["134"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["134"]["Text"] = [[Troll]];
G2L["134"]["Name"] = [[TrollTCOPG]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOSideBar.DetectionTCOPG
G2L["135"] = Instance.new("TextButton", G2L["130"]);
G2L["135"]["TextWrapped"] = true;
G2L["135"]["TextSize"] = 12;
G2L["135"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["135"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["135"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["135"]["Size"] = UDim2.new(0, 80, 0, 25);
G2L["135"]["LayoutOrder"] = 2;
G2L["135"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["135"]["Text"] = [[Detection]];
G2L["135"]["Name"] = [[DetectionTCOPG]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage
G2L["136"] = Instance.new("ScrollingFrame", G2L["14"]);
G2L["136"]["Active"] = true;
G2L["136"]["BorderSizePixel"] = 0;
G2L["136"]["CanvasPosition"] = Vector2.new(0, 100);
G2L["136"]["Name"] = [[TCOTrollPage]];
G2L["136"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["136"]["Size"] = UDim2.new(0, 349, 0, 233);
G2L["136"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["136"]["Position"] = UDim2.new(0.02329, 0, 0.09398, 0);
G2L["136"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["136"]["ScrollBarThickness"] = 0;
G2L["136"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide
G2L["137"] = Instance.new("Frame", G2L["136"]);
G2L["137"]["BorderSizePixel"] = 0;
G2L["137"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["137"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["137"]["Position"] = UDim2.new(0.51003, 0, 0.00439, 0);
G2L["137"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["137"]["Name"] = [[TCOTrollRightSide]];
G2L["137"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.UIListLayout
G2L["138"] = Instance.new("UIListLayout", G2L["137"]);
G2L["138"]["Padding"] = UDim.new(0, 7);
G2L["138"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup
G2L["139"] = Instance.new("Frame", G2L["137"]);
G2L["139"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["139"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["139"]["Size"] = UDim2.new(0, 170, 0, 106);
G2L["139"]["Position"] = UDim2.new(0, 0, 0.29722, 0);
G2L["139"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["139"]["Name"] = [[GriefGroup]];
G2L["139"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GriefScript
G2L["13a"] = Instance.new("LocalScript", G2L["139"]);
G2L["13a"]["Name"] = [[GriefScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GriefToolTip
G2L["13b"] = Instance.new("LocalScript", G2L["139"]);
G2L["13b"]["Name"] = [[GriefToolTip]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.UIListLayout
G2L["13c"] = Instance.new("UIListLayout", G2L["139"]);
G2L["13c"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GriefAccentColor
G2L["13d"] = Instance.new("Frame", G2L["139"]);
G2L["13d"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["13d"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["13d"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["13d"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["13d"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["13d"]["Name"] = [[GriefAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GriefTitle
G2L["13e"] = Instance.new("TextLabel", G2L["139"]);
G2L["13e"]["TextWrapped"] = true;
G2L["13e"]["BorderSizePixel"] = 0;
G2L["13e"]["TextSize"] = 14;
G2L["13e"]["TextScaled"] = true;
G2L["13e"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["13e"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["13e"]["TextColor3"] = Color3.fromRGB(190, 0, 0);
G2L["13e"]["BackgroundTransparency"] = 0.95;
G2L["13e"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["13e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["13e"]["Text"] = [[Grief (IN PROGESS)]];
G2L["13e"]["LayoutOrder"] = 1;
G2L["13e"]["Name"] = [[GriefTitle]];
G2L["13e"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GriefTitle.GriefMinimize
G2L["13f"] = Instance.new("TextButton", G2L["13e"]);
G2L["13f"]["BorderSizePixel"] = 0;
G2L["13f"]["TextSize"] = 14;
G2L["13f"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["13f"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["13f"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["13f"]["BackgroundTransparency"] = 1;
G2L["13f"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["13f"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["13f"]["Text"] = [[-]];
G2L["13f"]["Name"] = [[GriefMinimize]];
G2L["13f"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.SelectDRow
G2L["140"] = Instance.new("Frame", G2L["139"]);
G2L["140"]["BorderSizePixel"] = 0;
G2L["140"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["140"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["140"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["140"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["140"]["Name"] = [[SelectDRow]];
G2L["140"]["LayoutOrder"] = 3;
G2L["140"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.SelectDRow.SelectDScript
G2L["141"] = Instance.new("LocalScript", G2L["140"]);
G2L["141"]["Name"] = [[SelectDScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.SelectDRow.SelectDDropdown
G2L["142"] = Instance.new("TextButton", G2L["140"]);
G2L["142"]["TextSize"] = 14;
G2L["142"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["142"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["142"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["142"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["142"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["142"]["Text"] = [[]];
G2L["142"]["Name"] = [[SelectDDropdown]];
G2L["142"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GPLabelRow
G2L["143"] = Instance.new("Frame", G2L["139"]);
G2L["143"]["BorderSizePixel"] = 0;
G2L["143"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["143"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["143"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["143"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["143"]["Name"] = [[GPLabelRow]];
G2L["143"]["LayoutOrder"] = 2;
G2L["143"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GPLabelRow.GPLabelText
G2L["144"] = Instance.new("TextLabel", G2L["143"]);
G2L["144"]["BorderSizePixel"] = 0;
G2L["144"]["TextSize"] = 14;
G2L["144"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["144"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["144"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["144"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["144"]["BackgroundTransparency"] = 1;
G2L["144"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["144"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["144"]["Text"] = [[Select Players:]];
G2L["144"]["Name"] = [[GPLabelText]];
G2L["144"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GPLabelRow.GPLabelHelp
G2L["145"] = Instance.new("TextButton", G2L["143"]);
G2L["145"]["TextSize"] = 14;
G2L["145"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["145"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["145"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["145"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["145"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["145"]["Text"] = [[?]];
G2L["145"]["Name"] = [[GPLabelHelp]];
G2L["145"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.HLBlockRow
G2L["146"] = Instance.new("Frame", G2L["139"]);
G2L["146"]["BorderSizePixel"] = 0;
G2L["146"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["146"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["146"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["146"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["146"]["Name"] = [[HLBlockRow]];
G2L["146"]["LayoutOrder"] = 4;
G2L["146"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.HLBlockRow.HLBlockScript
G2L["147"] = Instance.new("LocalScript", G2L["146"]);
G2L["147"]["Name"] = [[HLBlockScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.HLBlockRow.HLBlockTickBox
G2L["148"] = Instance.new("TextButton", G2L["146"]);
G2L["148"]["TextSize"] = 14;
G2L["148"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["148"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["148"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["148"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["148"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["148"]["Text"] = [[]];
G2L["148"]["Name"] = [[HLBlockTickBox]];
G2L["148"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.HLBlockRow.HLBlockText
G2L["149"] = Instance.new("TextLabel", G2L["146"]);
G2L["149"]["BorderSizePixel"] = 0;
G2L["149"]["TextSize"] = 14;
G2L["149"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["149"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["149"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["149"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["149"]["BackgroundTransparency"] = 1;
G2L["149"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["149"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["149"]["Text"] = [[Highlight Blocks]];
G2L["149"]["Name"] = [[HLBlockText]];
G2L["149"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.ToggleGriefRow
G2L["14a"] = Instance.new("Frame", G2L["139"]);
G2L["14a"]["BorderSizePixel"] = 0;
G2L["14a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["14a"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["14a"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["14a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["14a"]["Name"] = [[ToggleGriefRow]];
G2L["14a"]["LayoutOrder"] = 6;
G2L["14a"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.ToggleGriefRow.ToggleGriefScript
G2L["14b"] = Instance.new("LocalScript", G2L["14a"]);
G2L["14b"]["Name"] = [[ToggleGriefScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.ToggleGriefRow.ToggleGriefButton
G2L["14c"] = Instance.new("TextButton", G2L["14a"]);
G2L["14c"]["TextSize"] = 14;
G2L["14c"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["14c"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["14c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["14c"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["14c"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["14c"]["Text"] = [[Start Grief]];
G2L["14c"]["Name"] = [[ToggleGriefButton]];
G2L["14c"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide
G2L["14d"] = Instance.new("Frame", G2L["136"]);
G2L["14d"]["BorderSizePixel"] = 0;
G2L["14d"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["14d"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["14d"]["Position"] = UDim2.new(0, 0, 0.00439, 0);
G2L["14d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["14d"]["Name"] = [[TCOTrollLeftSide]];
G2L["14d"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.UIListLayout
G2L["14e"] = Instance.new("UIListLayout", G2L["14d"]);
G2L["14e"]["Padding"] = UDim.new(0, 7);
G2L["14e"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup
G2L["14f"] = Instance.new("Frame", G2L["14d"]);
G2L["14f"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["14f"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["14f"]["Size"] = UDim2.new(0, 170, 0, 144);
G2L["14f"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["14f"]["Name"] = [[BypassGroup]];
G2L["14f"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BypassScript
G2L["150"] = Instance.new("LocalScript", G2L["14f"]);
G2L["150"]["Name"] = [[BypassScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BypassToolTip
G2L["151"] = Instance.new("LocalScript", G2L["14f"]);
G2L["151"]["Name"] = [[BypassToolTip]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.UIListLayout
G2L["152"] = Instance.new("UIListLayout", G2L["14f"]);
G2L["152"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BypassAccentColor
G2L["153"] = Instance.new("Frame", G2L["14f"]);
G2L["153"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["153"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["153"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["153"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["153"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["153"]["Name"] = [[BypassAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BypassTitle
G2L["154"] = Instance.new("TextLabel", G2L["14f"]);
G2L["154"]["TextWrapped"] = true;
G2L["154"]["BorderSizePixel"] = 0;
G2L["154"]["TextSize"] = 14;
G2L["154"]["TextScaled"] = true;
G2L["154"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["154"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["154"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["154"]["BackgroundTransparency"] = 0.95;
G2L["154"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["154"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["154"]["Text"] = [[Tool Bypass]];
G2L["154"]["LayoutOrder"] = 1;
G2L["154"]["Name"] = [[BypassTitle]];
G2L["154"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BypassTitle.BypassMinimize
G2L["155"] = Instance.new("TextButton", G2L["154"]);
G2L["155"]["BorderSizePixel"] = 0;
G2L["155"]["TextSize"] = 14;
G2L["155"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["155"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["155"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["155"]["BackgroundTransparency"] = 1;
G2L["155"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["155"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["155"]["Text"] = [[-]];
G2L["155"]["Name"] = [[BypassMinimize]];
G2L["155"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.PaintBypassRow
G2L["156"] = Instance.new("Frame", G2L["14f"]);
G2L["156"]["BorderSizePixel"] = 0;
G2L["156"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["156"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["156"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["156"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["156"]["Name"] = [[PaintBypassRow]];
G2L["156"]["LayoutOrder"] = 5;
G2L["156"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.PaintBypassRow.PaintBypassScript
G2L["157"] = Instance.new("LocalScript", G2L["156"]);
G2L["157"]["Name"] = [[PaintBypassScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.PaintBypassRow.PaintBypassTickBox
G2L["158"] = Instance.new("TextButton", G2L["156"]);
G2L["158"]["TextSize"] = 14;
G2L["158"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["158"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["158"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["158"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["158"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["158"]["Text"] = [[]];
G2L["158"]["Name"] = [[PaintBypassTickBox]];
G2L["158"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.PaintBypassRow.PaintBypassText
G2L["159"] = Instance.new("TextLabel", G2L["156"]);
G2L["159"]["BorderSizePixel"] = 0;
G2L["159"]["TextSize"] = 14;
G2L["159"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["159"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["159"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["159"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["159"]["BackgroundTransparency"] = 1;
G2L["159"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["159"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["159"]["Text"] = [[Paint]];
G2L["159"]["Name"] = [[PaintBypassText]];
G2L["159"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.DeleteBypassRow
G2L["15a"] = Instance.new("Frame", G2L["14f"]);
G2L["15a"]["BorderSizePixel"] = 0;
G2L["15a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["15a"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["15a"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["15a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["15a"]["Name"] = [[DeleteBypassRow]];
G2L["15a"]["LayoutOrder"] = 4;
G2L["15a"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.DeleteBypassRow.DeleteBypassScript
G2L["15b"] = Instance.new("LocalScript", G2L["15a"]);
G2L["15b"]["Name"] = [[DeleteBypassScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.DeleteBypassRow.DeleteBypassTickBox
G2L["15c"] = Instance.new("TextButton", G2L["15a"]);
G2L["15c"]["TextSize"] = 14;
G2L["15c"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["15c"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["15c"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["15c"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["15c"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["15c"]["Text"] = [[]];
G2L["15c"]["Name"] = [[DeleteBypassTickBox]];
G2L["15c"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.DeleteBypassRow.DeleteBypassText
G2L["15d"] = Instance.new("TextLabel", G2L["15a"]);
G2L["15d"]["BorderSizePixel"] = 0;
G2L["15d"]["TextSize"] = 14;
G2L["15d"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["15d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["15d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["15d"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["15d"]["BackgroundTransparency"] = 1;
G2L["15d"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["15d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["15d"]["Text"] = [[Delete]];
G2L["15d"]["Name"] = [[DeleteBypassText]];
G2L["15d"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShapeBypassRow
G2L["15e"] = Instance.new("Frame", G2L["14f"]);
G2L["15e"]["BorderSizePixel"] = 0;
G2L["15e"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["15e"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["15e"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["15e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["15e"]["Name"] = [[ShapeBypassRow]];
G2L["15e"]["LayoutOrder"] = 6;
G2L["15e"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShapeBypassRow.ShapeBypassScript
G2L["15f"] = Instance.new("LocalScript", G2L["15e"]);
G2L["15f"]["Name"] = [[ShapeBypassScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShapeBypassRow.ShapeBypassTickBox
G2L["160"] = Instance.new("TextButton", G2L["15e"]);
G2L["160"]["TextSize"] = 14;
G2L["160"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["160"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["160"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["160"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["160"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["160"]["Text"] = [[]];
G2L["160"]["Name"] = [[ShapeBypassTickBox]];
G2L["160"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShapeBypassRow.ShapeBypassText
G2L["161"] = Instance.new("TextLabel", G2L["15e"]);
G2L["161"]["BorderSizePixel"] = 0;
G2L["161"]["TextSize"] = 14;
G2L["161"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["161"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["161"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["161"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["161"]["BackgroundTransparency"] = 1;
G2L["161"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["161"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["161"]["Text"] = [[Shape]];
G2L["161"]["Name"] = [[ShapeBypassText]];
G2L["161"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShovelBypassRow
G2L["162"] = Instance.new("Frame", G2L["14f"]);
G2L["162"]["BorderSizePixel"] = 0;
G2L["162"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["162"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["162"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["162"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["162"]["Name"] = [[ShovelBypassRow]];
G2L["162"]["LayoutOrder"] = 7;
G2L["162"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShovelBypassRow.ShovelBypassScript
G2L["163"] = Instance.new("LocalScript", G2L["162"]);
G2L["163"]["Name"] = [[ShovelBypassScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShovelBypassRow.ShovelBypassTickBox
G2L["164"] = Instance.new("TextButton", G2L["162"]);
G2L["164"]["TextSize"] = 14;
G2L["164"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["164"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["164"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["164"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["164"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["164"]["Text"] = [[]];
G2L["164"]["Name"] = [[ShovelBypassTickBox]];
G2L["164"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShovelBypassRow.ShovelBypassText
G2L["165"] = Instance.new("TextLabel", G2L["162"]);
G2L["165"]["BorderSizePixel"] = 0;
G2L["165"]["TextSize"] = 14;
G2L["165"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["165"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["165"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["165"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["165"]["BackgroundTransparency"] = 1;
G2L["165"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["165"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["165"]["Text"] = [[Shovel]];
G2L["165"]["Name"] = [[ShovelBypassText]];
G2L["165"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.SignBypassRow
G2L["166"] = Instance.new("Frame", G2L["14f"]);
G2L["166"]["BorderSizePixel"] = 0;
G2L["166"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["166"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["166"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["166"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["166"]["Name"] = [[SignBypassRow]];
G2L["166"]["LayoutOrder"] = 8;
G2L["166"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.SignBypassRow.SignBypassScript
G2L["167"] = Instance.new("LocalScript", G2L["166"]);
G2L["167"]["Name"] = [[SignBypassScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.SignBypassRow.SignBypassTickBox
G2L["168"] = Instance.new("TextButton", G2L["166"]);
G2L["168"]["TextSize"] = 14;
G2L["168"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["168"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["168"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["168"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["168"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["168"]["Text"] = [[]];
G2L["168"]["Name"] = [[SignBypassTickBox]];
G2L["168"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.SignBypassRow.SignBypassText
G2L["169"] = Instance.new("TextLabel", G2L["166"]);
G2L["169"]["BorderSizePixel"] = 0;
G2L["169"]["TextSize"] = 14;
G2L["169"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["169"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["169"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["169"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["169"]["BackgroundTransparency"] = 1;
G2L["169"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["169"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["169"]["Text"] = [[Sign]];
G2L["169"]["Name"] = [[SignBypassText]];
G2L["169"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BuildBypassRow
G2L["16a"] = Instance.new("Frame", G2L["14f"]);
G2L["16a"]["BorderSizePixel"] = 0;
G2L["16a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["16a"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["16a"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["16a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["16a"]["Name"] = [[BuildBypassRow]];
G2L["16a"]["LayoutOrder"] = 3;
G2L["16a"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BuildBypassRow.BuildBypassScript
G2L["16b"] = Instance.new("LocalScript", G2L["16a"]);
G2L["16b"]["Name"] = [[BuildBypassScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BuildBypassRow.BuildBypassTickBox
G2L["16c"] = Instance.new("TextButton", G2L["16a"]);
G2L["16c"]["TextSize"] = 14;
G2L["16c"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["16c"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["16c"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["16c"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["16c"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["16c"]["Text"] = [[]];
G2L["16c"]["Name"] = [[BuildBypassTickBox]];
G2L["16c"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BuildBypassRow.BuildBypassText
G2L["16d"] = Instance.new("TextLabel", G2L["16a"]);
G2L["16d"]["BorderSizePixel"] = 0;
G2L["16d"]["TextSize"] = 14;
G2L["16d"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["16d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["16d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["16d"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["16d"]["BackgroundTransparency"] = 1;
G2L["16d"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["16d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["16d"]["Text"] = [[Build]];
G2L["16d"]["Name"] = [[BuildBypassText]];
G2L["16d"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup
G2L["16e"] = Instance.new("Frame", G2L["14d"]);
G2L["16e"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["16e"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["16e"]["Size"] = UDim2.new(0, 170, 0, 100);
G2L["16e"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["16e"]["Name"] = [[LagGroup]];
G2L["16e"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.LagScript
G2L["16f"] = Instance.new("LocalScript", G2L["16e"]);
G2L["16f"]["Name"] = [[LagScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.LagToolTip
G2L["170"] = Instance.new("LocalScript", G2L["16e"]);
G2L["170"]["Name"] = [[LagToolTip]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.UIListLayout
G2L["171"] = Instance.new("UIListLayout", G2L["16e"]);
G2L["171"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.LagAccentColor
G2L["172"] = Instance.new("Frame", G2L["16e"]);
G2L["172"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["172"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["172"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["172"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["172"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["172"]["Name"] = [[LagAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.LagTitle
G2L["173"] = Instance.new("TextLabel", G2L["16e"]);
G2L["173"]["TextWrapped"] = true;
G2L["173"]["BorderSizePixel"] = 0;
G2L["173"]["TextSize"] = 14;
G2L["173"]["TextScaled"] = true;
G2L["173"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["173"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["173"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["173"]["BackgroundTransparency"] = 0.95;
G2L["173"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["173"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["173"]["Text"] = [[Building]];
G2L["173"]["LayoutOrder"] = 1;
G2L["173"]["Name"] = [[LagTitle]];
G2L["173"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.LagTitle.LagMinimize
G2L["174"] = Instance.new("TextButton", G2L["173"]);
G2L["174"]["BorderSizePixel"] = 0;
G2L["174"]["TextSize"] = 14;
G2L["174"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["174"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["174"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["174"]["BackgroundTransparency"] = 1;
G2L["174"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["174"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["174"]["Text"] = [[-]];
G2L["174"]["Name"] = [[LagMinimize]];
G2L["174"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SpamSelectRow
G2L["175"] = Instance.new("Frame", G2L["16e"]);
G2L["175"]["BorderSizePixel"] = 0;
G2L["175"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["175"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["175"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["175"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["175"]["Name"] = [[SpamSelectRow]];
G2L["175"]["LayoutOrder"] = 6;
G2L["175"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SpamSelectRow.SpamSelectScript
G2L["176"] = Instance.new("LocalScript", G2L["175"]);
G2L["176"]["Name"] = [[SpamSelectScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SpamSelectRow.SpamSelectHelp
G2L["177"] = Instance.new("TextButton", G2L["175"]);
G2L["177"]["TextSize"] = 14;
G2L["177"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["177"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["177"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["177"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["177"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["177"]["Text"] = [[?]];
G2L["177"]["Name"] = [[SpamSelectHelp]];
G2L["177"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SpamSelectRow.SpamSelectButton
G2L["178"] = Instance.new("TextButton", G2L["175"]);
G2L["178"]["TextSize"] = 14;
G2L["178"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["178"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["178"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["178"]["Size"] = UDim2.new(0, 135, 0, 16);
G2L["178"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["178"]["Text"] = [[Select Block]];
G2L["178"]["Name"] = [[SpamSelectButton]];
G2L["178"]["Position"] = UDim2.new(0.17751, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSpamRow
G2L["179"] = Instance.new("Frame", G2L["16e"]);
G2L["179"]["BorderSizePixel"] = 0;
G2L["179"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["179"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["179"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["179"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["179"]["Name"] = [[BlockSpamRow]];
G2L["179"]["LayoutOrder"] = 7;
G2L["179"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSpamRow.BlockSpamScript
G2L["17a"] = Instance.new("LocalScript", G2L["179"]);
G2L["17a"]["Name"] = [[BlockSpamScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSpamRow.BlockSpamButton
G2L["17b"] = Instance.new("TextButton", G2L["179"]);
G2L["17b"]["TextSize"] = 14;
G2L["17b"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["17b"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["17b"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["17b"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["17b"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["17b"]["Text"] = [[Start Spam]];
G2L["17b"]["Name"] = [[BlockSpamButton]];
G2L["17b"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SavePosRow
G2L["17c"] = Instance.new("Frame", G2L["16e"]);
G2L["17c"]["BorderSizePixel"] = 0;
G2L["17c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["17c"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["17c"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["17c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["17c"]["Name"] = [[SavePosRow]];
G2L["17c"]["LayoutOrder"] = 7;
G2L["17c"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SavePosRow.SavePosScript
G2L["17d"] = Instance.new("LocalScript", G2L["17c"]);
G2L["17d"]["Name"] = [[SavePosScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SavePosRow.SavePosTickBox
G2L["17e"] = Instance.new("TextButton", G2L["17c"]);
G2L["17e"]["TextSize"] = 14;
G2L["17e"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["17e"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["17e"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["17e"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["17e"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["17e"]["Text"] = [[]];
G2L["17e"]["Name"] = [[SavePosTickBox]];
G2L["17e"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SavePosRow.SavePosText
G2L["17f"] = Instance.new("TextLabel", G2L["17c"]);
G2L["17f"]["BorderSizePixel"] = 0;
G2L["17f"]["TextSize"] = 14;
G2L["17f"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["17f"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["17f"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["17f"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["17f"]["BackgroundTransparency"] = 1;
G2L["17f"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["17f"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["17f"]["Text"] = [[Prevent Delcubes]];
G2L["17f"]["Name"] = [[SavePosText]];
G2L["17f"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSizeRow
G2L["180"] = Instance.new("Frame", G2L["16e"]);
G2L["180"]["BorderSizePixel"] = 0;
G2L["180"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["180"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["180"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["180"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["180"]["Name"] = [[BlockSizeRow]];
G2L["180"]["LayoutOrder"] = 8;
G2L["180"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSizeRow.BlockSizeText
G2L["181"] = Instance.new("TextLabel", G2L["180"]);
G2L["181"]["BorderSizePixel"] = 0;
G2L["181"]["TextSize"] = 14;
G2L["181"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["181"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["181"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["181"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["181"]["BackgroundTransparency"] = 1;
G2L["181"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["181"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["181"]["Text"] = [[Block Size]];
G2L["181"]["Name"] = [[BlockSizeText]];
G2L["181"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSizeRow.BlockSizeChanger
G2L["182"] = Instance.new("TextBox", G2L["180"]);
G2L["182"]["Name"] = [[BlockSizeChanger]];
G2L["182"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["182"]["TextWrapped"] = true;
G2L["182"]["TextSize"] = 14;
G2L["182"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["182"]["TextScaled"] = true;
G2L["182"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["182"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["182"]["PlaceholderText"] = [[(1)]];
G2L["182"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["182"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["182"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["182"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSizeRow.BlockSizeChanger.BlockSizeScript
G2L["183"] = Instance.new("LocalScript", G2L["182"]);
G2L["183"]["Name"] = [[BlockSizeScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSizeRow.BlockSizeHelp
G2L["184"] = Instance.new("TextButton", G2L["180"]);
G2L["184"]["TextSize"] = 14;
G2L["184"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["184"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["184"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["184"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["184"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["184"]["Text"] = [[?]];
G2L["184"]["Name"] = [[BlockSizeHelp]];
G2L["184"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage
G2L["185"] = Instance.new("ScrollingFrame", G2L["14"]);
G2L["185"]["Visible"] = false;
G2L["185"]["Active"] = true;
G2L["185"]["BorderSizePixel"] = 0;
G2L["185"]["CanvasSize"] = UDim2.new(0, 0, 1.5, 0);
G2L["185"]["Name"] = [[TCODetectionPage]];
G2L["185"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["185"]["Size"] = UDim2.new(0, 349, 0, 233);
G2L["185"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["185"]["Position"] = UDim2.new(0.02329, 0, 0.09398, 0);
G2L["185"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["185"]["ScrollBarThickness"] = 0;
G2L["185"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide
G2L["186"] = Instance.new("Frame", G2L["185"]);
G2L["186"]["BorderSizePixel"] = 0;
G2L["186"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["186"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["186"]["Position"] = UDim2.new(0, 0, 0.00439, 0);
G2L["186"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["186"]["Name"] = [[TCODetectionLeftSide]];
G2L["186"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.UIListLayout
G2L["187"] = Instance.new("UIListLayout", G2L["186"]);
G2L["187"]["Padding"] = UDim.new(0, 7);
G2L["187"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup
G2L["188"] = Instance.new("Frame", G2L["186"]);
G2L["188"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["188"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["188"]["Size"] = UDim2.new(0, 170, 0, 120);
G2L["188"]["Position"] = UDim2.new(-0.02941, 0, 0.11111, 0);
G2L["188"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["188"]["Name"] = [[EnableGroup]];
G2L["188"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableScript
G2L["189"] = Instance.new("LocalScript", G2L["188"]);
G2L["189"]["Name"] = [[EnableScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableToolTip
G2L["18a"] = Instance.new("LocalScript", G2L["188"]);
G2L["18a"]["Name"] = [[EnableToolTip]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.UIListLayout
G2L["18b"] = Instance.new("UIListLayout", G2L["188"]);
G2L["18b"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableAccentColor
G2L["18c"] = Instance.new("Frame", G2L["188"]);
G2L["18c"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["18c"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["18c"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["18c"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["18c"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["18c"]["Name"] = [[EnableAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableTitle
G2L["18d"] = Instance.new("TextLabel", G2L["188"]);
G2L["18d"]["TextWrapped"] = true;
G2L["18d"]["BorderSizePixel"] = 0;
G2L["18d"]["TextSize"] = 14;
G2L["18d"]["TextScaled"] = true;
G2L["18d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["18d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["18d"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["18d"]["BackgroundTransparency"] = 0.95;
G2L["18d"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["18d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["18d"]["Text"] = [[Grief Monitor]];
G2L["18d"]["LayoutOrder"] = 1;
G2L["18d"]["Name"] = [[EnableTitle]];
G2L["18d"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableTitle.EnableMinimize
G2L["18e"] = Instance.new("TextButton", G2L["18d"]);
G2L["18e"]["BorderSizePixel"] = 0;
G2L["18e"]["TextSize"] = 14;
G2L["18e"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["18e"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["18e"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["18e"]["BackgroundTransparency"] = 1;
G2L["18e"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["18e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["18e"]["Text"] = [[-]];
G2L["18e"]["Name"] = [[EnableMinimize]];
G2L["18e"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableTitle.EnableHelp
G2L["18f"] = Instance.new("TextButton", G2L["18d"]);
G2L["18f"]["TextSize"] = 14;
G2L["18f"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["18f"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["18f"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["18f"]["Size"] = UDim2.new(0, 12, 0, 12);
G2L["18f"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["18f"]["Text"] = [[?]];
G2L["18f"]["Name"] = [[EnableHelp]];
G2L["18f"]["Position"] = UDim2.new(0.02367, 0, 0.15999, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableDeleteRow
G2L["190"] = Instance.new("Frame", G2L["188"]);
G2L["190"]["BorderSizePixel"] = 0;
G2L["190"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["190"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["190"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["190"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["190"]["Name"] = [[EnableDeleteRow]];
G2L["190"]["LayoutOrder"] = 2;
G2L["190"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableDeleteRow.EnableDeleteScript
G2L["191"] = Instance.new("LocalScript", G2L["190"]);
G2L["191"]["Name"] = [[EnableDeleteScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableDeleteRow.EnableDeleteTickBox
G2L["192"] = Instance.new("TextButton", G2L["190"]);
G2L["192"]["TextSize"] = 14;
G2L["192"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["192"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["192"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["192"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["192"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["192"]["Text"] = [[]];
G2L["192"]["Name"] = [[EnableDeleteTickBox]];
G2L["192"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableDeleteRow.EnableDeleteText
G2L["193"] = Instance.new("TextLabel", G2L["190"]);
G2L["193"]["BorderSizePixel"] = 0;
G2L["193"]["TextSize"] = 14;
G2L["193"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["193"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["193"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["193"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["193"]["BackgroundTransparency"] = 1;
G2L["193"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["193"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["193"]["Text"] = [[Detect Delete]];
G2L["193"]["Name"] = [[EnableDeleteText]];
G2L["193"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableToxifyRow
G2L["194"] = Instance.new("Frame", G2L["188"]);
G2L["194"]["BorderSizePixel"] = 0;
G2L["194"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["194"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["194"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["194"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["194"]["Name"] = [[EnableToxifyRow]];
G2L["194"]["LayoutOrder"] = 2;
G2L["194"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableToxifyRow.EnableToxifyScript
G2L["195"] = Instance.new("LocalScript", G2L["194"]);
G2L["195"]["Name"] = [[EnableToxifyScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableToxifyRow.EnableToxifyTickBox
G2L["196"] = Instance.new("TextButton", G2L["194"]);
G2L["196"]["TextSize"] = 14;
G2L["196"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["196"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["196"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["196"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["196"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["196"]["Text"] = [[]];
G2L["196"]["Name"] = [[EnableToxifyTickBox]];
G2L["196"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableToxifyRow.EnableToxifyText
G2L["197"] = Instance.new("TextLabel", G2L["194"]);
G2L["197"]["BorderSizePixel"] = 0;
G2L["197"]["TextSize"] = 14;
G2L["197"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["197"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["197"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["197"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["197"]["BackgroundTransparency"] = 1;
G2L["197"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["197"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["197"]["Text"] = [[Detect Toxify]];
G2L["197"]["Name"] = [[EnableToxifyText]];
G2L["197"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableAnchoredRow
G2L["198"] = Instance.new("Frame", G2L["188"]);
G2L["198"]["BorderSizePixel"] = 0;
G2L["198"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["198"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["198"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["198"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["198"]["Name"] = [[EnableAnchoredRow]];
G2L["198"]["LayoutOrder"] = 2;
G2L["198"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableAnchoredRow.EnableAnchoredScript
G2L["199"] = Instance.new("LocalScript", G2L["198"]);
G2L["199"]["Name"] = [[EnableAnchoredScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableAnchoredRow.EnableAnchoredTickBox
G2L["19a"] = Instance.new("TextButton", G2L["198"]);
G2L["19a"]["TextSize"] = 14;
G2L["19a"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["19a"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["19a"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["19a"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["19a"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["19a"]["Text"] = [[]];
G2L["19a"]["Name"] = [[EnableAnchoredTickBox]];
G2L["19a"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableAnchoredRow.EnableAnchoredText
G2L["19b"] = Instance.new("TextLabel", G2L["198"]);
G2L["19b"]["BorderSizePixel"] = 0;
G2L["19b"]["TextSize"] = 14;
G2L["19b"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["19b"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["19b"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["19b"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["19b"]["BackgroundTransparency"] = 1;
G2L["19b"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["19b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["19b"]["Text"] = [[Detect Unanchoring]];
G2L["19b"]["Name"] = [[EnableAnchoredText]];
G2L["19b"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableSpamRow
G2L["19c"] = Instance.new("Frame", G2L["188"]);
G2L["19c"]["BorderSizePixel"] = 0;
G2L["19c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["19c"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["19c"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["19c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["19c"]["Name"] = [[EnableSpamRow]];
G2L["19c"]["LayoutOrder"] = 2;
G2L["19c"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableSpamRow.EnableSpamScript
G2L["19d"] = Instance.new("LocalScript", G2L["19c"]);
G2L["19d"]["Name"] = [[EnableSpamScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableSpamRow.EnableSpamTickBox
G2L["19e"] = Instance.new("TextButton", G2L["19c"]);
G2L["19e"]["TextSize"] = 14;
G2L["19e"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["19e"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["19e"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["19e"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["19e"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["19e"]["Text"] = [[]];
G2L["19e"]["Name"] = [[EnableSpamTickBox]];
G2L["19e"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableSpamRow.EnableSpamText
G2L["19f"] = Instance.new("TextLabel", G2L["19c"]);
G2L["19f"]["BorderSizePixel"] = 0;
G2L["19f"]["TextSize"] = 14;
G2L["19f"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["19f"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["19f"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["19f"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["19f"]["BackgroundTransparency"] = 1;
G2L["19f"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["19f"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["19f"]["Text"] = [[Detect Build Spam]];
G2L["19f"]["Name"] = [[EnableSpamText]];
G2L["19f"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableExploitsRow
G2L["1a0"] = Instance.new("Frame", G2L["188"]);
G2L["1a0"]["BorderSizePixel"] = 0;
G2L["1a0"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1a0"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1a0"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1a0"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1a0"]["Name"] = [[EnableExploitsRow]];
G2L["1a0"]["LayoutOrder"] = 2;
G2L["1a0"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableExploitsRow.EnableExploitsGrief
G2L["1a1"] = Instance.new("LocalScript", G2L["1a0"]);
G2L["1a1"]["Name"] = [[EnableExploitsGrief]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableExploitsRow.EnableExploitsTickBox
G2L["1a2"] = Instance.new("TextButton", G2L["1a0"]);
G2L["1a2"]["TextSize"] = 14;
G2L["1a2"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1a2"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1a2"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1a2"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1a2"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1a2"]["Text"] = [[]];
G2L["1a2"]["Name"] = [[EnableExploitsTickBox]];
G2L["1a2"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableExploitsRow.EnableExploitsText
G2L["1a3"] = Instance.new("TextLabel", G2L["1a0"]);
G2L["1a3"]["BorderSizePixel"] = 0;
G2L["1a3"]["TextSize"] = 14;
G2L["1a3"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1a3"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1a3"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1a3"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1a3"]["BackgroundTransparency"] = 1;
G2L["1a3"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1a3"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1a3"]["Text"] = [[Detect Grief Aura]];
G2L["1a3"]["Name"] = [[EnableExploitsText]];
G2L["1a3"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup
G2L["1a4"] = Instance.new("Frame", G2L["186"]);
G2L["1a4"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["1a4"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["1a4"]["Size"] = UDim2.new(0, 170, 0, 44);
G2L["1a4"]["Position"] = UDim2.new(0, 0, 0.35278, 0);
G2L["1a4"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1a4"]["Name"] = [[MiscGroup]];
G2L["1a4"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.MiscScript
G2L["1a5"] = Instance.new("LocalScript", G2L["1a4"]);
G2L["1a5"]["Name"] = [[MiscScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.MiscToolTip
G2L["1a6"] = Instance.new("LocalScript", G2L["1a4"]);
G2L["1a6"]["Name"] = [[MiscToolTip]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.UIListLayout
G2L["1a7"] = Instance.new("UIListLayout", G2L["1a4"]);
G2L["1a7"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.MiscAccentColor
G2L["1a8"] = Instance.new("Frame", G2L["1a4"]);
G2L["1a8"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1a8"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["1a8"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["1a8"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["1a8"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["1a8"]["Name"] = [[MiscAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.MiscTitle
G2L["1a9"] = Instance.new("TextLabel", G2L["1a4"]);
G2L["1a9"]["TextWrapped"] = true;
G2L["1a9"]["BorderSizePixel"] = 0;
G2L["1a9"]["TextSize"] = 14;
G2L["1a9"]["TextScaled"] = true;
G2L["1a9"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1a9"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1a9"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["1a9"]["BackgroundTransparency"] = 0.95;
G2L["1a9"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["1a9"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1a9"]["Text"] = [[Misc]];
G2L["1a9"]["LayoutOrder"] = 1;
G2L["1a9"]["Name"] = [[MiscTitle]];
G2L["1a9"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.MiscTitle.MiscMinimize
G2L["1aa"] = Instance.new("TextButton", G2L["1a9"]);
G2L["1aa"]["BorderSizePixel"] = 0;
G2L["1aa"]["TextSize"] = 14;
G2L["1aa"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1aa"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1aa"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1aa"]["BackgroundTransparency"] = 1;
G2L["1aa"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["1aa"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1aa"]["Text"] = [[-]];
G2L["1aa"]["Name"] = [[MiscMinimize]];
G2L["1aa"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.LBoomboxRow
G2L["1ab"] = Instance.new("Frame", G2L["1a4"]);
G2L["1ab"]["BorderSizePixel"] = 0;
G2L["1ab"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1ab"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1ab"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1ab"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ab"]["Name"] = [[LBoomboxRow]];
G2L["1ab"]["LayoutOrder"] = 2;
G2L["1ab"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.LBoomboxRow.LBoomboxScript
G2L["1ac"] = Instance.new("LocalScript", G2L["1ab"]);
G2L["1ac"]["Name"] = [[LBoomboxScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.LBoomboxRow.LBoomboxTickBox
G2L["1ad"] = Instance.new("TextButton", G2L["1ab"]);
G2L["1ad"]["TextSize"] = 14;
G2L["1ad"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ad"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ad"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1ad"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1ad"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1ad"]["Text"] = [[]];
G2L["1ad"]["Name"] = [[LBoomboxTickBox]];
G2L["1ad"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.LBoomboxRow.LBoomboxText
G2L["1ae"] = Instance.new("TextLabel", G2L["1ab"]);
G2L["1ae"]["BorderSizePixel"] = 0;
G2L["1ae"]["TextSize"] = 14;
G2L["1ae"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1ae"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1ae"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1ae"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1ae"]["BackgroundTransparency"] = 1;
G2L["1ae"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1ae"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ae"]["Text"] = [[Detect Loud Boombox]];
G2L["1ae"]["Name"] = [[LBoomboxText]];
G2L["1ae"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide
G2L["1af"] = Instance.new("Frame", G2L["185"]);
G2L["1af"]["BorderSizePixel"] = 0;
G2L["1af"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["1af"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["1af"]["Position"] = UDim2.new(0.51003, 0, 0.00439, 0);
G2L["1af"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1af"]["Name"] = [[TCODetectionRightSide]];
G2L["1af"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.UIListLayout
G2L["1b0"] = Instance.new("UIListLayout", G2L["1af"]);
G2L["1b0"]["Padding"] = UDim.new(0, 7);
G2L["1b0"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup
G2L["1b1"] = Instance.new("Frame", G2L["1af"]);
G2L["1b1"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["1b1"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["1b1"]["Size"] = UDim2.new(0, 170, 0, 137);
G2L["1b1"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1b1"]["Name"] = [[GSettingsGroup]];
G2L["1b1"]["LayoutOrder"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GSettingsScript
G2L["1b2"] = Instance.new("LocalScript", G2L["1b1"]);
G2L["1b2"]["Name"] = [[GSettingsScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GSettingsToolTip
G2L["1b3"] = Instance.new("LocalScript", G2L["1b1"]);
G2L["1b3"]["Name"] = [[GSettingsToolTip]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.UIListLayout
G2L["1b4"] = Instance.new("UIListLayout", G2L["1b1"]);
G2L["1b4"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GSettingsTitle
G2L["1b5"] = Instance.new("TextLabel", G2L["1b1"]);
G2L["1b5"]["TextWrapped"] = true;
G2L["1b5"]["BorderSizePixel"] = 0;
G2L["1b5"]["TextSize"] = 14;
G2L["1b5"]["TextScaled"] = true;
G2L["1b5"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1b5"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1b5"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["1b5"]["BackgroundTransparency"] = 0.95;
G2L["1b5"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["1b5"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1b5"]["Text"] = [[Detection Settings]];
G2L["1b5"]["LayoutOrder"] = 1;
G2L["1b5"]["Name"] = [[GSettingsTitle]];
G2L["1b5"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GSettingsTitle.GSettingsMinimize
G2L["1b6"] = Instance.new("TextButton", G2L["1b5"]);
G2L["1b6"]["BorderSizePixel"] = 0;
G2L["1b6"]["TextSize"] = 14;
G2L["1b6"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1b6"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1b6"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1b6"]["BackgroundTransparency"] = 1;
G2L["1b6"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["1b6"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1b6"]["Text"] = [[-]];
G2L["1b6"]["Name"] = [[GSettingsMinimize]];
G2L["1b6"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GSettingsAccentColor
G2L["1b7"] = Instance.new("Frame", G2L["1b1"]);
G2L["1b7"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1b7"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["1b7"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["1b7"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["1b7"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["1b7"]["Name"] = [[GSettingsAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.TimeFilterRow
G2L["1b8"] = Instance.new("Frame", G2L["1b1"]);
G2L["1b8"]["BorderSizePixel"] = 0;
G2L["1b8"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1b8"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1b8"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1b8"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1b8"]["Name"] = [[TimeFilterRow]];
G2L["1b8"]["LayoutOrder"] = 3;
G2L["1b8"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.TimeFilterRow.TimeFilterText
G2L["1b9"] = Instance.new("TextLabel", G2L["1b8"]);
G2L["1b9"]["BorderSizePixel"] = 0;
G2L["1b9"]["TextSize"] = 14;
G2L["1b9"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1b9"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1b9"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1b9"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1b9"]["BackgroundTransparency"] = 1;
G2L["1b9"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1b9"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1b9"]["Text"] = [[Time Filter]];
G2L["1b9"]["Name"] = [[TimeFilterText]];
G2L["1b9"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.TimeFilterRow.TimeFilterChanger
G2L["1ba"] = Instance.new("TextBox", G2L["1b8"]);
G2L["1ba"]["Name"] = [[TimeFilterChanger]];
G2L["1ba"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1ba"]["TextWrapped"] = true;
G2L["1ba"]["TextSize"] = 14;
G2L["1ba"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1ba"]["TextScaled"] = true;
G2L["1ba"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ba"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1ba"]["PlaceholderText"] = [[(none)]];
G2L["1ba"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["1ba"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["1ba"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1ba"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.TimeFilterRow.TimeFilterChanger.TimeFilterScript
G2L["1bb"] = Instance.new("LocalScript", G2L["1ba"]);
G2L["1bb"]["Name"] = [[TimeFilterScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.TimeFilterRow.TimeFilterHelp
G2L["1bc"] = Instance.new("TextButton", G2L["1b8"]);
G2L["1bc"]["TextSize"] = 14;
G2L["1bc"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["1bc"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["1bc"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["1bc"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1bc"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["1bc"]["Text"] = [[?]];
G2L["1bc"]["Name"] = [[TimeFilterHelp]];
G2L["1bc"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListLabelRow
G2L["1bd"] = Instance.new("Frame", G2L["1b1"]);
G2L["1bd"]["BorderSizePixel"] = 0;
G2L["1bd"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1bd"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1bd"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1bd"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1bd"]["Name"] = [[WListLabelRow]];
G2L["1bd"]["LayoutOrder"] = 20;
G2L["1bd"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListLabelRow.WListLabelScript
G2L["1be"] = Instance.new("LocalScript", G2L["1bd"]);
G2L["1be"]["Name"] = [[WListLabelScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListLabelRow.WListLabelText
G2L["1bf"] = Instance.new("TextLabel", G2L["1bd"]);
G2L["1bf"]["BorderSizePixel"] = 0;
G2L["1bf"]["TextSize"] = 14;
G2L["1bf"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1bf"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1bf"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1bf"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1bf"]["BackgroundTransparency"] = 1;
G2L["1bf"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1bf"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1bf"]["Text"] = [[Whitelist Players:]];
G2L["1bf"]["Name"] = [[WListLabelText]];
G2L["1bf"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListLabelRow.WListLabelHelp
G2L["1c0"] = Instance.new("TextButton", G2L["1bd"]);
G2L["1c0"]["TextSize"] = 14;
G2L["1c0"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["1c0"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["1c0"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["1c0"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1c0"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["1c0"]["Text"] = [[?]];
G2L["1c0"]["Name"] = [[WListLabelHelp]];
G2L["1c0"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListRow
G2L["1c1"] = Instance.new("Frame", G2L["1b1"]);
G2L["1c1"]["BorderSizePixel"] = 0;
G2L["1c1"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1c1"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1c1"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1c1"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1c1"]["Name"] = [[WListRow]];
G2L["1c1"]["LayoutOrder"] = 21;
G2L["1c1"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListRow.WListScript
G2L["1c2"] = Instance.new("LocalScript", G2L["1c1"]);
G2L["1c2"]["Name"] = [[WListScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListRow.WListDropdown
G2L["1c3"] = Instance.new("TextButton", G2L["1c1"]);
G2L["1c3"]["TextSize"] = 14;
G2L["1c3"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1c3"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["1c3"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1c3"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["1c3"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1c3"]["Text"] = [[]];
G2L["1c3"]["Name"] = [[WListDropdown]];
G2L["1c3"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefInGameRow
G2L["1c4"] = Instance.new("Frame", G2L["1b1"]);
G2L["1c4"]["BorderSizePixel"] = 0;
G2L["1c4"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1c4"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1c4"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1c4"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1c4"]["Name"] = [[GriefInGameRow]];
G2L["1c4"]["LayoutOrder"] = 5;
G2L["1c4"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefInGameRow.GriefInGameScript
G2L["1c5"] = Instance.new("LocalScript", G2L["1c4"]);
G2L["1c5"]["Name"] = [[GriefInGameScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefInGameRow.GriefInGameTickBox
G2L["1c6"] = Instance.new("TextButton", G2L["1c4"]);
G2L["1c6"]["TextSize"] = 14;
G2L["1c6"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1c6"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1c6"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1c6"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1c6"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1c6"]["Text"] = [[]];
G2L["1c6"]["Name"] = [[GriefInGameTickBox]];
G2L["1c6"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefInGameRow.GriefInGameText
G2L["1c7"] = Instance.new("TextLabel", G2L["1c4"]);
G2L["1c7"]["BorderSizePixel"] = 0;
G2L["1c7"]["TextSize"] = 14;
G2L["1c7"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1c7"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1c7"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1c7"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1c7"]["BackgroundTransparency"] = 1;
G2L["1c7"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1c7"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1c7"]["Text"] = [[Abandoned Blocks]];
G2L["1c7"]["Name"] = [[GriefInGameText]];
G2L["1c7"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefEnlightenRow
G2L["1c8"] = Instance.new("Frame", G2L["1b1"]);
G2L["1c8"]["BorderSizePixel"] = 0;
G2L["1c8"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1c8"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1c8"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1c8"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1c8"]["Name"] = [[GriefEnlightenRow]];
G2L["1c8"]["LayoutOrder"] = 5;
G2L["1c8"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefEnlightenRow.GriefEnlightenScript
G2L["1c9"] = Instance.new("LocalScript", G2L["1c8"]);
G2L["1c9"]["Name"] = [[GriefEnlightenScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefEnlightenRow.GriefEnlightenTickBox
G2L["1ca"] = Instance.new("TextButton", G2L["1c8"]);
G2L["1ca"]["TextSize"] = 14;
G2L["1ca"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ca"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ca"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1ca"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1ca"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1ca"]["Text"] = [[]];
G2L["1ca"]["Name"] = [[GriefEnlightenTickBox]];
G2L["1ca"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefEnlightenRow.GriefEnlightenText
G2L["1cb"] = Instance.new("TextLabel", G2L["1c8"]);
G2L["1cb"]["BorderSizePixel"] = 0;
G2L["1cb"]["TextSize"] = 14;
G2L["1cb"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1cb"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1cb"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1cb"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1cb"]["BackgroundTransparency"] = 1;
G2L["1cb"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1cb"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1cb"]["Text"] = [[Detect Enlighteners]];
G2L["1cb"]["Name"] = [[GriefEnlightenText]];
G2L["1cb"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.CooldownGriefRow
G2L["1cc"] = Instance.new("Frame", G2L["1b1"]);
G2L["1cc"]["BorderSizePixel"] = 0;
G2L["1cc"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1cc"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1cc"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1cc"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1cc"]["Name"] = [[CooldownGriefRow]];
G2L["1cc"]["LayoutOrder"] = 3;
G2L["1cc"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.CooldownGriefRow.CooldownGriefText
G2L["1cd"] = Instance.new("TextLabel", G2L["1cc"]);
G2L["1cd"]["BorderSizePixel"] = 0;
G2L["1cd"]["TextSize"] = 14;
G2L["1cd"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1cd"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1cd"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1cd"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1cd"]["BackgroundTransparency"] = 1;
G2L["1cd"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1cd"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1cd"]["Text"] = [[Cooldown]];
G2L["1cd"]["Name"] = [[CooldownGriefText]];
G2L["1cd"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.CooldownGriefRow.CooldownGriefChanger
G2L["1ce"] = Instance.new("TextBox", G2L["1cc"]);
G2L["1ce"]["Name"] = [[CooldownGriefChanger]];
G2L["1ce"]["PlaceholderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1ce"]["TextWrapped"] = true;
G2L["1ce"]["TextSize"] = 14;
G2L["1ce"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1ce"]["TextScaled"] = true;
G2L["1ce"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ce"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1ce"]["PlaceholderText"] = [[(30)]];
G2L["1ce"]["Size"] = UDim2.new(0, 46, 0, 16);
G2L["1ce"]["Position"] = UDim2.new(0.69822, 0, 0.16, 0);
G2L["1ce"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1ce"]["Text"] = [[]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.CooldownGriefRow.CooldownGriefChanger.CooldownScript
G2L["1cf"] = Instance.new("LocalScript", G2L["1ce"]);
G2L["1cf"]["Name"] = [[CooldownScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.CooldownGriefRow.CooldownGriefHelp
G2L["1d0"] = Instance.new("TextButton", G2L["1cc"]);
G2L["1d0"]["TextSize"] = 14;
G2L["1d0"]["TextColor3"] = Color3.fromRGB(96, 96, 96);
G2L["1d0"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["1d0"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Bold, Enum.FontStyle.Normal);
G2L["1d0"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1d0"]["BorderColor3"] = Color3.fromRGB(82, 82, 82);
G2L["1d0"]["Text"] = [[?]];
G2L["1d0"]["Name"] = [[CooldownGriefHelp]];
G2L["1d0"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup
G2L["1d1"] = Instance.new("Frame", G2L["1af"]);
G2L["1d1"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["1d1"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["1d1"]["Size"] = UDim2.new(0, 170, 0, 82);
G2L["1d1"]["Position"] = UDim2.new(0, 0, 0.4, 0);
G2L["1d1"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1d1"]["Name"] = [[PunishGroup]];
G2L["1d1"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.PunishScript
G2L["1d2"] = Instance.new("LocalScript", G2L["1d1"]);
G2L["1d2"]["Name"] = [[PunishScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.PunishToolTip
G2L["1d3"] = Instance.new("LocalScript", G2L["1d1"]);
G2L["1d3"]["Name"] = [[PunishToolTip]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.UIListLayout
G2L["1d4"] = Instance.new("UIListLayout", G2L["1d1"]);
G2L["1d4"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.PunishAccentColor
G2L["1d5"] = Instance.new("Frame", G2L["1d1"]);
G2L["1d5"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1d5"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["1d5"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["1d5"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["1d5"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["1d5"]["Name"] = [[PunishAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.PunishTitle
G2L["1d6"] = Instance.new("TextLabel", G2L["1d1"]);
G2L["1d6"]["TextWrapped"] = true;
G2L["1d6"]["BorderSizePixel"] = 0;
G2L["1d6"]["TextSize"] = 14;
G2L["1d6"]["TextScaled"] = true;
G2L["1d6"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1d6"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1d6"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["1d6"]["BackgroundTransparency"] = 0.95;
G2L["1d6"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["1d6"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1d6"]["Text"] = [[Punish]];
G2L["1d6"]["LayoutOrder"] = 1;
G2L["1d6"]["Name"] = [[PunishTitle]];
G2L["1d6"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.PunishTitle.PunishMinimize
G2L["1d7"] = Instance.new("TextButton", G2L["1d6"]);
G2L["1d7"]["BorderSizePixel"] = 0;
G2L["1d7"]["TextSize"] = 14;
G2L["1d7"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1d7"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1d7"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1d7"]["BackgroundTransparency"] = 1;
G2L["1d7"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["1d7"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1d7"]["Text"] = [[-]];
G2L["1d7"]["Name"] = [[PunishMinimize]];
G2L["1d7"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.AlertGriefRow
G2L["1d8"] = Instance.new("Frame", G2L["1d1"]);
G2L["1d8"]["BorderSizePixel"] = 0;
G2L["1d8"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1d8"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1d8"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1d8"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1d8"]["Name"] = [[AlertGriefRow]];
G2L["1d8"]["LayoutOrder"] = 2;
G2L["1d8"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.AlertGriefRow.AlertGriefScript
G2L["1d9"] = Instance.new("LocalScript", G2L["1d8"]);
G2L["1d9"]["Name"] = [[AlertGriefScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.AlertGriefRow.AlertGriefTickBox
G2L["1da"] = Instance.new("TextButton", G2L["1d8"]);
G2L["1da"]["TextSize"] = 14;
G2L["1da"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1da"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1da"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1da"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1da"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1da"]["Text"] = [[]];
G2L["1da"]["Name"] = [[AlertGriefTickBox]];
G2L["1da"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.AlertGriefRow.AlertGriefText
G2L["1db"] = Instance.new("TextLabel", G2L["1d8"]);
G2L["1db"]["BorderSizePixel"] = 0;
G2L["1db"]["TextSize"] = 14;
G2L["1db"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1db"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1db"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1db"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1db"]["BackgroundTransparency"] = 1;
G2L["1db"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1db"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1db"]["Text"] = [[Alert in Chat]];
G2L["1db"]["Name"] = [[AlertGriefText]];
G2L["1db"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.ConfirmGriefRow
G2L["1dc"] = Instance.new("Frame", G2L["1d1"]);
G2L["1dc"]["BorderSizePixel"] = 0;
G2L["1dc"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1dc"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1dc"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1dc"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1dc"]["Name"] = [[ConfirmGriefRow]];
G2L["1dc"]["LayoutOrder"] = 3;
G2L["1dc"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.ConfirmGriefRow.ConfirmGriefScript
G2L["1dd"] = Instance.new("LocalScript", G2L["1dc"]);
G2L["1dd"]["Name"] = [[ConfirmGriefScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.ConfirmGriefRow.ConfirmGriefTickBox
G2L["1de"] = Instance.new("TextButton", G2L["1dc"]);
G2L["1de"]["TextSize"] = 14;
G2L["1de"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1de"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1de"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1de"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1de"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1de"]["Text"] = [[]];
G2L["1de"]["Name"] = [[ConfirmGriefTickBox]];
G2L["1de"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.ConfirmGriefRow.ConfirmGriefText
G2L["1df"] = Instance.new("TextLabel", G2L["1dc"]);
G2L["1df"]["BorderSizePixel"] = 0;
G2L["1df"]["TextSize"] = 14;
G2L["1df"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1df"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1df"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1df"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1df"]["BackgroundTransparency"] = 1;
G2L["1df"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1df"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1df"]["Text"] = [[Confirm Grief]];
G2L["1df"]["Name"] = [[ConfirmGriefText]];
G2L["1df"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.SFXGriefRow
G2L["1e0"] = Instance.new("Frame", G2L["1d1"]);
G2L["1e0"]["BorderSizePixel"] = 0;
G2L["1e0"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1e0"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1e0"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1e0"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1e0"]["Name"] = [[SFXGriefRow]];
G2L["1e0"]["LayoutOrder"] = 3;
G2L["1e0"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.SFXGriefRow.SFXGriefScript
G2L["1e1"] = Instance.new("LocalScript", G2L["1e0"]);
G2L["1e1"]["Name"] = [[SFXGriefScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.SFXGriefRow.SFXGriefTickBox
G2L["1e2"] = Instance.new("TextButton", G2L["1e0"]);
G2L["1e2"]["TextSize"] = 14;
G2L["1e2"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1e2"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1e2"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1e2"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1e2"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1e2"]["Text"] = [[]];
G2L["1e2"]["Name"] = [[SFXGriefTickBox]];
G2L["1e2"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.SFXGriefRow.SFXGriefText
G2L["1e3"] = Instance.new("TextLabel", G2L["1e0"]);
G2L["1e3"]["BorderSizePixel"] = 0;
G2L["1e3"]["TextSize"] = 14;
G2L["1e3"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1e3"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1e3"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1e3"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1e3"]["BackgroundTransparency"] = 1;
G2L["1e3"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1e3"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1e3"]["Text"] = [[Play Alert Sfx]];
G2L["1e3"]["Name"] = [[SFXGriefText]];
G2L["1e3"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage
G2L["1e4"] = Instance.new("ScrollingFrame", G2L["14"]);
G2L["1e4"]["Visible"] = false;
G2L["1e4"]["Active"] = true;
G2L["1e4"]["BorderSizePixel"] = 0;
G2L["1e4"]["Name"] = [[TCOMiscPage]];
G2L["1e4"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["1e4"]["Size"] = UDim2.new(0, 349, 0, 233);
G2L["1e4"]["ScrollBarImageColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1e4"]["Position"] = UDim2.new(0.02329, 0, 0.09398, 0);
G2L["1e4"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1e4"]["ScrollBarThickness"] = 0;
G2L["1e4"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide
G2L["1e5"] = Instance.new("Frame", G2L["1e4"]);
G2L["1e5"]["BorderSizePixel"] = 0;
G2L["1e5"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["1e5"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["1e5"]["Position"] = UDim2.new(0, 0, 0.00439, 0);
G2L["1e5"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1e5"]["Name"] = [[TCOMiscLeftSide]];
G2L["1e5"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.UIListLayout
G2L["1e6"] = Instance.new("UIListLayout", G2L["1e5"]);
G2L["1e6"]["Padding"] = UDim.new(0, 7);
G2L["1e6"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup
G2L["1e7"] = Instance.new("Frame", G2L["1e5"]);
G2L["1e7"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["1e7"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["1e7"]["Size"] = UDim2.new(0, 170, 0, 60);
G2L["1e7"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1e7"]["Name"] = [[MiscGroup]];
G2L["1e7"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.MiscScript
G2L["1e8"] = Instance.new("LocalScript", G2L["1e7"]);
G2L["1e8"]["Name"] = [[MiscScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.UIListLayout
G2L["1e9"] = Instance.new("UIListLayout", G2L["1e7"]);
G2L["1e9"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.MiscAccentColor
G2L["1ea"] = Instance.new("Frame", G2L["1e7"]);
G2L["1ea"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1ea"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["1ea"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["1ea"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["1ea"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["1ea"]["Name"] = [[MiscAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.MiscTitle
G2L["1eb"] = Instance.new("TextLabel", G2L["1e7"]);
G2L["1eb"]["TextWrapped"] = true;
G2L["1eb"]["BorderSizePixel"] = 0;
G2L["1eb"]["TextSize"] = 14;
G2L["1eb"]["TextScaled"] = true;
G2L["1eb"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1eb"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1eb"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["1eb"]["BackgroundTransparency"] = 0.95;
G2L["1eb"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["1eb"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1eb"]["Text"] = [[Random]];
G2L["1eb"]["LayoutOrder"] = 1;
G2L["1eb"]["Name"] = [[MiscTitle]];
G2L["1eb"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.MiscTitle.MiscMinimize
G2L["1ec"] = Instance.new("TextButton", G2L["1eb"]);
G2L["1ec"]["BorderSizePixel"] = 0;
G2L["1ec"]["TextSize"] = 14;
G2L["1ec"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1ec"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1ec"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1ec"]["BackgroundTransparency"] = 1;
G2L["1ec"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["1ec"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ec"]["Text"] = [[-]];
G2L["1ec"]["Name"] = [[MiscMinimize]];
G2L["1ec"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.LJoinXLRow
G2L["1ed"] = Instance.new("Frame", G2L["1e7"]);
G2L["1ed"]["BorderSizePixel"] = 0;
G2L["1ed"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1ed"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1ed"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1ed"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ed"]["Name"] = [[LJoinXLRow]];
G2L["1ed"]["LayoutOrder"] = 10;
G2L["1ed"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.LJoinXLRow.LJoinXLScript
G2L["1ee"] = Instance.new("LocalScript", G2L["1ed"]);
G2L["1ee"]["Name"] = [[LJoinXLScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.LJoinXLRow.LJoinXLButton
G2L["1ef"] = Instance.new("TextButton", G2L["1ed"]);
G2L["1ef"]["TextSize"] = 14;
G2L["1ef"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1ef"]["BackgroundColor3"] = Color3.fromRGB(25, 25, 25);
G2L["1ef"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1ef"]["Size"] = UDim2.new(0, 161, 0, 16);
G2L["1ef"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1ef"]["Text"] = [[Join Largest XL]];
G2L["1ef"]["Name"] = [[LJoinXLButton]];
G2L["1ef"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.NNoclipRow
G2L["1f0"] = Instance.new("Frame", G2L["1e7"]);
G2L["1f0"]["BorderSizePixel"] = 0;
G2L["1f0"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1f0"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1f0"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1f0"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1f0"]["Name"] = [[NNoclipRow]];
G2L["1f0"]["LayoutOrder"] = 2;
G2L["1f0"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.NNoclipRow.NNoclipScript
G2L["1f1"] = Instance.new("LocalScript", G2L["1f0"]);
G2L["1f1"]["Name"] = [[NNoclipScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.NNoclipRow.NNoclipTickBox
G2L["1f2"] = Instance.new("TextButton", G2L["1f0"]);
G2L["1f2"]["TextSize"] = 14;
G2L["1f2"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1f2"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1f2"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1f2"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1f2"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1f2"]["Text"] = [[]];
G2L["1f2"]["Name"] = [[NNoclipTickBox]];
G2L["1f2"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.NNoclipRow.NNoclipText
G2L["1f3"] = Instance.new("TextLabel", G2L["1f0"]);
G2L["1f3"]["BorderSizePixel"] = 0;
G2L["1f3"]["TextSize"] = 14;
G2L["1f3"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1f3"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1f3"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1f3"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1f3"]["BackgroundTransparency"] = 1;
G2L["1f3"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1f3"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1f3"]["Text"] = [[Noclip Bypass]];
G2L["1f3"]["Name"] = [[NNoclipText]];
G2L["1f3"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide
G2L["1f4"] = Instance.new("Frame", G2L["1e4"]);
G2L["1f4"]["BorderSizePixel"] = 0;
G2L["1f4"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 18);
G2L["1f4"]["Size"] = UDim2.new(0, 170, 0, 360);
G2L["1f4"]["Position"] = UDim2.new(0.51003, 0, 0.00439, 0);
G2L["1f4"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1f4"]["Name"] = [[TCOMiscRightSide]];
G2L["1f4"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.UIListLayout
G2L["1f5"] = Instance.new("UIListLayout", G2L["1f4"]);
G2L["1f5"]["Padding"] = UDim.new(0, 7);
G2L["1f5"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup
G2L["1f6"] = Instance.new("Frame", G2L["1f4"]);
G2L["1f6"]["BackgroundColor3"] = Color3.fromRGB(17, 17, 17);
G2L["1f6"]["BorderMode"] = Enum.BorderMode.Middle;
G2L["1f6"]["Size"] = UDim2.new(0, 170, 0, 104);
G2L["1f6"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1f6"]["Name"] = [[AlertGroup]];
G2L["1f6"]["LayoutOrder"] = 2;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.AlertScript
G2L["1f7"] = Instance.new("LocalScript", G2L["1f6"]);
G2L["1f7"]["Name"] = [[AlertScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.UIListLayout
G2L["1f8"] = Instance.new("UIListLayout", G2L["1f6"]);
G2L["1f8"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.AlertAccentColor
G2L["1f9"] = Instance.new("Frame", G2L["1f6"]);
G2L["1f9"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["1f9"]["AutomaticSize"] = Enum.AutomaticSize.X;
G2L["1f9"]["Size"] = UDim2.new(0, 169, 0, 2);
G2L["1f9"]["Position"] = UDim2.new(0.00294, 0, 0.00658, 0);
G2L["1f9"]["BorderColor3"] = Color3.fromRGB(0, 92, 189);
G2L["1f9"]["Name"] = [[AlertAccentColor]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.AlertTitle
G2L["1fa"] = Instance.new("TextLabel", G2L["1f6"]);
G2L["1fa"]["TextWrapped"] = true;
G2L["1fa"]["BorderSizePixel"] = 0;
G2L["1fa"]["TextSize"] = 14;
G2L["1fa"]["TextScaled"] = true;
G2L["1fa"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1fa"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1fa"]["TextColor3"] = Color3.fromRGB(190, 190, 190);
G2L["1fa"]["BackgroundTransparency"] = 0.95;
G2L["1fa"]["Size"] = UDim2.new(0, 169, 0, 15);
G2L["1fa"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1fa"]["Text"] = [[Chat Alerts]];
G2L["1fa"]["LayoutOrder"] = 1;
G2L["1fa"]["Name"] = [[AlertTitle]];
G2L["1fa"]["Position"] = UDim2.new(0.00294, 0, 0.03289, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.AlertTitle.AlertMinimize
G2L["1fb"] = Instance.new("TextButton", G2L["1fa"]);
G2L["1fb"]["BorderSizePixel"] = 0;
G2L["1fb"]["TextSize"] = 14;
G2L["1fb"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1fb"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1fb"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1fb"]["BackgroundTransparency"] = 1;
G2L["1fb"]["Size"] = UDim2.new(0, 20, 0, 15);
G2L["1fb"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1fb"]["Text"] = [[-]];
G2L["1fb"]["Name"] = [[AlertMinimize]];
G2L["1fb"]["Position"] = UDim2.new(0.88166, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.DonateSpyRow
G2L["1fc"] = Instance.new("Frame", G2L["1f6"]);
G2L["1fc"]["BorderSizePixel"] = 0;
G2L["1fc"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1fc"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1fc"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1fc"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1fc"]["Name"] = [[DonateSpyRow]];
G2L["1fc"]["LayoutOrder"] = 6;
G2L["1fc"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.DonateSpyRow.DonateSpyTickBox
G2L["1fd"] = Instance.new("TextButton", G2L["1fc"]);
G2L["1fd"]["TextSize"] = 14;
G2L["1fd"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1fd"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1fd"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1fd"]["ZIndex"] = 5;
G2L["1fd"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["1fd"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["1fd"]["Text"] = [[]];
G2L["1fd"]["Name"] = [[DonateSpyTickBox]];
G2L["1fd"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.DonateSpyRow.DonateSpyText
G2L["1fe"] = Instance.new("TextLabel", G2L["1fc"]);
G2L["1fe"]["BorderSizePixel"] = 0;
G2L["1fe"]["TextSize"] = 14;
G2L["1fe"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["1fe"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1fe"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["1fe"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1fe"]["BackgroundTransparency"] = 1;
G2L["1fe"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["1fe"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1fe"]["Text"] = [[Alert Donations]];
G2L["1fe"]["Name"] = [[DonateSpyText]];
G2L["1fe"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.MutedSpyRow
G2L["1ff"] = Instance.new("Frame", G2L["1f6"]);
G2L["1ff"]["BorderSizePixel"] = 0;
G2L["1ff"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["1ff"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["1ff"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["1ff"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["1ff"]["Name"] = [[MutedSpyRow]];
G2L["1ff"]["LayoutOrder"] = 6;
G2L["1ff"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.MutedSpyRow.MutedSpyTickBox
G2L["200"] = Instance.new("TextButton", G2L["1ff"]);
G2L["200"]["TextSize"] = 14;
G2L["200"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["200"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["200"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["200"]["ZIndex"] = 5;
G2L["200"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["200"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["200"]["Text"] = [[]];
G2L["200"]["Name"] = [[MutedSpyTickBox]];
G2L["200"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.MutedSpyRow.MutedSpyText
G2L["201"] = Instance.new("TextLabel", G2L["1ff"]);
G2L["201"]["BorderSizePixel"] = 0;
G2L["201"]["TextSize"] = 14;
G2L["201"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["201"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["201"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["201"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["201"]["BackgroundTransparency"] = 1;
G2L["201"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["201"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["201"]["Text"] = [[Alert Muted Msgs]];
G2L["201"]["Name"] = [[MutedSpyText]];
G2L["201"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.EnlightenLogRow
G2L["202"] = Instance.new("Frame", G2L["1f6"]);
G2L["202"]["BorderSizePixel"] = 0;
G2L["202"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["202"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["202"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["202"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["202"]["Name"] = [[EnlightenLogRow]];
G2L["202"]["LayoutOrder"] = 7;
G2L["202"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.EnlightenLogRow.EnlightenLogScript
G2L["203"] = Instance.new("LocalScript", G2L["202"]);
G2L["203"]["Name"] = [[EnlightenLogScript]];


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.EnlightenLogRow.EnlightenLogTickBox
G2L["204"] = Instance.new("TextButton", G2L["202"]);
G2L["204"]["TextSize"] = 14;
G2L["204"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["204"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["204"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["204"]["ZIndex"] = 5;
G2L["204"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["204"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["204"]["Text"] = [[]];
G2L["204"]["Name"] = [[EnlightenLogTickBox]];
G2L["204"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.EnlightenLogRow.EnlightenLogText
G2L["205"] = Instance.new("TextLabel", G2L["202"]);
G2L["205"]["BorderSizePixel"] = 0;
G2L["205"]["TextSize"] = 14;
G2L["205"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["205"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["205"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["205"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["205"]["BackgroundTransparency"] = 1;
G2L["205"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["205"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["205"]["Text"] = [[Alert Enlighteners]];
G2L["205"]["Name"] = [[EnlightenLogText]];
G2L["205"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.ArkSpyRow
G2L["206"] = Instance.new("Frame", G2L["1f6"]);
G2L["206"]["BorderSizePixel"] = 0;
G2L["206"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["206"]["Size"] = UDim2.new(0, 169, 0, 20);
G2L["206"]["Position"] = UDim2.new(0.00294, 0, 0.175, 0);
G2L["206"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["206"]["Name"] = [[ArkSpyRow]];
G2L["206"]["LayoutOrder"] = 6;
G2L["206"]["BackgroundTransparency"] = 1;


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.ArkSpyRow.ArkSpyTickBox
G2L["207"] = Instance.new("TextButton", G2L["206"]);
G2L["207"]["TextSize"] = 14;
G2L["207"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["207"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["207"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["207"]["ZIndex"] = 5;
G2L["207"]["Size"] = UDim2.new(0, 16, 0, 16);
G2L["207"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["207"]["Text"] = [[]];
G2L["207"]["Name"] = [[ArkSpyTickBox]];
G2L["207"]["Position"] = UDim2.new(0.02367, 0, 0.16, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.ArkSpyRow.ArkSpyText
G2L["208"] = Instance.new("TextLabel", G2L["206"]);
G2L["208"]["BorderSizePixel"] = 0;
G2L["208"]["TextSize"] = 14;
G2L["208"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["208"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["208"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["208"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["208"]["BackgroundTransparency"] = 1;
G2L["208"]["Size"] = UDim2.new(0, 88, 0, 20);
G2L["208"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["208"]["Text"] = [[Alert Hidden Cmds]];
G2L["208"]["Name"] = [[ArkSpyText]];
G2L["208"]["Position"] = UDim2.new(0.17751, 0, 0, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.OPENCONSOLE
G2L["209"] = Instance.new("TextButton", G2L["14"]);
G2L["209"]["TextWrapped"] = true;
G2L["209"]["TextSize"] = 12;
G2L["209"]["TextScaled"] = true;
G2L["209"]["TextColor3"] = Color3.fromRGB(125, 125, 125);
G2L["209"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["209"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["209"]["Size"] = UDim2.new(0, 60, 0, 25);
G2L["209"]["LayoutOrder"] = 5;
G2L["209"]["BorderColor3"] = Color3.fromRGB(58, 58, 58);
G2L["209"]["Text"] = [[OPEN CONSOLE]];
G2L["209"]["Name"] = [[OPENCONSOLE]];
G2L["209"]["Position"] = UDim2.new(-0.186, 0, 0.906, 0);


-- StarterGui.ScreenGui.MainFrame.ContentArea.OPENCONSOLE.LocalScript
G2L["20a"] = Instance.new("LocalScript", G2L["209"]);



-- StarterGui.ScreenGui.MainFrame.TopBar
G2L["20b"] = Instance.new("Frame", G2L["8"]);
G2L["20b"]["BorderSizePixel"] = 0;
G2L["20b"]["BackgroundColor3"] = Color3.fromRGB(23, 23, 23);
G2L["20b"]["Size"] = UDim2.new(0, 442, 0, 25);
G2L["20b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["20b"]["Name"] = [[TopBar]];


-- StarterGui.ScreenGui.MainFrame.TopBar.MinimizeDragScript
G2L["20c"] = Instance.new("LocalScript", G2L["20b"]);
G2L["20c"]["Name"] = [[MinimizeDragScript]];


-- StarterGui.ScreenGui.MainFrame.TopBar.TITLEScript
G2L["20d"] = Instance.new("LocalScript", G2L["20b"]);
G2L["20d"]["Name"] = [[TITLEScript]];


-- StarterGui.ScreenGui.MainFrame.TopBar.MinimizeGUI
G2L["20e"] = Instance.new("TextButton", G2L["20b"]);
G2L["20e"]["BorderSizePixel"] = 0;
G2L["20e"]["TextSize"] = 14;
G2L["20e"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["20e"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["20e"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["20e"]["BackgroundTransparency"] = 1;
G2L["20e"]["Size"] = UDim2.new(0, 25, 0, 25);
G2L["20e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["20e"]["Text"] = [[-]];
G2L["20e"]["Name"] = [[MinimizeGUI]];
G2L["20e"]["Position"] = UDim2.new(0.99919, -25, 0, 0);


-- StarterGui.ScreenGui.MainFrame.TopBar.TITLE
G2L["20f"] = Instance.new("TextLabel", G2L["20b"]);
G2L["20f"]["BorderSizePixel"] = 0;
G2L["20f"]["TextSize"] = 12;
G2L["20f"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["20f"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["20f"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["20f"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["20f"]["BackgroundTransparency"] = 1;
G2L["20f"]["RichText"] = true;
G2L["20f"]["Size"] = UDim2.new(0, 200, 0, 25);
G2L["20f"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["20f"]["Text"] = [[AGAR WARE | 60 fps | players: 1]];
G2L["20f"]["Name"] = [[TITLE]];


-- StarterGui.ScreenGui.KeybindFrame
G2L["210"] = Instance.new("Frame", G2L["1"]);
G2L["210"]["BorderSizePixel"] = 0;
G2L["210"]["BackgroundColor3"] = Color3.fromRGB(255, 0, 0);
G2L["210"]["Size"] = UDim2.new(0, 80, 0, 88);
G2L["210"]["Position"] = UDim2.new(0.70017, 0, 0.01856, 0);
G2L["210"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["210"]["Name"] = [[KeybindFrame]];
G2L["210"]["BackgroundTransparency"] = 0.9;


-- StarterGui.ScreenGui.KeybindFrame.KeybindHideScript
G2L["211"] = Instance.new("LocalScript", G2L["210"]);
G2L["211"]["Name"] = [[KeybindHideScript]];


-- StarterGui.ScreenGui.KeybindFrame.KeybindLockScript
G2L["212"] = Instance.new("LocalScript", G2L["210"]);
G2L["212"]["Name"] = [[KeybindLockScript]];


-- StarterGui.ScreenGui.KeybindFrame.KeybindDragScript
G2L["213"] = Instance.new("LocalScript", G2L["210"]);
G2L["213"]["Name"] = [[KeybindDragScript]];


-- StarterGui.ScreenGui.KeybindFrame.HideGUI
G2L["214"] = Instance.new("TextButton", G2L["210"]);
G2L["214"]["TextSize"] = 14;
G2L["214"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["214"]["BackgroundColor3"] = Color3.fromRGB(23, 23, 23);
G2L["214"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["214"]["Size"] = UDim2.new(0, 80, 0, 25);
G2L["214"]["LayoutOrder"] = 1;
G2L["214"]["BorderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["214"]["Text"] = [[Hide GUI]];
G2L["214"]["Name"] = [[HideGUI]];


-- StarterGui.ScreenGui.KeybindFrame.UIListLayout
G2L["215"] = Instance.new("UIListLayout", G2L["210"]);
G2L["215"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.KeybindFrame.LockGUI
G2L["216"] = Instance.new("TextButton", G2L["210"]);
G2L["216"]["TextSize"] = 14;
G2L["216"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["216"]["BackgroundColor3"] = Color3.fromRGB(23, 23, 23);
G2L["216"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["216"]["Size"] = UDim2.new(0, 80, 0, 25);
G2L["216"]["LayoutOrder"] = 2;
G2L["216"]["BorderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["216"]["Text"] = [[Lock GUI]];
G2L["216"]["Name"] = [[LockGUI]];
G2L["216"]["Position"] = UDim2.new(0, 0, 0.28409, 0);


-- StarterGui.ScreenGui.KeybindFrame.Version
G2L["217"] = Instance.new("TextButton", G2L["210"]);
G2L["217"]["TextWrapped"] = true;
G2L["217"]["TextSize"] = 14;
G2L["217"]["TextScaled"] = true;
G2L["217"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["217"]["BackgroundColor3"] = Color3.fromRGB(23, 23, 23);
G2L["217"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["217"]["Size"] = UDim2.new(0, 80, 0, 10);
G2L["217"]["LayoutOrder"] = 20;
G2L["217"]["BorderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["217"]["Text"] = [[GUI Version:]];
G2L["217"]["Name"] = [[Version]];
G2L["217"]["Position"] = UDim2.new(0, 0, 0.79545, 0);


-- StarterGui.ScreenGui.KeybindFrame.VersionNumber
G2L["218"] = Instance.new("TextButton", G2L["210"]);
G2L["218"]["TextWrapped"] = true;
G2L["218"]["TextSize"] = 14;
G2L["218"]["TextScaled"] = true;
G2L["218"]["TextColor3"] = Color3.fromRGB(0, 124, 255);
G2L["218"]["BackgroundColor3"] = Color3.fromRGB(23, 23, 23);
G2L["218"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["218"]["Size"] = UDim2.new(0, 80, 0, 15);
G2L["218"]["LayoutOrder"] = 30;
G2L["218"]["BorderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["218"]["Text"] = [[10]];
G2L["218"]["Name"] = [[VersionNumber]];
G2L["218"]["Position"] = UDim2.new(0, 0, 1.25, 0);


-- StarterGui.ScreenGui.KeybindFrame.DragGUI
G2L["219"] = Instance.new("TextButton", G2L["210"]);
G2L["219"]["TextSize"] = 14;
G2L["219"]["TextColor3"] = Color3.fromRGB(154, 154, 154);
G2L["219"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["219"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["219"]["Size"] = UDim2.new(0, 80, 0, 25);
G2L["219"]["LayoutOrder"] = 3;
G2L["219"]["BorderColor3"] = Color3.fromRGB(0, 124, 255);
G2L["219"]["Text"] = [[DRAG]];
G2L["219"]["Name"] = [[DragGUI]];


-- StarterGui.ScreenGui.ConfirmGriefFrame
G2L["21a"] = Instance.new("Frame", G2L["1"]);
G2L["21a"]["Visible"] = false;
G2L["21a"]["BorderSizePixel"] = 0;
G2L["21a"]["BackgroundColor3"] = Color3.fromRGB(0, 84, 173);
G2L["21a"]["Size"] = UDim2.new(0, 135, 0, 161);
G2L["21a"]["Position"] = UDim2.new(0.66486, 0, 0.2513, 0);
G2L["21a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["21a"]["Name"] = [[ConfirmGriefFrame]];


-- StarterGui.ScreenGui.ConfirmGriefFrame.UIListLayout
G2L["21b"] = Instance.new("UIListLayout", G2L["21a"]);
G2L["21b"]["SortOrder"] = Enum.SortOrder.LayoutOrder;


-- StarterGui.ScreenGui.ConfirmGriefFrame.FlingConfirm
G2L["21c"] = Instance.new("TextButton", G2L["21a"]);
G2L["21c"]["TextSize"] = 16;
G2L["21c"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["21c"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["21c"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["21c"]["Size"] = UDim2.new(0, 135, 0, 47);
G2L["21c"]["LayoutOrder"] = 3;
G2L["21c"]["BorderColor3"] = Color3.fromRGB(255, 255, 255);
G2L["21c"]["Text"] = [[FLING PLAYER]];
G2L["21c"]["Name"] = [[FlingConfirm]];


-- StarterGui.ScreenGui.ConfirmGriefFrame.AlertConfirm
G2L["21d"] = Instance.new("TextButton", G2L["21a"]);
G2L["21d"]["TextWrapped"] = true;
G2L["21d"]["TextSize"] = 16;
G2L["21d"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["21d"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
G2L["21d"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["21d"]["Size"] = UDim2.new(0, 135, 0, 47);
G2L["21d"]["LayoutOrder"] = 3;
G2L["21d"]["BorderColor3"] = Color3.fromRGB(255, 255, 255);
G2L["21d"]["Text"] = [[ALERT IN CHAT]];
G2L["21d"]["Name"] = [[AlertConfirm]];
G2L["21d"]["Position"] = UDim2.new(0, 0, 0.58065, 0);


-- StarterGui.ScreenGui.ConfirmGriefFrame.ConfirmTimer
G2L["21e"] = Instance.new("TextLabel", G2L["21a"]);
G2L["21e"]["TextWrapped"] = true;
G2L["21e"]["TextSize"] = 14;
G2L["21e"]["TextScaled"] = true;
G2L["21e"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["21e"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["21e"]["TextColor3"] = Color3.fromRGB(0, 45, 86);
G2L["21e"]["Size"] = UDim2.new(0, 135, 0, 14);
G2L["21e"]["BorderColor3"] = Color3.fromRGB(0, 67, 132);
G2L["21e"]["Text"] = [[(20.0)]];
G2L["21e"]["LayoutOrder"] = 10;
G2L["21e"]["Name"] = [[ConfirmTimer]];
G2L["21e"]["Position"] = UDim2.new(0, 0, 0.0452, 0);


-- StarterGui.ScreenGui.ConfirmGriefFrame.FalseAlertConfirm
G2L["21f"] = Instance.new("TextButton", G2L["21a"]);
G2L["21f"]["TextWrapped"] = true;
G2L["21f"]["TextSize"] = 20;
G2L["21f"]["TextScaled"] = true;
G2L["21f"]["TextColor3"] = Color3.fromRGB(255, 0, 0);
G2L["21f"]["BackgroundColor3"] = Color3.fromRGB(58, 0, 0);
G2L["21f"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["21f"]["Size"] = UDim2.new(0, 135, 0, 23);
G2L["21f"]["LayoutOrder"] = 4;
G2L["21f"]["BorderColor3"] = Color3.fromRGB(255, 255, 255);
G2L["21f"]["Text"] = [[FALSE ALERT & CANCEL]];
G2L["21f"]["Name"] = [[FalseAlertConfirm]];
G2L["21f"]["Position"] = UDim2.new(0, 0, 0.97191, 0);


-- StarterGui.ScreenGui.ConfirmGriefFrame.CancelConfirm
G2L["220"] = Instance.new("TextButton", G2L["21a"]);
G2L["220"]["TextWrapped"] = true;
G2L["220"]["TextSize"] = 16;
G2L["220"]["TextColor3"] = Color3.fromRGB(255, 0, 0);
G2L["220"]["BackgroundColor3"] = Color3.fromRGB(58, 0, 0);
G2L["220"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["220"]["Size"] = UDim2.new(0, 135, 0, 23);
G2L["220"]["LayoutOrder"] = 4;
G2L["220"]["BorderColor3"] = Color3.fromRGB(255, 255, 255);
G2L["220"]["Text"] = [[EXIT]];
G2L["220"]["Name"] = [[CancelConfirm]];
G2L["220"]["Position"] = UDim2.new(0, 0, 0.97191, 0);


-- StarterGui.ScreenGui.ConfirmGriefFrame.GriefTargetLabel
G2L["221"] = Instance.new("TextLabel", G2L["21a"]);
G2L["221"]["TextWrapped"] = true;
G2L["221"]["TextSize"] = 14;
G2L["221"]["TextScaled"] = true;
G2L["221"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["221"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["221"]["TextColor3"] = Color3.fromRGB(0, 45, 86);
G2L["221"]["Size"] = UDim2.new(0, 135, 0, 28);
G2L["221"]["BorderColor3"] = Color3.fromRGB(0, 67, 132);
G2L["221"]["Text"] = [[POSSIBLE GRIEF: Username]];
G2L["221"]["Name"] = [[GriefTargetLabel]];


-- StarterGui.ScreenGui.ConfirmGriefFrame.PauseConfirm
G2L["222"] = Instance.new("TextButton", G2L["21a"]);
G2L["222"]["TextWrapped"] = true;
G2L["222"]["TextSize"] = 16;
G2L["222"]["TextColor3"] = Color3.fromRGB(0, 45, 86);
G2L["222"]["BackgroundColor3"] = Color3.fromRGB(0, 124, 255);
G2L["222"]["FontFace"] = Font.new([[rbxasset://fonts/families/Inconsolata.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["222"]["Size"] = UDim2.new(0, 135, 0, 24);
G2L["222"]["LayoutOrder"] = 9;
G2L["222"]["BorderColor3"] = Color3.fromRGB(0, 67, 132);
G2L["222"]["Text"] = [[PAUSE TIMER]];
G2L["222"]["Name"] = [[PauseConfirm]];
G2L["222"]["Position"] = UDim2.new(0, 0, 0.75776, 0);


-- StarterGui.ScreenGui.Webhook
G2L["223"] = Instance.new("Folder", G2L["1"]);
G2L["223"]["Name"] = [[Webhook]];


-- StarterGui.ScreenGui.Webhook.UniversalLog
G2L["224"] = Instance.new("LocalScript", G2L["223"]);
G2L["224"]["Name"] = [[UniversalLog]];


-- StarterGui.ScreenGui.Webhook.TCOLog
G2L["225"] = Instance.new("LocalScript", G2L["223"]);
G2L["225"]["Name"] = [[TCOLog]];


-- StarterGui.ScreenGui.Webhook.IngameLog
G2L["226"] = Instance.new("LocalScript", G2L["223"]);
G2L["226"]["Name"] = [[IngameLog]];


-- StarterGui.ScreenGui.Webhook.PealzLog
G2L["227"] = Instance.new("LocalScript", G2L["223"]);
G2L["227"]["Name"] = [[PealzLog]];


-- StarterGui.ScreenGui.MoveToCorGui
local function C_2()
local script = G2L["2"];
	local CoreGui = game:GetService("CoreGui")
	local RunService = game:GetService("RunService")
	
	-- Only move to CoreGui when running in an actual game, not Studio
	if not RunService:IsStudio() then
		local gui = script.Parent
		task.wait()
		gui.Parent = CoreGui
	end
end;
task.spawn(C_2);
-- StarterGui.ScreenGui.ChatQueue
local function C_3()
local script = G2L["3"];
	-- ChatQueue
	-- All scripts use shared.sendChat(message) instead of sending directly
	-- This queues messages and sends them one at a time with a small gap
	
	local TextChatService = game:GetService("TextChatService")
	
	local queue = {}
	local isProcessing = false
	
	local function processQueue()
		if isProcessing then return end
		if #queue == 0 then return end
		isProcessing = true
	
		while #queue > 0 do
			local message = table.remove(queue, 1)
			if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
				TextChatService.TextChannels.RBXGeneral:SendAsync(message)
			else
				game:GetService("ReplicatedStorage")
					.DefaultChatSystemChatEvents
					.SayMessageRequest
					:FireServer(message, "All")
			end
			task.wait(0.3)
		end
	
		isProcessing = false
	end
	
	shared.sendChat = function(message)
		table.insert(queue, message)
		task.spawn(processQueue)
	end
end;
task.spawn(C_3);
-- StarterGui.ScreenGui.WelcomeMessage
local function C_4()
local script = G2L["4"];
	local TextChatService = game:GetService("TextChatService")
	local Players = game:GetService("Players")
	local TCO_PLACE_IDS = {
		11137575513,
		12943245078,
		12943247001,
		108097274488844,
	}
	task.defer(function()
		local systemChannel = TextChatService.TextChannels:FindFirstChild("RBXSystem")
		if systemChannel then
			systemChannel:DisplaySystemMessage("Thank you for using my script - agarvvoid")
			task.wait(0.1)
			local currentPlaceId = game.PlaceId
			local isValidPlace = false
			for _, id in ipairs(TCO_PLACE_IDS) do
				if currentPlaceId == id then
					isValidPlace = true
					break
				end
			end
			if isValidPlace then
				local topPlayer = nil
				local topTime = -1
				for _, player in ipairs(Players:GetPlayers()) do
					local leaderstats = player:FindFirstChild("leaderstats")
					if leaderstats then
						local timeVal = leaderstats:FindFirstChild("Time")
						if timeVal and timeVal.Value > topTime then
							topTime = timeVal.Value
							topPlayer = player
						end
					end
				end
				if topPlayer then
					local localPlayer = Players.LocalPlayer
					local isFriend = false
					local success, result = pcall(function()
						return localPlayer:IsFriendsWith(topPlayer.UserId)
					end)
					if success then
						isFriend = result
					end
					local displayName = topPlayer.DisplayName
					local label = isFriend and (displayName .. " (Friend)") or displayName
					systemChannel:DisplaySystemMessage("Current Admin: " .. label)
				end
			end
		end
	end)
end;
task.spawn(C_4);
-- StarterGui.ScreenGui.UnloadBLACKLIST
local function C_5()
local script = G2L["5"];
	do
		local Players = game:GetService("Players")
		local LocalPlayer = Players.LocalPlayer
	
		-- The ID you want to hide from seeing you use the script
		local BLOCKED_ID = 8615658150
	
		-- Exception list: these users will NOT trigger the unload
		local EXCEPTIONS = {
			9763171531, -- pealz
			10304502709, -- mental
			2581030028, -- valtiel
			10340049663, -- evilguy
			10587072700, -- agarv alt
			574381128, -- agarv
		}
	
		local function isException(userId)
			for _, id in ipairs(EXCEPTIONS) do
				if id == userId then
					return true
				end
			end
			return false
		end
	
		local function isBlocked(player)
			if isException(player.UserId) then
				return false
			end
			return player.UserId == BLOCKED_ID and player ~= LocalPlayer
		end
	
		local function unloadScript()
			pcall(function()
				local screenGui = LocalPlayer.PlayerGui:FindFirstChild("ScreenGui")
				if screenGui then
					screenGui:Destroy()
					print("Script unloaded: Blacklisted user detected.")
				end
			end)
		end
	
		-- Check everyone currently in the server
		for _, player in ipairs(Players:GetPlayers()) do
			if isBlocked(player) then 
				unloadScript() 
				return 
			end
		end
	
		-- Check for anyone who joins later
		Players.PlayerAdded:Connect(function(player)
			if isBlocked(player) then 
				unloadScript() 
			end
		end)
	end
end;
task.spawn(C_5);
-- StarterGui.ScreenGui.AGARVOWNERLABEL
local function C_6()
local script = G2L["6"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	local OWNER_IDS = {
		574381128,   -- main
		10587072700, -- alt 1
		8308247571,  -- alt 2
	}
	
	local function isOwner(player)
		for _, id in ipairs(OWNER_IDS) do
			if player.UserId == id then
				return true
			end
		end
		return false
	end
	
	local INVIS_NAMES = {
		"Ryansocooolsmith",
		"agarvvoid",
		"agarv_bbc",
	}
	
	local function anyOwnerInvisible()
		for _, player in ipairs(Players:GetPlayers()) do
			for _, name in ipairs(INVIS_NAMES) do
				if player.Name == name and player.Character then
					local torso = player.Character:FindFirstChild("UpperTorso")
					if torso and torso.Transparency == 1 then
						return true
					end
				end
			end
		end
		return false
	end
	
	local function addTag(player)
		if not isOwner(player) then return end
	
		local function applyToChar(char)
			if not char then return end
			print("[DEBUG] anyOwnerInvisible =", anyOwnerInvisible())
			if anyOwnerInvisible() then
				local existing = char:FindFirstChild("AgarWareTag")
				if existing then existing:Destroy() end
				-- also check hrp
				local hrp = char:FindFirstChild("HumanoidRootPart")
				if hrp then
					local tag = hrp:FindFirstChild("AgarWareTag")
					if tag then tag:Destroy() end
				end
				return
			end
			-- Remove existing tag if any
			local existing = char:FindFirstChild("AgarWareTag")
			if existing then existing:Destroy() end
	
			local hrp = char:WaitForChild("HumanoidRootPart", 10)
			if not hrp then return end
	
			local billboard = Instance.new("BillboardGui")
			billboard.Name = "AgarWareTag"
			billboard.Size = UDim2.new(0, 200, 0, 40)
			billboard.StudsOffset = Vector3.new(0, 3, 0)
			billboard.AlwaysOnTop = true
			billboard.ResetOnSpawn = false
			billboard.Parent = hrp
	
			local label = Instance.new("TextLabel", billboard)
			label.Size = UDim2.new(1, 0, 1, 0)
			label.BackgroundTransparency = 1
			label.Text = "AGAR WARE OWNER"
			label.TextColor3 = Color3.fromRGB(0, 140, 255)
			label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
			label.TextStrokeTransparency = 0
			label.Font = Enum.Font.Code
			label.TextScaled = true
	
			local cam = workspace.CurrentCamera
			local conn
			conn = game:GetService("RunService").RenderStepped:Connect(function()
				if not billboard.Parent then
					conn:Disconnect()
					return
				end
				local dist = (cam.CFrame.Position - hrp.Position).Magnitude
				local scale = math.clamp(50 / dist, 1/3, 1)
				billboard.Size = UDim2.new(0, 200 * scale, 0, 40 * scale)
			end)
		end
	
		if player.Character then
			applyToChar(player.Character)
		end
	
		player.CharacterAdded:Connect(applyToChar)
	end
	
	-- Check all current players
	for _, player in ipairs(Players:GetPlayers()) do
		addTag(player)
	end
	
	
	local function watchInvisPlayer(player)
		for _, name in ipairs(INVIS_NAMES) do
			if player.Name == name then
				local function watchChar(char)
					if not char then return end
					local torso = char:WaitForChild("UpperTorso", 5)
					if not torso then return end
					torso:GetPropertyChangedSignal("Transparency"):Connect(function()
						-- Reapply tags for all owners when visibility changes
						for _, p in ipairs(Players:GetPlayers()) do
							if isOwner(p) and p.Character then
								addTag(p)
							end
						end
					end)
				end
				if player.Character then watchChar(player.Character) end
				player.CharacterAdded:Connect(watchChar)
			end
		end
	end
	
	for _, player in ipairs(Players:GetPlayers()) do
		watchInvisPlayer(player)
	end
	Players.PlayerAdded:Connect(watchInvisPlayer)
	
	-- Check players who join later
	Players.PlayerAdded:Connect(addTag)
end;
task.spawn(C_6);
-- StarterGui.ScreenGui.GriefDetectorConsole
local function C_7()
local script = G2L["7"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	local ScreenGui = script.Parent
	
	--------------------------------------------------
	-- ENSURE ALL SHARED EVENTS EXIST FIRST
	--------------------------------------------------
	
	shared.GriefEvent         = shared.GriefEvent         or Instance.new("BindableEvent")
	shared.DeleteEvent        = shared.DeleteEvent        or Instance.new("BindableEvent")
	shared.ToxifyEvent        = shared.ToxifyEvent        or Instance.new("BindableEvent")
	shared.UnanchorEvent      = shared.UnanchorEvent      or Instance.new("BindableEvent")
	shared.BuildEvent         = shared.BuildEvent         or Instance.new("BindableEvent")
	shared.ExploitGriefEvent  = shared.ExploitGriefEvent  or Instance.new("BindableEvent")
	shared.BoomboxLoudEvent   = shared.BoomboxLoudEvent   or Instance.new("BindableEvent")
	shared.DeleteGriefEvent   = shared.DeleteGriefEvent   or Instance.new("BindableEvent")
	shared.ToxifyGriefEvent   = shared.ToxifyGriefEvent   or Instance.new("BindableEvent")
	shared.UnanchorGriefEvent = shared.UnanchorGriefEvent or Instance.new("BindableEvent")
	shared.BuildGriefEvent    = shared.BuildGriefEvent    or Instance.new("BindableEvent")
	
	--------------------------------------------------
	-- DETECTION ATTRIBUTES
	--------------------------------------------------
	
	local DETECTION_ATTRS = {
		"DeleteLogsEnabled",
		"ToxifyLogsEnabled",
		"AnchoredLogsEnabled",
		"SpamLogsEnabled",
		"ExploitsLogsEnabled",
	}
	
	--------------------------------------------------
	-- SETTINGS - DELETE/TOXIFY/UNANCHOR CANDIDATE
	--------------------------------------------------
	
	local MIN_DELAY    = 0.06
	local MAX_DELAY    = 0.15
	local IDEAL_DELAY  = 0.1
	local MAX_DISTANCE = 30
	
	--------------------------------------------------
	-- SETTINGS - AURA ONLY
	--------------------------------------------------
	
	local AURA_MIN_DELAY       = 0.0
	local AURA_MAX_DELAY       = 0.15
	local COUNT_WINDOW         = 6.5
	local AURA_COUNT_THRESHOLD = 25
	local AURA_MAG_THRESHOLD   = 30  -- player must be FURTHER than this to count as aura
	
	--------------------------------------------------
	-- SOUND IDS
	--------------------------------------------------
	
	local BRICK_SOUND_ID = "rbxassetid://9117183621"
	local PAINT_SOUND_ID = "rbxassetid://18473826"
	
	--------------------------------------------------
	-- STATE
	--------------------------------------------------
	
	local isRunning         = false
	local connections       = {}
	local recentDeleteSound = {}
	local recentPaintSound  = {}
	local brickBirth        = {}
	local auraCounters      = {}
	local auraAlerted       = {}
	
	--------------------------------------------------
	-- CHECK IF ANY DETECTION IS ON
	--------------------------------------------------
	
	local function anyDetectionOn()
		for _, attr in ipairs(DETECTION_ATTRS) do
			if ScreenGui:GetAttribute(attr) == true then return true end
		end
		return false
	end
	
	local function isDeleteOn()   return ScreenGui:GetAttribute("DeleteLogsEnabled")   == true end
	local function isToxifyOn()   return ScreenGui:GetAttribute("ToxifyLogsEnabled")   == true end
	local function isAnchoredOn() return ScreenGui:GetAttribute("AnchoredLogsEnabled") == true end
	local function isSpamOn()     return ScreenGui:GetAttribute("SpamLogsEnabled")     == true end
	local function isExploitsOn() return ScreenGui:GetAttribute("ExploitsLogsEnabled") == true end
	
	--------------------------------------------------
	-- HELPERS
	--------------------------------------------------
	
	local function getHRP(player)
		if not player.Character then return nil end
		return player.Character:FindFirstChild("HumanoidRootPart")
	end
	
	-- Best candidate for delete/toxify/unanchor (picks closest to IDEAL_DELAY)
	local function getBestCandidate(soundTable, eventAt)
		local best = nil
		local bestDiff = math.huge
		for _, player in ipairs(Players:GetPlayers()) do
			local soundTime = soundTable[player.UserId]
			if not soundTime then continue end
			local delay = soundTime - eventAt
			if delay < MIN_DELAY or delay > MAX_DELAY then continue end
			local diff = math.abs(delay - IDEAL_DELAY)
			if diff < bestDiff then
				best = player
				bestDiff = diff
			end
		end
		return best
	end
	
	-- First candidate for aura (just takes whoever played sound first in window)
	local function getFirstCandidate(soundTable, eventAt)
		for _, player in ipairs(Players:GetPlayers()) do
			local soundTime = soundTable[player.UserId]
			if not soundTime then continue end
			local delay = soundTime - eventAt
			if delay >= AURA_MIN_DELAY and delay <= AURA_MAX_DELAY then
				return player
			end
		end
		return nil
	end
	
	--------------------------------------------------
	-- AURA COUNTER
	--------------------------------------------------
	
	local function getAuraCount(userId, actionType, now)
		auraCounters[userId] = auraCounters[userId] or {}
		auraCounters[userId][actionType] = auraCounters[userId][actionType] or {}
		local timestamps = auraCounters[userId][actionType]
		for i = #timestamps, 1, -1 do
			if now - timestamps[i] > COUNT_WINDOW then
				table.remove(timestamps, i)
			end
		end
		table.insert(timestamps, now)
		return #timestamps
	end
	
	--------------------------------------------------
	-- AURA EXPLOIT CHECK
	-- Only counts and flags if player is FURTHER than AURA_MAG_THRESHOLD from the brick
	--------------------------------------------------
	
	local function checkAuraExploit(soundTable, brickPos, actionType, eventAt, ownerName)
		if not isExploitsOn() then return end
	
		local player = getFirstCandidate(soundTable, eventAt)
		if not player then return end
	
		local isSelf = player.Name == ownerName
		if isSelf then return end
	
		local hrp = getHRP(player)
		local magNum = hrp and (hrp.Position - brickPos).Magnitude or 0
	
		-- Only count if player is FAR from the brick
		if magNum <= AURA_MAG_THRESHOLD then return end
	
		local count = getAuraCount(player.UserId, actionType, eventAt)
		local key = player.UserId .. "_" .. actionType
	
		if count >= AURA_COUNT_THRESHOLD then
			if not auraAlerted[key] then
				auraAlerted[key] = true
				shared.ExploitGriefEvent:Fire(player)
				task.delay(COUNT_WINDOW, function()
					auraAlerted[key] = nil
				end)
			end
		else
			auraAlerted[key] = nil
		end
	end
	
	--------------------------------------------------
	-- SOUND TRACKING
	--------------------------------------------------
	
	local function hookHRP(player, hrp)
		local function trackSound(obj)
			if not obj:IsA("Sound") then return end
			local lastTime = 0
			local wasPlaying = false
	
			if obj.SoundId == BRICK_SOUND_ID then
				local c = RunService.Heartbeat:Connect(function()
					if not obj.Parent then return end
					local t = obj.TimePosition
					local playing = obj.IsPlaying
					if playing then
						if not wasPlaying or (lastTime > 0.05 and t < 0.05) then
							recentDeleteSound[player.UserId] = tick()
						end
					end
					lastTime = t
					wasPlaying = playing
				end)
				table.insert(connections, c)
	
			elseif obj.SoundId == PAINT_SOUND_ID then
				local c = RunService.Heartbeat:Connect(function()
					if not obj.Parent then return end
					local t = obj.TimePosition
					local playing = obj.IsPlaying
					if playing then
						if not wasPlaying or (lastTime > 0.05 and t < 0.05) then
							recentPaintSound[player.UserId] = tick()
						end
					end
					lastTime = t
					wasPlaying = playing
				end)
				table.insert(connections, c)
			end
		end
	
		for _, obj in pairs(hrp:GetDescendants()) do
			trackSound(obj)
		end
		local c = hrp.DescendantAdded:Connect(trackSound)
		table.insert(connections, c)
	end
	
	local function watchPlayer(player)
		local c = player.CharacterAdded:Connect(function(char)
			local hrp = char:WaitForChild("HumanoidRootPart", 10)
			if hrp then hookHRP(player, hrp) end
		end)
		table.insert(connections, c)
		if player.Character then
			local hrp = player.Character:FindFirstChild("HumanoidRootPart")
			if hrp then hookHRP(player, hrp) end
		end
	end
	
	--------------------------------------------------
	-- BRICK WATCHERS
	--------------------------------------------------
	
	local function watchBrick(brick, ownerName)
		-- TOXIFY: Light added
		if not brick:FindFirstChild("Light") then
			local c = brick.ChildAdded:Connect(function(child)
				if child.Name ~= "Light" then return end
				if not isToxifyOn() and not isExploitsOn() then return end
				local paintedAt = tick()
				local brickPos = brick.Position
				task.delay(MAX_DELAY, function()
					if isToxifyOn() then
						local player = getBestCandidate(recentPaintSound, paintedAt)
						if player then
							local hrp = getHRP(player)
							local dist = hrp and (hrp.Position - brickPos).Magnitude or 0
							-- Only fire ToxifyEvent if player is within normal range
							if dist <= AURA_MAG_THRESHOLD then
								local isSelf = player.Name == ownerName
								local ownerPlayer = Players:FindFirstChild(ownerName)
								shared.ToxifyEvent:Fire(
									player.UserId,
									ownerPlayer and ownerPlayer.UserId or nil,
									isSelf,
									ownerName,
									brickPos
								)
							end
						end
					end
					checkAuraExploit(recentPaintSound, brickPos, "toxify", paintedAt, ownerName)
				end)
			end)
			table.insert(connections, c)
		end
	
		-- UNANCHOR: Drag added
		if not brick:FindFirstChild("Drag") then
			local c = brick.ChildAdded:Connect(function(child)
				if child.Name ~= "Drag" then return end
				if not isAnchoredOn() and not isExploitsOn() then return end
				local unanchoredAt = tick()
				local brickPos = brick.Position
				task.delay(MAX_DELAY, function()
					if isAnchoredOn() then
						local player = getBestCandidate(recentPaintSound, unanchoredAt)
						if player then
							local hrp = getHRP(player)
							local dist = hrp and (hrp.Position - brickPos).Magnitude or 0
							-- Only fire UnanchorEvent if player is within normal range
							if dist <= AURA_MAG_THRESHOLD then
								local isSelf = player.Name == ownerName
								local ownerPlayer = Players:FindFirstChild(ownerName)
								shared.UnanchorEvent:Fire(
									player.UserId,
									ownerPlayer and ownerPlayer.UserId or nil,
									isSelf,
									ownerName,
									brickPos
								)
							end
						end
					end
					checkAuraExploit(recentPaintSound, brickPos, "unanchor", unanchoredAt, ownerName)
				end)
			end)
			table.insert(connections, c)
		end
	end
	
	local function watchFolder(folder)
		local ownerName = folder.Name
	
		-- DELETE
		local c1 = folder.ChildRemoved:Connect(function(child)
			if child.Name ~= "Brick" then return end
			if not isDeleteOn() and not isExploitsOn() then return end
			local removedAt = tick()
			local brickPos = child.Position
			local debugId = child:GetDebugId()
			task.delay(MAX_DELAY, function()
				if isDeleteOn() then
					local player = getBestCandidate(recentDeleteSound, removedAt)
					if player then
						local hrp = getHRP(player)
						local dist = hrp and (hrp.Position - brickPos).Magnitude or 0
						-- Only fire DeleteEvent if player is within normal range
						-- If they are far away it is likely an aura exploit, not a normal delete
						if dist <= AURA_MAG_THRESHOLD then
							local isSelf = player.Name == ownerName
							local ownerPlayer = Players:FindFirstChild(ownerName)
							local age = "Unknown"
							if brickBirth[debugId] then
								age = string.format("%.0f", removedAt - brickBirth[debugId])
								brickBirth[debugId] = nil
							end
							shared.DeleteEvent:Fire(
								player.UserId,
								ownerPlayer and ownerPlayer.UserId or nil,
								isSelf,
								ownerName,
								age,
								brickPos
							)
						end
					end
				end
				checkAuraExploit(recentDeleteSound, brickPos, "delete", removedAt, ownerName)
			end)
		end)
		table.insert(connections, c1)
	
		-- BRICK ADDED
		local c2 = folder.ChildAdded:Connect(function(child)
			if child.Name ~= "Brick" then return end
			brickBirth[child:GetDebugId()] = tick()
	
			if isSpamOn() then
				local ownerPlayer = Players:FindFirstChild(ownerName)
				if ownerPlayer then
					shared.BuildEvent:Fire(ownerPlayer.UserId)
				end
			end
	
			watchBrick(child, ownerName)
		end)
		table.insert(connections, c2)
	
		-- Watch existing bricks
		for _, child in ipairs(folder:GetChildren()) do
			if child.Name == "Brick" then
				brickBirth[child:GetDebugId()] = brickBirth[child:GetDebugId()] or tick()
				watchBrick(child, ownerName)
			end
		end
	end
	
	--------------------------------------------------
	-- START / STOP
	--------------------------------------------------
	
	local function startWatching()
		if isRunning then return end
		isRunning = true
		print("[GriefDetectorConsole] Started")
	
		local bricksFolder = workspace:WaitForChild("Bricks")
	
		for _, p in ipairs(Players:GetPlayers()) do
			watchPlayer(p)
		end
		local c1 = Players.PlayerAdded:Connect(watchPlayer)
		table.insert(connections, c1)
	
		for _, folder in ipairs(bricksFolder:GetChildren()) do
			watchFolder(folder)
		end
		local c2 = bricksFolder.ChildAdded:Connect(watchFolder)
		table.insert(connections, c2)
	end
	
	local function stopWatching()
		if not isRunning then return end
		isRunning = false
		print("[GriefDetectorConsole] Stopped")
	
		for _, c in ipairs(connections) do
			pcall(function() c:Disconnect() end)
		end
		connections       = {}
		recentDeleteSound = {}
		recentPaintSound  = {}
		auraCounters      = {}
		auraAlerted       = {}
	end
	
	--------------------------------------------------
	-- WATCH ATTRIBUTE CHANGES
	--------------------------------------------------
	
	for _, attr in ipairs(DETECTION_ATTRS) do
		ScreenGui:GetAttributeChangedSignal(attr):Connect(function()
			if anyDetectionOn() then
				startWatching()
			else
				stopWatching()
			end
		end)
	end
	
	if anyDetectionOn() then
		startWatching()
	end
end;
task.spawn(C_7);
-- StarterGui.ScreenGui.MainFrame.SideBar.PageHandler
local function C_b()
local script = G2L["b"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	if not LocalPlayer then
		Players:GetPropertyChangedSignal("LocalPlayer"):Wait()
		LocalPlayer = Players.LocalPlayer
	end
	local PlayerGui = LocalPlayer:WaitForChild("PlayerGui", 60)
	
	local ScreenGui
	repeat ScreenGui = PlayerGui:FindFirstChild("ScreenGui") if not ScreenGui then task.wait(0.1) end until ScreenGui
	local MainFrame
	repeat MainFrame = ScreenGui:FindFirstChild("MainFrame") if not MainFrame then task.wait(0.1) end until MainFrame
	local SideBar
	repeat SideBar = MainFrame:FindFirstChild("SideBar") if not SideBar then task.wait(0.1) end until SideBar
	local ContentArea
	repeat ContentArea = MainFrame:FindFirstChild("ContentArea") if not ContentArea then task.wait(0.1) end until ContentArea
	local TCOSideBar
	repeat TCOSideBar = ContentArea:FindFirstChild("TCOSideBar") if not TCOSideBar then task.wait(0.1) end until TCOSideBar
	
	-- Main sidebar pages
	local pageConfig = {
		{buttonName = "MainPG",     pageName = "MainPage"},
		{buttonName = "SettingsPG", pageName = "SettingsPage"},
		{buttonName = "VisualPG",   pageName = "VisualPage"},
		{buttonName = "TCOPG",      pageName = "TCOMainPage"},
		{buttonName = "MiscPG",     pageName = "MiscPage"},
	}
	
	-- TCO sub-pages config
	local tcoPageConfig = {
		{buttonName = "MainTCOPG",      pageName = "TCOMainPage"},
		{buttonName = "MiscTCOPG",      pageName = "TCOMiscPage"},
		{buttonName = "DetectionTCOPG", pageName = "TCODetectionPage"},
		{buttonName = "TrollTCOPG",     pageName = "TCOTrollPage"},
	}
	
	local ACTIVE_COLOR   = Color3.fromRGB(18, 18, 18)
	local INACTIVE_COLOR = Color3.fromRGB(0, 0, 0)
	
	-- ✅ NEW TEXT COLORS
	local ACTIVE_TEXT_COLOR = Color3.fromRGB(0, 123, 255)
	local INACTIVE_TEXT_COLOR = Color3.fromRGB(255, 255, 255)
	
	local tcoPageNames = {}
	for _, config in tcoPageConfig do
		tcoPageNames[config.pageName] = true
	end
	
	local currentPage  = nil
	local activeButton = nil
	local activeTCOButton = nil
	
	local function setTCOSideBarVisible(visible)
		TCOSideBar.Visible = visible
	end
	
	local function switchTCOPage(buttonName, pageName)
		for _, config in tcoPageConfig do
			local page = ContentArea:FindFirstChild(config.pageName)
			if page then page.Visible = false end
		end
	
		-- reset TCO buttons
		for _, config in tcoPageConfig do
			local button = TCOSideBar:FindFirstChild(config.buttonName)
			if button and button:IsA("TextButton") then
				button.BackgroundColor3 = INACTIVE_COLOR
				button.TextColor3 = INACTIVE_TEXT_COLOR -- ✅
			end
		end
	
		local selectedPage = ContentArea:FindFirstChild(pageName)
		if selectedPage then
			selectedPage.Visible = true
			currentPage = pageName
		else
			warn("TCO Page not found: " .. pageName)
		end
	
		local selectedButton = TCOSideBar:FindFirstChild(buttonName)
		if selectedButton and selectedButton:IsA("TextButton") then
			selectedButton.BackgroundColor3 = ACTIVE_COLOR
			selectedButton.TextColor3 = ACTIVE_TEXT_COLOR -- ✅
			activeTCOButton = buttonName
		end
	end
	
	local function switchPage(buttonName, pageName)
		local isTCOPage = tcoPageNames[pageName]
	
		for _, config in pageConfig do
			if not tcoPageNames[config.pageName] then
				local page = ContentArea:FindFirstChild(config.pageName)
				if page then page.Visible = false end
			end
		end
	
		if not isTCOPage then
			for _, config in tcoPageConfig do
				local page = ContentArea:FindFirstChild(config.pageName)
				if page then page.Visible = false end
			end
		end
	
		-- reset main buttons
		for _, config in pageConfig do
			local button = SideBar:FindFirstChild(config.buttonName)
			if button and button:IsA("TextButton") then
				button.BackgroundColor3 = INACTIVE_COLOR
				button.TextColor3 = INACTIVE_TEXT_COLOR -- ✅
			end
		end
	
		setTCOSideBarVisible(isTCOPage)
	
		if isTCOPage then
			switchTCOPage(activeTCOButton or "MainTCOPG", activeTCOButton and 
				(function()
					for _, c in tcoPageConfig do
						if c.buttonName == activeTCOButton then return c.pageName end
					end
				end)() or "TCOMainPage")
		else
			local selectedPage = ContentArea:FindFirstChild(pageName)
			if selectedPage then
				selectedPage.Visible = true
				currentPage = pageName
			else
				warn("Page not found: " .. pageName)
			end
		end
	
		local selectedButton = SideBar:FindFirstChild(buttonName)
		if selectedButton and selectedButton:IsA("TextButton") then
			selectedButton.BackgroundColor3 = ACTIVE_COLOR
			selectedButton.TextColor3 = ACTIVE_TEXT_COLOR -- ✅
			activeButton = buttonName
		end
	end
	
	-- connect main buttons
	for _, config in pageConfig do
		local button = SideBar:FindFirstChild(config.buttonName)
		if button and button:IsA("TextButton") then
			button.MouseButton1Click:Connect(function()
				switchPage(config.buttonName, config.pageName)
			end)
		else
			warn("Button not found: " .. config.buttonName)
		end
	end
	
	-- connect TCO buttons
	for _, config in tcoPageConfig do
		local button = TCOSideBar:FindFirstChild(config.buttonName)
		if button and button:IsA("TextButton") then
			button.MouseButton1Click:Connect(function()
				switchTCOPage(config.buttonName, config.pageName)
			end)
		else
			warn("TCO Button not found: " .. config.buttonName)
		end
	end
	
	setTCOSideBarVisible(false)
	task.wait(0.1)
	switchPage("MainPG", "MainPage")
	
	print("AGAR WARE LOADED")
end;
task.spawn(C_b);
-- StarterGui.ScreenGui.MainFrame.SideBar.TCOPG.TCOIngame
local function C_12()
local script = G2L["12"];
	local TCOPG = script.Parent -- if script is inside TCOPG
	-- or find it:
	-- local TCOPG = game.Players.LocalPlayer.PlayerGui.ScreenGui.MainFrame.SideBar.TCOPG
	
	local TCO_PLACE_IDS = {
		11137575513,
		12943245078,
		12943247001,
		108097274488844,
	}
	
	local isTCO = false
	for _, id in ipairs(TCO_PLACE_IDS) do
		if game.PlaceId == id then
			isTCO = true
			break
		end
	end
	
	if not isTCO then
		TCOPG.Text = "TCO (not ingame)"
	end
end;
task.spawn(C_12);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.MovementScript
local function C_19()
local script = G2L["19"];
	-- Get references to GUI elements
	local MovementGroup = script.Parent
	local MovementMinimize = MovementGroup.MovementTitle.MovementMinimize
	
	-- Automatically find all Row frames inside MovementGroup
	local function getRows()
		local rows = {}
		for _, child in ipairs(MovementGroup:GetChildren()) do
			if child:IsA("Frame") and string.sub(child.Name, -3) == "Row" then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	-- Groupbox minimize functionality
	local isMinimized = false
	local originalSize = MovementGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	MovementMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
	
		if isMinimized then
			MovementGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			MovementMinimize.Text = "+"
		else
			MovementGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			MovementMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_19);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.WalkspeedRow.WalkspeedScript
local function C_1f()
local script = G2L["1f"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local TextService = game:GetService("TextService")
	
	-- Get references to GUI elements
	local WalkspeedRow = script.Parent
	local WalkspeedTickBox = WalkspeedRow.WalkspeedTickBox
	local WalkspeedSpeedChanger = WalkspeedRow.WalkspeedSpeedChanger
	local MovementGroup = WalkspeedRow.Parent
	local MovementAccentColor = MovementGroup.MovementAccentColor
	
	-- State variables
	local isEnabled = false
	local currentSpeed = 16
	local lastValidSpeed = 16
	local isInfinite = false
	
	local accentColor = MovementAccentColor.BackgroundColor3
	
	WalkspeedTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	WalkspeedSpeedChanger.Text = "(16)"
	
	-- Get the top level ScreenGui to parent tooltip to
	local function getScreenGui()
		local parent = WalkspeedRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	-- Create tooltip frame
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "WalkspeedTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Changes your character walk speed"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local function showTooltip()
		local absPos = WalkspeedTickBox.AbsolutePosition
		local absSize = WalkspeedTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end
	
	local function hideTooltip()
		tooltipFrame.Visible = false
	end
	
	WalkspeedTickBox.MouseEnter:Connect(showTooltip)
	WalkspeedTickBox.MouseLeave:Connect(hideTooltip)
	
	local function updateWalkspeed()
		local character = LocalPlayer.Character
		if character then
			local humanoid = character:FindFirstChildOfClass("Humanoid")
			if humanoid then
				if isInfinite then
					humanoid.WalkSpeed = math.huge
				else
					humanoid.WalkSpeed = currentSpeed
				end
			end
		end
	end
	
	local function setTickboxState(ticked)
		if ticked then
			WalkspeedTickBox.BackgroundColor3 = accentColor
		else
			WalkspeedTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	WalkspeedTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			updateWalkspeed()
		else
			local character = LocalPlayer.Character
			if character then
				local humanoid = character:FindFirstChildOfClass("Humanoid")
				if humanoid then
					humanoid.WalkSpeed = 16
				end
			end
		end
	end)
	
	WalkspeedSpeedChanger.FocusLost:Connect(function()
		local text = string.lower(WalkspeedSpeedChanger.Text)
	
		if text == "inf" then
			isInfinite = true
			currentSpeed = math.huge
			lastValidSpeed = math.huge
			WalkspeedSpeedChanger.Text = "inf"
		else
			local number = tonumber(text)
			if number then
				isInfinite = false
				currentSpeed = number
				lastValidSpeed = number
				WalkspeedSpeedChanger.Text = tostring(number)
			else
				if lastValidSpeed == math.huge then
					WalkspeedSpeedChanger.Text = "inf"
				else
					WalkspeedSpeedChanger.Text = tostring(lastValidSpeed)
				end
			end
		end
	
		if isEnabled then
			updateWalkspeed()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function(character)
		if isEnabled then
			local humanoid = character:WaitForChild("Humanoid")
			if isInfinite then
				humanoid.WalkSpeed = math.huge
			else
				humanoid.WalkSpeed = currentSpeed
			end
		end
	end)
end;
task.spawn(C_1f);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.JumppowerRow.JumppowerScript
local function C_24()
local script = G2L["24"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local TextService = game:GetService("TextService")
	
	-- Get references to GUI elements
	local JumppowerRow = script.Parent
	local JumppowerTickBox = JumppowerRow.JumppowerTickBox
	local JumppowerJumpChanger = JumppowerRow.JumppowerJumpChanger
	local MovementGroup = JumppowerRow.Parent
	local MovementAccentColor = MovementGroup.MovementAccentColor
	
	-- State variables
	local isEnabled = false
	local currentJump = 50
	local lastValidJump = 50
	local isInfinite = false
	
	local accentColor = MovementAccentColor.BackgroundColor3
	
	JumppowerTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	JumppowerJumpChanger.Text = "(50)"
	
	-- Get the top level ScreenGui to parent tooltip to
	local function getScreenGui()
		local parent = JumppowerRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	-- Create tooltip frame
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "JumppowerTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Changes your character jump power"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local function showTooltip()
		local absPos = JumppowerTickBox.AbsolutePosition
		local absSize = JumppowerTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end
	
	local function hideTooltip()
		tooltipFrame.Visible = false
	end
	
	JumppowerTickBox.MouseEnter:Connect(showTooltip)
	JumppowerTickBox.MouseLeave:Connect(hideTooltip)
	
	local function updateJumppower()
		local character = LocalPlayer.Character
		if character then
			local humanoid = character:FindFirstChildOfClass("Humanoid")
			if humanoid then
				humanoid.UseJumpPower = true
				if isInfinite then
					humanoid.JumpPower = math.huge
				else
					humanoid.JumpPower = currentJump
				end
			end
		end
	end
	
	local function setTickboxState(ticked)
		if ticked then
			JumppowerTickBox.BackgroundColor3 = accentColor
		else
			JumppowerTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	JumppowerTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			updateJumppower()
		else
			local character = LocalPlayer.Character
			if character then
				local humanoid = character:FindFirstChildOfClass("Humanoid")
				if humanoid then
					humanoid.UseJumpPower = true
					humanoid.JumpPower = 50
				end
			end
		end
	end)
	
	JumppowerJumpChanger.FocusLost:Connect(function()
		local text = string.lower(JumppowerJumpChanger.Text)
	
		if text == "inf" then
			isInfinite = true
			currentJump = math.huge
			lastValidJump = math.huge
			JumppowerJumpChanger.Text = "inf"
		else
			local number = tonumber(text)
			if number then
				isInfinite = false
				currentJump = number
				lastValidJump = number
				JumppowerJumpChanger.Text = tostring(number)
			else
				if lastValidJump == math.huge then
					JumppowerJumpChanger.Text = "inf"
				else
					JumppowerJumpChanger.Text = tostring(lastValidJump)
				end
			end
		end
	
		if isEnabled then
			updateJumppower()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function(character)
		if isEnabled then
			local humanoid = character:WaitForChild("Humanoid")
			humanoid.UseJumpPower = true
			if isInfinite then
				humanoid.JumpPower = math.huge
			else
				humanoid.JumpPower = currentJump
			end
		end
	end)
end;
task.spawn(C_24);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.FlyRow.FlyScript
local function C_29()
local script = G2L["29"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local UserInputService = game:GetService("UserInputService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local FlyRow = script.Parent
	local FlyTickBox = FlyRow.FlyTickBox
	local FlySpeedChanger = FlyRow.FlySpeedChanger
	local MovementGroup = FlyRow.Parent
	local MovementAccentColor = MovementGroup.MovementAccentColor
	
	local isEnabled = false
	local currentSpeed = 50
	local lastValidSpeed = 50
	local nowe = false
	local tpwalking = false
	
	local accentColor = MovementAccentColor.BackgroundColor3
	
	FlyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	FlySpeedChanger.Text = "(50)"
	
	local function getScreenGui()
		local parent = FlyRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "FlyTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Allows your character to fly. E = up, Q = down"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	FlyTickBox.MouseEnter:Connect(function()
		local absPos = FlyTickBox.AbsolutePosition
		local absSize = FlyTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	FlyTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			FlyTickBox.BackgroundColor3 = accentColor
		else
			FlyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local function getSpeed()
		return currentSpeed
	end
	
	local function enableFly()
		local chr = LocalPlayer.Character
		if not chr then return end
		local hum = chr:FindFirstChildOfClass("Humanoid")
		if not hum then return end
	
		nowe = true
		tpwalking = true
	
		for _, state in pairs(Enum.HumanoidStateType:GetEnumItems()) do
			pcall(function() hum:SetStateEnabled(state, false) end)
		end
		hum:ChangeState(Enum.HumanoidStateType.Swimming)
		hum.PlatformStand = true
	
		-- Freeze animations properly
		chr.Animate.Disabled = true
		task.wait()
		for _, track in pairs(hum:GetPlayingAnimationTracks()) do
			track:Stop(0)
		end
	
		spawn(function()
			local hb = RunService.Heartbeat
			while tpwalking and nowe do
				hb:Wait()
				chr = LocalPlayer.Character
				hum = chr and chr:FindFirstChildOfClass("Humanoid")
				if chr and hum and hum.Parent then
					if hum.MoveDirection.Magnitude > 0 then
						chr:TranslateBy(hum.MoveDirection * getSpeed() / 50)
					end
				end
			end
		end)
	
		local isR6 = hum.RigType == Enum.HumanoidRigType.R6
		local torso = isR6 and chr:FindFirstChild("Torso") or chr:FindFirstChild("UpperTorso")
		if not torso then return end
	
		local bg = Instance.new("BodyGyro")
		bg.P = 9e4
		bg.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
		bg.CFrame = torso.CFrame
		bg.Parent = torso
	
		local bv = Instance.new("BodyVelocity")
		bv.Velocity = Vector3.new(0, 0.1, 0)
		bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
		bv.Parent = torso
	
		local ctrl = {f=0, b=0, l=0, r=0}
		local lastctrl = {f=0, b=0, l=0, r=0}
		local speed = 0
	
		spawn(function()
			while nowe do
				RunService.RenderStepped:Wait()
				local maxspeed = getSpeed()
				local camCF = workspace.CurrentCamera.CFrame
	
				ctrl.f = UserInputService:IsKeyDown(Enum.KeyCode.W) and 1 or 0
				ctrl.b = UserInputService:IsKeyDown(Enum.KeyCode.S) and -1 or 0
				ctrl.l = UserInputService:IsKeyDown(Enum.KeyCode.A) and -1 or 0
				ctrl.r = UserInputService:IsKeyDown(Enum.KeyCode.D) and 1 or 0
	
				if ctrl.l + ctrl.r ~= 0 or ctrl.f + ctrl.b ~= 0 then
					speed = math.min(speed + 0.5 + speed/maxspeed, maxspeed)
				elseif speed ~= 0 then
					speed = math.max(speed - 1, 0)
				end
	
				local flatLook = Vector3.new(camCF.LookVector.X, 0, camCF.LookVector.Z).Unit
				local flatRight = Vector3.new(camCF.RightVector.X, 0, camCF.RightVector.Z).Unit
	
				if (ctrl.l + ctrl.r) ~= 0 or (ctrl.f + ctrl.b) ~= 0 then
					bv.Velocity = (flatLook * (ctrl.f+ctrl.b) + flatRight * (ctrl.l+ctrl.r)) * speed
					lastctrl = {f=ctrl.f, b=ctrl.b, l=ctrl.l, r=ctrl.r}
				elseif speed ~= 0 then
					bv.Velocity = (flatLook * (lastctrl.f+lastctrl.b) + flatRight * (lastctrl.l+lastctrl.r)) * speed
				else
					bv.Velocity = Vector3.new(0, 0, 0)
				end
	
				bg.CFrame = camCF
	
				-- E and Q for up and down
				if UserInputService:IsKeyDown(Enum.KeyCode.E) then
					torso.CFrame = torso.CFrame + Vector3.new(0, 1, 0)
				elseif UserInputService:IsKeyDown(Enum.KeyCode.Q) then
					torso.CFrame = torso.CFrame + Vector3.new(0, -1, 0)
				end
			end
	
			bg:Destroy()
			bv:Destroy()
		end)
	end
	
	local function disableFly()
		nowe = false
		tpwalking = false
	
		local chr = LocalPlayer.Character
		if not chr then return end
		local hum = chr:FindFirstChildOfClass("Humanoid")
		if not hum then return end
	
		for _, state in pairs(Enum.HumanoidStateType:GetEnumItems()) do
			pcall(function() hum:SetStateEnabled(state, true) end)
		end
		hum:ChangeState(Enum.HumanoidStateType.RunningNoPhysics)
		hum.PlatformStand = false
		chr.Animate.Disabled = false
	end
	
	FlyTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			enableFly()
		else
			disableFly()
		end
	end)
	
	FlySpeedChanger.FocusLost:Connect(function()
		local text = string.lower(FlySpeedChanger.Text)
		local number = tonumber(text)
	
		if number and number > 0 then
			currentSpeed = number
			lastValidSpeed = number
			FlySpeedChanger.Text = tostring(number)
		else
			FlySpeedChanger.Text = tostring(lastValidSpeed)
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			isEnabled = false
			setTickboxState(false)
			disableFly()
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disableFly()
		end
	end)
end;
task.spawn(C_29);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.InfJumpRow.InfJumpScript
local function C_2e()
local script = G2L["2e"];
	local UserInputService = game:GetService("UserInputService")
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local TextService = game:GetService("TextService")
	
	local InfJumpRow = script.Parent
	local InfJumpTickBox = InfJumpRow.InfJumpTickBox
	local MovementGroup = InfJumpRow.Parent
	local MovementAccentColor = MovementGroup.MovementAccentColor
	
	local isEnabled = false
	local connection = nil
	local accentColor = MovementAccentColor.BackgroundColor3
	
	InfJumpTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local function getScreenGui()
		local parent = InfJumpRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "InfJumpTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Allows your character to jump infinitely"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	InfJumpTickBox.MouseEnter:Connect(function()
		local absPos = InfJumpTickBox.AbsolutePosition
		local absSize = InfJumpTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	InfJumpTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			InfJumpTickBox.BackgroundColor3 = accentColor
		else
			InfJumpTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	InfJumpTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			connection = UserInputService.JumpRequest:Connect(function()
				local character = LocalPlayer.Character
				if not character then return end
				local humanoid = character:FindFirstChildOfClass("Humanoid")
				if humanoid then
					humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
				end
			end)
		else
			if connection then
				connection:Disconnect()
				connection = nil
			end
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if connection then
				connection:Disconnect()
				connection = nil
			end
		end
	end)
end;
task.spawn(C_2e);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MovementGroup.ShiftLockRow.ShiftLockScript
local function C_32()
local script = G2L["32"];
	local Players = game:GetService("Players")
	local UserInputService = game:GetService("UserInputService")
	local RunService = game:GetService("RunService")
	local ContextActionService = game:GetService("ContextActionService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local ShiftLockRow = script.Parent
	local ShiftLockTickBox = ShiftLockRow.ShiftLockTickBox
	local MovementGroup = ShiftLockRow.Parent
	local MovementAccentColor = MovementGroup.MovementAccentColor
	
	local isEnabled = false
	local active = nil
	local lastPressTime = 0
	local debounceTime = 0.2
	
	local function getAccent()
		return MovementAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = ShiftLockRow
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	-- Tooltip
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "ShiftLockTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipLabel = Instance.new("TextLabel")
	tooltipLabel.Name = "TooltipText"
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Adds a shiftlock button to your screen on mobile"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.XY
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.ZIndex = 101
	tooltipLabel.Parent = tooltipFrame
	
	ShiftLockTickBox.MouseEnter:Connect(function()
		local abs = ShiftLockTickBox.AbsolutePosition
		local size = ShiftLockTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	ShiftLockTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ShiftLock UI
	local shiftLockGui = nil
	local imageButton = nil
	
	local states = {
		OFF = "rbxasset://textures/ui/mouseLock_off@2x.png",
		ON = "rbxasset://textures/ui/mouseLock_on@2x.png"
	}
	
	local MAX_LENGTH = 900000
	local ENABLED_OFFSET = CFrame.new(1.7, 0, 0)
	local DISABLED_OFFSET = CFrame.new(-1.7, 0, 0)
	
	local function getCharacterParts()
		local character = LocalPlayer.Character
		if not character then return nil, nil, nil end
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		local hrp = character:FindFirstChild("HumanoidRootPart")
		local camera = workspace.CurrentCamera
		return humanoid, hrp, camera
	end
	
	local function updateImage(state)
		if imageButton then
			imageButton.Image = states[state]
		end
	end
	
	local function disableShiftlock()
		local humanoid, hrp, camera = getCharacterParts()
		if humanoid then humanoid.AutoRotate = true end
		if camera then camera.CFrame = camera.CFrame * DISABLED_OFFSET end
		updateImage("OFF")
		if active then
			active:Disconnect()
			active = nil
		end
	end
	
	local function enableShiftlock()
		local humanoid, hrp, camera = getCharacterParts()
		if not humanoid or not hrp or not camera then return end
		humanoid.AutoRotate = false
		hrp.CFrame = CFrame.new(hrp.Position, Vector3.new(
			camera.CFrame.LookVector.X * MAX_LENGTH,
			hrp.Position.Y,
			camera.CFrame.LookVector.Z * MAX_LENGTH
			))
		camera.CFrame = camera.CFrame * ENABLED_OFFSET
		updateImage("ON")
	end
	
	local function shiftLockToggle()
		local currentTime = tick()
		if currentTime - lastPressTime < debounceTime then return end
		lastPressTime = currentTime
	
		if not active then
			active = RunService.RenderStepped:Connect(function()
				enableShiftlock()
			end)
		else
			disableShiftlock()
		end
	end
	
	local function createUI()
		-- Cleanup old UI
		if shiftLockGui then
			shiftLockGui:Destroy()
			shiftLockGui = nil
			imageButton = nil
		end
	
		local playerGui = LocalPlayer:WaitForChild("PlayerGui")
	
		shiftLockGui = Instance.new("ScreenGui")
		shiftLockGui.Name = "ShiftLockGui"
		shiftLockGui.ResetOnSpawn = false
		shiftLockGui.DisplayOrder = 999
		shiftLockGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
		shiftLockGui.Parent = playerGui
	
		imageButton = Instance.new("ImageButton")
		imageButton.BackgroundTransparency = 1
		imageButton.Position = UDim2.new(0.921914339, 0, 0.552375436, 0)
		imageButton.Size = UDim2.new(0.0636147112, 0, 0.0661305636, 0)
		imageButton.SizeConstraint = Enum.SizeConstraint.RelativeXX
		imageButton.Image = states.OFF
		imageButton.Modal = true
		imageButton.Active = true
		imageButton.Parent = shiftLockGui
	
		-- Draggable logic
		local dragging = false
		local dragStart = Vector2.new()
		local startPos = UDim2.new()
	
		imageButton.InputBegan:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.Touch then
				dragging = true
				dragStart = input.Position
				startPos = imageButton.Position
			end
		end)
	
		imageButton.InputEnded:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.Touch then
				dragging = false
			end
		end)
	
		RunService.RenderStepped:Connect(function()
			if dragging then
				local touchPos = UserInputService:GetTouchPosition()
				if not touchPos then return end
				local delta = touchPos - dragStart
				local newPos = UDim2.new(
					startPos.X.Scale, startPos.X.Offset + delta.X,
					startPos.Y.Scale, startPos.Y.Offset + delta.Y
				)
				imageButton.Position = newPos
			end
		end)
	
		imageButton.MouseButton1Click:Connect(function()
			shiftLockToggle()
		end)
	
		-- PC: bind shift key
		ContextActionService:BindAction("ShiftLockToggle", function(_, inputState)
			if inputState == Enum.UserInputState.Begin then
				shiftLockToggle()
			end
		end, false, Enum.KeyCode.LeftShift)
	end
	
	local function destroyUI()
		disableShiftlock()
		ContextActionService:UnbindAction("ShiftLockToggle")
		if shiftLockGui then
			shiftLockGui:Destroy()
			shiftLockGui = nil
			imageButton = nil
		end
	end
	
	ShiftLockTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		ShiftLockTickBox.BackgroundColor3 = isEnabled and getAccent() or Color3.new(0, 0, 0)
		if isEnabled then
			createUI()
		else
			destroyUI()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			task.wait(0.5)
			createUI()
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			destroyUI()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_32);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.MiscScript
local function C_36()
local script = G2L["36"];
	local MiscGroup = script.Parent
	local MiscMinimize = MiscGroup.MiscTitle.MiscMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(MiscGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = MiscGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	MiscMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			MiscGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			MiscMinimize.Text = "+"
		else
			MiscGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			MiscMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_36);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.RejoinRow.RejoinScript
local function C_3c()
local script = G2L["3c"];
	local Players = game:GetService("Players")
	local TeleportService = game:GetService("TeleportService")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local RejoinRow = script.Parent
	local RejoinButton = RejoinRow.RejoinButton
	
	local HOLD_TIME = 1
	local GREY = Color3.fromRGB(80, 80, 80)
	local WHITE = Color3.new(1, 1, 1)
	
	-- Wait for GUI to be fully loaded
	if not game:IsLoaded() then
		game.Loaded:Wait()
	end
	
	-- Clear the button's default text
	RejoinButton.Text = ""
	
	local fillBar = Instance.new("Frame")
	fillBar.Name = "FillBar"
	fillBar.Size = UDim2.new(0, 0, 1, 0)
	fillBar.Position = UDim2.new(0, 0, 0, 0)
	fillBar.BackgroundColor3 = GREY
	fillBar.BorderSizePixel = 0
	fillBar.ZIndex = 1
	fillBar.Parent = RejoinButton
	
	local btnLabel = Instance.new("TextLabel")
	btnLabel.Name = "BtnLabel"
	btnLabel.Size = UDim2.new(1, 0, 1, 0)
	btnLabel.Position = UDim2.new(0, 0, 0, 0)
	btnLabel.BackgroundTransparency = 1
	btnLabel.Text = "Rejoin Server"
	btnLabel.TextColor3 = WHITE
	btnLabel.TextSize = 14
	btnLabel.Font = Enum.Font.Code
	btnLabel.ZIndex = 2
	btnLabel.Parent = RejoinButton
	
	local holding = false
	local progress = 0
	local connection = nil
	
	local function rejoin()
		local queueteleport = (syn and syn.queue_on_teleport) or queue_on_teleport or (fluxus and fluxus.queue_on_teleport)
		if queueteleport then
			pcall(function()
				queueteleport([[loadstring(game:HttpGet("https://pastebin.com/raw/sLjp6RPD"))()]])
			end)
		end
		TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
	end
	
	local function stopHold()
		holding = false
		if connection then
			connection:Disconnect()
			connection = nil
		end
		progress = 0
		fillBar.Size = UDim2.new(0, 0, 1, 0)
		fillBar.BackgroundColor3 = GREY
		btnLabel.Text = "Rejoin Server"
		btnLabel.TextColor3 = WHITE
	end
	
	RejoinButton.MouseButton1Down:Connect(function()
		holding = true
		progress = 0
		btnLabel.Text = "Hold to Rejoin..."
		fillBar.BackgroundColor3 = GREY
	
		connection = RunService.RenderStepped:Connect(function(dt)
			if not holding then return end
			progress = progress + (dt / HOLD_TIME)
			progress = math.clamp(progress, 0, 1)
			fillBar.Size = UDim2.new(progress, 0, 1, 0)
			if progress >= 1 then
				btnLabel.Text = "Rejoining..."
				stopHold()
				task.wait(0.1)
				rejoin()
			end
		end)
	end)
	
	RejoinButton.MouseButton1Up:Connect(function()
		stopHold()
	end)
	
	RejoinButton.MouseLeave:Connect(function()
		stopHold()
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if connection then connection:Disconnect() end
		end
	end)
end;
task.spawn(C_3c);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.GravityRow.GravityScript
local function C_3f()
local script = G2L["3f"];
	local Players = game:GetService("Players")
	
	local LocalPlayer = Players.LocalPlayer
	
	local GravityRow = script.Parent
	local GravityTickBox = GravityRow.GravityTickBox
	local GravityChanger = GravityRow.GravityChanger
	local MiscGroup = GravityRow.Parent
	local MiscAccentColor = MiscGroup.MiscAccentColor
	
	local isEnabled = false
	local DEFAULT_GRAVITY = 196.2
	
	local function getAccent()
		return MiscAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = GravityRow
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "GravityTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Changes workspace gravity. Default is 196.2. Set to 0 for no gravity, or type inf for max gravity."
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	GravityTickBox.MouseEnter:Connect(function()
		local abs = GravityTickBox.AbsolutePosition
		local size = GravityTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	GravityTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function getGravity()
		local text = string.lower(GravityChanger.Text)
		if text == "inf" or text == "infinity" then
			return math.huge
		end
		local num = tonumber(GravityChanger.Text)
		if num == 0 then return 0 end
		return num and num > 0 and num or DEFAULT_GRAVITY
	end
	
	local function applyGravity()
		if isEnabled then
			workspace.Gravity = getGravity()
		else
			workspace.Gravity = DEFAULT_GRAVITY
		end
	end
	
	local function enableGravity()
		isEnabled = true
		GravityTickBox.BackgroundColor3 = getAccent()
		applyGravity()
	end
	
	local function disableGravity()
		isEnabled = false
		GravityTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		workspace.Gravity = DEFAULT_GRAVITY
	end
	
	GravityTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disableGravity()
		else
			enableGravity()
		end
	end)
	
	GravityChanger:GetPropertyChangedSignal("Text"):Connect(function()
		if isEnabled then
			applyGravity()
		end
	end)
	
	GravityChanger.FocusLost:Connect(function()
		local text = string.lower(GravityChanger.Text)
		if text == "inf" or text == "infinity" or text == "" then return end
		local num = tonumber(GravityChanger.Text)
		if num == nil then
			GravityChanger.Text = ""
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function(character)
		character:WaitForChild("Humanoid")
		applyGravity()
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disableGravity()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_3f);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.MiscGroup.BoostFPSRow.BoostFPSScript
local function C_44()
local script = G2L["44"];
	local Lighting = game:GetService("Lighting")
	local RunService = game:GetService("RunService")
	
	local BoostFPSRow = script.Parent
	local BoostFPSTickBox = BoostFPSRow.BoostFPSTickBox
	local MiscGroup = BoostFPSRow.Parent
	local MiscAccentColor = MiscGroup.MiscAccentColor
	
	local isEnabled = false
	local descendantConn = nil
	
	local savedSettings = {
		WaterWaveSize = nil,
		WaterWaveSpeed = nil,
		WaterReflectance = nil,
		WaterTransparency = nil,
		GlobalShadows = nil,
		FogEnd = nil,
		FogStart = nil,
		QualityLevel = nil,
	}
	
	local savedParts = {}
	local savedDecals = {}
	local savedEffects = {}
	
	local function getAccent()
		return MiscAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = BoostFPSRow
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(57, 57, 57)
	stroke.Thickness = 1
	stroke.Parent = tooltipFrame
	
	local padding = Instance.new("UIPadding")
	padding.PaddingTop = UDim.new(0, 6)
	padding.PaddingBottom = UDim.new(0, 6)
	padding.PaddingLeft = UDim.new(0, 6)
	padding.PaddingRight = UDim.new(0, 6)
	padding.Parent = tooltipFrame
	
	local tooltipLabel = Instance.new("TextLabel")
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Reduces graphics and removes effects to boost FPS"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	tooltipLabel.Parent = tooltipFrame
	
	BoostFPSTickBox.MouseEnter:Connect(function()
		local abs = BoostFPSTickBox.AbsolutePosition
		local size = BoostFPSTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	BoostFPSTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function enable()
		isEnabled = true
		BoostFPSTickBox.BackgroundColor3 = getAccent()
	
		local Terrain = workspace:FindFirstChildWhichIsA("Terrain")
		if Terrain then
			savedSettings.WaterWaveSize = Terrain.WaterWaveSize
			savedSettings.WaterWaveSpeed = Terrain.WaterWaveSpeed
			savedSettings.WaterReflectance = Terrain.WaterReflectance
			savedSettings.WaterTransparency = Terrain.WaterTransparency
			Terrain.WaterWaveSize = 0
			Terrain.WaterWaveSpeed = 0
			Terrain.WaterReflectance = 0
			Terrain.WaterTransparency = 1
		end
	
		savedSettings.GlobalShadows = Lighting.GlobalShadows
		savedSettings.FogEnd = Lighting.FogEnd
		savedSettings.FogStart = Lighting.FogStart
		savedSettings.QualityLevel = settings().Rendering.QualityLevel
	
		Lighting.GlobalShadows = false
		Lighting.FogEnd = 9e9
		Lighting.FogStart = 9e9
		settings().Rendering.QualityLevel = 1
	
		savedParts = {}
		savedDecals = {}
		savedEffects = {}
	
		for _, v in pairs(game:GetDescendants()) do
			if v:IsA("BasePart") then
				savedParts[v] = {
					CastShadow = v.CastShadow,
					Material = v.Material,
					Reflectance = v.Reflectance,
					BackSurface = v.BackSurface,
					BottomSurface = v.BottomSurface,
					FrontSurface = v.FrontSurface,
					LeftSurface = v.LeftSurface,
					RightSurface = v.RightSurface,
					TopSurface = v.TopSurface,
				}
				v.CastShadow = false
				v.Material = Enum.Material.Plastic
				v.Reflectance = 0
				v.BackSurface = Enum.SurfaceType.SmoothNoOutlines
				v.BottomSurface = Enum.SurfaceType.SmoothNoOutlines
				v.FrontSurface = Enum.SurfaceType.SmoothNoOutlines
				v.LeftSurface = Enum.SurfaceType.SmoothNoOutlines
				v.RightSurface = Enum.SurfaceType.SmoothNoOutlines
				v.TopSurface = Enum.SurfaceType.SmoothNoOutlines
			elseif v:IsA("Decal") then
				savedDecals[v] = {
					Transparency = v.Transparency,
					Texture = v.Texture,
				}
				v.Transparency = 1
				v.Texture = ""
			elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
				savedEffects[v] = {Lifetime = v.Lifetime}
				v.Lifetime = NumberRange.new(0)
			end
		end
	
		for _, v in pairs(Lighting:GetDescendants()) do
			if v:IsA("PostEffect") then
				savedEffects[v] = {Enabled = v.Enabled}
				v.Enabled = false
			end
		end
	
		descendantConn = workspace.DescendantAdded:Connect(function(child)
			task.spawn(function()
				if child:IsA("ForceField") or child:IsA("Sparkles") or child:IsA("Smoke") or child:IsA("Fire") or child:IsA("Beam") then
					RunService.Heartbeat:Wait()
					child:Destroy()
				elseif child:IsA("BasePart") then
					child.CastShadow = false
				end
			end)
		end)
	end
	
	local function disable()
		isEnabled = false
		BoostFPSTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
		if descendantConn then
			descendantConn:Disconnect()
			descendantConn = nil
		end
	
		local Terrain = workspace:FindFirstChildWhichIsA("Terrain")
		if Terrain then
			if savedSettings.WaterWaveSize then Terrain.WaterWaveSize = savedSettings.WaterWaveSize end
			if savedSettings.WaterWaveSpeed then Terrain.WaterWaveSpeed = savedSettings.WaterWaveSpeed end
			if savedSettings.WaterReflectance then Terrain.WaterReflectance = savedSettings.WaterReflectance end
			if savedSettings.WaterTransparency then Terrain.WaterTransparency = savedSettings.WaterTransparency end
		end
	
		if savedSettings.GlobalShadows ~= nil then Lighting.GlobalShadows = savedSettings.GlobalShadows end
		if savedSettings.FogEnd then Lighting.FogEnd = savedSettings.FogEnd end
		if savedSettings.FogStart then Lighting.FogStart = savedSettings.FogStart end
		if savedSettings.QualityLevel then settings().Rendering.QualityLevel = savedSettings.QualityLevel end
	
		for v, data in pairs(savedParts) do
			pcall(function()
				v.CastShadow = data.CastShadow
				v.Material = data.Material
				v.Reflectance = data.Reflectance
				v.BackSurface = data.BackSurface
				v.BottomSurface = data.BottomSurface
				v.FrontSurface = data.FrontSurface
				v.LeftSurface = data.LeftSurface
				v.RightSurface = data.RightSurface
				v.TopSurface = data.TopSurface
			end)
		end
	
		for v, data in pairs(savedDecals) do
			pcall(function()
				v.Transparency = data.Transparency
				v.Texture = data.Texture
			end)
		end
	
		for v, data in pairs(savedEffects) do
			pcall(function()
				if data.Lifetime then
					v.Lifetime = data.Lifetime
				elseif data.Enabled ~= nil then
					v.Enabled = data.Enabled
				end
			end)
		end
	
		savedParts = {}
		savedDecals = {}
		savedEffects = {}
	end
	
	BoostFPSTickBox.MouseButton1Click:Connect(function()
		if isEnabled then disable() else enable() end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_44);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TrollScript
local function C_48()
local script = G2L["48"];
	local TrollGroup = script.Parent
	local TrollMinimize = TrollGroup.TrollTitle.TrollMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(TrollGroup:GetChildren()) do
			if child:IsA("Frame") and string.sub(child.Name, -3) == "Row" then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = TrollGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	TrollMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			TrollGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			TrollMinimize.Text = "+"
		else
			TrollGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			TrollMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_48);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainLeftSide.TrollGroup.TouchFlingRow.TouchFlingScript
local function C_4e()
local script = G2L["4e"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local GriefFlingRow = script.Parent
	local GriefFlingTickBox = GriefFlingRow.GriefFlingTickBox
	
	local ProtectionGroup = GriefFlingRow.Parent
	local ProtectionAccentColor = ProtectionGroup.ProtectionAccentColor
	
	local WListRow = ProtectionGroup:FindFirstChild("WListRow")
	
	local function isWhitelisted(playerName)
		if not WListRow then return false end
		local wl = WListRow:GetAttribute("Whitelist") or ""
		for _, name in ipairs(wl:split(",")) do
			if name == playerName then return true end
		end
		return false
	end
	
	local isEnabled = false
	local deleteLog = {}
	local DELETE_LIMIT = 10
	local TIME_WINDOW = 60
	local DELETE_RADIUS = 24
	
	-- queue of players to fling
	local flingQueue = {}
	local currentlyFlinging = false
	
	GriefFlingTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "GriefFlingTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Automatically flings a player when they are detected griefing. Works best for non-admins. This can accidentally fling nearby players to the griefer"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	GriefFlingTickBox.MouseEnter:Connect(function()
		local abs = GriefFlingTickBox.AbsolutePosition
		local size = GriefFlingTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	
	GriefFlingTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- SEND CHAT
	-- ============================================================
	local TextChatService = game:GetService("TextChatService")
	local function sendChat(message)
		pcall(function()
			if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
				TextChatService.TextChannels.RBXGeneral:SendAsync(message)
			else
				game:GetService("ReplicatedStorage")
					.DefaultChatSystemChatEvents
					.SayMessageRequest
					:FireServer(message, "All")
			end
		end)
	end
	
	-- ============================================================
	-- FREEZE / UNFREEZE ANIMATIONS
	-- ============================================================
	local frozenTracks = {}
	
	local function freezeAnimations(character)
		frozenTracks = {}
		local hum = character:FindFirstChildWhichIsA("Humanoid")
		if not hum then return end
		local animator = hum:FindFirstChildWhichIsA("Animator")
		if not animator then return end
		for _, track in pairs(animator:GetPlayingAnimationTracks()) do
			table.insert(frozenTracks, {track = track, speed = track.Speed})
			track:AdjustSpeed(0)
		end
	end
	
	local function unfreezeAnimations(character)
		for _, data in pairs(frozenTracks) do
			pcall(function()
				data.track:AdjustSpeed(data.speed)
			end)
		end
		frozenTracks = {}
	end
	
	-- ============================================================
	-- FLING ONE PLAYER
	-- returns true if flung, false if failed
	-- ============================================================
	local rotations = {
		CFrame.Angles(0, 0, 0),
		CFrame.Angles(0, math.pi, 0),
		CFrame.Angles(math.pi, math.pi, 0),
		CFrame.Angles(math.pi, 0, 0),
		CFrame.Angles(0, 0, 0),
		CFrame.Angles(0, math.pi, 0),
	}
	
	local function flingOne(TargetPlayer, savedPos)
		local myChar = LocalPlayer.Character
		if not myChar then return false end
		local myHRP = myChar:FindFirstChild("HumanoidRootPart")
		local myHum = myChar:FindFirstChildWhichIsA("Humanoid")
		if not myHRP or not myHum then return false end
	
		local tChar = TargetPlayer.Character
		if not tChar then return false end
		local tHRP = tChar:FindFirstChild("HumanoidRootPart")
		if not tHRP then return false end
	
		workspace.CurrentCamera.CameraSubject = myHum
	
		local rotIndex = 1
		local lastVelocity = 0
		local highVelocityFrames = 0
		local flung = false
		local failed = false
		local startTime = tick()
	
		local flingConn
		flingConn = RunService.Heartbeat:Connect(function()
	
			if not isEnabled then
				flung = true
				failed = true
			end
	
			if not tHRP or not tHRP.Parent or not TargetPlayer.Parent then
				flung = true
				failed = true
			end
	
			if tick() - startTime >= 10 then
				flung = true
				failed = true
			end
	
			if not flung then
				local currentVel = tHRP.AssemblyLinearVelocity.Magnitude
				local velDelta = currentVel - lastVelocity
				lastVelocity = currentVel
	
				if currentVel > 1000 and velDelta > 500 then
					highVelocityFrames = highVelocityFrames + 1
				else
					highVelocityFrames = 0
				end
	
				if highVelocityFrames >= 2 then
					flung = true
				end
			end
	
			if flung then
				flingConn:Disconnect()
				return
			end
	
			if tHRP and tHRP.Parent then
	
				-- teleport around player (same as your script)
				local rot = rotations[rotIndex]
				myHRP.CFrame = tHRP.CFrame * rot
				myChar:SetPrimaryPartCFrame(tHRP.CFrame * rot)
	
				-- APPLY TOUCHFLING PHYSICS
				local vel = myHRP.Velocity
				myHRP.Velocity = vel * 999999999 + Vector3.new(0, 999999999, 0)
	
				RunService.RenderStepped:Wait()
	
				myHRP.Velocity = vel
	
				RunService.Stepped:Wait()
	
				myHRP.Velocity = vel + Vector3.new(0,0.1,0)
	
				rotIndex = rotIndex + 1
				if rotIndex > #rotations then
					myHRP.CFrame = savedPos
					myChar:SetPrimaryPartCFrame(savedPos)
					rotIndex = 1
				end
			end
		end)
	
		repeat RunService.Heartbeat:Wait() until flung or not flingConn.Connected
	
		return not failed
	end
	
	-- ============================================================
	-- FLING LOOP - processes queue
	-- ============================================================
	local function processFlingQueue()
		if currentlyFlinging then return end
		currentlyFlinging = true
	
		local myChar = LocalPlayer.Character
		if not myChar then currentlyFlinging = false return end
		local myHRP = myChar:FindFirstChild("HumanoidRootPart")
		local myHum = myChar:FindFirstChildWhichIsA("Humanoid")
		if not myHRP or not myHum then currentlyFlinging = false return end
	
		-- save position once for whole session
		local savedPos = myHRP.CFrame
	
		freezeAnimations(myChar)
		workspace.CurrentCamera.CameraSubject = myHum
	
		while #flingQueue > 0 and isEnabled do
			local target = table.remove(flingQueue, 1)
	
			-- skip if player left
			if not target or not target.Parent then continue end
			if not target.Character then continue end
	
			local success = flingOne(target, savedPos)
	
			if not success then
				sendChat("🚫Failed to Fling")
			end
	
			-- tp back to saved pos between targets
			if myHRP and myHRP.Parent then
				myHRP.CFrame = savedPos
				myChar:SetPrimaryPartCFrame(savedPos)
				for _, x in pairs(myChar:GetChildren()) do
					if x:IsA("BasePart") then
						x.Velocity = Vector3.new()
						x.RotVelocity = Vector3.new()
						x.AssemblyLinearVelocity = Vector3.new()
						x.AssemblyAngularVelocity = Vector3.new()
					end
				end
			end
		end
	
		-- all done, tp back and cleanup
		if myHRP and myHRP.Parent then
			myHRP.CFrame = savedPos
			myChar:SetPrimaryPartCFrame(savedPos)
	
			for _, x in pairs(myChar:GetChildren()) do
				if x:IsA("BasePart") then
					x.Velocity = Vector3.new()
					x.RotVelocity = Vector3.new()
					x.AssemblyLinearVelocity = Vector3.new()
					x.AssemblyAngularVelocity = Vector3.new()
				end
			end
	
			local protBV = Instance.new("BodyVelocity")
			protBV.Velocity = Vector3.new(0, 0, 0)
			protBV.MaxForce = Vector3.new(1/0, 1/0, 1/0)
			protBV.Parent = myHRP
	
			task.wait(0.5)
	
			if protBV and protBV.Parent then protBV:Destroy() end
	
			for _, x in pairs(myChar:GetChildren()) do
				if x:IsA("BasePart") then
					x.Velocity = Vector3.new()
					x.RotVelocity = Vector3.new()
					x.AssemblyLinearVelocity = Vector3.new()
					x.AssemblyAngularVelocity = Vector3.new()
				end
			end
		end
	
		workspace.CurrentCamera.CameraSubject = myHum
		unfreezeAnimations(myChar)
		currentlyFlinging = false
	end
	
	local function queueFling(player)
		-- dont add duplicates
		for _, p in ipairs(flingQueue) do
			if p == player then return end
		end
		table.insert(flingQueue, player)
		task.spawn(processFlingQueue)
	end
	
	-- ============================================================
	-- UTILITY
	-- ============================================================
	local function holdingDelete(player)
		local char = player.Character
		if not char then return false end
		for _, obj in pairs(char:GetChildren()) do
			if obj:IsA("Tool") and obj.Name:lower():find("delete") then
				return true
			end
		end
		return false
	end
	
	-- ============================================================
	-- DETECTION
	-- ============================================================
	local bricksFolder = nil
	local previousBricks = {}
	
	local function getOwner(brick)
		if not bricksFolder then return "Server" end
		if brick:IsDescendantOf(bricksFolder) then
			local parent = brick.Parent
			if parent and Players:FindFirstChild(parent.Name) then
				return parent.Name
			end
		end
		return "Server"
	end
	
	local function snapshot()
		local current = {}
		if not bricksFolder then return current end
		for _, obj in pairs(bricksFolder:GetDescendants()) do
			if obj:IsA("BasePart") and obj.Name == "Brick" then
				current[obj] = {
					position = obj.Position,
					owner = getOwner(obj)
				}
			end
		end
		return current
	end
	
	RunService.Heartbeat:Connect(function()
		if not isEnabled then return end
		if not bricksFolder then return end
	
		local currentBricks = snapshot()
	
		for oldBrick, data in pairs(previousBricks) do
			if not currentBricks[oldBrick] then
				local oldPos = data.position
				local ownerName = data.owner
				local closest = nil
				local closestDist = DELETE_RADIUS
	
				for _, player in pairs(Players:GetPlayers()) do
					local char = player.Character
					if char and char:FindFirstChild("HumanoidRootPart") then
						local dist = (char.HumanoidRootPart.Position - oldPos).Magnitude
						if dist <= closestDist and holdingDelete(player) then
							closestDist = dist
							closest = player
						end
					end
				end
	
				if closest and closest.Name ~= ownerName and not isWhitelisted(closest.Name) then
					deleteLog[closest.Name] = deleteLog[closest.Name] or {}
					table.insert(deleteLog[closest.Name], tick())
	
					local now = tick()
					local valid = {}
					for _, t in ipairs(deleteLog[closest.Name]) do
						if now - t <= TIME_WINDOW then
							table.insert(valid, t)
						end
					end
					deleteLog[closest.Name] = valid
	
					if #valid >= DELETE_LIMIT then
						queueFling(closest)
						deleteLog[closest.Name] = {}
					end
				end
			end
		end
	
		previousBricks = currentBricks
	end)
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local function enable()
		local safeRow = ProtectionGroup:FindFirstChild("GriefFlingSafeRow")
		if safeRow then
			local safeTickBox = safeRow:FindFirstChild("GriefFlingSafeTickBox")
			if safeTickBox then
				safeTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
			end
			ProtectionGroup:SetAttribute("SafeFlingEnabled", false)
		end
		isEnabled = true
		flingQueue = {}
		bricksFolder = workspace:FindFirstChild("Bricks")
		if bricksFolder then
			previousBricks = snapshot()
		end
		if ProtectionAccentColor then
			GriefFlingTickBox.BackgroundColor3 = ProtectionAccentColor.BackgroundColor3
		else
			GriefFlingTickBox.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
		end
	end
	
	local function disable()
		isEnabled = false
		deleteLog = {}
		previousBricks = {}
		flingQueue = {}
		GriefFlingTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	end
	
	-- ============================================================
	-- TICKBOX TOGGLE
	-- ============================================================
	GriefFlingTickBox.MouseButton1Click:Connect(function()
		local forcedOff = ProtectionGroup:GetAttribute("FlingEnabled") == false
		if forcedOff then
			ProtectionGroup:SetAttribute("FlingEnabled", nil)
			enable()
		elseif isEnabled then
			disable()
		else
			enable()
		end
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_4e);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiScript
local function C_54()
local script = G2L["54"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AntiGroup = script.Parent
	local AntiMinimize = AntiGroup.AntiTitle.AntiMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(AntiGroup:GetChildren()) do
			if child:IsA("Frame") and string.sub(child.Name, -3) == "Row" then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = AntiGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	AntiMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
	
		if isMinimized then
			AntiGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			AntiMinimize.Text = "+"
		else
			AntiGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			AntiMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_54);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiFlingRow.AntiFlingScript
local function C_5a()
local script = G2L["5a"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AntiFlingRow = script.Parent
	local AntiFlingTickBox = AntiFlingRow:WaitForChild("AntiFlingTickBox")
	local AntiGroup = AntiFlingRow.Parent
	local AntiAccentColor = AntiGroup:WaitForChild("AntiAccentColor")
	
	local isEnabled = false
	local steppedConnection = nil
	local accentColor = AntiAccentColor.BackgroundColor3
	
	AntiFlingTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local function getScreenGui()
		local parent = AntiFlingRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiFlingTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Disables collision with other players"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	AntiFlingTickBox.MouseEnter:Connect(function()
		local absPos = AntiFlingTickBox.AbsolutePosition
		local absSize = AntiFlingTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	AntiFlingTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			AntiFlingTickBox.BackgroundColor3 = accentColor
		else
			AntiFlingTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local function setCanCollide(state)
		for _, player in pairs(Players:GetPlayers()) do
			if player ~= LocalPlayer and player.Character then
				for _, part in pairs(player.Character:GetDescendants()) do
					if part:IsA("BasePart") then
						part.CanCollide = state
					end
				end
			end
		end
	end
	
	-- Handle new players joining
	Players.PlayerAdded:Connect(function(player)
		if isEnabled then
			player.CharacterAdded:Connect(function(character)
				for _, part in pairs(character:GetDescendants()) do
					if part:IsA("BasePart") then
						part.CanCollide = false
					end
				end
			end)
		end
	end)
	
	local function enableAntiFling()
		setCanCollide(false)
		steppedConnection = RunService.Stepped:Connect(function()
			for _, player in pairs(Players:GetPlayers()) do
				if player ~= LocalPlayer and player.Character then
					for _, part in pairs(player.Character:GetDescendants()) do
						if part:IsA("BasePart") then
							part.CanCollide = false
						end
					end
				end
			end
		end)
	end
	
	local function disableAntiFling()
		if steppedConnection then
			steppedConnection:Disconnect()
			steppedConnection = nil
		end
		setCanCollide(true)
	end
	
	AntiFlingTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			enableAntiFling()
			print("Anti-Fling: ENABLED")
		else
			disableAntiFling()
			print("Anti-Fling: DISABLED")
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function(character)
		if isEnabled then
			-- Re-enable anti-fling after respawn
			setCanCollide(false)
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disableAntiFling()
		end
	end)
end;
task.spawn(C_5a);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiAfkRow.AntiAfkScript
local function C_5e()
local script = G2L["5e"];
	local Players = game:GetService("Players")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AntiAfkRow = script.Parent
	local AntiAfkTickBox = AntiAfkRow.AntiAfkTickBox
	local AntiGroup = AntiAfkRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	
	local isEnabled = false
	local connection = nil
	local accentColor = AntiAccentColor.BackgroundColor3
	
	AntiAfkTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local function getScreenGui()
		local parent = AntiAfkRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiAfkTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Prevents you from being kicked for being AFK"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	AntiAfkTickBox.MouseEnter:Connect(function()
		local absPos = AntiAfkTickBox.AbsolutePosition
		local absSize = AntiAfkTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	AntiAfkTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			AntiAfkTickBox.BackgroundColor3 = accentColor
		else
			AntiAfkTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local function enableAntiAfk()
		-- Fires a fake input every 60 seconds to prevent afk detection
		connection = task.spawn(function()
			while isEnabled do
				local virtualUser = game:GetService("VirtualUser")
				virtualUser:CaptureController()
				virtualUser:ClickButton2(Vector2.new())
				task.wait(60)
			end
		end)
	end
	
	local function disableAntiAfk()
		if connection then
			task.cancel(connection)
			connection = nil
		end
	end
	
	AntiAfkTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			enableAntiAfk()
		else
			disableAntiAfk()
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			disableAntiAfk()
		end
	end)
end;
task.spawn(C_5e);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiRagdollRow.AntiRagdollScript
local function C_62()
local script = G2L["62"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AntiRagdollRow = script.Parent
	local AntiRagdollTickBox = AntiRagdollRow.AntiRagdollTickBox
	local AntiGroup = AntiRagdollRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	
	local isEnabled = false
	local steppedConnection = nil
	local accentColor = AntiAccentColor.BackgroundColor3
	
	AntiRagdollTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local function getScreenGui()
		local parent = AntiRagdollRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiRagdollTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Prevents your character from ragdolling. May not work in all games"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	AntiRagdollTickBox.MouseEnter:Connect(function()
		local absPos = AntiRagdollTickBox.AbsolutePosition
		local absSize = AntiRagdollTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	AntiRagdollTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			AntiRagdollTickBox.BackgroundColor3 = accentColor
		else
			AntiRagdollTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local function enableAntiRagdoll()
		local character = LocalPlayer.Character
		if not character then return end
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
	
		steppedConnection = RunService.Stepped:Connect(function()
			if not isEnabled then return end
			local chr = LocalPlayer.Character
			if not chr then return end
			local hum = chr:FindFirstChildOfClass("Humanoid")
			if not hum then return end
	
			-- Disable ragdoll states
			hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
			hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
	
			-- Remove any BallSocketConstraints or HingeConstraints added by ragdoll systems
			for _, part in pairs(chr:GetDescendants()) do
				if part:IsA("BallSocketConstraint") or part:IsA("HingeConstraint") then
					if part:FindFirstAncestorOfClass("Motor6D") == nil then
						part.Enabled = false
					end
				end
			end
	
			-- Keep humanoid upright
			if hum:GetState() == Enum.HumanoidStateType.Ragdoll or
				hum:GetState() == Enum.HumanoidStateType.FallingDown then
				hum:ChangeState(Enum.HumanoidStateType.GettingUp)
			end
		end)
	end
	
	local function disableAntiRagdoll()
		if steppedConnection then
			steppedConnection:Disconnect()
			steppedConnection = nil
		end
	
		local character = LocalPlayer.Character
		if not character then return end
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
	
		humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
		humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
	
		for _, part in pairs(character:GetDescendants()) do
			if part:IsA("BallSocketConstraint") or part:IsA("HingeConstraint") then
				part.Enabled = true
			end
		end
	end
	
	AntiRagdollTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			enableAntiRagdoll()
		else
			disableAntiRagdoll()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			task.wait(0.1)
			enableAntiRagdoll()
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			disableAntiRagdoll()
		end
	end)
end;
task.spawn(C_62);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiVoidRow.AntiVoidScript
local function C_66()
local script = G2L["66"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AntiVoidRow = script.Parent
	local AntiVoidTickBox = AntiVoidRow.AntiVoidTickBox
	local AntiGroup = AntiVoidRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	
	local isEnabled = false
	local accentColor = AntiAccentColor.BackgroundColor3
	
	AntiVoidTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local function getScreenGui()
		local parent = AntiVoidRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiVoidTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Prevents your character from falling into the void"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	AntiVoidTickBox.MouseEnter:Connect(function()
		local absPos = AntiVoidTickBox.AbsolutePosition
		local absSize = AntiVoidTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	AntiVoidTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			AntiVoidTickBox.BackgroundColor3 = accentColor
		else
			AntiVoidTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local fpsdiv = 5
	local originalDestroyHeight = workspace.FallenPartsDestroyHeight
	local dhoffset = 5
	local dhto = 25
	local connectsextra = {}
	local charcframe = nil
	local tpcframe = nil
	local stopped = false
	local steppedConnection = nil
	local charaddedConnection = nil
	
	local function dochar(character)
		coroutine.wrap(function()
			repeat task.wait() until character:FindFirstChildWhichIsA("Humanoid")
			local hum = character:FindFirstChildWhichIsA("Humanoid")
			local state = hum:GetState()
			table.insert(connectsextra, hum.StateChanged:Connect(function(old, new)
				state = new
			end))
			local oldstate = nil
			while hum ~= nil and hum.Parent ~= nil and not stopped do
				task.wait()
				if state ~= oldstate and (state ~= Enum.HumanoidStateType.Jumping and state ~= Enum.HumanoidStateType.Freefall)
					or (state == Enum.HumanoidStateType.Running or state == Enum.HumanoidStateType.Landed) then
					tpcframe = charcframe
				end
				oldstate = state
			end
		end)()
	end
	
	local function fixchar(character, part, destroyheightnew)
		if character then
			local piv = character:GetPivot()
			character:PivotTo(CFrame.new(piv.Position.X, workspace.FallenPartsDestroyHeight + dhto + character:GetExtentsSize().Y, piv.Position.Z))
		end
		if part then
			part.Velocity = Vector3.new(0, 0, 0)
			if character then
				for _, v in pairs(character:GetChildren()) do
					if v:IsA("BasePart") then
						v.Velocity = Vector3.new(0, 0, 0)
					end
				end
			end
		end
	end
	
	local function enableAntiVoid()
		stopped = false
		workspace.FallenPartsDestroyHeight = -50000
		local destroyheightnew = originalDestroyHeight + dhoffset
	
		local character = LocalPlayer.Character
		if character then
			dochar(character)
		end
	
		charaddedConnection = LocalPlayer.CharacterAdded:Connect(function(newCharacter)
			dochar(newCharacter)
		end)
	
		steppedConnection = RunService.Stepped:Connect(function()
			local chr = LocalPlayer.Character
			if chr ~= nil and chr:FindFirstChildWhichIsA("BasePart") then
				local part = chr:FindFirstChild("HumanoidRootPart") or chr:FindFirstChildWhichIsA("BasePart")
				local cfr = (chr:FindFirstChild("HumanoidRootPart") and chr.HumanoidRootPart.CFrame) or chr:GetPivot()
				charcframe = cfr
	
				if cfr.Position.Y < destroyheightnew then
					fixchar(chr, part, destroyheightnew)
				end
	
				local partvel = part.Velocity
				if (partvel.Y / fpsdiv) + part.Position.Y < destroyheightnew then
					fixchar(chr, part, destroyheightnew)
				end
			end
		end)
	end
	
	local function disableAntiVoid()
		stopped = true
		workspace.FallenPartsDestroyHeight = originalDestroyHeight
	
		if steppedConnection then
			steppedConnection:Disconnect()
			steppedConnection = nil
		end
		if charaddedConnection then
			charaddedConnection:Disconnect()
			charaddedConnection = nil
		end
		for _, v in pairs(connectsextra) do
			v:Disconnect()
		end
		connectsextra = {}
	end
	
	AntiVoidTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			enableAntiVoid()
		else
			disableAntiVoid()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			originalDestroyHeight = workspace.FallenPartsDestroyHeight ~= -50000 and workspace.FallenPartsDestroyHeight or originalDestroyHeight
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isEnabled then
				disableAntiVoid()
			end
		end
	end)
end;
task.spawn(C_66);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKBRow.AntiKBScript
local function C_6a()
local script = G2L["6a"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AntiKBRow = script.Parent
	local AntiKBTickBox = AntiKBRow.AntiKBTickBox
	local AntiGroup = AntiKBRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	
	local isEnabled = false
	local connection = nil
	local accentColor = AntiAccentColor.BackgroundColor3
	
	AntiKBTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local function getScreenGui()
		local parent = AntiKBRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiKBTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Prevents your character from being knocked back. May not work in all games"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	AntiKBTickBox.MouseEnter:Connect(function()
		local absPos = AntiKBTickBox.AbsolutePosition
		local absSize = AntiKBTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	AntiKBTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			AntiKBTickBox.BackgroundColor3 = accentColor
		else
			AntiKBTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local function enableAntiKB()
		connection = RunService.Heartbeat:Connect(function()
			if not isEnabled then return end
			local character = LocalPlayer.Character
			if not character then return end
			local hrp = character:FindFirstChild("HumanoidRootPart")
			if not hrp then return end
			hrp.AssemblyLinearVelocity = Vector3.new(0, hrp.AssemblyLinearVelocity.Y, 0)
			hrp.AssemblyAngularVelocity = Vector3.zero
		end)
	end
	
	local function disableAntiKB()
		if connection then
			connection:Disconnect()
			connection = nil
		end
	end
	
	AntiKBTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			enableAntiKB()
		else
			disableAntiKB()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			disableAntiKB()
			enableAntiKB()
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			disableAntiKB()
		end
	end)
end;
task.spawn(C_6a);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.AntiGroup.AntiKickRow.AntiKickScript
local function C_6e()
local script = G2L["6e"];
	local Players = game:GetService("Players")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AntiKickRow = script.Parent
	local AntiKickTickBox = AntiKickRow.AntiKickTickBox
	local AntiGroup = AntiKickRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	
	local isEnabled = false
	local oldhmmi = nil
	local oldhmmnc = nil
	local oldKickFunction = nil
	
	local function getAccent()
		return AntiAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = AntiKickRow
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiKickTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Prevents the game from kicking you via LocalScript. Does not work on server-side kicks."
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	AntiKickTickBox.MouseEnter:Connect(function()
		local abs = AntiKickTickBox.AbsolutePosition
		local size = AntiKickTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	AntiKickTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function enableAntiKick()
		isEnabled = true
		AntiKickTickBox.BackgroundColor3 = getAccent()
		if not hookmetamethod then return end
	
		if hookfunction then
			oldKickFunction = hookfunction(LocalPlayer.Kick, function() end)
		end
	
		oldhmmi = hookmetamethod(game, "__index", function(self, method)
			if self == LocalPlayer and method:lower() == "kick" then
				return error("Expected ':' not '.' calling member function Kick", 2)
			end
			return oldhmmi(self, method)
		end)
	
		oldhmmnc = hookmetamethod(game, "__namecall", function(self, ...)
			if self == LocalPlayer and getnamecallmethod():lower() == "kick" then
				return
			end
			return oldhmmnc(self, ...)
		end)
	end
	
	local function disableAntiKick()
		isEnabled = false
		AntiKickTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
		if oldKickFunction and hookfunction then
			pcall(function()
				hookfunction(LocalPlayer.Kick, oldKickFunction)
			end)
			oldKickFunction = nil
		end
	
		if oldhmmi and hookmetamethod then
			pcall(function()
				hookmetamethod(game, "__index", oldhmmi)
			end)
			oldhmmi = nil
		end
	
		if oldhmmnc and hookmetamethod then
			pcall(function()
				hookmetamethod(game, "__namecall", oldhmmnc)
			end)
			oldhmmnc = nil
		end
	end
	
	AntiKickTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disableAntiKick()
		else
			enableAntiKick()
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isEnabled then disableAntiKick() end
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_6e);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.CharacterScript
local function C_72()
local script = G2L["72"];
	-- Get references to GUI elements
	local CharacterGroup = script.Parent
	local CharacterMinimize = CharacterGroup.CharacterTitle.CharacterMinimize
	
	-- Automatically find all Row frames inside CharacterGroup
	local function getRows()
		local rows = {}
		for _, child in ipairs(CharacterGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	-- Groupbox minimize functionality
	local isMinimized = false
	local originalSize = CharacterGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	CharacterMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
	
		if isMinimized then
			CharacterGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			CharacterMinimize.Text = "+"
		else
			CharacterGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			CharacterMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_72);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.NoclipRow.NoclipScript
local function C_78()
local script = G2L["78"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local NoclipRow = script.Parent
	local NoclipTickBox = NoclipRow.NoclipTickBox
	local CharacterGroup = NoclipRow.Parent
	local CharacterAccentColor = CharacterGroup.CharacterAccentColor
	
	local isEnabled = false
	local steppedConnection = nil
	local accentColor = CharacterAccentColor.BackgroundColor3
	
	NoclipTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local nocollideParts = {"Head", "Left Arm", "Right Arm", "LeftHand", "RightHand", "LeftLowerArm", "RightLowerArm", "LeftUpperArm", "RightUpperArm"}
	
	local function getScreenGui()
		local parent = NoclipRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "NoclipTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Allows your character to walk through walls"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	NoclipTickBox.MouseEnter:Connect(function()
		local absPos = NoclipTickBox.AbsolutePosition
		local absSize = NoclipTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	NoclipTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			NoclipTickBox.BackgroundColor3 = accentColor
		else
			NoclipTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local function setCollisionDefaults()
		local character = LocalPlayer.Character
		if not character then return end
		for _, part in pairs(character:GetChildren()) do
			if part:IsA("BasePart") then
				local shouldCollide = true
				for _, name in pairs(nocollideParts) do
					if part.Name == name then
						shouldCollide = false
						break
					end
				end
				part.CanCollide = shouldCollide
			end
		end
	end
	
	local function enableNoclip()
		steppedConnection = RunService.Stepped:Connect(function()
			if isEnabled then
				local character = LocalPlayer.Character
				if not character then return end
				for _, part in pairs(character:GetChildren()) do
					if part:IsA("BasePart") then
						part.CanCollide = false
					end
				end
			end
		end)
	end
	
	local function disableNoclip()
		if steppedConnection then
			steppedConnection:Disconnect()
			steppedConnection = nil
		end
		setCollisionDefaults()
		local character = LocalPlayer.Character
		if character then
			local humanoid = character:FindFirstChildOfClass("Humanoid")
			if humanoid then
				humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
			end
		end
	end
	
	NoclipTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			enableNoclip()
		else
			disableNoclip()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			isEnabled = false
			setTickboxState(false)
			disableNoclip()
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			if steppedConnection then
				steppedConnection:Disconnect()
				steppedConnection = nil
			end
			setCollisionDefaults()
		end
	end)
end;
task.spawn(C_78);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.FreezeAnimRow.FreezeAnimScript
local function C_7c()
local script = G2L["7c"];
	local Players = game:GetService("Players")
	
	local LocalPlayer = Players.LocalPlayer
	
	local FreezeAnimRow = script.Parent
	local FreezeAnimTickBox = FreezeAnimRow.FreezeAnimTickBox
	local CharacterGroup = FreezeAnimRow.Parent
	local CharacterAccentColor = CharacterGroup.CharacterAccentColor
	
	local isEnabled = false
	local accentColor = CharacterAccentColor.BackgroundColor3
	
	FreezeAnimTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	-- Wait for GUI to be fully loaded
	if not game:IsLoaded() then
		game.Loaded:Wait()
	end
	
	-- Wait for the tickbox to be fully positioned
	repeat
		task.wait()
	until FreezeAnimTickBox.AbsoluteSize.X > 0
	
	local function getScreenGui()
		local parent = FreezeAnimRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "FreezeAnimTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Freezes your character animation in its current pose"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	FreezeAnimTickBox.MouseEnter:Connect(function()
		local absPos = FreezeAnimTickBox.AbsolutePosition
		local absSize = FreezeAnimTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	FreezeAnimTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			FreezeAnimTickBox.BackgroundColor3 = accentColor
		else
			FreezeAnimTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local function freezeAnimations()
		local character = LocalPlayer.Character
		if not character then return end
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
		local animate = character:FindFirstChild("Animate")
		if animate then
			animate.Disabled = true
		end
		for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
			track:AdjustSpeed(0)
		end
	end
	
	local function unfreezeAnimations()
		local character = LocalPlayer.Character
		if not character then return end
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
		local animate = character:FindFirstChild("Animate")
		if animate then
			animate.Disabled = false
		end
		for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
			track:AdjustSpeed(1)
		end
	end
	
	FreezeAnimTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			freezeAnimations()
		else
			unfreezeAnimations()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			isEnabled = false
			setTickboxState(false)
			unfreezeAnimations()
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isEnabled then
				unfreezeAnimations()
			end
		end
	end)
end;
task.spawn(C_7c);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpSpawnRow.TpSpawnScript
local function C_80()
local script = G2L["80"];
	local Players = game:GetService("Players")
	
	local LocalPlayer = Players.LocalPlayer
	
	local TpSpawnRow = script.Parent
	local TpSpawnButton = TpSpawnRow.TpSpawnButton
	
	TpSpawnButton.MouseButton1Click:Connect(function()
		local character = LocalPlayer.Character
		if not character then return end
		local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
		if not humanoidRootPart then return end
	
		local spawnLocation = workspace:FindFirstChildOfClass("SpawnLocation")
		if spawnLocation then
			humanoidRootPart.CFrame = CFrame.new(spawnLocation.Position + Vector3.new(0, 5, 0))
		else
			humanoidRootPart.CFrame = CFrame.new(0, 5, 0)
		end
	end)
end;
task.spawn(C_80);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.GodModeRow.GodModeScript
local function C_83()
local script = G2L["83"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local GodModeRow = script.Parent
	local GodModeTickBox = GodModeRow.GodModeTickBox
	local CharacterGroup = GodModeRow.Parent
	local CharacterAccentColor = CharacterGroup.CharacterAccentColor
	
	local isEnabled = false
	local Connections = {}
	local accentColor = CharacterAccentColor.BackgroundColor3
	
	GodModeTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local function getScreenGui()
		local parent = GodModeRow
		while parent do
			if parent:IsA("ScreenGui") then
				return parent
			end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "GodModeTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.Name = "TooltipText"
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Makes your character invincible. May not work in all games"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	GodModeTickBox.MouseEnter:Connect(function()
		local absPos = GodModeTickBox.AbsolutePosition
		local absSize = GodModeTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, absPos.X + absSize.X + 10, 0, absPos.Y)
		tooltipFrame.Visible = true
	end)
	GodModeTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function setTickboxState(ticked)
		if ticked then
			GodModeTickBox.BackgroundColor3 = accentColor
		else
			GodModeTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	local function enableGodmode()
		for _, conn in pairs(Connections) do
			if conn then conn:Disconnect() end
		end
		Connections = {}
	
		local character = LocalPlayer.Character
		if not character then return end
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
	
		humanoid.MaxHealth = math.huge
		humanoid.Health = math.huge
	
		table.insert(Connections, RunService.RenderStepped:Connect(function()
			if not isEnabled then return end
			if shared.GodModePaused then return end
			if humanoid and humanoid.Parent then
				humanoid.MaxHealth = math.huge
				humanoid.Health = math.huge
			end
		end))
	
		table.insert(Connections, humanoid:GetPropertyChangedSignal("Health"):Connect(function()
			if isEnabled and humanoid and humanoid.Parent then
				if humanoid.Health < math.huge then
					humanoid.MaxHealth = math.huge
					humanoid.Health = math.huge
				end
			end
		end))
	
		table.insert(Connections, humanoid.Died:Connect(function()
			if isEnabled then
				task.wait()
				humanoid.Health = math.huge
			end
		end))
	end
	
	local function disableGodmode()
		for _, conn in pairs(Connections) do
			if conn then conn:Disconnect() end
		end
		Connections = {}
	
		task.wait()
		local character = LocalPlayer.Character
		if character then
			local humanoid = character:FindFirstChildOfClass("Humanoid")
			if humanoid then
				humanoid.MaxHealth = 100
				task.wait()
				humanoid.Health = 100
			end
		end
	end
	
	GodModeTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		setTickboxState(isEnabled)
	
		if isEnabled then
			enableGodmode()
		else
			disableGodmode()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function(character)
		if isEnabled then
			character:WaitForChild("Humanoid")
			task.wait(0.1)
			enableGodmode()
		end
	end)
	
	script.Parent.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			for _, conn in pairs(Connections) do
				if conn then conn:Disconnect() end
			end
			Connections = {}
			local character = LocalPlayer.Character
			if character then
				local humanoid = character:FindFirstChildOfClass("Humanoid")
				if humanoid then
					humanoid.MaxHealth = 100
					humanoid.Health = 100
				end
			end
		end
	end)
end;
task.spawn(C_83);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow2.TpPlayerScript2
local function C_87()
local script = G2L["87"];
	local Players = game:GetService("Players")
	
	local LocalPlayer = Players.LocalPlayer
	
	local TpPlayerRow2 = script.Parent
	local TpPlayerButton = TpPlayerRow2.TpPlayerButton
	local TpPlayerRow1 = TpPlayerRow2.Parent.TpPlayerRow1
	
	-- Wait for GUI to be fully loaded
	if not game:IsLoaded() then
		game.Loaded:Wait()
	end
	
	TpPlayerButton.MouseButton1Click:Connect(function()
		local selectedName = TpPlayerRow1:GetAttribute("SelectedPlayer")
	
		if not selectedName or selectedName == "" then
			return
		end
	
		local targetPlayer = Players:FindFirstChild(selectedName)
		if not targetPlayer then return end
	
		local targetCharacter = targetPlayer.Character
		if not targetCharacter then return end
	
		local targetHRP = targetCharacter:FindFirstChild("HumanoidRootPart")
		if not targetHRP then return end
	
		local myCharacter = LocalPlayer.Character
		if not myCharacter then return end
	
		local myHRP = myCharacter:FindFirstChild("HumanoidRootPart")
		if not myHRP then return end
	
		myHRP.CFrame = targetHRP.CFrame + Vector3.new(0, 3, 0)
	end)
end;
task.spawn(C_87);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.TpPlayerRow1.TpPlayerScript
local function C_8b()
local script = G2L["8b"];
	local Players = game:GetService("Players")
	local UserInputService = game:GetService("UserInputService")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local TpPlayerRow1 = script.Parent
	local TpPlayerDropdown = TpPlayerRow1.TpPlayerDropdown
	local CharacterGroup = TpPlayerRow1.Parent
	
	local selectedPlayer = nil
	local isOpen = false
	local showDisplay = false
	local showFriends = false
	local friendsList = {}
	
	local ROW_HEIGHT = 16
	local MAX_ROWS = 7
	local BG = Color3.fromRGB(24, 24, 24)
	local BORDER = Color3.fromRGB(57, 57, 57)
	local WHITE = Color3.new(1, 1, 1)
	local DIM = Color3.fromRGB(120, 120, 120)
	local BLUE = Color3.fromRGB(0, 123, 255)
	local HOVER = Color3.fromRGB(36, 36, 36)
	local BTN_BG = Color3.fromRGB(36, 36, 36)
	
	-- Wait for GUI to be fully loaded
	if not game:IsLoaded() then
		game.Loaded:Wait()
	end
	
	-- Wait for the dropdown to be fully positioned
	repeat
		task.wait()
	until TpPlayerDropdown.AbsoluteSize.X > 0
	
	local mainLabel = Instance.new("TextLabel")
	mainLabel.Name = "MainLabel"
	mainLabel.Size = UDim2.new(1, -24, 1, 0)
	mainLabel.Position = UDim2.new(0, 0, 0, 0)
	mainLabel.BackgroundTransparency = 1
	mainLabel.Text = "Select Player"
	mainLabel.TextColor3 = WHITE
	mainLabel.TextSize = 14
	mainLabel.Font = Enum.Font.Code
	mainLabel.TextXAlignment = Enum.TextXAlignment.Center
	mainLabel.TextTruncate = Enum.TextTruncate.AtEnd
	mainLabel.ZIndex = 2
	mainLabel.Parent = TpPlayerDropdown
	
	local mainArrow = Instance.new("TextLabel")
	mainArrow.Name = "MainArrow"
	mainArrow.Size = UDim2.new(0, 24, 1, 0)
	mainArrow.Position = UDim2.new(1, -24, 0, 0)
	mainArrow.BackgroundTransparency = 1
	mainArrow.Text = "▼"
	mainArrow.TextColor3 = WHITE
	mainArrow.TextSize = 14
	mainArrow.Font = Enum.Font.Code
	mainArrow.TextXAlignment = Enum.TextXAlignment.Center
	mainArrow.ZIndex = 2
	mainArrow.Parent = TpPlayerDropdown
	
	local function getScreenGui()
		local parent = TpPlayerRow1
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local BOTTOM_BAR_HEIGHT = ROW_HEIGHT + 2
	
	local panel = Instance.new("Frame")
	panel.Name = "DropdownPanel"
	panel.BackgroundColor3 = BG
	panel.BorderSizePixel = 0
	panel.Size = UDim2.new(0, 161, 0, ROW_HEIGHT)
	panel.ZIndex = 999
	panel.Visible = false
	panel.Parent = getScreenGui()
	
	local panelStroke = Instance.new("UIStroke")
	panelStroke.Color = BORDER
	panelStroke.Thickness = 1
	panelStroke.Parent = panel
	
	local searchBox = Instance.new("TextBox")
	searchBox.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
	searchBox.Position = UDim2.new(0, 0, 0, 0)
	searchBox.BackgroundColor3 = BG
	searchBox.BorderSizePixel = 0
	searchBox.Text = ""
	searchBox.PlaceholderText = "Search for users..."
	searchBox.PlaceholderColor3 = DIM
	searchBox.TextColor3 = WHITE
	searchBox.TextSize = 14
	searchBox.Font = Enum.Font.Code
	searchBox.TextXAlignment = Enum.TextXAlignment.Left
	searchBox.ClearTextOnFocus = false
	searchBox.ZIndex = 1000
	searchBox.Parent = panel
	
	local searchPad = Instance.new("UIPadding")
	searchPad.PaddingLeft = UDim.new(0, 6)
	searchPad.PaddingRight = UDim.new(0, 24)
	searchPad.Parent = searchBox
	
	local searchArrow = Instance.new("TextButton")
	searchArrow.Size = UDim2.new(0, 24, 0, ROW_HEIGHT)
	searchArrow.Position = UDim2.new(1, -24, 0, 0)
	searchArrow.BackgroundTransparency = 1
	searchArrow.Text = "▲"
	searchArrow.TextColor3 = WHITE
	searchArrow.TextSize = 14
	searchArrow.Font = Enum.Font.Code
	searchArrow.BorderSizePixel = 0
	searchArrow.ZIndex = 1001
	searchArrow.Parent = panel
	
	local scrollFrame = Instance.new("ScrollingFrame")
	scrollFrame.Position = UDim2.new(0, 0, 0, ROW_HEIGHT)
	scrollFrame.Size = UDim2.new(1, 0, 0, 0)
	scrollFrame.BackgroundTransparency = 1
	scrollFrame.BorderSizePixel = 0
	scrollFrame.ScrollBarThickness = 2
	scrollFrame.ScrollBarImageColor3 = BORDER
	scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
	scrollFrame.ZIndex = 1000
	scrollFrame.Parent = panel
	
	local listLayout = Instance.new("UIListLayout")
	listLayout.SortOrder = Enum.SortOrder.LayoutOrder
	listLayout.Parent = scrollFrame
	
	local bottomBar = Instance.new("Frame")
	bottomBar.Size = UDim2.new(1, 0, 0, BOTTOM_BAR_HEIGHT)
	bottomBar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
	bottomBar.BorderSizePixel = 0
	bottomBar.ZIndex = 1000
	bottomBar.Parent = panel
	
	local bottomStroke = Instance.new("UIStroke")
	bottomStroke.Color = BORDER
	bottomStroke.Thickness = 1
	bottomStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	bottomStroke.Parent = bottomBar
	
	local displayBtn = Instance.new("TextButton")
	displayBtn.Size = UDim2.new(0.5, -1, 1, 0)
	displayBtn.Position = UDim2.new(0, 0, 0, 0)
	displayBtn.BackgroundColor3 = BTN_BG
	displayBtn.BorderSizePixel = 0
	displayBtn.Text = "Show Display"
	displayBtn.TextColor3 = WHITE
	displayBtn.TextSize = 11
	displayBtn.Font = Enum.Font.Code
	displayBtn.ZIndex = 1001
	displayBtn.Parent = bottomBar
	
	local divider = Instance.new("Frame")
	divider.Size = UDim2.new(0, 1, 1, 0)
	divider.Position = UDim2.new(0.5, -1, 0, 0)
	divider.BackgroundColor3 = BORDER
	divider.BorderSizePixel = 0
	divider.ZIndex = 1001
	divider.Parent = bottomBar
	
	local friendsBtn = Instance.new("TextButton")
	friendsBtn.Size = UDim2.new(0.5, 0, 1, 0)
	friendsBtn.Position = UDim2.new(0.5, 1, 0, 0)
	friendsBtn.BackgroundColor3 = BTN_BG
	friendsBtn.BorderSizePixel = 0
	friendsBtn.Text = "Show Friends"
	friendsBtn.TextColor3 = WHITE
	friendsBtn.TextSize = 11
	friendsBtn.Font = Enum.Font.Code
	friendsBtn.ZIndex = 1001
	friendsBtn.Parent = bottomBar
	
	local cachedThumbs = {}
	local function getThumb(userId)
		if cachedThumbs[userId] then return cachedThumbs[userId] end
		local ok, img = pcall(function()
			return Players:GetUserThumbnailAsync(userId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size48x48)
		end)
		local result = ok and img or ""
		cachedThumbs[userId] = result
		return result
	end
	
	local function loadFriends()
		friendsList = {}
		local ok, pages = pcall(function()
			return LocalPlayer:GetFriendsAsync(200)
		end)
		if not ok then return end
		local function processPage()
			for _, v in pairs(pages:GetCurrentPage()) do
				friendsList[v.Id] = true
			end
		end
		processPage()
		while not pages.IsFinished do
			local ok2 = pcall(function() pages:AdvanceToNextPageAsync() end)
			if not ok2 then break end
			processPage()
		end
	end
	
	task.spawn(loadFriends)
	
	local function updatePanelSize(rowCount)
		local visibleRows = math.min(rowCount, MAX_ROWS)
		local scrollH = visibleRows * ROW_HEIGHT
		scrollFrame.Size = UDim2.new(1, 0, 0, scrollH)
		panel.Size = UDim2.new(0, 161, 0, ROW_HEIGHT + scrollH + BOTTOM_BAR_HEIGHT)
		scrollFrame.CanvasSize = UDim2.new(0, 0, 0, rowCount * ROW_HEIGHT)
		bottomBar.Position = UDim2.new(0, 0, 0, ROW_HEIGHT + scrollH)
	end
	
	local function buildList(filter)
		for _, child in pairs(scrollFrame:GetChildren()) do
			if child:IsA("Frame") then child:Destroy() end
		end
	
		local playerList = {}
		for _, p in pairs(Players:GetPlayers()) do
			if p ~= LocalPlayer then
				if not showFriends or friendsList[p.UserId] then
					table.insert(playerList, p)
				end
			end
		end
	
		table.sort(playerList, function(a, b)
			local nameA = showDisplay and a.DisplayName or a.Name
			local nameB = showDisplay and b.DisplayName or b.Name
			return nameA:lower() < nameB:lower()
		end)
	
		local filtered = {}
		for _, p in ipairs(playerList) do
			local displayText = showDisplay and p.DisplayName or p.Name
			if filter == "" or displayText:lower():find(filter:lower(), 1, true) then
				table.insert(filtered, p)
			end
		end
	
		for i, p in ipairs(filtered) do
			local isSelected = selectedPlayer == p
			local displayText = showDisplay and p.DisplayName or p.Name
	
			local row = Instance.new("Frame")
			row.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
			row.BackgroundColor3 = isSelected and HOVER or BG
			row.BorderSizePixel = 0
			row.LayoutOrder = i
			row.ZIndex = 1001
			row.Parent = scrollFrame
	
			local thumb = Instance.new("ImageLabel")
			thumb.Size = UDim2.new(0, 12, 0, 12)
			thumb.Position = UDim2.new(1, -16, 0.5, -6)
			thumb.BackgroundTransparency = 1
			thumb.Image = ""
			thumb.ZIndex = 1002
			thumb.Parent = row
	
			task.spawn(function()
				thumb.Image = getThumb(p.UserId)
			end)
	
			local nameLabel = Instance.new("TextLabel")
			nameLabel.Size = UDim2.new(1, -22, 1, 0)
			nameLabel.Position = UDim2.new(0, 6, 0, 0)
			nameLabel.BackgroundTransparency = 1
			nameLabel.Text = displayText
			nameLabel.TextColor3 = isSelected and BLUE or WHITE
			nameLabel.TextSize = 14
			nameLabel.Font = Enum.Font.Code
			nameLabel.TextXAlignment = Enum.TextXAlignment.Left
			nameLabel.TextTruncate = Enum.TextTruncate.AtEnd
			nameLabel.ZIndex = 1002
			nameLabel.Parent = row
	
			local rowBtn = Instance.new("TextButton")
			rowBtn.Size = UDim2.new(1, 0, 1, 0)
			rowBtn.BackgroundTransparency = 1
			rowBtn.Text = ""
			rowBtn.ZIndex = 1003
			rowBtn.Parent = row
	
			rowBtn.MouseEnter:Connect(function()
				if selectedPlayer ~= p then
					row.BackgroundColor3 = HOVER
				end
			end)
			rowBtn.MouseLeave:Connect(function()
				if selectedPlayer ~= p then
					row.BackgroundColor3 = BG
				end
			end)
	
			rowBtn.MouseButton1Click:Connect(function()
				selectedPlayer = p
				TpPlayerRow1:SetAttribute("SelectedPlayer", p.Name)
				isOpen = false
				panel.Visible = false
				mainLabel.Visible = true
				mainArrow.Visible = true
				mainArrow.Text = "▼"
				mainLabel.Text = showDisplay and p.DisplayName or p.Name
				mainLabel.TextColor3 = WHITE
			end)
		end
	
		updatePanelSize(#filtered)
	end
	
	local function openDropdown()
		isOpen = true
		mainLabel.Visible = false
		mainArrow.Visible = false
	
		-- Ensure the dropdown has valid position before opening
		repeat
			task.wait()
		until TpPlayerDropdown.AbsoluteSize.X > 0
	
		local absPos = TpPlayerDropdown.AbsolutePosition
		local absSize = TpPlayerDropdown.AbsoluteSize
		panel.Position = UDim2.new(0, absPos.X, 0, absPos.Y + absSize.Y)
		panel.Size = UDim2.new(0, absSize.X, 0, ROW_HEIGHT)
		panel.Visible = true
	
		searchBox.Text = ""
		buildList("")
	end
	
	local function closeDropdown()
		isOpen = false
		panel.Visible = false
		mainLabel.Visible = true
		mainArrow.Visible = true
		mainArrow.Text = "▼"
		if selectedPlayer then
			mainLabel.Text = showDisplay and selectedPlayer.DisplayName or selectedPlayer.Name
			mainLabel.TextColor3 = WHITE
		else
			mainLabel.Text = "Select Player"
			mainLabel.TextColor3 = WHITE
		end
	end
	
	displayBtn.MouseButton1Click:Connect(function()
		showDisplay = not showDisplay
		displayBtn.Text = showDisplay and "Show Username" or "Show Display"
		if selectedPlayer then
			mainLabel.Text = showDisplay and selectedPlayer.DisplayName or selectedPlayer.Name
		end
		if isOpen then
			buildList(searchBox.Text)
		end
	end)
	
	friendsBtn.MouseButton1Click:Connect(function()
		showFriends = not showFriends
		friendsBtn.Text = showFriends and "Show All" or "Show Friends"
		if isOpen then
			buildList(searchBox.Text)
		end
	end)
	
	TpPlayerDropdown.MouseButton1Click:Connect(function()
		if isOpen then
			closeDropdown()
		else
			openDropdown()
		end
	end)
	
	searchArrow.MouseButton1Click:Connect(function()
		closeDropdown()
	end)
	
	searchBox:GetPropertyChangedSignal("Text"):Connect(function()
		buildList(searchBox.Text)
	end)
	
	UserInputService.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 and isOpen then
			local mousePos = input.Position
			local panelPos = panel.AbsolutePosition
			local panelSize = panel.AbsoluteSize
			local btnPos = TpPlayerDropdown.AbsolutePosition
			local btnSize = TpPlayerDropdown.AbsoluteSize
	
			local inPanel = mousePos.X >= panelPos.X and mousePos.X <= panelPos.X + panelSize.X
				and mousePos.Y >= panelPos.Y and mousePos.Y <= panelPos.Y + panelSize.Y
			local inBtn = mousePos.X >= btnPos.X and mousePos.X <= btnPos.X + btnSize.X
				and mousePos.Y >= btnPos.Y and mousePos.Y <= btnPos.Y + btnSize.Y
	
			if not inPanel and not inBtn then
				closeDropdown()
			end
		end
	end)
	
	Players.PlayerRemoving:Connect(function(p)
		if selectedPlayer == p then
			selectedPlayer = nil
			TpPlayerRow1:SetAttribute("SelectedPlayer", "")
			mainLabel.Text = "Select Player"
			mainLabel.TextColor3 = WHITE
		end
		if isOpen then
			buildList(searchBox.Text)
		end
	end)
	
	Players.PlayerAdded:Connect(function()
		if isOpen then
			buildList(searchBox.Text)
		end
	end)
	
	TpPlayerRow1:SetAttribute("SelectedPlayer", "")
end;
task.spawn(C_8b);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MainPage.MainRightSide.CharacterGroup.ResetRow.ResetScript
local function C_8e()
local script = G2L["8e"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	local ResetRow = script.Parent
	local ResetButton = ResetRow.ResetButton
	local CharacterGroup = ResetRow.Parent
	local GodModeTickBox = CharacterGroup.GodModeRow.GodModeTickBox
	
	local HOLD_TIME = 0.5
	local GREY = Color3.fromRGB(80, 80, 80)
	local RED = Color3.fromRGB(180, 30, 30)
	local WHITE = Color3.new(1, 1, 1)
	
	-- Wait for GUI to be fully loaded
	if not game:IsLoaded() then
		game.Loaded:Wait()
	end
	
	-- Clear the button's default text
	ResetButton.Text = ""
	
	local fillBar = Instance.new("Frame")
	fillBar.Name = "FillBar"
	fillBar.Size = UDim2.new(0, 0, 1, 0)
	fillBar.Position = UDim2.new(0, 0, 0, 0)
	fillBar.BackgroundColor3 = GREY
	fillBar.BorderSizePixel = 0
	fillBar.ZIndex = 1
	fillBar.Parent = ResetButton
	
	local btnLabel = Instance.new("TextLabel")
	btnLabel.Name = "BtnLabel"
	btnLabel.Size = UDim2.new(1, 0, 1, 0)
	btnLabel.Position = UDim2.new(0, 0, 0, 0)
	btnLabel.BackgroundTransparency = 1
	btnLabel.Text = "Reset Character"
	btnLabel.TextColor3 = WHITE
	btnLabel.TextSize = 14
	btnLabel.Font = Enum.Font.Code
	btnLabel.ZIndex = 2
	btnLabel.Parent = ResetButton
	
	local holding = false
	local progress = 0
	local connection = nil
	
	local function isGodModeOn()
		return GodModeTickBox.BackgroundColor3 ~= Color3.fromRGB(0, 0, 0)
	end
	
	local function reset()
		local character = LocalPlayer.Character
		if character then
			local humanoid = character:FindFirstChildOfClass("Humanoid")
			if humanoid then
				humanoid.MaxHealth = 100
				humanoid.Health = 0
			end
		end
	end
	
	local function stopHold()
		holding = false
		if connection then
			connection:Disconnect()
			connection = nil
		end
		progress = 0
		fillBar.Size = UDim2.new(0, 0, 1, 0)
		fillBar.BackgroundColor3 = GREY
		btnLabel.Text = "Reset Character"
		btnLabel.TextColor3 = WHITE
	end
	
	ResetButton.MouseButton1Down:Connect(function()
		if isGodModeOn() then
			btnLabel.Text = "Disable God Mode to Reset"
			btnLabel.TextColor3 = WHITE
			btnLabel.TextScaled = true
			btnLabel.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
			btnLabel.BackgroundTransparency = 0
			btnLabel.BorderSizePixel = 0
			local stroke = btnLabel:FindFirstChildOfClass("UIStroke") or Instance.new("UIStroke", btnLabel)
			stroke.Color = Color3.fromRGB(57, 57, 57)
			stroke.Thickness = 1
			holding = false
			return
		end
	
		holding = true
		progress = 0
		btnLabel.Text = "Hold to Reset..."
		fillBar.BackgroundColor3 = GREY
	
		connection = RunService.RenderStepped:Connect(function(dt)
			if not holding then return end
			progress = progress + (dt / HOLD_TIME)
			progress = math.clamp(progress, 0, 1)
			fillBar.Size = UDim2.new(progress, 0, 1, 0)
	
			if progress >= 1 then
				btnLabel.Text = "Resetting..."
				stopHold()
				task.wait(0.1)
				reset()
			end
		end)
	end)
	
	ResetButton.MouseButton1Up:Connect(function()
		stopHold()
	end)
	
	ResetButton.MouseLeave:Connect(function()
		stopHold()
	end)
end;
task.spawn(C_8e);
-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPScript
local function C_94()
local script = G2L["94"];
	local ESPGroup = script.Parent
	local ESPMinimize = ESPGroup.ESPTitle.ESPMinimize
	local function getRows()
		local rows = {}
		for _, child in ipairs(ESPGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	local isMinimized = false
	local originalSize = ESPGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	ESPMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			ESPGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			ESPMinimize.Text = "+"
		else
			ESPGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			ESPMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_94);
-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPNameRow.ESPNameScript
local function C_9a()
local script = G2L["9a"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	local ESPGroup = script.Parent.Parent
	local ESPAccentColor = ESPGroup:WaitForChild("ESPAccentColor")
	local ESPNameTickBox = script.Parent:WaitForChild("ESPNameTickBox")
	
	local isEnabled = false
	local ESPColor = Color3.fromRGB(255, 255, 255)
	local ESPGuis = {}
	local renderConnection = nil
	
	ESPGroup:GetAttributeChangedSignal("ESPColor"):Connect(function()
		local newColor = ESPGroup:GetAttribute("ESPColor")
		if newColor then
			ESPColor = newColor
			for plr, data in pairs(ESPGuis) do
				if data and data.NameLabel then
					data.NameLabel.TextColor3 = newColor
				end
			end
		end
	end)
	
	local function getAccent()
		return ESPAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = ESPGroup
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(57, 57, 57)
	stroke.Thickness = 1
	stroke.Parent = tooltipFrame
	
	local padding = Instance.new("UIPadding")
	padding.PaddingTop = UDim.new(0, 6)
	padding.PaddingBottom = UDim.new(0, 6)
	padding.PaddingLeft = UDim.new(0, 6)
	padding.PaddingRight = UDim.new(0, 6)
	padding.Parent = tooltipFrame
	
	local tooltipLabel = Instance.new("TextLabel")
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Shows player names above their character"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	tooltipLabel.Parent = tooltipFrame
	
	ESPNameTickBox.MouseEnter:Connect(function()
		local abs = ESPNameTickBox.AbsolutePosition
		local size = ESPNameTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	ESPNameTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function createNameGui(targetPlayer)
		local char = targetPlayer.Character
		if not char then return end
		local head = char:FindFirstChild("Head")
		if not head then return end
	
		local old = head:FindFirstChild("ESPNameGui_" .. targetPlayer.Name)
		if old then old:Destroy() end
	
		local bb = Instance.new("BillboardGui")
		bb.Name = "ESPNameGui_" .. targetPlayer.Name
		bb.Size = UDim2.new(0, 200, 0, 30)
		bb.StudsOffset = Vector3.new(0, 3, 0)
		bb.Adornee = head
		bb.AlwaysOnTop = true
		bb.Enabled = true
		bb.Parent = head
	
		local label = Instance.new("TextLabel")
		label.Name = "NameLabel"
		label.Size = UDim2.new(1, 0, 1, 0)
		label.BackgroundTransparency = 1
		label.Text = targetPlayer.Name
		label.TextColor3 = ESPColor
		label.TextStrokeTransparency = 0
		label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
		label.TextSize = 14
		label.Font = Enum.Font.GothamSemibold
		label.Parent = bb
	
		ESPGuis[targetPlayer] = {BillboardGui = bb, NameLabel = label}
	
		local humanoid = char:FindFirstChildOfClass("Humanoid")
		if humanoid then
			humanoid.Died:Connect(function()
				if ESPGuis[targetPlayer] and ESPGuis[targetPlayer].BillboardGui then
					ESPGuis[targetPlayer].BillboardGui:Destroy()
				end
				ESPGuis[targetPlayer] = nil
			end)
		end
	end
	
	local function removeNameGui(targetPlayer)
		if ESPGuis[targetPlayer] then
			if ESPGuis[targetPlayer].BillboardGui then
				ESPGuis[targetPlayer].BillboardGui:Destroy()
			end
			ESPGuis[targetPlayer] = nil
		end
	end
	
	local function startRender()
		if renderConnection then renderConnection:Disconnect() end
		renderConnection = RunService.RenderStepped:Connect(function()
			local myChar = LocalPlayer.Character
			local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
			local maxDist = ESPGroup:GetAttribute("ESPMaxDist") or 1000
			local espEnabled = ESPGroup:GetAttribute("ESPEnabled")
			local useDisplay = ESPGroup.Parent.Parent.VisualRightSide:FindFirstChild("ESPSGroup") and ESPGroup.Parent.Parent.VisualRightSide.ESPSGroup:GetAttribute("UseDisplayName")
			local showDist = ESPGroup:GetAttribute("ShowDist")
			for plr, data in pairs(ESPGuis) do
				if data and data.NameLabel and data.BillboardGui then
					local targetChar = plr.Character
					local targetHRP = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
					if myHRP and targetHRP then
						local dist = (myHRP.Position - targetHRP.Position).Magnitude
						local distText = string.format("%.1f", dist)
						local name = useDisplay and plr.DisplayName or plr.Name
						if dist <= maxDist and espEnabled then
							data.BillboardGui.Enabled = true
							if isEnabled and showDist then
								data.NameLabel.Text = name .. " | " .. distText
							elseif isEnabled then
								data.NameLabel.Text = name
							elseif showDist then
								data.NameLabel.Text = distText
							else
								data.BillboardGui.Enabled = false
							end
							data.NameLabel.TextColor3 = ESPColor
						else
							data.BillboardGui.Enabled = false
						end
					end
				end
			end
		end)
	end
	
	local function enable()
		isEnabled = true
		ESPNameTickBox.BackgroundColor3 = getAccent()
		for _, plr in pairs(Players:GetPlayers()) do
			if plr ~= LocalPlayer then
				if not ESPGuis[plr] then
					createNameGui(plr)
				end
				plr.CharacterAdded:Connect(function()
					task.wait(0.5)
					if isEnabled or ESPGroup:GetAttribute("ShowDist") then
						createNameGui(plr)
					end
				end)
			end
		end
		startRender()
	end
	
	local function disable()
		isEnabled = false
		ESPNameTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		local showDist = ESPGroup:GetAttribute("ShowDist")
		if not showDist then
			if renderConnection then
				renderConnection:Disconnect()
				renderConnection = nil
			end
			for plr in pairs(ESPGuis) do
				removeNameGui(plr)
			end
		end
	end
	
	Players.PlayerAdded:Connect(function(plr)
		if (isEnabled or ESPGroup:GetAttribute("ShowDist")) and plr ~= LocalPlayer then
			createNameGui(plr)
			plr.CharacterAdded:Connect(function()
				task.wait(0.5)
				if isEnabled or ESPGroup:GetAttribute("ShowDist") then
					createNameGui(plr)
				end
			end)
		end
	end)
	
	Players.PlayerRemoving:Connect(function(plr)
		removeNameGui(plr)
	end)
	
	ESPNameTickBox.MouseButton1Click:Connect(function()
		if isEnabled then disable() else enable() end
	end)
	
	ESPGroup:GetAttributeChangedSignal("ShowDist"):Connect(function()
		local showDist = ESPGroup:GetAttribute("ShowDist")
		if not isEnabled and not showDist then
			if renderConnection then
				renderConnection:Disconnect()
				renderConnection = nil
			end
			for plr in pairs(ESPGuis) do
				removeNameGui(plr)
			end
		elseif not isEnabled and showDist then
			for _, plr in pairs(Players:GetPlayers()) do
				if plr ~= LocalPlayer then
					createNameGui(plr)
				end
			end
			startRender()
		end
	end)
	
	ESPGroup:GetAttributeChangedSignal("ESPEnabled"):Connect(function()
		local espEnabled = ESPGroup:GetAttribute("ESPEnabled")
		if espEnabled then
			if isEnabled or ESPGroup:GetAttribute("ShowDist") then
				for _, plr in pairs(Players:GetPlayers()) do
					if plr ~= LocalPlayer then
						if not ESPGuis[plr] then
							createNameGui(plr)
						else
							ESPGuis[plr].BillboardGui.Enabled = true
						end
					end
				end
				startRender()
			end
		else
			for plr, data in pairs(ESPGuis) do
				if data and data.BillboardGui then
					data.BillboardGui.Enabled = false
				end
			end
		end
	end)
	
	ESPGroup.Parent.Parent.VisualRightSide.ESPSGroup:GetAttributeChangedSignal("UseDisplayName"):Connect(function()
		local useDisplay = ESPGroup.Parent.Parent.VisualRightSide.ESPSGroup:GetAttribute("UseDisplayName")
		for plr, data in pairs(ESPGuis) do
			if data and data.NameLabel then
				data.NameLabel.Text = useDisplay and plr.DisplayName or plr.Name
			end
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_9a);
-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPMaxDistRow.ESPMaxDistScript
local function C_9e()
local script = G2L["9e"];
	local ESPGroup = script.Parent.Parent
	local ESPMaxDistRow = script.Parent
	local ESPMaxDistTickBox = ESPMaxDistRow:WaitForChild("ESPMaxDistTickBox")
	local ESPMaxDistChanger = ESPMaxDistRow:WaitForChild("ESPMaxDistChanger")
	
	local defaultDist = 1000
	
	local function getScreenGui()
		local parent = ESPGroup
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(57, 57, 57)
	stroke.Thickness = 1
	stroke.Parent = tooltipFrame
	
	local padding = Instance.new("UIPadding")
	padding.PaddingTop = UDim.new(0, 6)
	padding.PaddingBottom = UDim.new(0, 6)
	padding.PaddingLeft = UDim.new(0, 6)
	padding.PaddingRight = UDim.new(0, 6)
	padding.Parent = tooltipFrame
	
	local tooltipLabel = Instance.new("TextLabel")
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Maximum distance to show ESP on players. Default is 1000, INF to see all players "
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.XY
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.ZIndex = 101
	tooltipLabel.Parent = tooltipFrame
	
	ESPMaxDistTickBox.MouseEnter:Connect(function()
		local abs = ESPMaxDistTickBox.AbsolutePosition
		local size = ESPMaxDistTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	ESPMaxDistTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	ESPGroup:SetAttribute("ESPMaxDist", defaultDist)
	ESPMaxDistChanger.PlaceholderText = tostring(defaultDist)
	
	ESPMaxDistChanger.FocusLost:Connect(function()
		local input = ESPMaxDistChanger.Text
		if input:lower() == "inf" then
			ESPGroup:SetAttribute("ESPMaxDist", math.huge)
			ESPMaxDistChanger.PlaceholderText = "inf"
			ESPMaxDistChanger.Text = ""
		else
			local num = tonumber(input)
			if num and num > 0 then
				num = math.clamp(num, 1, 100000)
				ESPGroup:SetAttribute("ESPMaxDist", num)
				ESPMaxDistChanger.PlaceholderText = tostring(num)
				ESPMaxDistChanger.Text = ""
			else
				ESPMaxDistChanger.Text = ""
			end
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			ESPGroup:SetAttribute("ESPMaxDist", 1000)
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_9e);
-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPDistanceRow.ESPDistanceScript
local function C_a3()
local script = G2L["a3"];
	local Players = game:GetService("Players")
	local ESPGroup = script.Parent.Parent
	local ESPAccentColor = ESPGroup:WaitForChild("ESPAccentColor")
	local ESPDistanceTickBox = script.Parent:WaitForChild("ESPDistanceTickBox")
	
	local isEnabled = false
	
	local function getAccent()
		return ESPAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = ESPGroup
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(57, 57, 57)
	stroke.Thickness = 1
	stroke.Parent = tooltipFrame
	
	local padding = Instance.new("UIPadding")
	padding.PaddingTop = UDim.new(0, 6)
	padding.PaddingBottom = UDim.new(0, 6)
	padding.PaddingLeft = UDim.new(0, 6)
	padding.PaddingRight = UDim.new(0, 6)
	padding.Parent = tooltipFrame
	
	local tooltipLabel = Instance.new("TextLabel")
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Shows distance to players next to their name"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	tooltipLabel.Parent = tooltipFrame
	
	ESPDistanceTickBox.MouseEnter:Connect(function()
		local abs = ESPDistanceTickBox.AbsolutePosition
		local size = ESPDistanceTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	ESPDistanceTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	ESPDistanceTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		if isEnabled then
			ESPDistanceTickBox.BackgroundColor3 = getAccent()
		else
			ESPDistanceTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
		ESPGroup:SetAttribute("ShowDist", isEnabled)
	end)
	
	ESPGroup:GetAttributeChangedSignal("ESPEnabled"):Connect(function()
		local espEnabled = ESPGroup:GetAttribute("ESPEnabled")
		if not espEnabled then
			ESPGroup:SetAttribute("ShowDist", false)
		elseif espEnabled and isEnabled then
			ESPGroup:SetAttribute("ShowDist", true)
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			ESPGroup:SetAttribute("ShowDist", false)
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_a3);
-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualLeftSide.ESPGroup.ESPOutlineRow.ESPOutlineScript
local function C_a7()
local script = G2L["a7"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	local ESPGroup = script.Parent.Parent
	local ESPAccentColor = ESPGroup:WaitForChild("ESPAccentColor")
	local ESPOutlineTickBox = script.Parent:WaitForChild("ESPOutlineTickBox")
	
	local isEnabled = false
	local ESPColor = Color3.fromRGB(255, 255, 255)
	
	ESPGroup:GetAttributeChangedSignal("ESPColor"):Connect(function()
		local newColor = ESPGroup:GetAttribute("ESPColor")
		if newColor then
			ESPColor = newColor
			for plr, data in pairs(ESPGuis) do
				if data then
					if data.NameLabel then
						data.NameLabel.TextColor3 = newColor
					end
					if data.DistLabel then
						data.DistLabel.TextColor3 = newColor
					end
					if typeof(data) == "Instance" and data:IsA("Highlight") then
						data.OutlineColor = newColor
					end
				end
			end
		end
	end)
	local ESPGuis = {}
	
	local function getAccent()
		return ESPAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = ESPGroup
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(57, 57, 57)
	stroke.Thickness = 1
	stroke.Parent = tooltipFrame
	
	local padding = Instance.new("UIPadding")
	padding.PaddingTop = UDim.new(0, 6)
	padding.PaddingBottom = UDim.new(0, 6)
	padding.PaddingLeft = UDim.new(0, 6)
	padding.PaddingRight = UDim.new(0, 6)
	padding.Parent = tooltipFrame
	
	local tooltipLabel = Instance.new("TextLabel")
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Shows an outline around players through walls"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	tooltipLabel.Parent = tooltipFrame
	
	ESPOutlineTickBox.MouseEnter:Connect(function()
		local abs = ESPOutlineTickBox.AbsolutePosition
		local size = ESPOutlineTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	ESPOutlineTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function createOutline(targetPlayer)
		local char = targetPlayer.Character
		if not char then return end
	
		for _, v in pairs(char:GetChildren()) do
			if v:IsA("Highlight") and v.Name == "ESPOutlineHighlight" then
				v:Destroy()
			end
		end
	
		local highlight = Instance.new("Highlight")
		highlight.Name = "ESPOutlineHighlight"
		highlight.FillTransparency = 1
		highlight.OutlineColor = ESPColor
		highlight.OutlineTransparency = 0
		highlight.Enabled = true
	
		RunService.RenderStepped:Connect(function()
			local espEnabled = ESPGroup:GetAttribute("ESPEnabled") ~= false
			if not isEnabled or not espEnabled then
				highlight.Enabled = false
				return
			end
			local myChar = LocalPlayer.Character
			local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
			local targetChar = targetPlayer.Character
			local targetHRP = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
			if myHRP and targetHRP then
				local maxDist = ESPGroup:GetAttribute("ESPMaxDist") or 1000
				local dist = (myHRP.Position - targetHRP.Position).Magnitude
				highlight.Enabled = dist <= maxDist
			end
		end)
		highlight.Parent = char
	
		ESPGuis[targetPlayer] = highlight
	
		local humanoid = char:FindFirstChildOfClass("Humanoid")
		if humanoid then
			humanoid.Died:Connect(function()
				if ESPGuis[targetPlayer] then
					ESPGuis[targetPlayer]:Destroy()
					ESPGuis[targetPlayer] = nil
				end
			end)
		end
	end
	
	local function removeOutline(targetPlayer)
		if ESPGuis[targetPlayer] then
			ESPGuis[targetPlayer]:Destroy()
			ESPGuis[targetPlayer] = nil
		end
	end
	
	local function enable()
		isEnabled = true
		ESPOutlineTickBox.BackgroundColor3 = getAccent()
		for _, plr in pairs(Players:GetPlayers()) do
			if plr ~= LocalPlayer then
				createOutline(plr)
				plr.CharacterAdded:Connect(function()
					task.wait(0.5)
					if isEnabled then createOutline(plr) end
				end)
			end
		end
	end
	
	local function disable()
		isEnabled = false
		ESPOutlineTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		local espEnabled = ESPGroup:GetAttribute("ESPEnabled") ~= false
		if not espEnabled then
			for plr in pairs(ESPGuis) do
				removeOutline(plr)
			end
		end
	end
	
	Players.PlayerAdded:Connect(function(plr)
		if isEnabled and plr ~= LocalPlayer then
			createOutline(plr)
			plr.CharacterAdded:Connect(function()
				task.wait(0.5)
				if isEnabled then createOutline(plr) end
			end)
		end
	end)
	
	Players.PlayerRemoving:Connect(function(plr)
		removeOutline(plr)
	end)
	
	ESPOutlineTickBox.MouseButton1Click:Connect(function()
		if isEnabled then disable() else enable() end
	end)
	
	ESPGroup:GetAttributeChangedSignal("ESPEnabled"):Connect(function()
		local espEnabled = ESPGroup:GetAttribute("ESPEnabled") ~= false
		if not espEnabled then
			for plr in pairs(ESPGuis) do
				removeOutline(plr)
			end
		elseif isEnabled then
			for _, plr in pairs(Players:GetPlayers()) do
				if plr ~= LocalPlayer then
					if not ESPGuis[plr] then
						createOutline(plr)
					end
				end
			end
		end
	end)
	ESPGroup:GetAttributeChangedSignal("ESPEnabled"):Connect(function()
		local espEnabled = ESPGroup:GetAttribute("ESPEnabled")
		for plr, highlight in pairs(ESPGuis) do
			if highlight and typeof(highlight) == "Instance" then
				highlight.Enabled = espEnabled and isEnabled
			end
		end
	end)
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_a7);
-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPSScript
local function C_ad()
local script = G2L["ad"];
	local ESPSGroup = script.Parent
	local ESPSMinimize = ESPSGroup.ESPSTitle.ESPSMinimize
	local function getRows()
		local rows = {}
		for _, child in ipairs(ESPSGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	local isMinimized = false
	local originalSize = ESPSGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	ESPSMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			ESPSGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			ESPSMinimize.Text = "+"
		else
			ESPSGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			ESPSMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_ad);
-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPEnableRow.ESPEnableScript
local function C_b3()
local script = G2L["b3"];
	local ESPSGroup = script.Parent.Parent
	local ESPSAccentColor = ESPSGroup.ESPSAccentColor
	
	local ESPEnableTickBox = script.Parent:WaitForChild("ESPEnableTickBox")
	
	local isEnabled = false
	
	local function getAccent()
		return ESPSAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = ESPSGroup
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "ESPEnableTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Enable or disable ESP"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	ESPEnableTickBox.MouseEnter:Connect(function()
		local abs = ESPEnableTickBox.AbsolutePosition
		local size = ESPEnableTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	ESPEnableTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	ESPEnableTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		if isEnabled then
			ESPEnableTickBox.BackgroundColor3 = getAccent()
		else
			ESPEnableTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
		local visualPage = ESPSGroup.Parent.Parent
		local espGroup = visualPage.VisualLeftSide:FindFirstChild("ESPGroup")
		if espGroup then
			espGroup:SetAttribute("ESPEnabled", isEnabled)
		end
	end)
	
	ESPEnableTickBox.BackgroundColor3 = getAccent()
	isEnabled = true
	local visualPage = ESPSGroup.Parent.Parent
	local espGroup = visualPage.VisualLeftSide:FindFirstChild("ESPGroup")
	if espGroup then
		espGroup:SetAttribute("ESPEnabled", true)
	end
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_b3);
-- StarterGui.ScreenGui.MainFrame.ContentArea.VisualPage.VisualRightSide.ESPSGroup.ESPUserRow.ESPUserScript
local function C_b7()
local script = G2L["b7"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local ESPSGroup = script.Parent.Parent
	local ESPSAccentColor = ESPSGroup:WaitForChild("ESPSAccentColor")
	local ESPUserTickBox = script.Parent:WaitForChild("ESPUserTickBox")
	
	local isEnabled = false
	
	local function getAccent()
		return ESPSAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = ESPSGroup
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(57, 57, 57)
	stroke.Thickness = 1
	stroke.Parent = tooltipFrame
	
	local padding = Instance.new("UIPadding")
	padding.PaddingTop = UDim.new(0, 6)
	padding.PaddingBottom = UDim.new(0, 6)
	padding.PaddingLeft = UDim.new(0, 6)
	padding.PaddingRight = UDim.new(0, 6)
	padding.Parent = tooltipFrame
	
	local tooltipLabel = Instance.new("TextLabel")
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Shows display name instead of username on Name ESP"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	tooltipLabel.Parent = tooltipFrame
	
	ESPUserTickBox.MouseEnter:Connect(function()
		local abs = ESPUserTickBox.AbsolutePosition
		local size = ESPUserTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	ESPUserTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	ESPUserTickBox.MouseButton1Click:Connect(function()
		isEnabled = not isEnabled
		if isEnabled then
			ESPUserTickBox.BackgroundColor3 = getAccent()
		else
			ESPUserTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
		ESPSGroup:SetAttribute("UseDisplayName", isEnabled)
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			ESPUserTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_b7);
-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsLeftSide.MainSettingsGroup.MainSettingsScript
local function C_be()
local script = G2L["be"];
	local MainSettingsGroup = script.Parent
	local MainSettingsMinimize = MainSettingsGroup.MainSettingsTitle.MainSettingsMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(MainSettingsGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = MainSettingsGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	MainSettingsMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			MainSettingsGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			MainSettingsMinimize.Text = "+"
		else
			MainSettingsGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			MainSettingsMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_be);
-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.ExploitsScript
local function C_c6()
local script = G2L["c6"];
	local ExploitsGroup = script.Parent
	local ExploitsMinimize = ExploitsGroup.ExploitsTitle.ExploitsMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(ExploitsGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = ExploitsGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	ExploitsMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			ExploitsGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			ExploitsMinimize.Text = "+"
		else
			ExploitsGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			ExploitsMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_c6);
-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.IYRow.IYScript
local function C_cc()
local script = G2L["cc"];
	local IYRow = script.Parent
	local IYButton = IYRow.IYButton
	local originalText = IYButton.Text
	local SCRIPT_URL = "https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"
	
	IYButton.MouseButton1Click:Connect(function()
		IYButton.Text = "LOADING..."
		task.spawn(function()
			local success, err = pcall(function()
				local code = game:HttpGet(SCRIPT_URL)
				loadstring(code)()
			end)
			if success then
				IYButton.Text = "SUCCESSFULLY EXECUTED"
				task.wait(3)
				IYButton.Text = originalText
			else
				IYButton.Text = "FAILED TO EXECUTE"
				task.wait(3)
				IYButton.Text = originalText
			end
		end)
	end)
end;
task.spawn(C_cc);
-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.SSRow.SSScript
local function C_cf()
local script = G2L["cf"];
	local SSRow = script.Parent
	local SSButton = SSRow.SSButton
	local originalText = SSButton.Text
	local SCRIPT_URL = "https://raw.githubusercontent.com/infyiff/backup/main/SimpleSpyV3/main.lua"
	
	SSButton.MouseButton1Click:Connect(function()
		SSButton.Text = "LOADING..."
		task.spawn(function()
			local success, err = pcall(function()
				local code = game:HttpGet(SCRIPT_URL)
				loadstring(code)()
			end)
			if success then
				SSButton.Text = "SUCCESSFULLY EXECUTED"
				task.wait(3)
				SSButton.Text = originalText
			else
				SSButton.Text = "FAILED TO EXECUTE"
				task.wait(3)
				SSButton.Text = originalText
			end
		end)
	end)
end;
task.spawn(C_cf);
-- StarterGui.ScreenGui.MainFrame.ContentArea.SettingsPage.SettingsRightSide.ExploitsGroup.FlingScriptRow.FlingScriptScript
local function C_d2()
local script = G2L["d2"];
	local FlingScriptRow = script.Parent
	local FlingScriptButton = FlingScriptRow.FlingScriptButton
	local originalText = FlingScriptButton.Text
	local SCRIPT_URL = "https://rawscripts.net/raw/Universal-Script-Fling-gui-42897"
	
	FlingScriptButton.MouseButton1Click:Connect(function()
		FlingScriptButton.Text = "LOADING..."
		task.spawn(function()
			local success, err = pcall(function()
				local code = game:HttpGet(SCRIPT_URL)
				loadstring(code)()
			end)
			if success then
				FlingScriptButton.Text = "SUCCESSFULLY EXECUTED"
				task.wait(3)
				FlingScriptButton.Text = originalText
			else
				FlingScriptButton.Text = "FAILED TO EXECUTE"
				task.wait(3)
				FlingScriptButton.Text = originalText
			end
		end)
	end)
end;
task.spawn(C_d2);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.MainScript
local function C_d8()
local script = G2L["d8"];
	local MainGroup = script.Parent
	local MainMinimize = MiscGroup.MainTitle.MainMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(MainGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = MainGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	MainMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			MainGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			MainMinimize.Text = "+"
		else
			MainGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			MainMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_d8);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ChatSpyRow.ChatSpyScript
local function C_de()
local script = G2L["de"];
	-- ============================================================
	-- CHATSPY + ARKSPY + DONATESPY + MUTEDSPY (single OnIncomingMessage)
	-- ============================================================
	
	local Players = game:GetService("Players")
	local TextChatService = game:GetService("TextChatService")
	local LocalPlayer = Players.LocalPlayer
	
	-- ============================================================
	-- CHATSPY UI REFS
	-- ============================================================
	local ChatSpyRow = script.Parent
	local ChatSpyTickBox = ChatSpyRow.ChatSpyTickBox
	local ChatSpyText = ChatSpyRow.ChatSpyText
	local MainGroup = ChatSpyRow.Parent
	local MainAccentColor = MainGroup.MainAccentColor
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local ScreenGui = (function()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end)()
	
	-- ============================================================
	-- OTHER UI REFS
	-- ============================================================
	local ArkSpyTickBox = nil
	local ProtectionAccentColor = nil
	local DonateSpyTickBox = nil
	local MutedSpyTickBox = nil
	local MiscAccentColor = nil
	
	for _, desc in pairs(ScreenGui:GetDescendants()) do
		if desc.Name == "ArkSpyRow" then
			ArkSpyTickBox = desc:FindFirstChild("ArkSpyTickBox")
			ProtectionAccentColor = desc.Parent:FindFirstChild("AlertAccentColor")
		end
		if desc.Name == "DonateSpyRow" then
			DonateSpyTickBox = desc:FindFirstChild("DonateSpyTickBox")
			MiscAccentColor = desc.Parent:FindFirstChild("AlertAccentColor")
		end
		if desc.Name == "MutedSpyRow" then
			MutedSpyTickBox = desc:FindFirstChild("MutedSpyTickBox")
			if not MiscAccentColor then
				MiscAccentColor = desc.Parent:FindFirstChild("AlertAccentColor")
			end
		end
	end
	
	-- ============================================================
	-- BOTREPLY UI REFS
	-- ============================================================
	local BotReplyTickBox = nil
	local BotReplyPrivTickBox = nil
	local BotReplyCaseTickBox = nil
	local BotReplyContainsTickBox = nil
	local BotAccentColor = nil
	local ChatTriggerBox = nil
	local ChatReplyBox = nil
	local BotReplyArkTickBox = nil
	local BotMinTimeTickBox = nil
	local BotMinTimeChanger = nil
	
	for _, desc in pairs(ScreenGui:GetDescendants()) do
		if desc.Name == "BotReplyRow" then
			BotReplyTickBox = desc:FindFirstChild("BotReplyTickBox")
			BotAccentColor = desc.Parent:FindFirstChild("BotAccentColor")
		end
		if desc.Name == "BotReplyPrivRow" then
			BotReplyPrivTickBox = desc:FindFirstChild("BotReplyPrivTickBox")
		end
		if desc.Name == "BotReplyCaseRow" then
			BotReplyCaseTickBox = desc:FindFirstChild("BotReplyCaseTickBox")
		end
		if desc.Name == "BotReplyContainsRow" then
			BotReplyContainsTickBox = desc:FindFirstChild("BotReplyContainsTickBox")
		end
		if desc.Name == "ChatTriggerRow" then
			ChatTriggerBox = desc:FindFirstChild("ChatTriggerBox")
		end
		if desc.Name == "ChatReplyRow" then
			ChatReplyBox = desc:FindFirstChild("ChatReplyBox")
		end
		if desc.Name == "BotReplyArkRow" then
			BotReplyArkTickBox = desc:FindFirstChild("BotReplyArkTickBox")
		end
		if desc.Name == "BotMinTimeRow" then
			BotMinTimeTickBox = desc:FindFirstChild("BotMinTimeTickBox")
			BotMinTimeChanger = desc:FindFirstChild("BotMinTimeChanger")
		end
	end
	
	-- ============================================================
	-- STATE
	-- ============================================================
	local spychat = false
	local isEnabled = false
	local arkSpyOn = false
	local donateSpyOn = false
	local mutedSpyOn = false
	local botReplyOn = false
	
	-- ============================================================
	-- TCO COLOR TABLES
	-- ============================================================
	local isog = workspace:FindFirstChild("Cubes") ~= nil
	
	local namecolors = {
		peasant  = {150, 103, 102},
		arken    = {4,   175, 236},
		admin    = {245, 205, 48},
		hidden   = {255, 0,   0},
		iqgenius = {255, 179, 179},
		iqdumb   = {200, 0,   0},
	}
	if isog then
		namecolors.peasant = {128, 128, 128}
	end
	
	local namecolorshex = {}
	for i, v in pairs(namecolors) do
		namecolorshex[i] = "#" .. Color3.fromRGB(table.unpack(v)):ToHex()
	end
	
	-- ============================================================
	-- COMMAND LIST (ArkSpy)
	-- ============================================================
	local commandSet = {}
	for _, cmd in ipairs({
		"airstrike","autumn","ball","bear","bigbrain","bkit","blaster","blind","boink","bomb",
		"boombox","bow","bring","btool","btools","candy","car","carpet","cash","classic",
		"claws","clearinv","clearsky","clicktp","clip","cloak","clone","clouds","cloudy","cmds",
		"colorful","colorless","creeper","cubes","curse","cursed","dancegun","darkspring","day",
		"debug","deer","delclones","delcubes","depth","disarm","dmg","drone","dtool",
		"dumb","dwarf","enlighten","epic","explode","fame","fat","ff","fire","firespell",
		"fireworks","flail","flashbang","fling","fly","fog","food","fov","freefall","freeze",
		"gauntlet","gear","genius","getseed","giant","glitch","gloves","glow","god","goto",
		"gravity","ham","headless","headsize","heal","height","hookgun","hour","hp","implode",
		"invisible","iq","jail","joinog","joinvc","joinxl","jumppower","katana","lantern",
		"lasergun","launch","launcher","makeway","mapnoise","mapseed","mapsize","maptide",
		"maxhp","memeify","mines","missiles","modern","monke","moonsize","morph","mute",
		"myopic","name","namek","nameon","nameoof","night","noclip","oof","ooflines","outlines",
		"paint","paper","pistol","players","pointers","portalgun","pov","premium","protest",
		"pumpkin","r15","r6","rage","railgun","raptor","rat","rctank","realistic","reset",
		"rocket","scythe","seasize","seatide","shark","short","shotgun","shovel","sign","sit",
		"size","skinny","smart","smoke","smollbrain","smooth","snowball","spam","spark","speed",
		"spring","stand","stool","storm","summer","sunsize","swim","sword","tackle","tall",
		"targets","taser","timeon","timeoof","tnt","tommy","torch","tospawn","toy","trail",
		"unblind","uncurse","unff","unfire","unfly","unfreeze","unglow","ungod","unjail",
		"unmute","unmyopic","unsmoke","unspark","untrail","uptime","visible","wear","width",
		"winter","yang","yin","kick","time","filesize"
		}) do
		commandSet[cmd] = true
	end
	
	-- ============================================================
	-- SHARED HELPERS
	-- ============================================================
	local function sendChat(message)
		if shared.sendChat then
			shared.sendChat(message)
		end
	end
	
	local function isMuted(player)
		return player:HasTag("Muted")
	end
	
	-- ============================================================
	-- BOTREPLY HELPERS
	-- ============================================================
	
	local function botReplyTickOn(tickbox)
		if not tickbox then return false end
		return tickbox.BackgroundColor3 ~= Color3.new(0,0,0)
	end
	
	local ChatBListBox = nil
	for _, desc in pairs(ScreenGui:GetDescendants()) do
		if desc.Name == "ChatBListBox" then
			ChatBListBox = desc
			break
		end
	end
	
	local function isBlacklisted(player)
		if not ChatBListBox then return false end
		local text = ChatBListBox.Text
		if text == "" or text == ChatBListBox.PlaceholderText then return false end
		local lowerName = player.Name:lower()
		for entry in text:gmatch("[^,]+") do
			local trimmed = entry:match("^%s*(.-)%s*$"):lower()
			if trimmed ~= "" and trimmed == lowerName then
				return true
			end
		end
		return false
	end
	
	local function getTrigger()
		if not ChatTriggerBox then return nil end
		local t = ChatTriggerBox.Text
		if t == "" or t == ChatTriggerBox.PlaceholderText then return nil end
		return t
	end
	
	local function getReply()
		if not ChatReplyBox then return nil end
		local t = ChatReplyBox.Text
		if t == "" or t == ChatReplyBox.PlaceholderText then return nil end
		return t
	end
	
	local function botMessageMatches(msg, trigger)
		local caseInsensitive = not botReplyTickOn(BotReplyCaseTickBox)
		local contains = botReplyTickOn(BotReplyContainsTickBox)
		local m = caseInsensitive and msg:lower() or msg
		local t = caseInsensitive and trigger:lower() or trigger
		if contains then
			for word in m:gmatch("%S+") do
				if word == t then return true end
			end
			return false
		else
			local firstWord = m:match("^(%S+)") or ""
			return firstWord == t
		end
	end
	
	local function convertForCommands(str)
		return str:gsub("_", ".")
	end
	
	local function buildReply(text, player)
		local usernameForCommand = convertForCommands(player.Name)
		local displayForCommand = convertForCommands(player.DisplayName)
		text = text:gsub("%[Username%]", usernameForCommand)
		text = text:gsub("%[Display%]", displayForCommand)
		return text
	end
	
	-- ============================================================
	-- BOT MIN TIME CHECK
	-- ============================================================
	
	local function getPlayerTime(player)
		local ls = player:FindFirstChild("leaderstats")
		if not ls then return nil end
		local t = ls:FindFirstChild("Time")
		if not t then return nil end
		return t.Value
	end
	
	local function passesMinTimeRequirement(player)
		if not BotMinTimeTickBox then return true end
		if not botReplyTickOn(BotMinTimeTickBox) then return true end
		if not BotMinTimeChanger then return true end
		local input = BotMinTimeChanger.Text
		if not input or input == "" or input == BotMinTimeChanger.PlaceholderText then return true end
		local lessThan = input:match("^<(%d+)$")
		if lessThan then
			local threshold = tonumber(lessThan)
			if not threshold then return true end
			local playerTime = getPlayerTime(player)
			if playerTime == nil then return true end
			return playerTime < threshold
		end
		local exact = tonumber(input)
		if not exact then return true end
		local playerTime = getPlayerTime(player)
		if playerTime == nil then return true end
		return playerTime >= exact
	end
	
	-- ============================================================
	-- ARKSPY HELPERS
	-- ============================================================
	local function isAdmin(player)
		local chosenTeam = game:GetService("Teams"):FindFirstChild("Chosen")
		if not chosenTeam then return false end
		return player.Team == chosenTeam
	end
	
	local function hasArkenstone(player)
		local char = workspace:FindFirstChild(player.Name)
		if not char then return false end
		return char:FindFirstChild("The Arkenstone") ~= nil
	end
	
	local function messageHasCommand(msg)
		local stripped = msg:sub(1,1) == ";" and msg:sub(2) or msg
		local firstWord = stripped:match("^(%S+)") or ""
		return commandSet[firstWord:lower()] == true
	end
	
	-- ============================================================
	-- DONATESPY HELPERS
	-- ============================================================
	local function findPlayerByPartialName(partial)
		local lowerPartial = partial:lower()
		for _, p in pairs(Players:GetPlayers()) do
			if p.Name:lower():sub(1, #lowerPartial) == lowerPartial then return p end
			if p.DisplayName:lower():sub(1, #lowerPartial) == lowerPartial then return p end
		end
		return nil
	end
	
	local function parseDonate(msg, sender)
		local stripped = msg:sub(1,1) == ";" and msg:sub(2) or msg
		stripped = stripped:match("^%s*(.-)%s*$")
		local rest = stripped:match("^[Dd][Oo][Nn][Aa][Tt][Ee]%s+(.*)")
		if not rest then return nil, nil end
		local parts = {}
		for word in rest:gmatch("%S+") do table.insert(parts, word) end
		if #parts == 0 then return nil, nil end
		local targetPartial = parts[1]
		local amount = 60
		if #parts >= 2 then
			local num = tonumber(parts[2])
			if not num or num < 1 then return nil, nil end
			amount = math.floor(num)
		end
		local target = findPlayerByPartialName(targetPartial)
		if not target then return nil, nil end
		if target == sender then return nil, nil end
		local senderTime = getPlayerTime(sender)
		if senderTime == nil then return nil, nil end
		if senderTime < amount then return nil, nil end
		return target, amount
	end
	
	-- ============================================================
	-- TOOLTIP BUILDER
	-- ============================================================
	local function makeTooltip(parent, text)
		local frame = Instance.new("Frame")
		frame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
		frame.BorderSizePixel = 0
		frame.ZIndex = 100
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Size = UDim2.new(0, 212, 0, 0)
		frame.Visible = false
		frame.Parent = parent
		local stroke = Instance.new("UIStroke", frame)
		stroke.Color = Color3.fromRGB(57, 57, 57)
		local padding = Instance.new("UIPadding", frame)
		padding.PaddingTop = UDim.new(0, 6)
		padding.PaddingBottom = UDim.new(0, 6)
		padding.PaddingLeft = UDim.new(0, 6)
		padding.PaddingRight = UDim.new(0, 6)
		local label = Instance.new("TextLabel", frame)
		label.BackgroundTransparency = 1
		label.Text = text
		label.TextColor3 = Color3.new(1, 1, 1)
		label.TextSize = 14
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Top
		label.Font = Enum.Font.GothamSemibold
		label.TextWrapped = true
		label.Size = UDim2.new(0, 200, 0, 0)
		label.AutomaticSize = Enum.AutomaticSize.Y
		label.ZIndex = 101
		return frame
	end
	
	local function hookTooltip(tickbox, tooltipFrame)
		tickbox.MouseEnter:Connect(function()
			local abs = tickbox.AbsolutePosition
			local size = tickbox.AbsoluteSize
			tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
			tooltipFrame.Visible = true
		end)
		tickbox.MouseLeave:Connect(function()
			tooltipFrame.Visible = false
		end)
	end
	
	-- ============================================================
	-- BUILD TOOLTIPS
	-- ============================================================
	local chatSpyTooltip = makeTooltip(ScreenGui, "Reveals hidden commands, muted players, and IQ chat with emoji labels")
	hookTooltip(ChatSpyTickBox, chatSpyTooltip)
	
	local arkSpyTooltip = makeTooltip(ScreenGui, "Announces and reveals hidden commands used by admins and enlightened players. Spy chat must be enabled")
	if ArkSpyTickBox then
		ArkSpyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		hookTooltip(ArkSpyTickBox, arkSpyTooltip)
	end
	
	local donateSpyTooltip = makeTooltip(ScreenGui, "Announces donations made by any player, including silent and muted donate commands. Spy chat must be enabled")
	if DonateSpyTickBox then
		DonateSpyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		hookTooltip(DonateSpyTickBox, donateSpyTooltip)
	end
	
	local mutedSpyTooltip = makeTooltip(ScreenGui, "Announces anything typed by muted players in chat. This will not annouce players using ; . Spy chat must be enabled")
	if MutedSpyTickBox then
		MutedSpyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		hookTooltip(MutedSpyTickBox, mutedSpyTooltip)
	end
	
	-- ============================================================
	-- SINGLE MERGED OnIncomingMessage HANDLER
	-- ============================================================
	TextChatService.OnIncomingMessage = function(mdata)
		local plr = mdata.TextSource
			and mdata.TextSource.UserId
			and Players:GetPlayerByUserId(mdata.TextSource.UserId)
		if not plr then return end
	
		local TCO_PLACE_IDS = {
			11137575513,
			12943245078,
			12943247001,
			108097274488844,
		}
	
		local isTCO = false
		for _, id in ipairs(TCO_PLACE_IDS) do
			if game.PlaceId == id then isTCO = true; break end
		end
		if not isTCO then return end
	
		local cn
		if plr.Neutral == true then
			if plr:GetAttribute("Arken") == true then
				cn = "arken"
			else
				cn = "peasant"
			end
		else
			cn = "admin"
		end
	
		local muted = plr:HasTag("Muted")
		if muted then
			cn = "hidden"
			if not spychat then mdata.Text = "" end
		end
	
		local hidden = false
		if string.sub(mdata.Text, 1, 1) == ";" then
			if spychat then
				cn = "hidden"
				hidden = true
			else
				mdata.Text = ""
			end
		end
	
		local iqEmoji = nil
		if spychat and plr:GetAttribute("IQ") then
			local iq = plr:GetAttribute("IQ")
			if iq >= 200 then
				iqEmoji = "🧠"
				cn = "iqgenius"
			elseif iq <= 50 then
				iqEmoji = "🤪"
				cn = "iqdumb"
			end
		end
	
		local icon = ""
		if spychat then
			if hidden then
				icon = "🚫 "
			elseif muted then
				icon = "🤐 "
			elseif iqEmoji then
				icon = iqEmoji .. " "
			end
		end
	
		local r   = namecolors[cn][1]
		local g   = namecolors[cn][2]
		local b   = namecolors[cn][3]
		local hex = namecolorshex[cn]
		local displayName = plr.DisplayName
	
		if isog then
			mdata.PrefixText = "<font color=\"" .. hex .. "\"><b><font color='rgb(" .. r .. "," .. g .. "," .. b .. ")'>" .. icon .. "[" .. displayName .. "]: </font></b></font>"
		else
			mdata.PrefixText = "<font color=\"" .. hex .. "\"><i><font color='rgb(" .. r .. "," .. g .. "," .. b .. ")'>" .. icon .. "(" .. displayName .. ") </font></i></font>"
		end
	
		if plr == LocalPlayer then return end
	
		local rawMsg = mdata.Text or ""
		if rawMsg == "" then return end
		local silent = rawMsg:sub(1,1) == ";"
		local mutedPlayer = isMuted(plr)
		local mutedPrefix = mutedPlayer and "MUTED " or ""
	
		-- ---- ARKSPY ----
		local caughtByArk = false
		if arkSpyOn then
			local admin = isAdmin(plr)
			local ark = hasArkenstone(plr)
			if admin or ark then
				local shouldAnnounce = false
				if mutedPlayer then
					if messageHasCommand(rawMsg) then
						shouldAnnounce = true
						caughtByArk = true
					end
				else
					if silent and messageHasCommand(rawMsg) then
						shouldAnnounce = true
					end
				end
				if shouldAnnounce then
					task.spawn(function()
						sendChat(mutedPrefix .. plr.DisplayName .. ": " .. rawMsg)
					end)
				end
			end
		end
	
		-- ---- DONATESPY ----
		local caughtByDonate = false
		if donateSpyOn then
			local isDonate = rawMsg:lower():match("^;?%s*donate%s") ~= nil
				or rawMsg:lower():match("^;?%s*donate$") ~= nil
			if isDonate then
				local shouldAnnounce = false
				if mutedPlayer then
					shouldAnnounce = true
					caughtByDonate = true
				elseif silent then
					shouldAnnounce = true
				end
				if shouldAnnounce then
					local target, amount = parseDonate(rawMsg, plr)
					if target and amount then
						task.spawn(function()
							sendChat(mutedPrefix .. plr.DisplayName .. ": " .. rawMsg)
						end)
					end
				end
			end
		end
	
		-- ---- MUTEDSPY ----
		if mutedSpyOn and mutedPlayer then
			if not caughtByArk and not caughtByDonate then
				task.spawn(function()
					sendChat("MUTED " .. plr.DisplayName .. ": " .. rawMsg)
				end)
			end
		end
	
		-- ---- BOTREPLY ----
		if botReplyOn then
			local trigger = getTrigger()
			local reply = getReply()
			if trigger and reply then
				if botMessageMatches(rawMsg, trigger) and passesMinTimeRequirement(plr) and not isBlacklisted(plr) then
					local finalReply = buildReply(reply, plr)
					task.spawn(function()
						local messageToSend = finalReply
						local usePrivate = botReplyTickOn(BotReplyPrivTickBox)
						local useArk = botReplyTickOn(BotReplyArkTickBox) and shared.BotReplyArkSend ~= nil
						if useArk then
							shared.BotReplyArkSend(function()
								if usePrivate and shared.BotReplyPrivSend then
									shared.BotReplyPrivSend(plr, messageToSend)
								else
									sendChat(messageToSend)
								end
							end)
						else
							if usePrivate and shared.BotReplyPrivSend then
								shared.BotReplyPrivSend(plr, messageToSend)
							else
								sendChat(messageToSend)
							end
						end
					end)
				end
			end
		end
	end
	
	-- ============================================================
	-- CHATSPY ENABLE / DISABLE
	-- ============================================================
	ChatSpyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local function chatEnable()
		isEnabled = true
		spychat = true
		ChatSpyTickBox.BackgroundColor3 = MainAccentColor.BackgroundColor3
	end
	
	local function chatDisable()
		isEnabled = false
		spychat = false
		ChatSpyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	end
	
	ChatSpyTickBox.MouseButton1Click:Connect(function()
		if isEnabled then chatDisable() else chatEnable() end
	end)
	
	-- ============================================================
	-- ARKSPY ENABLE / DISABLE
	-- ============================================================
	local function arkEnable()
		arkSpyOn = true
		if ArkSpyTickBox then
			ArkSpyTickBox.BackgroundColor3 = ProtectionAccentColor and ProtectionAccentColor.BackgroundColor3 or Color3.fromRGB(0, 170, 255)
		end
	end
	
	local function arkDisable()
		arkSpyOn = false
		if ArkSpyTickBox then
			ArkSpyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	if ArkSpyTickBox then
		ArkSpyTickBox.MouseButton1Click:Connect(function()
			if arkSpyOn then arkDisable() else arkEnable() end
		end)
	end
	
	-- ============================================================
	-- DONATESPY ENABLE / DISABLE
	-- ============================================================
	local function donateEnable()
		donateSpyOn = true
		if DonateSpyTickBox then
			DonateSpyTickBox.BackgroundColor3 = MiscAccentColor and MiscAccentColor.BackgroundColor3 or Color3.fromRGB(0, 170, 255)
		end
	end
	
	local function donateDisable()
		donateSpyOn = false
		if DonateSpyTickBox then
			DonateSpyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	if DonateSpyTickBox then
		DonateSpyTickBox.MouseButton1Click:Connect(function()
			if donateSpyOn then donateDisable() else donateEnable() end
		end)
	end
	
	-- ============================================================
	-- MUTEDSPY ENABLE / DISABLE
	-- ============================================================
	local function mutedEnable()
		mutedSpyOn = true
		if MutedSpyTickBox then
			MutedSpyTickBox.BackgroundColor3 = MiscAccentColor and MiscAccentColor.BackgroundColor3 or Color3.fromRGB(0, 170, 255)
		end
	end
	
	local function mutedDisable()
		mutedSpyOn = false
		if MutedSpyTickBox then
			MutedSpyTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
	
	if MutedSpyTickBox then
		MutedSpyTickBox.MouseButton1Click:Connect(function()
			if mutedSpyOn then mutedDisable() else mutedEnable() end
		end)
	end
	
	-- ============================================================
	-- BOTREPLY ENABLE / DISABLE
	-- ============================================================
	local function botReplyEnable()
		botReplyOn = true
		if BotReplyTickBox and BotAccentColor then
			BotReplyTickBox.BackgroundColor3 = BotAccentColor.BackgroundColor3
		end
	end
	
	local function botReplyDisable()
		botReplyOn = false
		if BotReplyTickBox then
			BotReplyTickBox.BackgroundColor3 = Color3.new(0,0,0)
		end
	end
	
	if BotMinTimeTickBox then
		BotMinTimeTickBox.BackgroundColor3 = Color3.new(0,0,0)
		BotMinTimeTickBox.MouseButton1Click:Connect(function()
			if botReplyTickOn(BotMinTimeTickBox) then
				BotMinTimeTickBox.BackgroundColor3 = Color3.new(0,0,0)
			else
				BotMinTimeTickBox.BackgroundColor3 = BotAccentColor and BotAccentColor.BackgroundColor3 or Color3.fromRGB(0,170,255)
			end
		end)
	end
	
	if BotReplyTickBox then
		BotReplyTickBox.BackgroundColor3 = Color3.new(0,0,0)
		BotReplyTickBox.MouseButton1Click:Connect(function()
			if botReplyOn then botReplyDisable() else botReplyEnable() end
		end)
	end
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			spychat = false
			arkSpyOn = false
			donateSpyOn = false
			mutedSpyOn = false
			chatSpyTooltip:Destroy()
			arkSpyTooltip:Destroy()
			donateSpyTooltip:Destroy()
			mutedSpyTooltip:Destroy()
		end
	end)
end;
task.spawn(C_de);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.ADODeathRow.ADODeathScript
local function C_e2()
local script = G2L["e2"];
	-- ============================================================
	-- AUTO DROP ON DEATH SCRIPT
	-- Drops ALL tools (including Enlighten) on death.
	-- Picks them back up as fast as possible on respawn.
	-- ============================================================
	
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local ADODeathRow = script.Parent
	local ADODeathTickBox = ADODeathRow.ADODeathTickBox
	local ADODeathText = ADODeathRow.ADODeathText
	local MainGroup = ADODeathRow.Parent
	local MainAccentColor = MainGroup.MainAccentColor
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke", tooltipFrame)
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	
	local tooltipPadding = Instance.new("UIPadding", tooltipFrame)
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	
	local tooltipLabel = Instance.new("TextLabel", tooltipFrame)
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Auto drops ALL tools (including Enlighten) on death and picks them back up instantly on respawn"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	
	ADODeathTickBox.MouseEnter:Connect(function()
		local abs = ADODeathTickBox.AbsolutePosition
		local size = ADODeathTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	ADODeathTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- STATE
	-- ============================================================
	local isEnabled  = false
	local charConn   = nil
	local TAG        = "ADODeathDropped"
	
	-- ============================================================
	-- DROP ALL TOOLS (death)
	-- ============================================================
	local function dropAllTools()
		local char = LocalPlayer.Character
		if not char then return end
	
		-- Pull backpack into character so everything is in one place
		for _, tool in ipairs(LocalPlayer.Backpack:GetChildren()) do
			if tool:IsA("Tool") then
				tool.Parent = char
			end
		end
	
		task.wait(0.05)
	
		-- Drop every tool into workspace, Enlighten included
		for _, tool in ipairs(char:GetChildren()) do
			if tool:IsA("Tool") then
				tool:AddTag(TAG)
				tool.Parent = workspace
			end
		end
	end
	
	-- ============================================================
	-- PICKUP TOOLS (respawn)
	-- Tries immediately, then retries every 0.1s for up to 3s
	-- so tools are grabbed the moment the humanoid is ready,
	-- before other players can snatch them.
	-- ============================================================
	local function pickupDroppedTools(char)
		local deadline = tick() + 3  -- give up after 3 seconds
	
		repeat
			local hum = char:FindFirstChildWhichIsA("Humanoid")
			if hum and hum.Health > 0 then
				for _, obj in ipairs(workspace:GetChildren()) do
					if obj:IsA("Tool") and obj:HasTag(TAG) and obj:FindFirstChild("Handle") then
						hum:EquipTool(obj)
					end
				end
				-- Check if any tagged tools are still in workspace
				local remaining = false
				for _, obj in ipairs(workspace:GetChildren()) do
					if obj:IsA("Tool") and obj:HasTag(TAG) then
						remaining = true
						break
					end
				end
				if not remaining then break end  -- all picked up, done
			end
			task.wait(0.1)
		until tick() > deadline or not isEnabled
	end
	
	-- ============================================================
	-- CHARACTER SETUP
	-- ============================================================
	local function setupCharacter(char)
		-- Start pickup attempt immediately — don't wait for humanoid first
		task.spawn(function()
			pickupDroppedTools(char)
		end)
	
		-- Watch for death on this character
		local hum = char:FindFirstChildWhichIsA("Humanoid")
			or char:WaitForChild("Humanoid", 10)
		if not hum then return end
	
		hum.Died:Connect(function()
			if not isEnabled then return end
			task.wait(0.05)
			dropAllTools()
		end)
	end
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local function enable()
		isEnabled = true
		ADODeathTickBox.BackgroundColor3 = MainAccentColor.BackgroundColor3
	
		if LocalPlayer.Character then
			setupCharacter(LocalPlayer.Character)
		end
	
		charConn = LocalPlayer.CharacterAdded:Connect(function(char)
			if isEnabled then
				setupCharacter(char)
			end
		end)
	end
	
	local function disable()
		isEnabled = false
		ADODeathTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
		if charConn then
			charConn:Disconnect()
			charConn = nil
		end
	end
	
	-- ============================================================
	-- TICKBOX TOGGLE
	-- ============================================================
	ADODeathTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disable()
		else
			enable()
		end
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isEnabled then disable() end
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_e2);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.APickupRow.APickupScript
local function C_e6()
local script = G2L["e6"];
	-- ============================================================
	-- AUTO PICKUP SCRIPT (STEALTH MODE V3)
	-- Automatically picks up dropped tools and instantly unequips them
	-- You can manually re-equip stolen tools to use them
	-- ============================================================
	
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local APickupRow = script.Parent
	local APickupTickBox = APickupRow.APickupTickBox
	local MainGroup = APickupRow.Parent
	local MainAccentColor = MainGroup.MainAccentColor
	
	-- Tag used by Auto Drop on Death script
	local DEATH_TAG = "ADODeathDropped"
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke", tooltipFrame)
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	
	local tooltipPadding = Instance.new("UIPadding", tooltipFrame)
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	
	local tooltipLabel = Instance.new("TextLabel", tooltipFrame)
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Stealthily picks up all dropped tools and hides them in backpack (manually equip stolen tools to use them)"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	
	APickupTickBox.MouseEnter:Connect(function()
		local abs = APickupTickBox.AbsolutePosition
		local size = APickupTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	APickupTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- STATE
	-- ============================================================
	local isEnabled = false
	local pickupLoop = nil
	local justStolen = {}  -- Track tools that were JUST stolen (temporary lockout)
	
	-- ============================================================
	-- AUTO PICKUP LOOP (STEALTH MODE)
	-- ============================================================
	local function startPickupLoop()
		pickupLoop = task.spawn(function()
			while isEnabled do
				local char = LocalPlayer.Character
				local hum = char and char:FindFirstChildWhichIsA("Humanoid")
	
				-- Only pickup if alive (Health > 0)
				if hum and hum.Health > 0 then
					-- Remember what tool we're currently holding
					local currentlyEquipped = char:FindFirstChildOfClass("Tool")
	
					for _, obj in ipairs(workspace:GetChildren()) do
						if obj:IsA("Tool") and obj:FindFirstChild("Handle") then
							-- Skip tools tagged by Auto Drop on Death
							if not obj:HasTag(DEATH_TAG) then
								-- Try to equip the tool (picks it up)
								local success = pcall(function()
									hum:EquipTool(obj)
								end)
	
								if success then
									-- Mark this tool as just stolen (for 2 seconds)
									justStolen[obj] = true
	
									-- Wait a tiny bit for the tool to be fully equipped
									task.wait(0.05)
	
									-- Force it to backpack multiple times to be sure
									for i = 1, 3 do
										if obj.Parent == char then
											obj.Parent = LocalPlayer.Backpack
										end
										task.wait(0.02)
									end
	
									-- Re-equip the tool we were holding before
									if currentlyEquipped and currentlyEquipped.Parent == LocalPlayer.Backpack then
										task.wait(0.03)
										hum:EquipTool(currentlyEquipped)
									end
	
									-- Remove the "just stolen" flag after 2 seconds
									task.delay(2, function()
										justStolen[obj] = nil
									end)
								end
							end
						end
					end
				end
	
				task.wait(0.1) -- Check every 0.1 seconds
			end
		end)
	end
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local function enable()
		isEnabled = true
		APickupTickBox.BackgroundColor3 = MainAccentColor.BackgroundColor3
		justStolen = {}  -- Reset just stolen list
		startPickupLoop()
	end
	
	local function disable()
		isEnabled = false
		APickupTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
		if pickupLoop then
			task.cancel(pickupLoop)
			pickupLoop = nil
		end
	
		justStolen = {}  -- Clear the list
	end
	
	-- ============================================================
	-- TICKBOX TOGGLE
	-- ============================================================
	APickupTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disable()
		else
			enable()
		end
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isEnabled then disable() end
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_e6);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.MainGroup.AntiEnliRow.AntiEnliScript
local function C_ea()
local script = G2L["ea"];
	-- ============================================================
	-- ANTI ENLIGHTEN DROP SCRIPT
	-- Prevents you from dropping Enlighten (The Arkenstone)
	-- ============================================================
	
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local AntiEnliRow = script.Parent
	local AntiEnliTickBox = AntiEnliRow.AntiEnliTickBox
	local MainGroup = AntiEnliRow.Parent
	local MainAccentColor = MainGroup.MainAccentColor
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke", tooltipFrame)
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	
	local tooltipPadding = Instance.new("UIPadding", tooltipFrame)
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	
	local tooltipLabel = Instance.new("TextLabel", tooltipFrame)
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Prevents you from dropping Enlighten (The Arkenstone) - instantly picks it back up"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	
	AntiEnliTickBox.MouseEnter:Connect(function()
		local abs = AntiEnliTickBox.AbsolutePosition
		local size = AntiEnliTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	AntiEnliTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- STATE
	-- ============================================================
	local isEnabled = false
	local connections = {}
	
	-- ============================================================
	-- ENLIGHTEN NAMES (add variations if needed)
	-- ============================================================
	local ENLIGHTEN_NAMES = {
		"Enlighten",
		"The Arkenstone",
		"Arkenstone"
	}
	
	local function isEnlighten(tool)
		if not tool:IsA("Tool") then return false end
		for _, name in ipairs(ENLIGHTEN_NAMES) do
			if tool.Name:lower():find(name:lower()) then
				return true
			end
		end
		return false
	end
	
	-- ============================================================
	-- MONITOR TOOL DROPS
	-- ============================================================
	local function monitorEnlighten(tool)
		if not isEnlighten(tool) then return end
	
		local conn = tool.AncestryChanged:Connect(function()
			if not isEnabled then return end
	
			-- If Enlighten is dropped into workspace, pick it back up immediately
			if tool.Parent == workspace then
				task.wait(0.05) -- Small delay to ensure it's fully dropped
	
				local char = LocalPlayer.Character
				local hum = char and char:FindFirstChildWhichIsA("Humanoid")
	
				if hum and hum.Health > 0 then
					pcall(function()
						hum:EquipTool(tool)
					end)
				end
			end
		end)
	
		table.insert(connections, conn)
	end
	
	-- ============================================================
	-- SETUP CHARACTER
	-- ============================================================
	local function setupCharacter(char)
		-- Monitor existing tools in character
		for _, tool in ipairs(char:GetChildren()) do
			monitorEnlighten(tool)
		end
	
		-- Monitor tools in backpack
		for _, tool in ipairs(LocalPlayer.Backpack:GetChildren()) do
			monitorEnlighten(tool)
		end
	
		-- Monitor new tools added to character
		local charConn = char.ChildAdded:Connect(function(child)
			if isEnabled then
				monitorEnlighten(child)
			end
		end)
		table.insert(connections, charConn)
	
		-- Monitor new tools added to backpack
		local backpackConn = LocalPlayer.Backpack.ChildAdded:Connect(function(child)
			if isEnabled then
				monitorEnlighten(child)
			end
		end)
		table.insert(connections, backpackConn)
	end
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local function enable()
		isEnabled = true
		AntiEnliTickBox.BackgroundColor3 = MainAccentColor.BackgroundColor3
	
		if LocalPlayer.Character then
			setupCharacter(LocalPlayer.Character)
		end
	
		local charConn = LocalPlayer.CharacterAdded:Connect(function(char)
			if isEnabled then
				setupCharacter(char)
			end
		end)
		table.insert(connections, charConn)
	end
	
	local function disable()
		isEnabled = false
		AntiEnliTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
		-- Disconnect all connections
		for _, conn in ipairs(connections) do
			conn:Disconnect()
		end
		connections = {}
	end
	
	-- ============================================================
	-- TICKBOX TOGGLE
	-- ============================================================
	AntiEnliTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disable()
		else
			enable()
		end
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isEnabled then disable() end
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_ea);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.RepScript
local function C_ee()
local script = G2L["ee"];
	local RepGroup = script.Parent
	local RepMinimize = RepGroup.RepTitle.RepMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(RepGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = RepGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	RepMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
	
		if isMinimized then
			RepGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			RepMinimize.Text = "+"
		else
			RepGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			RepMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_ee);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitFixRow.BKitFixScript
local function C_f4()
local script = G2L["f4"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local BKitFixRow = script.Parent
	local BKitFixButton = BKitFixRow.BKitFixButton
	local BKitFixHelp = BKitFixRow.BKitFixHelp
	local originalText = BKitFixButton.Text
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP SETUP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke", tooltipFrame)
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	
	local tooltipPadding = Instance.new("UIPadding", tooltipFrame)
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	
	local tooltipLabel = Instance.new("TextLabel", tooltipFrame)
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Fixes Bkit tools not working properly. Fixing it only works on you, and you can only build on existing blocks"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	
	BKitFixHelp.MouseEnter:Connect(function()
		local abs = BKitFixHelp.AbsolutePosition
		local size = BKitFixHelp.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	
	BKitFixHelp.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- FIX FUNCTION
	-- Finds any tool in character or backpack and resets its script
	-- No need to hold tool out
	-- ============================================================
	local function fixBkit()
		local rs = game:GetService("ReplicatedStorage")
		if not rs:FindFirstChild("Brick") then
			local brick = Instance.new("Part")
			brick.Name = "Brick"
			brick.Parent = rs
		end
	
		local character = LocalPlayer.Character
		if not character then return end
	
		local function resetToolScript(tool)
			local s = tool:FindFirstChild("Script")
			if s then
				s.Disabled = true
				task.wait()
				s.Disabled = false
			end
		end
	
		-- Reset scripts in character (equipped tools)
		for _, v in pairs(character:GetChildren()) do
			if v:IsA("Tool") then
				resetToolScript(v)
			end
		end
	
		-- Reset scripts in backpack (unequipped tools)
		for _, v in pairs(LocalPlayer.Backpack:GetChildren()) do
			if v:IsA("Tool") then
				resetToolScript(v)
			end
		end
	end
	
	local function onCharacterAdded(character)
		task.wait(1)
		fixBkit()
	end
	
	if LocalPlayer.Character then
		onCharacterAdded(LocalPlayer.Character)
	end
	LocalPlayer.CharacterAdded:Connect(onCharacterAdded)
	
	-- ============================================================
	-- BUTTON CLICK
	-- ============================================================
	BKitFixButton.MouseButton1Click:Connect(function()
		BKitFixButton.Text = "FIXING..."
		task.spawn(function()
			local success, err = pcall(fixBkit)
			if success then
				BKitFixButton.Text = "FIXED!"
				task.wait(3)
				BKitFixButton.Text = originalText
			else
				BKitFixButton.Text = "FAILED"
				task.wait(3)
				BKitFixButton.Text = originalText
			end
		end)
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_f4);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainLeftSide.RepGroup.BKitBreakRow.BKitBreakScript
local function C_f8()
local script = G2L["f8"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local BKitBreakRow = script.Parent
	local BKitBreakButton = BKitBreakRow.BKitBreakButton
	local originalText = BKitBreakButton.Text
	
	BKitBreakButton.MouseButton1Click:Connect(function()
		BKitBreakButton.Text = "BREAKING..."
		task.spawn(function()
			local success, err = pcall(function()
				local character = LocalPlayer.Character
				if not character then error("No character") end
	
				-- Find delete tool in character or backpack, no equipping needed
				local deleteTool = character:FindFirstChild("Delete")
					or LocalPlayer.Backpack:FindFirstChild("Delete")
				if not deleteTool then error("Delete tool not found") end
	
				local event = deleteTool:WaitForChild("Script"):WaitForChild("Event")
	
				local rs = game:GetService("ReplicatedStorage")
				local brick = rs:FindFirstChild("Brick")
				if not brick then error("No Brick in ReplicatedStorage") end
	
				event:FireServer(brick, character.HumanoidRootPart.Position)
			end)
	
			if success then
				BKitBreakButton.Text = "BROKEN!"
				task.wait(3)
				BKitBreakButton.Text = originalText
			else
				BKitBreakButton.Text = "FAILED"
				task.wait(3)
				BKitBreakButton.Text = originalText
			end
		end)
	end)
end;
task.spawn(C_f8);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiScript
local function C_fd()
local script = G2L["fd"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AntiGroup = script.Parent
	local AntiMinimize = AntiGroup.AntiTitle.AntiMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(AntiGroup:GetChildren()) do
			if child:IsA("Frame") and string.sub(child.Name, -3) == "Row" then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = AntiGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	AntiMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
	
		if isMinimized then
			AntiGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			AntiMinimize.Text = "+"
		else
			AntiGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			AntiMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_fd);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiNanRow.AntiNanScript
local function C_102()
local script = G2L["102"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local AntiNanRow = script.Parent
	local AntiNanTickBox = AntiNanRow.AntiNanTickBox
	local AntiGroup = AntiNanRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	
	local isEnabled = false
	local connection = nil
	
	local function getAccent()
		return AntiAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = AntiNanRow
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiNanTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Prevents maptide nan from sending you to the void on respawn. Aleast one player must be not glitched for this to work"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	AntiNanTickBox.MouseEnter:Connect(function()
		local abs = AntiNanTickBox.AbsolutePosition
		local size = AntiNanTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	AntiNanTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function isNaN(n)
		return n ~= n
	end
	
	local function isSafe(cf)
		local p = cf.Position
		return not (isNaN(p.X) or isNaN(p.Y) or isNaN(p.Z) or
			math.abs(p.X) == math.huge or
			math.abs(p.Y) == math.huge or
			math.abs(p.Z) == math.huge)
	end
	
	local function onCharacterAdded(character)
		if not isEnabled then return end
	
		local hrp = character:WaitForChild("HumanoidRootPart")
	
		hrp.Anchored = true
	
		task.wait(0.1)
	
		if not isSafe(hrp.CFrame) then
			hrp.CFrame = CFrame.new(0, 50, 0)
		end
	
		for _, plr in pairs(Players:GetPlayers()) do
			if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
				local otherHRP = plr.Character.HumanoidRootPart
				if isSafe(otherHRP.CFrame) then
					hrp.CFrame = otherHRP.CFrame + Vector3.new(0, 5, 0)
					break
				end
			end
		end
	
		hrp.Anchored = false
	end
	
	local function enable()
		isEnabled = true
		AntiNanTickBox.BackgroundColor3 = getAccent()
		connection = LocalPlayer.CharacterAdded:Connect(onCharacterAdded)
		if LocalPlayer.Character then
			onCharacterAdded(LocalPlayer.Character)
		end
	end
	
	local function disable()
		isEnabled = false
		AntiNanTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		if connection then
			connection:Disconnect()
			connection = nil
		end
	end
	
	AntiNanTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disable()
		else
			enable()
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_102);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiVisualRow.AntiVisualScript
local function C_107()
local script = G2L["107"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local Lighting = game:GetService("Lighting")
	local LocalPlayer = Players.LocalPlayer
	
	local AntiVisualRow = script.Parent
	local AntiGroup = AntiVisualRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	local AntiVisualTickBox = AntiVisualRow.AntiVisualTickBox
	
	local isEnabled = false
	AntiVisualTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiVisualTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Prevents visual commands from affecting your screen. Blocks out myopic, blind, fog and colorless"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	AntiVisualTickBox.MouseEnter:Connect(function()
		local abs = AntiVisualTickBox.AbsolutePosition
		local size = AntiVisualTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	AntiVisualTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- CONTINUOUS OVERRIDE LOOP
	-- ============================================================
	RunService.RenderStepped:Connect(function()
		if not isEnabled then return end
	
		-- Anti Myopic
		local blur = Lighting:FindFirstChild("Blur")
		if blur then
			blur.Enabled = false
		end
	
		-- Anti Blind
		local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
		if playerGui then
			local blind = playerGui:FindFirstChild("Blind")
			if blind then
				local frame = blind:FindFirstChildOfClass("Frame")
				if frame then
					frame.BackgroundTransparency = 1
				end
			end
		end
	
		-- Anti Fog
		local fog = Lighting:FindFirstChild("Fog")
		if fog then
			fog.Density = 0
		end
	
		-- Anti Colorless
		local rgb = Lighting:FindFirstChild("RGB")
		if rgb then
			rgb.Enabled = false
		end
	end)
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local function enable()
		isEnabled = true
		AntiVisualTickBox.BackgroundColor3 = AntiAccentColor.BackgroundColor3
	end
	
	local function disable()
		isEnabled = false
		AntiVisualTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	end
	
	AntiVisualTickBox.MouseButton1Click:Connect(function()
		if isEnabled then disable() else enable() end
	end)
	
	-- expose for EnableAnti and DisableAnti buttons
	shared.AntiVisualEnable = enable
	shared.AntiVisualDisable = disable
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			tooltipFrame:Destroy()
			isEnabled = false
		end
	end)
end;
task.spawn(C_107);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiJailRow.AntiJailScript
local function C_10b()
local script = G2L["10b"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	local AntiJailRow = script.Parent
	local AntiGroup = AntiJailRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	local AntiJailTickBox = AntiJailRow.AntiJailTickBox
	
	local isEnabled = false
	AntiJailTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiJailTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Prevents the jail command from trapping you. Other players can see your jail cell on their screens"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	AntiJailTickBox.MouseEnter:Connect(function()
		local abs = AntiJailTickBox.AbsolutePosition
		local size = AntiJailTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	AntiJailTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- CONTINUOUS OVERRIDE LOOP
	-- ============================================================
	RunService.RenderStepped:Connect(function()
		if not isEnabled then return end
	
		local char = workspace:FindFirstChild(LocalPlayer.Name)
		if not char then return end
	
		local jail = char:FindFirstChild("Jail")
		if not jail then return end
	
		for _, part in pairs(jail:GetChildren()) do
			if part:IsA("BasePart") and part.Name == "Part" then
				part.CanCollide = false
				part.Transparency = 1
			end
		end
	end)
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local function enable()
		isEnabled = true
		AntiJailTickBox.BackgroundColor3 = AntiAccentColor.BackgroundColor3
	end
	
	local function disable()
		isEnabled = false
		AntiJailTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	end
	
	AntiJailTickBox.MouseButton1Click:Connect(function()
		if isEnabled then disable() else enable() end
	end)
	
	shared.AntiJailEnable = enable
	shared.AntiJailDisable = disable
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			tooltipFrame:Destroy()
			isEnabled = false
		end
	end)
end;
task.spawn(C_10b);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMainPage.TCOMainRightSide.AntiGroup.AntiFreezeRow.AntiFreezeScript
local function C_10f()
local script = G2L["10f"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	local AntiFreezeRow = script.Parent
	local AntiGroup = AntiFreezeRow.Parent
	local AntiAccentColor = AntiGroup.AntiAccentColor
	local AntiFreezeTickBox = AntiFreezeRow.AntiFreezeTickBox
	
	local isEnabled = false
	AntiFreezeTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AntiFreezeTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Automatically resets your character when the freeze command is applied to you. Please disable god mode for this to work"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	AntiFreezeTickBox.MouseEnter:Connect(function()
		local abs = AntiFreezeTickBox.AbsolutePosition
		local size = AntiFreezeTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	AntiFreezeTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- RESET LOGIC
	-- ============================================================
	local isResetting = false
	
	local function doReset()
		if isResetting then return end
		isResetting = true
	
		-- pause godmode so it doesnt fight the reset
		shared.GodModePaused = true
	
		local char = LocalPlayer.Character
		if char then
			local humanoid = char:FindFirstChildOfClass("Humanoid")
			if humanoid then
				-- set health to 0 to trigger reset
				humanoid.MaxHealth = 100
				humanoid.Health = 0
			end
		end
	
		-- wait for respawn then unpause godmode
		LocalPlayer.CharacterAdded:Wait()
		task.wait(0.2)
		shared.GodModePaused = false
		isResetting = false
	end
	
	-- ============================================================
	-- DETECTION LOOP
	-- ============================================================
	local watchConn = nil
	
	local function startWatching()
		if watchConn then watchConn:Disconnect() end
		watchConn = RunService.Heartbeat:Connect(function()
			if not isEnabled then return end
			local char = workspace:FindFirstChild(LocalPlayer.Name)
			if not char then return end
			if char:FindFirstChild("Hielo") then
				doReset()
			end
		end)
	end
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local function enable()
		isEnabled = true
		AntiFreezeTickBox.BackgroundColor3 = AntiAccentColor.BackgroundColor3
		startWatching()
	end
	
	local function disable()
		isEnabled = false
		AntiFreezeTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		if watchConn then
			watchConn:Disconnect()
			watchConn = nil
		end
	end
	
	AntiFreezeTickBox.MouseButton1Click:Connect(function()
		if isEnabled then disable() else enable() end
	end)
	
	shared.AntiFreezeEnable = enable
	shared.AntiFreezeDisable = disable
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			tooltipFrame:Destroy()
			isEnabled = false
			if watchConn then watchConn:Disconnect() end
			shared.GodModePaused = false
		end
	end)
end;
task.spawn(C_10f);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.Misc1Script
local function C_116()
local script = G2L["116"];
	local Misc1Group = script.Parent
	local Misc1Minimize = Misc1Group.Misc1Title.Misc1Minimize
	local function getRows()
		local rows = {}
		for _, child in ipairs(Misc1Group:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	local isMinimized = false
	local originalSize = Misc1Group.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	Misc1Minimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			Misc1Group.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			Misc1Minimize.Text = "+"
		else
			Misc1Group.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			Misc1Minimize.Text = "-"
		end
	end)
end;
task.spawn(C_116);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.WWalkRow.WWalkScript
local function C_11c()
local script = G2L["11c"];
	-- ============================================================
	-- SCRIPT START
	-- ============================================================
	
	local WWalkRow = script.Parent
	local WWalkButton = WWalkRow.WWalkButton
	local MiscGroup = WWalkRow.Parent
	local MiscAccentColor = MiscGroup.MiscAccentColor
	
	local player = game.Players.LocalPlayer
	local RunService = game:GetService("RunService")
	local UserGameSettings = UserSettings():GetService("UserGameSettings")
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP SETUP
	-- ============================================================
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke", tooltipFrame)
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	
	local tooltipPadding = Instance.new("UIPadding", tooltipFrame)
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	
	local tooltipLabel = Instance.new("TextLabel", tooltipFrame)
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Allows you to walk on walls and ceilings. THIS IS UNDO-ABLE, REJOINING WILL FIX IT"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	
	local WWalkHelp = WWalkRow.WWalkHelp
	
	WWalkHelp.MouseEnter:Connect(function()
		local abs = WWalkHelp.AbsolutePosition
		local size = WWalkHelp.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	
	WWalkHelp.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- BUTTON TEXT HELPERS
	-- ============================================================
	local originalText = WWalkButton.Text
	
	local function setButtonText(text, useTextScaled)
		WWalkButton.Text = text
		WWalkButton.Font = Enum.Font.Inconsolata
		WWalkButton.TextScaled = useTextScaled or false
	end
	
	local function resetButtonText()
		WWalkButton.Text = originalText
		WWalkButton.Font = Enum.Font.Inconsolata
		WWalkButton.TextScaled = false
	end
	
	-- ============================================================
	-- CAMERA SETTINGS
	-- ============================================================
	-- Saved BEFORE executing so we can restore them after reset
	local savedMinZoom     = player.CameraMinZoomDistance
	local savedMaxZoom     = player.CameraMaxZoomDistance
	local savedSensitivity = UserGameSettings.GamepadCameraSensitivity
	
	-- ── ZOOM LIMITS ──────────────────────────────────────────────
	-- How close/far the camera can be while wall walk is active.
	-- ZOOM_MIN must stay >= 0.5. The transparency fade kicks in under
	-- 2 studs — the loop below overrides that so you stay visible.
	local ZOOM_MIN = 0.5   -- << change this to adjust how close you can zoom
	local ZOOM_MAX = 128   -- << change this to adjust how far you can zoom out
	
	-- ── SENSITIVITY ──────────────────────────────────────────────
	-- The wall walk script can mess up your camera sensitivity after reset.
	-- Set USE_MANUAL_SENSITIVITY = true and set MANUAL_SENSITIVITY to your
	-- preferred value to force it back on reset.
	-- Default Roblox mobile sensitivity is around 2. Range: 0.1 to 10.
	local USE_MANUAL_SENSITIVITY = false  -- << set true to force a fixed value
	local MANUAL_SENSITIVITY     = 2      -- << your preferred sensitivity (0.1–10)
	
	-- ============================================================
	-- STATE
	-- ============================================================
	local isExecuted   = false
	local debounce     = false
	local zoomLoopConn = nil
	
	-- ============================================================
	-- PER-FRAME LOOP
	-- Runs every frame while wall walk is active:
	--   1. Keeps zoom min/max unlocked so you can zoom in and out freely
	--   2. Resets LocalTransparencyModifier on all character parts every
	--      frame so the wall walk camera can never make you invisible
	-- ============================================================
	local function startLoop()
		if zoomLoopConn then return end
	
		zoomLoopConn = RunService.RenderStepped:Connect(function()
			-- Keep zoom range free
			if player.CameraMinZoomDistance ~= ZOOM_MIN then
				player.CameraMinZoomDistance = ZOOM_MIN
			end
			if player.CameraMaxZoomDistance ~= ZOOM_MAX then
				player.CameraMaxZoomDistance = ZOOM_MAX
			end
	
			-- Prevent character from going invisible on your screen.
			-- The wall walk camera sets LocalTransparencyModifier > 0 when
			-- you zoom in. We reset it to 0 every frame to undo that.
			local char = player.Character
			if char then
				for _, part in ipairs(char:GetDescendants()) do
					if part:IsA("BasePart") and part.LocalTransparencyModifier ~= 0 then
						part.LocalTransparencyModifier = 0
					end
				end
			end
		end)
	end
	
	local function stopLoop()
		if zoomLoopConn then
			zoomLoopConn:Disconnect()
			zoomLoopConn = nil
		end
	end
	
	-- ============================================================
	-- RESTORE CAMERA (called on reset/death/script remove)
	-- ============================================================
	local function restoreCamera()
		stopLoop()
		player.CameraMinZoomDistance = savedMinZoom
		player.CameraMaxZoomDistance = savedMaxZoom
		if USE_MANUAL_SENSITIVITY then
			UserGameSettings.GamepadCameraSensitivity = MANUAL_SENSITIVITY
		else
			UserGameSettings.GamepadCameraSensitivity = savedSensitivity
		end
	end
	
	-- On character reset or death, restore camera and reset button
	player.CharacterAdded:Connect(function()
		restoreCamera()
		isExecuted = false
		debounce   = false
		resetButtonText()
	end)
	
	-- ============================================================
	-- BUTTON CLICK
	-- ============================================================
	WWalkButton.MouseButton1Click:Connect(function()
	
		-- While showing "Executed!" text: clicking just resets text, no re-run
		if isExecuted then
			isExecuted = false
			debounce   = false
			resetButtonText()
			return
		end
	
		if debounce then return end
		debounce = true
	
		-- Snapshot camera settings right before executing
		savedMinZoom     = player.CameraMinZoomDistance
		savedMaxZoom     = player.CameraMaxZoomDistance
		savedSensitivity = UserGameSettings.GamepadCameraSensitivity
	
		local ok, err = pcall(function()
			loadstring(game:HttpGet("https://pastebin.com/raw/zXk4Rq2r"))()
		end)
	
		if ok then
			isExecuted = true
			setButtonText("Executed! Please reset to turn it off", true)
			-- Start the frame loop to keep zoom working and char visible
			startLoop()
	
		else
			-- Failed: show message 3 seconds then auto-reset
			setButtonText("Failed to Execute", false)
			task.wait(3)
			if not isExecuted then
				resetButtonText()
				debounce = false
			end
		end
	end)
	
	-- ============================================================
	-- CLEANUP ON SCRIPT REMOVE
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			restoreCamera()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_11c);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.SpinRow.SpinScript
local function C_120()
local script = G2L["120"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local SpinRow = script.Parent
	local SpinTickBox = SpinRow.SpinTickBox
	local SpinChanger = SpinRow.SpinChanger
	local MiscGroup = SpinRow.Parent
	local MiscAccentColor = MiscGroup.MiscAccentColor
	
	local isEnabled = false
	local spinConnection = nil
	local spinSpeed = 1
	
	local function getAccent()
		return MiscAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = SpinRow
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "SpinTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Spins your character"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	SpinTickBox.MouseEnter:Connect(function()
		local abs = SpinTickBox.AbsolutePosition
		local size = SpinTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	SpinTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	SpinChanger.FocusLost:Connect(function()
		local num = tonumber(SpinChanger.Text)
		if num and num > 0 then
			spinSpeed = num
		else
			SpinChanger.Text = tostring(spinSpeed)
		end
	end)
	
	local function enableSpin()
		isEnabled = true
		SpinTickBox.BackgroundColor3 = getAccent()
		local angle = 0
		spinConnection = RunService.RenderStepped:Connect(function(dt)
			local char = LocalPlayer.Character
			if not char then return end
			local hrp = char:FindFirstChild("HumanoidRootPart")
			if not hrp then return end
			angle = angle + (-spinSpeed * 3 * dt * 60)
			hrp.CFrame = CFrame.new(hrp.Position) * CFrame.Angles(0, math.rad(angle), 0)
		end)
	end
	
	local function disableSpin()
		isEnabled = false
		SpinTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		if spinConnection then
			spinConnection:Disconnect()
			spinConnection = nil
		end
	end
	
	SpinTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disableSpin()
		else
			enableSpin()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			if spinConnection then
				spinConnection:Disconnect()
				spinConnection = nil
			end
			task.wait(0.5)
			enableSpin()
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disableSpin()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_120);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.FakeLagRow.FakeLagScript
local function C_125()
local script = G2L["125"];
	local Players = game:GetService("Players")
	
	local LocalPlayer = Players.LocalPlayer
	
	local FakeLagRow = script.Parent
	local FakeLagTickBox = FakeLagRow.FakeLagTickBox
	local FakeLagChanger = FakeLagRow.FakeLagChanger
	local MiscGroup = FakeLagRow.Parent
	local MiscAccentColor = MiscGroup.MiscAccentColor
	
	local isEnabled = false
	local lagLoop = nil
	local lagSpeed = 1
	
	local function getAccent()
		return MiscAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = FakeLagRow
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "FakeLagTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Simulates lag by freezing your character. Lower speed = more lag."
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	FakeLagTickBox.MouseEnter:Connect(function()
		local abs = FakeLagTickBox.AbsolutePosition
		local size = FakeLagTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	FakeLagTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	FakeLagChanger.FocusLost:Connect(function()
		local num = tonumber(FakeLagChanger.Text)
		if num and num > 0 then
			lagSpeed = num
		else
			FakeLagChanger.Text = tostring(lagSpeed)
		end
	end)
	
	local function fakeLagLoop()
		while isEnabled do
			local char = LocalPlayer.Character
			if char and char:FindFirstChild("HumanoidRootPart") then
				local root = char.HumanoidRootPart
				local freezeTime = math.clamp(0.8 / (lagSpeed * 3), 0.05, 0.8)
				local unfreezeTime = math.clamp(0.2 / (lagSpeed * 3), 0.05, 0.5)
				root.Anchored = true
				task.wait(freezeTime)
				root.Anchored = false
				task.wait(unfreezeTime)
			else
				task.wait(0.2)
			end
		end
	end
	
	local function enableFakeLag()
		isEnabled = true
		FakeLagTickBox.BackgroundColor3 = getAccent()
		lagLoop = task.spawn(fakeLagLoop)
	end
	
	local function disableFakeLag()
		isEnabled = false
		FakeLagTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		local char = LocalPlayer.Character
		if char and char:FindFirstChild("HumanoidRootPart") then
			char.HumanoidRootPart.Anchored = false
		end
	end
	
	FakeLagTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disableFakeLag()
		else
			enableFakeLag()
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			task.wait(0.5)
			lagLoop = task.spawn(fakeLagLoop)
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disableFakeLag()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_125);
-- StarterGui.ScreenGui.MainFrame.ContentArea.MiscPage.MiscLeftSide.Misc1Group.AnimSpeedRow.AnimSpeedScript
local function C_12a()
local script = G2L["12a"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local AnimSpeedRow = script.Parent
	local AnimSpeedTickBox = AnimSpeedRow.AnimSpeedTickBox
	local AnimSpeedChanger = AnimSpeedRow.AnimSpeedChanger
	local MiscGroup = AnimSpeedRow.Parent
	local MiscAccentColor = MiscGroup.MiscAccentColor
	
	local isEnabled = false
	local speedConnection = nil
	local animSpeed = 1
	local DEFAULT_SPEED = 1
	
	local function getAccent()
		return MiscAccentColor.BackgroundColor3
	end
	
	local function getScreenGui()
		local parent = AnimSpeedRow
		while parent do
			if parent:IsA("ScreenGui") then return parent end
			parent = parent.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "AnimSpeedTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.XY
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Changes your animation speed. Set to 0 to freeze animations, higher = faster."
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.AutomaticSize = Enum.AutomaticSize.XY
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	AnimSpeedTickBox.MouseEnter:Connect(function()
		local abs = AnimSpeedTickBox.AbsolutePosition
		local size = AnimSpeedTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	AnimSpeedTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	local function getSpeed()
		local num = tonumber(AnimSpeedChanger.Text)
		if num == nil then return animSpeed end
		if num < 0 then return animSpeed end
		return num
	end
	
	local function applyAnimSpeed(speed)
		local character = LocalPlayer.Character
		if not character then return end
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
		for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
			track:AdjustSpeed(speed)
		end
	end
	
	local function enableAnimSpeed()
		isEnabled = true
		AnimSpeedTickBox.BackgroundColor3 = getAccent()
		speedConnection = RunService.Heartbeat:Connect(function()
			applyAnimSpeed(getSpeed())
		end)
	end
	
	local function disableAnimSpeed()
		isEnabled = false
		AnimSpeedTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
		if speedConnection then
			speedConnection:Disconnect()
			speedConnection = nil
		end
		applyAnimSpeed(DEFAULT_SPEED)
	end
	
	AnimSpeedTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disableAnimSpeed()
		else
			enableAnimSpeed()
		end
	end)
	
	AnimSpeedChanger.FocusLost:Connect(function()
		if AnimSpeedChanger.Text == "" then return end
		local num = tonumber(AnimSpeedChanger.Text)
		if num == nil or num < 0 then
			AnimSpeedChanger.Text = tostring(animSpeed)
		else
			animSpeed = num
		end
	end)
	
	LocalPlayer.CharacterAdded:Connect(function()
		if isEnabled then
			task.wait(0.5)
			enableAnimSpeed()
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disableAnimSpeed()
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_12a);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GriefScript
local function C_13a()
local script = G2L["13a"];
	-- GriefScript (inside GriefGroup)
	local GriefGroup = script.Parent
	local GriefMinimize = GriefGroup.GriefTitle.GriefMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(GriefGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = GriefGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	GriefMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
	
		if isMinimized then
			GriefGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			GriefMinimize.Text = "+"
		else
			GriefGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			GriefMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_13a);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.GriefToolTip
local function C_13b()
local script = G2L["13b"];
	-- ============================================================
	-- BKIT GROUP TOOLTIP SCRIPT
	-- Tooltips for BkitGroup features
	-- ============================================================
	local BkitGroup = script.Parent
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then
				return p
			end
			p = p.Parent
		end
	end
	
	local ScreenGui = getScreenGui()
	if not ScreenGui then return end
	
	-- ============================================================
	-- FIND ELEMENTS IN BKITGROUP
	-- ============================================================
	local function findDescendant(name)
		for _, v in pairs(BkitGroup:GetDescendants()) do
			if v.Name == name then
				return v
			end
		end
	end
	
	local BlockSpamLabelHelp = findDescendant("BlockSpamLabelHelp")
	local FollowBlockTickBox = findDescendant("FollowBlockTickBox")
	
	-- ============================================================
	-- TOOLTIP BUILDER
	-- ============================================================
	local function makeTooltip(text)
		local frame = Instance.new("Frame")
		frame.BackgroundColor3 = Color3.fromRGB(16,16,16)
		frame.BorderSizePixel = 0
		frame.ZIndex = 100
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Size = UDim2.new(0, 212, 0, 0)
		frame.Visible = false
		frame.Parent = ScreenGui
	
		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(57,57,57)
		stroke.Parent = frame
	
		local padding = Instance.new("UIPadding")
		padding.PaddingTop = UDim.new(0,6)
		padding.PaddingBottom = UDim.new(0,6)
		padding.PaddingLeft = UDim.new(0,6)
		padding.PaddingRight = UDim.new(0,6)
		padding.Parent = frame
	
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Text = text
		label.TextColor3 = Color3.new(1,1,1)
		label.TextSize = 14
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Top
		label.Font = Enum.Font.GothamSemibold
		label.TextWrapped = true
		label.Size = UDim2.new(0,200,0,0)
		label.AutomaticSize = Enum.AutomaticSize.Y
		label.ZIndex = 101
		label.Parent = frame
	
		return frame
	end
	
	local function hookTooltip(button, tooltipFrame)
		if not button then return end
	
		button.MouseEnter:Connect(function()
			local abs = button.AbsolutePosition
			local size = button.AbsoluteSize
			tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
			tooltipFrame.Visible = true
		end)
	
		button.MouseLeave:Connect(function()
			tooltipFrame.Visible = false
		end)
	end
	
	-- ============================================================
	-- CREATE TOOLTIPS
	-- ============================================================
	local blockSpamHelpTooltip = makeTooltip("Select an unanchored block. To start spam building, press Start Spam. Used for lag machines and more. Block spam only works when you're near the selected block. Use Follow Block to stay attached to the selected block.")
	local followBlockTooltip = makeTooltip("Attaches you to the selected unanchored block so you move with it. Disables block collision.")
	
	-- ============================================================
	-- HOOK THEM
	-- ============================================================
	hookTooltip(BlockSpamLabelHelp, blockSpamHelpTooltip)
	hookTooltip(FollowBlockTickBox, followBlockTooltip)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			blockSpamHelpTooltip:Destroy()
			followBlockTooltip:Destroy()
		end
	end)
end;
task.spawn(C_13b);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.SelectDRow.SelectDScript
local function C_141()
local script = G2L["141"];
	local Players = game:GetService("Players")
	local UserInputService = game:GetService("UserInputService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local SelectDRow = script.Parent
	local SelectDDropdown = SelectDRow.SelectDDropdown
	local GriefGroup = SelectDRow.Parent
	local GriefAccentColor = GriefGroup:WaitForChild("GriefAccentColor")
	
	local selectedPlayers = {}
	local isOpen = false
	local showDisplay = false
	
	local ROW_HEIGHT = 16
	local MAX_ROWS = 7
	local BG = Color3.fromRGB(24, 24, 24)
	local BORDER = Color3.fromRGB(57, 57, 57)
	local WHITE = Color3.new(1, 1, 1)
	local DIM = Color3.fromRGB(120, 120, 120)
	local BLUE = Color3.fromRGB(0, 123, 255)
	local HOVER = Color3.fromRGB(36, 36, 36)
	local BTN_BG = Color3.fromRGB(36, 36, 36)
	
	if not game:IsLoaded() then
		game.Loaded:Wait()
	end
	
	repeat task.wait() until SelectDDropdown.AbsoluteSize.X > 0
	
	-- ============================================================
	-- MAIN LABEL + ARROW
	-- ============================================================
	local mainLabel = Instance.new("TextLabel")
	mainLabel.Name = "MainLabel"
	mainLabel.Size = UDim2.new(1, -24, 1, 0)
	mainLabel.Position = UDim2.new(0, 0, 0, 0)
	mainLabel.BackgroundTransparency = 1
	mainLabel.Text = "Select Players"
	mainLabel.TextColor3 = WHITE
	mainLabel.TextSize = 14
	mainLabel.Font = Enum.Font.Code
	mainLabel.TextXAlignment = Enum.TextXAlignment.Center
	mainLabel.TextTruncate = Enum.TextTruncate.AtEnd
	mainLabel.ZIndex = 2
	mainLabel.Parent = SelectDDropdown
	
	local mainArrow = Instance.new("TextLabel")
	mainArrow.Name = "MainArrow"
	mainArrow.Size = UDim2.new(0, 24, 1, 0)
	mainArrow.Position = UDim2.new(1, -24, 0, 0)
	mainArrow.BackgroundTransparency = 1
	mainArrow.Text = "▼"
	mainArrow.TextColor3 = WHITE
	mainArrow.TextSize = 14
	mainArrow.Font = Enum.Font.Code
	mainArrow.TextXAlignment = Enum.TextXAlignment.Center
	mainArrow.ZIndex = 2
	mainArrow.Parent = SelectDDropdown
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	local BOTTOM_BAR_HEIGHT = ROW_HEIGHT + 2
	local TOTAL_BOTTOM_HEIGHT = (BOTTOM_BAR_HEIGHT * 2) + 1
	
	-- ============================================================
	-- PANEL
	-- ============================================================
	local panel = Instance.new("Frame")
	panel.Name = "SelectDPanel"
	panel.BackgroundColor3 = BG
	panel.BorderSizePixel = 0
	panel.Size = UDim2.new(0, 161, 0, ROW_HEIGHT)
	panel.ZIndex = 999
	panel.Visible = false
	panel.Parent = getScreenGui()
	
	local panelStroke = Instance.new("UIStroke")
	panelStroke.Color = BORDER
	panelStroke.Thickness = 1
	panelStroke.Parent = panel
	
	local searchBox = Instance.new("TextBox")
	searchBox.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
	searchBox.Position = UDim2.new(0, 0, 0, 0)
	searchBox.BackgroundColor3 = BG
	searchBox.BorderSizePixel = 0
	searchBox.Text = ""
	searchBox.PlaceholderText = "Search for users..."
	searchBox.PlaceholderColor3 = DIM
	searchBox.TextColor3 = WHITE
	searchBox.TextSize = 14
	searchBox.Font = Enum.Font.Code
	searchBox.TextXAlignment = Enum.TextXAlignment.Left
	searchBox.ClearTextOnFocus = false
	searchBox.ZIndex = 1000
	searchBox.Parent = panel
	
	local searchPad = Instance.new("UIPadding")
	searchPad.PaddingLeft = UDim.new(0, 6)
	searchPad.PaddingRight = UDim.new(0, 24)
	searchPad.Parent = searchBox
	
	local searchArrow = Instance.new("TextButton")
	searchArrow.Size = UDim2.new(0, 24, 0, ROW_HEIGHT)
	searchArrow.Position = UDim2.new(1, -24, 0, 0)
	searchArrow.BackgroundTransparency = 1
	searchArrow.Text = "▲"
	searchArrow.TextColor3 = WHITE
	searchArrow.TextSize = 14
	searchArrow.Font = Enum.Font.Code
	searchArrow.BorderSizePixel = 0
	searchArrow.ZIndex = 1001
	searchArrow.Parent = panel
	
	local scrollFrame = Instance.new("ScrollingFrame")
	scrollFrame.Position = UDim2.new(0, 0, 0, ROW_HEIGHT)
	scrollFrame.Size = UDim2.new(1, 0, 0, 0)
	scrollFrame.BackgroundTransparency = 1
	scrollFrame.BorderSizePixel = 0
	scrollFrame.ScrollBarThickness = 2
	scrollFrame.ScrollBarImageColor3 = BORDER
	scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
	scrollFrame.ZIndex = 1000
	scrollFrame.Parent = panel
	
	local listLayout = Instance.new("UIListLayout")
	listLayout.SortOrder = Enum.SortOrder.LayoutOrder
	listLayout.Parent = scrollFrame
	
	local bottomBar = Instance.new("Frame")
	bottomBar.Size = UDim2.new(1, 0, 0, TOTAL_BOTTOM_HEIGHT)
	bottomBar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
	bottomBar.BorderSizePixel = 0
	bottomBar.ZIndex = 1000
	bottomBar.Parent = panel
	
	local bottomStroke = Instance.new("UIStroke")
	bottomStroke.Color = BORDER
	bottomStroke.Thickness = 1
	bottomStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	bottomStroke.Parent = bottomBar
	
	-- Row 1: Select All / Unselect All
	local selectAllBtn = Instance.new("TextButton")
	selectAllBtn.Size = UDim2.new(0.5, -1, 0, BOTTOM_BAR_HEIGHT)
	selectAllBtn.Position = UDim2.new(0, 0, 0, 0)
	selectAllBtn.BackgroundColor3 = BTN_BG
	selectAllBtn.BorderSizePixel = 0
	selectAllBtn.Text = "Select All"
	selectAllBtn.TextColor3 = WHITE
	selectAllBtn.TextSize = 11
	selectAllBtn.Font = Enum.Font.Code
	selectAllBtn.ZIndex = 1001
	selectAllBtn.Parent = bottomBar
	
	local divider1 = Instance.new("Frame")
	divider1.Size = UDim2.new(0, 1, 0, BOTTOM_BAR_HEIGHT)
	divider1.Position = UDim2.new(0.5, -1, 0, 0)
	divider1.BackgroundColor3 = BORDER
	divider1.BorderSizePixel = 0
	divider1.ZIndex = 1001
	divider1.Parent = bottomBar
	
	local unselectAllBtn = Instance.new("TextButton")
	unselectAllBtn.Size = UDim2.new(0.5, 0, 0, BOTTOM_BAR_HEIGHT)
	unselectAllBtn.Position = UDim2.new(0.5, 1, 0, 0)
	unselectAllBtn.BackgroundColor3 = BTN_BG
	unselectAllBtn.BorderSizePixel = 0
	unselectAllBtn.Text = "Unselect All"
	unselectAllBtn.TextColor3 = WHITE
	unselectAllBtn.TextSize = 11
	unselectAllBtn.Font = Enum.Font.Code
	unselectAllBtn.ZIndex = 1001
	unselectAllBtn.Parent = bottomBar
	
	-- Divider between Row 1 and Row 2
	local rowDivider = Instance.new("Frame")
	rowDivider.Size = UDim2.new(1, 0, 0, 1)
	rowDivider.Position = UDim2.new(0, 0, 0, BOTTOM_BAR_HEIGHT)
	rowDivider.BackgroundColor3 = BORDER
	rowDivider.BorderSizePixel = 0
	rowDivider.ZIndex = 1001
	rowDivider.Parent = bottomBar
	
	-- Row 2: Show Display (full width)
	local displayBtn = Instance.new("TextButton")
	displayBtn.Size = UDim2.new(1, 0, 0, BOTTOM_BAR_HEIGHT)
	displayBtn.Position = UDim2.new(0, 0, 0, BOTTOM_BAR_HEIGHT + 1)
	displayBtn.BackgroundColor3 = BTN_BG
	displayBtn.BorderSizePixel = 0
	displayBtn.Text = "Show Display"
	displayBtn.TextColor3 = WHITE
	displayBtn.TextSize = 11
	displayBtn.Font = Enum.Font.Code
	displayBtn.ZIndex = 1001
	displayBtn.Parent = bottomBar
	
	-- ============================================================
	-- THUMBNAILS
	-- ============================================================
	local cachedThumbs = {}
	local function getThumb(userId)
		if cachedThumbs[userId] then return cachedThumbs[userId] end
		local ok, img = pcall(function()
			return Players:GetUserThumbnailAsync(userId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size48x48)
		end)
		local result = ok and img or ""
		cachedThumbs[userId] = result
		return result
	end
	
	-- ============================================================
	-- LABEL UPDATE
	-- ============================================================
	local function updateMainLabel()
		local count = 0
		local names = {}
		for name, _ in pairs(selectedPlayers) do
			count = count + 1
			local player = Players:FindFirstChild(name)
			if player then
				local displayText = showDisplay and player.DisplayName or player.Name
				table.insert(names, displayText)
			end
		end
		if count == 0 then
			mainLabel.Text = "Select Players"
		elseif count <= 2 then
			mainLabel.Text = table.concat(names, ", ")
		else
			mainLabel.Text = count .. " Players Selected"
		end
	end
	
	-- ============================================================
	-- SHARED ATTRIBUTE (so other scripts can read it)
	-- ============================================================
	local function updateSelectedPlayers()
		local names = {}
		for name, _ in pairs(selectedPlayers) do
			table.insert(names, name)
		end
		SelectDRow:SetAttribute("SelectedPlayers", table.concat(names, ","))
		-- Sync to shared so other scripts can read it directly
		shared.GriefSelectedPlayers = selectedPlayers
	end
	
	-- ============================================================
	-- LIST BUILD
	-- ============================================================
	local function updatePanelSize(rowCount)
		local visibleRows = math.min(rowCount, MAX_ROWS)
		local scrollH = visibleRows * ROW_HEIGHT
		scrollFrame.Size = UDim2.new(1, 0, 0, scrollH)
		panel.Size = UDim2.new(0, 161, 0, ROW_HEIGHT + scrollH + TOTAL_BOTTOM_HEIGHT)
		scrollFrame.CanvasSize = UDim2.new(0, 0, 0, rowCount * ROW_HEIGHT)
		bottomBar.Position = UDim2.new(0, 0, 0, ROW_HEIGHT + scrollH)
	end
	
	local function buildList(filter)
		for _, child in pairs(scrollFrame:GetChildren()) do
			if child:IsA("Frame") then child:Destroy() end
		end
	
		local playerList = {}
		for _, p in pairs(Players:GetPlayers()) do
			table.insert(playerList, p)
		end
	
		table.sort(playerList, function(a, b)
			local nameA = showDisplay and a.DisplayName or a.Name
			local nameB = showDisplay and b.DisplayName or b.Name
			return nameA:lower() < nameB:lower()
		end)
	
		local filtered = {}
		for _, p in ipairs(playerList) do
			local displayText = showDisplay and p.DisplayName or p.Name
			if filter == "" or displayText:lower():find(filter:lower(), 1, true) then
				table.insert(filtered, p)
			end
		end
	
		for i, p in ipairs(filtered) do
			local isSelected = selectedPlayers[p.Name] ~= nil
			local displayText = showDisplay and p.DisplayName or p.Name
	
			local row = Instance.new("Frame")
			row.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
			row.BackgroundColor3 = isSelected and HOVER or BG
			row.BorderSizePixel = 0
			row.LayoutOrder = i
			row.ZIndex = 1001
			row.Parent = scrollFrame
	
			local thumb = Instance.new("ImageLabel")
			thumb.Size = UDim2.new(0, 12, 0, 12)
			thumb.Position = UDim2.new(1, -16, 0.5, -6)
			thumb.BackgroundTransparency = 1
			thumb.Image = ""
			thumb.ZIndex = 1002
			thumb.Parent = row
	
			task.spawn(function()
				thumb.Image = getThumb(p.UserId)
			end)
	
			local nameLabel = Instance.new("TextLabel")
			nameLabel.Size = UDim2.new(1, -22, 1, 0)
			nameLabel.Position = UDim2.new(0, 6, 0, 0)
			nameLabel.BackgroundTransparency = 1
			nameLabel.Text = displayText
			nameLabel.TextColor3 = isSelected and BLUE or WHITE
			nameLabel.TextSize = 14
			nameLabel.Font = Enum.Font.Code
			nameLabel.TextXAlignment = Enum.TextXAlignment.Left
			nameLabel.TextTruncate = Enum.TextTruncate.AtEnd
			nameLabel.ZIndex = 1002
			nameLabel.Parent = row
	
			local rowBtn = Instance.new("TextButton")
			rowBtn.Size = UDim2.new(1, 0, 1, 0)
			rowBtn.BackgroundTransparency = 1
			rowBtn.Text = ""
			rowBtn.ZIndex = 1003
			rowBtn.Parent = row
	
			rowBtn.MouseEnter:Connect(function()
				row.BackgroundColor3 = HOVER
			end)
			rowBtn.MouseLeave:Connect(function()
				if not selectedPlayers[p.Name] then
					row.BackgroundColor3 = BG
				end
			end)
	
			rowBtn.MouseButton1Click:Connect(function()
				if selectedPlayers[p.Name] then
					selectedPlayers[p.Name] = nil
					nameLabel.TextColor3 = WHITE
					row.BackgroundColor3 = BG
				else
					selectedPlayers[p.Name] = true
					nameLabel.TextColor3 = BLUE
					row.BackgroundColor3 = HOVER
				end
				updateMainLabel()
				updateSelectedPlayers()
			end)
		end
	
		updatePanelSize(#filtered)
	end
	
	-- ============================================================
	-- SELECT ALL / UNSELECT ALL
	-- ============================================================
	selectAllBtn.MouseButton1Click:Connect(function()
		for _, p in pairs(Players:GetPlayers()) do
			selectedPlayers[p.Name] = true
		end
		updateMainLabel()
		updateSelectedPlayers()
		buildList(searchBox.Text)
	end)
	
	unselectAllBtn.MouseButton1Click:Connect(function()
		selectedPlayers = {}
		updateMainLabel()
		updateSelectedPlayers()
		buildList(searchBox.Text)
	end)
	
	-- ============================================================
	-- SHOW DISPLAY / SHOW USERNAME
	-- ============================================================
	displayBtn.MouseButton1Click:Connect(function()
		showDisplay = not showDisplay
		displayBtn.Text = showDisplay and "Show Username" or "Show Display"
		updateMainLabel()
		if isOpen then buildList(searchBox.Text) end
	end)
	
	-- ============================================================
	-- OPEN / CLOSE
	-- ============================================================
	local function openDropdown()
		isOpen = true
		mainLabel.Visible = false
		mainArrow.Visible = false
	
		repeat task.wait() until SelectDDropdown.AbsoluteSize.X > 0
	
		local absPos = SelectDDropdown.AbsolutePosition
		local absSize = SelectDDropdown.AbsoluteSize
		panel.Position = UDim2.new(0, absPos.X + (absSize.X - 161) / 2, 0, absPos.Y + absSize.Y)
		panel.Size = UDim2.new(0, absSize.X, 0, ROW_HEIGHT)
		panel.Visible = true
	
		searchBox.Text = ""
		buildList("")
	end
	
	local function closeDropdown()
		isOpen = false
		panel.Visible = false
		mainLabel.Visible = true
		mainArrow.Visible = true
		mainArrow.Text = "▼"
		updateMainLabel()
	end
	
	SelectDDropdown.MouseButton1Click:Connect(function()
		if isOpen then
			closeDropdown()
		else
			openDropdown()
		end
	end)
	
	searchArrow.MouseButton1Click:Connect(function()
		closeDropdown()
	end)
	
	searchBox:GetPropertyChangedSignal("Text"):Connect(function()
		buildList(searchBox.Text)
	end)
	
	UserInputService.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 and isOpen then
			local mousePos = input.Position
			local panelPos = panel.AbsolutePosition
			local panelSize = panel.AbsoluteSize
			local btnPos = SelectDDropdown.AbsolutePosition
			local btnSize = SelectDDropdown.AbsoluteSize
	
			local inPanel = mousePos.X >= panelPos.X and mousePos.X <= panelPos.X + panelSize.X
				and mousePos.Y >= panelPos.Y and mousePos.Y <= panelPos.Y + panelSize.Y
			local inBtn = mousePos.X >= btnPos.X and mousePos.X <= btnPos.X + btnSize.X
				and mousePos.Y >= btnPos.Y and mousePos.Y <= btnPos.Y + btnSize.Y
	
			if not inPanel and not inBtn then
				closeDropdown()
			end
		end
	end)
	
	Players.PlayerRemoving:Connect(function(p)
		if selectedPlayers[p.Name] then
			selectedPlayers[p.Name] = nil
			updateMainLabel()
			updateSelectedPlayers()
		end
		if isOpen then buildList(searchBox.Text) end
	end)
	
	Players.PlayerAdded:Connect(function()
		if isOpen then buildList(searchBox.Text) end
	end)
	
	SelectDRow:SetAttribute("SelectedPlayers", "")
	updateMainLabel()
end;
task.spawn(C_141);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.HLBlockRow.HLBlockScript
local function C_147()
local script = G2L["147"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local HLBlockRow = script.Parent
	local GriefGroup = HLBlockRow.Parent
	local GriefAccentColor = GriefGroup:WaitForChild("GriefAccentColor")
	local HLBlockTickBox = HLBlockRow:WaitForChild("HLBlockTickBox")
	
	-- Get the SelectD row to access selected players
	local SelectDRow = GriefGroup:FindFirstChild("SelectDRow")
	
	-- State
	local isEnabled = false
	local highlights = {} -- Store all highlights so we can remove them later
	
	-- ============================================================
	-- UPDATE VISUAL
	-- ============================================================
	local function updateVisual()
		if isEnabled then
			HLBlockTickBox.BackgroundColor3 = GriefAccentColor.BackgroundColor3
		else
			HLBlockTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	updateVisual()
	
	-- ============================================================
	-- GET SELECTED PLAYERS
	-- ============================================================
	local function getSelectedPlayers()
		-- Method 1: Use shared table (fastest)
		if shared.GriefSelectedPlayers then
			return shared.GriefSelectedPlayers
		end
	
		-- Method 2: Parse attribute (fallback)
		if SelectDRow then
			local attr = SelectDRow:GetAttribute("SelectedPlayers")
			if attr and attr ~= "" then
				local players = {}
				for name in string.gmatch(attr, "[^,]+") do
					players[name] = true
				end
				return players
			end
		end
	
		return {}
	end
	
	-- ============================================================
	-- GET PLAYER'S BLOCKS
	-- ============================================================
	local function getPlayerBlocks(playerName)
		local blocks = {}
		local bricksFolder = workspace:FindFirstChild("Bricks")
		if not bricksFolder then return blocks end
	
		local playerFolder = bricksFolder:FindFirstChild(playerName)
		if not playerFolder then return blocks end
	
		-- Get all bricks in the player's folder
		for _, brick in ipairs(playerFolder:GetChildren()) do
			if brick:IsA("BasePart") and brick.Name == "Brick" then
				table.insert(blocks, brick)
			end
		end
	
		return blocks
	end
	
	-- ============================================================
	-- REMOVE ALL HIGHLIGHTS
	-- ============================================================
	local function removeAllHighlights()
		for _, highlight in ipairs(highlights) do
			if highlight and highlight.Parent then
				highlight:Destroy()
			end
		end
		highlights = {}
		print("[HLBlock] Removed all highlights")
	end
	
	-- ============================================================
	-- HIGHLIGHT BLOCKS
	-- ============================================================
	local function highlightBlocks()
		removeAllHighlights()
	
		local selectedPlayers = getSelectedPlayers()
		local totalBlocks = 0
	
		for playerName, _ in pairs(selectedPlayers) do
			local blocks = getPlayerBlocks(playerName)
	
			for _, brick in ipairs(blocks) do
				-- Create red cham highlight (no outline)
				local highlight = Instance.new("Highlight")
				highlight.Adornee = brick
				highlight.FillColor = Color3.fromRGB(255, 0, 0) -- Red
				highlight.OutlineColor = Color3.fromRGB(255, 0, 0) -- Same as fill
				highlight.FillTransparency = 0.7 -- More transparent
				highlight.OutlineTransparency = 1 -- No outline
				highlight.Parent = brick
	
				table.insert(highlights, highlight)
				totalBlocks = totalBlocks + 1
			end
		end
	
		print("[HLBlock] Highlighted " .. totalBlocks .. " blocks from selected players")
	end
	
	-- ============================================================
	-- WATCH FOR NEW BLOCKS
	-- ============================================================
	local blockWatcher = nil
	
	local function startWatching()
		local bricksFolder = workspace:FindFirstChild("Bricks")
		if not bricksFolder then
			print("[HLBlock] Bricks folder not found")
			return
		end
	
		blockWatcher = bricksFolder.DescendantAdded:Connect(function(descendant)
			if not isEnabled then return end
			if not descendant:IsA("BasePart") or descendant.Name ~= "Brick" then return end
	
			-- Check if this brick belongs to a selected player
			local playerFolder = descendant.Parent
			if not playerFolder or playerFolder.Parent ~= bricksFolder then return end
	
			local selectedPlayers = getSelectedPlayers()
			if selectedPlayers[playerFolder.Name] then
				-- Highlight this new brick
				task.wait(0.1) -- Wait a bit for the brick to settle
	
				if descendant and descendant:IsDescendantOf(workspace) then
					local highlight = Instance.new("Highlight")
					highlight.Adornee = descendant
					highlight.FillColor = Color3.fromRGB(255, 0, 0)
					highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
					highlight.FillTransparency = 0.7 -- More transparent
					highlight.OutlineTransparency = 1 -- No outline
					highlight.Parent = descendant
	
					table.insert(highlights, highlight)
				end
			end
		end)
	end
	
	local function stopWatching()
		if blockWatcher then
			blockWatcher:Disconnect()
			blockWatcher = nil
		end
	end
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local function enable()
		isEnabled = true
		updateVisual()
	
		highlightBlocks()
		startWatching()
	
		print("[HLBlock] ON")
	end
	
	local function disable()
		isEnabled = false
		updateVisual()
	
		removeAllHighlights()
		stopWatching()
	
		print("[HLBlock] OFF")
	end
	
	-- ============================================================
	-- TICKBOX TOGGLE
	-- ============================================================
	HLBlockTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disable()
		else
			enable()
		end
	end)
	
	-- ============================================================
	-- WATCH FOR SELECTION CHANGES
	-- ============================================================
	if SelectDRow then
		SelectDRow:GetAttributeChangedSignal("SelectedPlayers"):Connect(function()
			if isEnabled then
				-- Re-highlight with new selection
				highlightBlocks()
			end
		end)
	end
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isEnabled then
				disable()
			end
			print("[HLBlock] Cleaned up")
		end
	end)
	
	-- ============================================================
	-- AUTO-ENABLE ON STARTUP
	-- ============================================================
	enable()
end;
task.spawn(C_147);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollRightSide.GriefGroup.ToggleGriefRow.ToggleGriefScript
local function C_14b()
local script = G2L["14b"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local RunService = game:GetService("RunService")
	
	local ToggleGriefRow = script.Parent
	local GriefGroup = ToggleGriefRow.Parent
	local GriefAccentColor = GriefGroup:WaitForChild("GriefAccentColor")
	local ToggleGriefButton = ToggleGriefRow:WaitForChild("ToggleGriefButton")
	
	-- Get the SelectD row to access selected players
	local SelectDRow = GriefGroup:FindFirstChild("SelectDRow")
	
	-- State
	local isGriefing = false
	local griefLoop = nil
	local deleteTool = nil
	local deleteEvent = nil
	local lastRefreshTime = 0
	
	-- Text states
	local TEXT_START = "Start Grief"
	local TEXT_STOP = "Stop Grief"
	local TEXT_NO_BLOCKS = "No Blocks Found"
	
	-- ============================================================
	-- GET SELECTED PLAYERS
	-- ============================================================
	local function getSelectedPlayers()
		-- Method 1: Use shared table (fastest)
		if shared.GriefSelectedPlayers then
			return shared.GriefSelectedPlayers
		end
	
		-- Method 2: Parse attribute (fallback)
		if SelectDRow then
			local attr = SelectDRow:GetAttribute("SelectedPlayers")
			if attr and attr ~= "" then
				local players = {}
				for name in string.gmatch(attr, "[^,]+") do
					players[name] = true
				end
				return players
			end
		end
	
		return {}
	end
	
	-- ============================================================
	-- GET ALL TARGET BLOCKS
	-- ============================================================
	local function getAllTargetBlocks()
		local blocks = {}
		local selectedPlayers = getSelectedPlayers()
		local bricksFolder = workspace:FindFirstChild("Bricks")
	
		if not bricksFolder then return blocks end
	
		for playerName, _ in pairs(selectedPlayers) do
			local playerFolder = bricksFolder:FindFirstChild(playerName)
			if playerFolder then
				for _, brick in ipairs(playerFolder:GetChildren()) do
					if brick:IsA("BasePart") and brick.Name == "Brick" then
						table.insert(blocks, brick)
					end
				end
			end
		end
	
		return blocks
	end
	
	-- ============================================================
	-- DELETE TOOL HELPERS
	-- ============================================================
	local function hasDeleteTool()
		return LocalPlayer.Backpack:FindFirstChild("Delete")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Delete"))
	end
	
	local function getDeleteEvent()
		local tool = hasDeleteTool()
		if tool then
			return tool:FindFirstChild("Script") and tool.Script:FindFirstChild("Event")
		end
		return nil
	end
	
	-- INSTANT re-equip and unequip function (same as DeleteBypass)
	local function refreshDeleteTool()
		local tool = hasDeleteTool()
		if not tool then
			return false
		end
	
		-- Store currently equipped tool to re-equip it after
		local previousTool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
	
		-- Move to backpack if it's in character
		if tool.Parent == LocalPlayer.Character then
			tool.Parent = LocalPlayer.Backpack
			task.wait()
		end
	
		-- Equip delete tool instantly
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
			LocalPlayer.Character.Humanoid:EquipTool(tool)
	
			-- Use RenderStepped to unequip on the very next frame (invisible to player)
			RunService.RenderStepped:Wait()
	
			-- Unequip immediately
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
				LocalPlayer.Character.Humanoid:UnequipTools()
			end
	
			-- Re-equip previous tool if there was one
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				task.wait()
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
					LocalPlayer.Character.Humanoid:EquipTool(previousTool)
				end
			end
	
			return true
		end
	
		return false
	end
	
	-- ============================================================
	-- START GRIEFING
	-- ============================================================
	local function startGrief()
		-- Check if delete tool exists
		if not hasDeleteTool() then
			ToggleGriefButton.Text = "Delete tool not found"
			task.wait(1)
			ToggleGriefButton.Text = TEXT_START
			return
		end
	
		-- Get all target blocks
		local targetBlocks = getAllTargetBlocks()
	
		if #targetBlocks == 0 then
			print("[ToggleGrief] No blocks found to delete")
			return
		end
	
		-- Initial refresh to activate delete tool
		if not refreshDeleteTool() then
			ToggleGriefButton.Text = "Failed to activate"
			task.wait(1)
			ToggleGriefButton.Text = TEXT_START
			return
		end
	
		isGriefing = true
		ToggleGriefButton.Text = TEXT_STOP
	
		local totalBlocks = #targetBlocks
		local deletedCount = 0
	
		print("[ToggleGrief] Starting grief - " .. totalBlocks .. " blocks to delete")
	
		-- Start the grief loop
		griefLoop = task.spawn(function()
			local refreshCounter = 0
	
			for i, block in ipairs(targetBlocks) do
				if not isGriefing then break end
	
				-- Check if block still exists before trying to delete
				if block and block:IsDescendantOf(workspace) then
					-- Get current delete tool and event
					local currentEvent = getDeleteEvent()
	
					if currentEvent then
						-- Fire delete event
						pcall(function()
							currentEvent:FireServer(block, block.Position)
						end)
	
						-- Count as attempted
						deletedCount = deletedCount + 1
					else
						-- Event lost, try to refresh
						refreshDeleteTool()
					end
				end
	
				-- Refresh tool every 50 attempts to prevent it from breaking
				refreshCounter = refreshCounter + 1
				if refreshCounter >= 50 then
					refreshDeleteTool()
					refreshCounter = 0
				end
	
				task.wait(0.05) -- Delete rate
			end
	
			-- Finished griefing - ran through all blocks
			if isGriefing then
				isGriefing = false
				ToggleGriefButton.Text = deletedCount .. " Blocks Deleted"
				print("[ToggleGrief] Finished - attempted " .. deletedCount .. " out of " .. totalBlocks .. " total")
	
				task.wait(1)
				ToggleGriefButton.Text = TEXT_START
			end
		end)
	end
	
	-- ============================================================
	-- STOP GRIEFING
	-- ============================================================
	local function stopGrief()
		isGriefing = false
		ToggleGriefButton.Text = TEXT_START
	
		if griefLoop then
			task.cancel(griefLoop)
			griefLoop = nil
		end
	
		print("[ToggleGrief] Grief stopped")
	end
	
	-- ============================================================
	-- BUTTON HOVER (show errors)
	-- ============================================================
	ToggleGriefButton.MouseEnter:Connect(function()
		if not isGriefing then
			local targetBlocks = getAllTargetBlocks()
			if #targetBlocks == 0 then
				ToggleGriefButton.Text = TEXT_NO_BLOCKS
			end
		end
	end)
	
	ToggleGriefButton.MouseLeave:Connect(function()
		if not isGriefing then
			-- Don't change text if it's showing a finish message
			if not ToggleGriefButton.Text:find("Blocks Deleted") then
				ToggleGriefButton.Text = TEXT_START
			end
		end
	end)
	
	-- ============================================================
	-- BUTTON CLICK HANDLER
	-- ============================================================
	ToggleGriefButton.MouseButton1Click:Connect(function()
		if isGriefing then
			-- Currently griefing -> Stop
			stopGrief()
		else
			-- Not griefing -> Start
			startGrief()
		end
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isGriefing then
				stopGrief()
			end
			print("[ToggleGrief] Cleaned up")
		end
	end)
end;
task.spawn(C_14b);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BypassScript
local function C_150()
local script = G2L["150"];
	-- BypassScript (inside BypassGroup)
	local BypassGroup = script.Parent
	local BypassMinimize = BypassGroup.BypassTitle.BypassMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(BypassGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = BypassGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	BypassMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			BypassGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			BypassMinimize.Text = "+"
		else
			BypassGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			BypassMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_150);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BypassToolTip
local function C_151()
local script = G2L["151"];
	-- ============================================================
	-- BYPASS TICKBOX TOOLTIP SCRIPT
	-- Tooltips for tool bypass tickboxes
	-- ============================================================
	local Group = script.Parent
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then
				return p
			end
			p = p.Parent
		end
	end
	
	local ScreenGui = getScreenGui()
	if not ScreenGui then return end
	
	-- ============================================================
	-- FIND TICKBOXES
	-- ============================================================
	local function findDescendant(name)
		for _, v in pairs(ScreenGui:GetDescendants()) do
			if v.Name == name then
				return v
			end
		end
	end
	
	local PaintBypassTickBox = findDescendant("PaintBypassTickBox")
	local DeleteBypassTickBox = findDescendant("DeleteBypassTickBox")
	local BuildBypassTickBox = findDescendant("BuildBypassTickBox")
	local ShapeBypassTickBox = findDescendant("ShapeBypassTickBox")
	local SignBypassTickBox = findDescendant("SignBypassTickBox")
	local ShovelBypassTickBox = findDescendant("ShovelBypassTickBox")
	
	-- ============================================================
	-- TOOLTIP BUILDER
	-- ============================================================
	local function makeTooltip(text)
		local frame = Instance.new("Frame")
		frame.BackgroundColor3 = Color3.fromRGB(16,16,16)
		frame.BorderSizePixel = 0
		frame.ZIndex = 100
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Size = UDim2.new(0, 212, 0, 0)
		frame.Visible = false
		frame.Parent = ScreenGui
	
		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(57,57,57)
		stroke.Parent = frame
	
		local padding = Instance.new("UIPadding")
		padding.PaddingTop = UDim.new(0,6)
		padding.PaddingBottom = UDim.new(0,6)
		padding.PaddingLeft = UDim.new(0,6)
		padding.PaddingRight = UDim.new(0,6)
		padding.Parent = frame
	
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Text = text
		label.TextColor3 = Color3.new(1,1,1)
		label.TextSize = 14
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Top
		label.Font = Enum.Font.GothamSemibold
		label.TextWrapped = true
		label.Size = UDim2.new(0,200,0,0)
		label.AutomaticSize = Enum.AutomaticSize.Y
		label.ZIndex = 101
		label.Parent = frame
	
		return frame
	end
	
	local function hookTooltip(button, tooltipFrame)
		if not button then return end
	
		button.MouseEnter:Connect(function()
			local abs = button.AbsolutePosition
			local size = button.AbsoluteSize
			tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
			tooltipFrame.Visible = true
		end)
	
		button.MouseLeave:Connect(function()
			tooltipFrame.Visible = false
		end)
	end
	
	-- ============================================================
	-- CREATE TOOLTIPS
	-- ============================================================
	local paintTooltip = makeTooltip("Allows you to use Paint without holding it.")
	local deleteTooltip = makeTooltip("Allows you to use Delete without holding it.")
	local buildTooltip = makeTooltip("Allows you to use Build without holding it.")
	local shapeTooltip = makeTooltip("Allows you to use Shape without holding it.")
	local signTooltip = makeTooltip("Allows you to use Sign without holding it.")
	local shovelTooltip = makeTooltip("Allows you to use Shovel without holding it.")
	
	-- ============================================================
	-- HOOK THEM
	-- ============================================================
	hookTooltip(PaintBypassTickBox, paintTooltip)
	hookTooltip(DeleteBypassTickBox, deleteTooltip)
	hookTooltip(BuildBypassTickBox, buildTooltip)
	hookTooltip(ShapeBypassTickBox, shapeTooltip)
	hookTooltip(SignBypassTickBox, signTooltip)
	hookTooltip(ShovelBypassTickBox, shovelTooltip)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			paintTooltip:Destroy()
			deleteTooltip:Destroy()
			buildTooltip:Destroy()
			shapeTooltip:Destroy()
			signTooltip:Destroy()
			shovelTooltip:Destroy()
		end
	end)
end;
task.spawn(C_151);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.PaintBypassRow.PaintBypassScript
local function C_157()
local script = G2L["157"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local PaintBypassRow = script.Parent
	local BypassGroup = PaintBypassRow.Parent
	local BypassAccentColor = BypassGroup:WaitForChild("BypassAccentColor")
	local PaintBypassTickBox = PaintBypassRow:WaitForChild("PaintBypassTickBox")
	local PaintBypassText = PaintBypassRow:WaitForChild("PaintBypassText")
	
	-- Find the Range Bypass value
	local BkitGroup = BypassGroup.Parent:FindFirstChild("BkitGroup")
	local RangeBypassEnabled = BkitGroup and BkitGroup:FindFirstChild("RangeBypassEnabled")
	
	local isEnabled = false
	local mouseConn = nil
	local backpackConn = nil
	local charConn = nil
	local lastClickTime = 0
	local clickCount = 0
	local AUTO_REFRESH_THRESHOLD = 5 -- Refresh after 5 rapid clicks with no painting
	
	local NORMAL_TEXT = "Paint"
	local NOT_FOUND_TEXT = "Tool not found"
	
	local function updateVisual()
		if isEnabled then
			PaintBypassTickBox.BackgroundColor3 = BypassAccentColor.BackgroundColor3
		else
			PaintBypassTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	updateVisual()
	
	local mouse = LocalPlayer:GetMouse()
	
	local function disable()
		if mouseConn then mouseConn:Disconnect(); mouseConn = nil end
		if backpackConn then backpackConn:Disconnect(); backpackConn = nil end
		if charConn then charConn:Disconnect(); charConn = nil end
	
		local paintUI = LocalPlayer.PlayerGui:FindFirstChild("Paint")
		if paintUI then paintUI.Enabled = false end
	
		print("[PaintBypass] OFF")
	end
	
	local function hasPaintTool()
		return LocalPlayer.Backpack:FindFirstChild("Paint")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Paint"))
	end
	
	-- INSTANT re-equip and unequip function (keeps UI visible)
	local function refreshPaintTool()
		local paintTool = hasPaintTool()
		if not paintTool then
			print("[PaintBypass] Paint tool not found, cannot refresh")
			return false
		end
	
		local paintUI = LocalPlayer.PlayerGui:FindFirstChild("Paint")
		if not paintUI then
			print("[PaintBypass] Paint UI not found, cannot refresh")
			return false
		end
	
		-- Store currently equipped tool to re-equip it after
		local previousTool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
	
		-- Move to backpack if it's in character
		if paintTool.Parent == LocalPlayer.Character then
			paintTool.Parent = LocalPlayer.Backpack
			task.wait()
		end
	
		-- Equip paint tool instantly
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
			LocalPlayer.Character.Humanoid:EquipTool(paintTool)
	
			-- Use RenderStepped to unequip on the very next frame (invisible to player)
			game:GetService("RunService").RenderStepped:Wait()
	
			-- Enable UI BEFORE unequipping
			paintUI.Enabled = true
	
			-- Unequip immediately
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
				LocalPlayer.Character.Humanoid:UnequipTools()
			end
	
			-- Wait a tiny bit and force UI to stay enabled
			task.wait(0.05)
			paintUI.Enabled = true
	
			-- Re-equip previous tool if there was one
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
					LocalPlayer.Character.Humanoid:EquipTool(previousTool)
				end
			end
	
			print("[PaintBypass] Tool refreshed (invisible, UI kept)")
			return true
		end
	
		return false
	end
	
	local function enable()
		local paintUI = LocalPlayer.PlayerGui:FindFirstChild("Paint")
		if not paintUI then
			print("[PaintBypass] Paint UI not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		local paintTool = hasPaintTool()
		if not paintTool then
			print("[PaintBypass] Paint tool not found in backpack or character")
			isEnabled = false
			updateVisual()
			return
		end
	
		local paintEvent = paintTool:FindFirstChild("Script") and paintTool.Script:FindFirstChild("Event")
		if not paintEvent then
			print("[PaintBypass] Paint event not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		-- Initial refresh (instant and invisible)
		refreshPaintTool()
	
		if mouseConn then mouseConn:Disconnect() end
		mouseConn = mouse.Button1Down:Connect(function()
			if not isEnabled then return end
			local target = mouse.Target
	
			if target and not paintUI.PickerUI.Visible then
				local currentTime = tick()
	
				-- Track clicks to detect if painting is not working
				if currentTime - lastClickTime < 0.5 then -- Clicks within 0.5 seconds
					clickCount = clickCount + 1
				else
					clickCount = 1
				end
				lastClickTime = currentTime
	
				-- If we've clicked rapidly many times, assume the tool needs refresh
				if clickCount >= AUTO_REFRESH_THRESHOLD then
					print("[PaintBypass] Auto-refresh triggered (tool may have stopped working)")
					refreshPaintTool()
					clickCount = 0 -- Reset counter
				end
	
				-- Check if Range Bypass is enabled
				local useInfiniteRange = RangeBypassEnabled and RangeBypassEnabled.Value
				local inRange = true
				if inRange then
					-- Re-find the paint tool and event in case it changed
					local currentPaintTool = hasPaintTool()
					if currentPaintTool then
						local currentEvent = currentPaintTool:FindFirstChild("Script") and currentPaintTool.Script:FindFirstChild("Event")
						local currentUI = LocalPlayer.PlayerGui:FindFirstChild("Paint")
	
						if currentEvent and currentUI then
							currentEvent:FireServer(
								target,
								mouse.TargetSurface,
								mouse.Hit.Position,
								currentUI.Button.Text,
								currentUI.Color.Value.BackgroundColor3,
								currentUI.Mater.Value.Text,
								currentUI.MaterUI.Spray.Input.Text
							)
						else
							print("[PaintBypass] Event or UI lost, refreshing...")
							refreshPaintTool()
						end
					end
				end
			end
		end)
	
		-- Watch backpack for paint tool removal
		if backpackConn then backpackConn:Disconnect() end
		backpackConn = LocalPlayer.Backpack.ChildRemoved:Connect(function(child)
			if child.Name == "Paint" then
				task.wait(0.2)
				if not hasPaintTool() then
					print("[PaintBypass] Paint tool removed, auto disabled")
					isEnabled = false
					updateVisual()
					disable()
				end
			end
		end)
	
		-- Watch character for paint tool removal
		if charConn then charConn:Disconnect() end
		if LocalPlayer.Character then
			charConn = LocalPlayer.Character.ChildRemoved:Connect(function(child)
				if child.Name == "Paint" then
					task.wait(0.2)
					if not hasPaintTool() then
						print("[PaintBypass] Paint tool removed from character, auto disabled")
						isEnabled = false
						updateVisual()
						disable()
					end
				end
			end)
		end
	
		print("[PaintBypass] ON")
	end
	
	--------------------------------------------------
	-- TOGGLE
	--------------------------------------------------
	PaintBypassTickBox.MouseButton1Down:Connect(function()
		-- Check if tool exists when clicking
		if not hasPaintTool() then
			PaintBypassText.Text = NOT_FOUND_TEXT
		end
	end)
	
	PaintBypassTickBox.MouseButton1Up:Connect(function()
		-- Reset text when releasing
		PaintBypassText.Text = NORMAL_TEXT
	end)
	
	PaintBypassTickBox.MouseButton1Click:Connect(function()
		-- Only toggle if tool exists
		if not hasPaintTool() then
			print("[PaintBypass] Cannot enable - Paint tool not found")
			return
		end
	
		isEnabled = not isEnabled
		updateVisual()
	
		if isEnabled then
			enable()
		else
			disable()
		end
	end)
	
	--------------------------------------------------
	-- CLEANUP
	--------------------------------------------------
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
		end
	end)
end;
task.spawn(C_157);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.DeleteBypassRow.DeleteBypassScript
local function C_15b()
local script = G2L["15b"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local DeleteBypassRow = script.Parent
	local BypassGroup = DeleteBypassRow.Parent
	local BypassAccentColor = BypassGroup:WaitForChild("BypassAccentColor")
	local DeleteBypassTickBox = DeleteBypassRow:WaitForChild("DeleteBypassTickBox")
	local DeleteBypassText = DeleteBypassRow:WaitForChild("DeleteBypassText")
	
	-- Find the Range Bypass value
	local BkitGroup = BypassGroup.Parent:FindFirstChild("BkitGroup")
	local RangeBypassEnabled = BkitGroup and BkitGroup:FindFirstChild("RangeBypassEnabled")
	
	local isEnabled = false
	local mouseConn = nil
	local backpackConn = nil
	local charConn = nil
	local lastClickTime = 0
	local clickCount = 0
	local AUTO_REFRESH_THRESHOLD = 5 -- Refresh after 5 rapid clicks with no deletion
	
	local NORMAL_TEXT = "Delete"
	local NOT_FOUND_TEXT = "Tool not found"
	
	local function updateVisual()
		if isEnabled then
			DeleteBypassTickBox.BackgroundColor3 = BypassAccentColor.BackgroundColor3
		else
			DeleteBypassTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	updateVisual()
	
	local mouse = LocalPlayer:GetMouse()
	
	local function disable()
		if mouseConn then mouseConn:Disconnect(); mouseConn = nil end
		if backpackConn then backpackConn:Disconnect(); backpackConn = nil end
		if charConn then charConn:Disconnect(); charConn = nil end
	
		print("[DeleteBypass] OFF")
	end
	
	local function hasDeleteTool()
		return LocalPlayer.Backpack:FindFirstChild("Delete")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Delete"))
	end
	
	-- INSTANT re-equip and unequip function
	local function refreshDeleteTool()
		local deleteTool = hasDeleteTool()
		if not deleteTool then
			print("[DeleteBypass] Delete tool not found, cannot refresh")
			return false
		end
	
		-- Store currently equipped tool to re-equip it after
		local previousTool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
	
		-- Move to backpack if it's in character
		if deleteTool.Parent == LocalPlayer.Character then
			deleteTool.Parent = LocalPlayer.Backpack
			task.wait()
		end
	
		-- Equip delete tool instantly
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
			LocalPlayer.Character.Humanoid:EquipTool(deleteTool)
	
			-- Use RenderStepped to unequip on the very next frame (invisible to player)
			game:GetService("RunService").RenderStepped:Wait()
	
			-- Unequip immediately
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
				LocalPlayer.Character.Humanoid:UnequipTools()
			end
	
			-- Re-equip previous tool if there was one
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				task.wait()
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
					LocalPlayer.Character.Humanoid:EquipTool(previousTool)
				end
			end
	
			print("[DeleteBypass] Tool refreshed (invisible)")
			return true
		end
	
		return false
	end
	
	local function enable()
		local deleteTool = hasDeleteTool()
		if not deleteTool then
			print("[DeleteBypass] Delete tool not found in backpack or character")
			isEnabled = false
			updateVisual()
			return
		end
	
		local deleteEvent = deleteTool:FindFirstChild("Script") and deleteTool.Script:FindFirstChild("Event")
		if not deleteEvent then
			print("[DeleteBypass] Delete event not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		-- Initial refresh
		refreshDeleteTool()
	
		if mouseConn then mouseConn:Disconnect() end
		mouseConn = mouse.Button1Down:Connect(function()
			if not isEnabled then return end
			local target = mouse.Target
	
			-- Only delete if target is a "Brick"
			if target and target.Name == "Brick" then
				local currentTime = tick()
	
				-- Track clicks to detect if deletion is not working
				if currentTime - lastClickTime < 0.5 then -- Clicks within 0.5 seconds
					clickCount = clickCount + 1
				else
					clickCount = 1
				end
				lastClickTime = currentTime
	
				-- If we've clicked rapidly many times, assume the tool needs refresh
				if clickCount >= AUTO_REFRESH_THRESHOLD then
					print("[DeleteBypass] Auto-refresh triggered (tool may have stopped working)")
					refreshDeleteTool()
					clickCount = 0 -- Reset counter
				end
	
				-- Check if Range Bypass is enabled
				local useInfiniteRange = RangeBypassEnabled and RangeBypassEnabled.Value
				local inRange = true
				if inRange then
					-- Re-find the delete tool and event in case it changed
					local currentDeleteTool = hasDeleteTool()
					if currentDeleteTool then
						local currentEvent = currentDeleteTool:FindFirstChild("Script") and currentDeleteTool.Script:FindFirstChild("Event")
						if currentEvent then
							currentEvent:FireServer(target, mouse.Hit.Position)
						else
							print("[DeleteBypass] Event lost, refreshing...")
							refreshDeleteTool()
						end
					end
				end
			end
		end)
	
		-- Watch backpack for delete tool removal
		if backpackConn then backpackConn:Disconnect() end
		backpackConn = LocalPlayer.Backpack.ChildRemoved:Connect(function(child)
			if child.Name == "Delete" then
				task.wait(0.2)
				if not hasDeleteTool() then
					print("[DeleteBypass] Delete tool removed, auto disabled")
					isEnabled = false
					updateVisual()
					disable()
				end
			end
		end)
	
		-- Watch character for delete tool removal
		if charConn then charConn:Disconnect() end
		if LocalPlayer.Character then
			charConn = LocalPlayer.Character.ChildRemoved:Connect(function(child)
				if child.Name == "Delete" then
					task.wait(0.2)
					if not hasDeleteTool() then
						print("[DeleteBypass] Delete tool removed from character, auto disabled")
						isEnabled = false
						updateVisual()
						disable()
					end
				end
			end)
		end
	
		print("[DeleteBypass] ON")
	end
	
	--------------------------------------------------
	-- TOGGLE
	--------------------------------------------------
	DeleteBypassTickBox.MouseButton1Down:Connect(function()
		-- Check if tool exists when clicking
		if not hasDeleteTool() then
			DeleteBypassText.Text = NOT_FOUND_TEXT
		end
	end)
	
	DeleteBypassTickBox.MouseButton1Up:Connect(function()
		-- Reset text when releasing
		DeleteBypassText.Text = NORMAL_TEXT
	end)
	
	DeleteBypassTickBox.MouseButton1Click:Connect(function()
		-- Only toggle if tool exists
		if not hasDeleteTool() then
			print("[DeleteBypass] Cannot enable - Delete tool not found")
			return
		end
	
		isEnabled = not isEnabled
		updateVisual()
	
		if isEnabled then
			enable()
		else
			disable()
		end
	end)
	
	--------------------------------------------------
	-- CLEANUP
	--------------------------------------------------
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
		end
	end)
end;
task.spawn(C_15b);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShapeBypassRow.ShapeBypassScript
local function C_15f()
local script = G2L["15f"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local ShapeBypassRow = script.Parent
	local BypassGroup = ShapeBypassRow.Parent
	local BypassAccentColor = BypassGroup:WaitForChild("BypassAccentColor")
	local ShapeBypassTickBox = ShapeBypassRow:WaitForChild("ShapeBypassTickBox")
	local ShapeBypassText = ShapeBypassRow:WaitForChild("ShapeBypassText")
	
	-- Find the Range Bypass value
	local BkitGroup = BypassGroup.Parent:FindFirstChild("BkitGroup")
	local RangeBypassEnabled = BkitGroup and BkitGroup:FindFirstChild("RangeBypassEnabled")
	
	local isEnabled = false
	local mouseConn = nil
	local backpackConn = nil
	local charConn = nil
	local lastClickTime = 0
	local clickCount = 0
	local AUTO_REFRESH_THRESHOLD = 5 -- Refresh after 5 rapid clicks with no shaping
	
	local NORMAL_TEXT = "Shape"
	local NOT_FOUND_TEXT = "Tool not found"
	
	local function updateVisual()
		if isEnabled then
			ShapeBypassTickBox.BackgroundColor3 = BypassAccentColor.BackgroundColor3
		else
			ShapeBypassTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	updateVisual()
	
	local mouse = LocalPlayer:GetMouse()
	
	local function disable()
		if mouseConn then mouseConn:Disconnect(); mouseConn = nil end
		if backpackConn then backpackConn:Disconnect(); backpackConn = nil end
		if charConn then charConn:Disconnect(); charConn = nil end
	
		local shapeUI = LocalPlayer.PlayerGui:FindFirstChild("Shape")
		if shapeUI then shapeUI.Enabled = false end
	
		print("[ShapeBypass] OFF")
	end
	
	local function hasShapeTool()
		return LocalPlayer.Backpack:FindFirstChild("Shape")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Shape"))
	end
	
	-- INSTANT re-equip and unequip function (keeps UI visible)
	local function refreshShapeTool()
		local shapeTool = hasShapeTool()
		if not shapeTool then
			print("[ShapeBypass] Shape tool not found, cannot refresh")
			return false
		end
	
		local shapeUI = LocalPlayer.PlayerGui:FindFirstChild("Shape")
		if not shapeUI then
			print("[ShapeBypass] Shape UI not found, cannot refresh")
			return false
		end
	
		-- Store currently equipped tool to re-equip it after
		local previousTool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
	
		-- Move to backpack if it's in character
		if shapeTool.Parent == LocalPlayer.Character then
			shapeTool.Parent = LocalPlayer.Backpack
			task.wait()
		end
	
		-- Equip shape tool instantly
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
			LocalPlayer.Character.Humanoid:EquipTool(shapeTool)
	
			-- Use RenderStepped to unequip on the very next frame (invisible to player)
			game:GetService("RunService").RenderStepped:Wait()
	
			-- Enable UI BEFORE unequipping
			shapeUI.Enabled = true
	
			-- Unequip immediately
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
				LocalPlayer.Character.Humanoid:UnequipTools()
			end
	
			-- Wait a tiny bit and force UI to stay enabled
			task.wait(0.05)
			shapeUI.Enabled = true
	
			-- Re-equip previous tool if there was one
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
					LocalPlayer.Character.Humanoid:EquipTool(previousTool)
				end
			end
	
			print("[ShapeBypass] Tool refreshed (invisible, UI kept)")
			return true
		end
	
		return false
	end
	
	local function enable()
		local shapeUI = LocalPlayer.PlayerGui:FindFirstChild("Shape")
		if not shapeUI then
			print("[ShapeBypass] Shape UI not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		local shapeTool = hasShapeTool()
		if not shapeTool then
			print("[ShapeBypass] Shape tool not found in backpack or character")
			isEnabled = false
			updateVisual()
			return
		end
	
		local shapeEvent = shapeTool:FindFirstChild("Script") and shapeTool.Script:FindFirstChild("Event")
		if not shapeEvent then
			print("[ShapeBypass] Shape event not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		-- Initial refresh (instant and invisible)
		refreshShapeTool()
	
		if mouseConn then mouseConn:Disconnect() end
		mouseConn = mouse.Button1Down:Connect(function()
			if not isEnabled then return end
			local target = mouse.Target
	
			-- Only works on "Brick" objects
			if target and target.Name == "Brick" then
				local currentTime = tick()
	
				-- Track clicks to detect if shaping is not working
				if currentTime - lastClickTime < 0.5 then -- Clicks within 0.5 seconds
					clickCount = clickCount + 1
				else
					clickCount = 1
				end
				lastClickTime = currentTime
	
				-- If we've clicked rapidly many times, assume the tool needs refresh
				if clickCount >= AUTO_REFRESH_THRESHOLD then
					print("[ShapeBypass] Auto-refresh triggered (tool may have stopped working)")
					refreshShapeTool()
					clickCount = 0 -- Reset counter
				end
	
				-- Check if Range Bypass is enabled
				local useInfiniteRange = RangeBypassEnabled and RangeBypassEnabled.Value
				local inRange = true
				if inRange then
					-- Re-find the shape tool and event in case it changed
					local currentShapeTool = hasShapeTool()
					if currentShapeTool then
						local currentEvent = currentShapeTool:FindFirstChild("Script") and currentShapeTool.Script:FindFirstChild("Event")
						local currentUI = LocalPlayer.PlayerGui:FindFirstChild("Shape")
	
						if currentEvent and currentUI then
							currentEvent:FireServer(
								target,
								mouse.TargetSurface,
								mouse.Hit.Position,
								currentUI.Button.Text
							)
						else
							print("[ShapeBypass] Event or UI lost, refreshing...")
							refreshShapeTool()
						end
					end
				end
			end
		end)
	
		-- Watch backpack for shape tool removal
		if backpackConn then backpackConn:Disconnect() end
		backpackConn = LocalPlayer.Backpack.ChildRemoved:Connect(function(child)
			if child.Name == "Shape" then
				task.wait(0.2)
				if not hasShapeTool() then
					print("[ShapeBypass] Shape tool removed, auto disabled")
					isEnabled = false
					updateVisual()
					disable()
				end
			end
		end)
	
		-- Watch character for shape tool removal
		if charConn then charConn:Disconnect() end
		if LocalPlayer.Character then
			charConn = LocalPlayer.Character.ChildRemoved:Connect(function(child)
				if child.Name == "Shape" then
					task.wait(0.2)
					if not hasShapeTool() then
						print("[ShapeBypass] Shape tool removed from character, auto disabled")
						isEnabled = false
						updateVisual()
						disable()
					end
				end
			end)
		end
	
		print("[ShapeBypass] ON")
	end
	
	--------------------------------------------------
	-- TOGGLE
	--------------------------------------------------
	ShapeBypassTickBox.MouseButton1Down:Connect(function()
		-- Check if tool exists when clicking
		if not hasShapeTool() then
			ShapeBypassText.Text = NOT_FOUND_TEXT
		end
	end)
	
	ShapeBypassTickBox.MouseButton1Up:Connect(function()
		-- Reset text when releasing
		ShapeBypassText.Text = NORMAL_TEXT
	end)
	
	ShapeBypassTickBox.MouseButton1Click:Connect(function()
		-- Only toggle if tool exists
		if not hasShapeTool() then
			print("[ShapeBypass] Cannot enable - Shape tool not found")
			return
		end
	
		isEnabled = not isEnabled
		updateVisual()
	
		if isEnabled then
			enable()
		else
			disable()
		end
	end)
	
	--------------------------------------------------
	-- CLEANUP
	--------------------------------------------------
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
		end
	end)
end;
task.spawn(C_15f);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.ShovelBypassRow.ShovelBypassScript
local function C_163()
local script = G2L["163"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local ShovelBypassRow = script.Parent
	local BypassGroup = ShovelBypassRow.Parent
	local BypassAccentColor = BypassGroup:WaitForChild("BypassAccentColor")
	local ShovelBypassTickBox = ShovelBypassRow:WaitForChild("ShovelBypassTickBox")
	local ShovelBypassText = ShovelBypassRow:WaitForChild("ShovelBypassText")
	
	-- Find the Range Bypass value
	local BkitGroup = BypassGroup.Parent:FindFirstChild("BkitGroup")
	local RangeBypassEnabled = BkitGroup and BkitGroup:FindFirstChild("RangeBypassEnabled")
	
	local isEnabled = false
	local mouseConn = nil
	local backpackConn = nil
	local charConn = nil
	local lastClickTime = 0
	local clickCount = 0
	local AUTO_REFRESH_THRESHOLD = 5 -- Refresh after 5 rapid clicks with no digging
	
	local NORMAL_TEXT = "Shovel"
	local NOT_FOUND_TEXT = "Tool not found"
	
	local function updateVisual()
		if isEnabled then
			ShovelBypassTickBox.BackgroundColor3 = BypassAccentColor.BackgroundColor3
		else
			ShovelBypassTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	updateVisual()
	
	local mouse = LocalPlayer:GetMouse()
	
	local function disable()
		if mouseConn then mouseConn:Disconnect(); mouseConn = nil end
		if backpackConn then backpackConn:Disconnect(); backpackConn = nil end
		if charConn then charConn:Disconnect(); charConn = nil end
	
		local shovelUI = LocalPlayer.PlayerGui:FindFirstChild("Shovel")
		if shovelUI then shovelUI.Enabled = false end
	
		print("[ShovelBypass] OFF")
	end
	
	local function hasShovelTool()
		return LocalPlayer.Backpack:FindFirstChild("Shovel")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Shovel"))
	end
	
	-- INSTANT re-equip and unequip function (keeps UI visible)
	local function refreshShovelTool()
		local shovelTool = hasShovelTool()
		if not shovelTool then
			print("[ShovelBypass] Shovel tool not found, cannot refresh")
			return false
		end
	
		local shovelUI = LocalPlayer.PlayerGui:FindFirstChild("Shovel")
		if not shovelUI then
			print("[ShovelBypass] Shovel UI not found, cannot refresh")
			return false
		end
	
		-- Store currently equipped tool to re-equip it after
		local previousTool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
	
		-- Move to backpack if it's in character
		if shovelTool.Parent == LocalPlayer.Character then
			shovelTool.Parent = LocalPlayer.Backpack
			task.wait()
		end
	
		-- Equip shovel tool instantly
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
			LocalPlayer.Character.Humanoid:EquipTool(shovelTool)
	
			-- Use RenderStepped to unequip on the very next frame (invisible to player)
			game:GetService("RunService").RenderStepped:Wait()
	
			-- Enable UI BEFORE unequipping
			shovelUI.Enabled = true
	
			-- Unequip immediately
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
				LocalPlayer.Character.Humanoid:UnequipTools()
			end
	
			-- Wait a tiny bit and force UI to stay enabled
			task.wait(0.05)
			shovelUI.Enabled = true
	
			-- Re-equip previous tool if there was one
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
					LocalPlayer.Character.Humanoid:EquipTool(previousTool)
				end
			end
	
			print("[ShovelBypass] Tool refreshed (invisible, UI kept)")
			return true
		end
	
		return false
	end
	
	local function enable()
		local shovelUI = LocalPlayer.PlayerGui:FindFirstChild("Shovel")
		if not shovelUI then
			print("[ShovelBypass] Shovel UI not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		local shovelTool = hasShovelTool()
		if not shovelTool then
			print("[ShovelBypass] Shovel tool not found in backpack or character")
			isEnabled = false
			updateVisual()
			return
		end
	
		local shovelEvent = shovelTool:FindFirstChild("Script") and shovelTool.Script:FindFirstChild("Event")
		if not shovelEvent then
			print("[ShovelBypass] Shovel event not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		-- Initial refresh (instant and invisible)
		refreshShovelTool()
	
		if mouseConn then mouseConn:Disconnect() end
		mouseConn = mouse.Button1Down:Connect(function()
			if not isEnabled then return end
			local target = mouse.Target
	
			if target then
				local currentTime = tick()
	
				-- Track clicks to detect if digging is not working
				if currentTime - lastClickTime < 0.5 then -- Clicks within 0.5 seconds
					clickCount = clickCount + 1
				else
					clickCount = 1
				end
				lastClickTime = currentTime
	
				-- If we've clicked rapidly many times, assume the tool needs refresh
				if clickCount >= AUTO_REFRESH_THRESHOLD then
					print("[ShovelBypass] Auto-refresh triggered (tool may have stopped working)")
					refreshShovelTool()
					clickCount = 0 -- Reset counter
				end
	
				-- Check if Range Bypass is enabled
				local useInfiniteRange = RangeBypassEnabled and RangeBypassEnabled.Value
				local inRange = true
				if inRange then
					-- Re-find the shovel tool and event in case it changed
					local currentShovelTool = hasShovelTool()
					if currentShovelTool then
						local currentEvent = currentShovelTool:FindFirstChild("Script") and currentShovelTool.Script:FindFirstChild("Event")
						local currentUI = LocalPlayer.PlayerGui:FindFirstChild("Shovel")
	
						if currentEvent and currentUI then
							currentEvent:FireServer(
								target,
								mouse.TargetSurface,
								mouse.Hit.Position,
								currentUI.Button.Text
							)
						else
							print("[ShovelBypass] Event or UI lost, refreshing...")
							refreshShovelTool()
						end
					end
				end
			end
		end)
	
		-- Watch backpack for shovel tool removal
		if backpackConn then backpackConn:Disconnect() end
		backpackConn = LocalPlayer.Backpack.ChildRemoved:Connect(function(child)
			if child.Name == "Shovel" then
				task.wait(0.2)
				if not hasShovelTool() then
					print("[ShovelBypass] Shovel tool removed, auto disabled")
					isEnabled = false
					updateVisual()
					disable()
				end
			end
		end)
	
		-- Watch character for shovel tool removal
		if charConn then charConn:Disconnect() end
		if LocalPlayer.Character then
			charConn = LocalPlayer.Character.ChildRemoved:Connect(function(child)
				if child.Name == "Shovel" then
					task.wait(0.2)
					if not hasShovelTool() then
						print("[ShovelBypass] Shovel tool removed from character, auto disabled")
						isEnabled = false
						updateVisual()
						disable()
					end
				end
			end)
		end
	
		print("[ShovelBypass] ON")
	end
	
	--------------------------------------------------
	-- TOGGLE
	--------------------------------------------------
	ShovelBypassTickBox.MouseButton1Down:Connect(function()
		-- Check if tool exists when clicking
		if not hasShovelTool() then
			ShovelBypassText.Text = NOT_FOUND_TEXT
		end
	end)
	
	ShovelBypassTickBox.MouseButton1Up:Connect(function()
		-- Reset text when releasing
		ShovelBypassText.Text = NORMAL_TEXT
	end)
	
	ShovelBypassTickBox.MouseButton1Click:Connect(function()
		-- Only toggle if tool exists
		if not hasShovelTool() then
			print("[ShovelBypass] Cannot enable - Shovel tool not found")
			return
		end
	
		isEnabled = not isEnabled
		updateVisual()
	
		if isEnabled then
			enable()
		else
			disable()
		end
	end)
	
	--------------------------------------------------
	-- CLEANUP
	--------------------------------------------------
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
		end
	end)
end;
task.spawn(C_163);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.SignBypassRow.SignBypassScript
local function C_167()
local script = G2L["167"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local SignBypassRow = script.Parent
	local BypassGroup = SignBypassRow.Parent
	local BypassAccentColor = BypassGroup:WaitForChild("BypassAccentColor")
	local SignBypassTickBox = SignBypassRow:WaitForChild("SignBypassTickBox")
	local SignBypassText = SignBypassRow:WaitForChild("SignBypassText")
	
	-- Find the Range Bypass value
	local BkitGroup = BypassGroup.Parent:FindFirstChild("BkitGroup")
	local RangeBypassEnabled = BkitGroup and BkitGroup:FindFirstChild("RangeBypassEnabled")
	
	local isEnabled = false
	local mouseConn = nil
	local backpackConn = nil
	local charConn = nil
	local lastClickTime = 0
	local clickCount = 0
	local AUTO_REFRESH_THRESHOLD = 5 -- Refresh after 5 rapid clicks with no sign placement
	
	local NORMAL_TEXT = "Sign"
	local NOT_FOUND_TEXT = "Tool not found"
	
	local function updateVisual()
		if isEnabled then
			SignBypassTickBox.BackgroundColor3 = BypassAccentColor.BackgroundColor3
		else
			SignBypassTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	updateVisual()
	
	local mouse = LocalPlayer:GetMouse()
	
	local function disable()
		if mouseConn then mouseConn:Disconnect(); mouseConn = nil end
		if backpackConn then backpackConn:Disconnect(); backpackConn = nil end
		if charConn then charConn:Disconnect(); charConn = nil end
	
		print("[SignBypass] OFF")
	end
	
	local function hasSignTool()
		return LocalPlayer.Backpack:FindFirstChild("Sign")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Sign"))
	end
	
	-- INSTANT re-equip and unequip function (no UI for Sign tool)
	local function refreshSignTool()
		local signTool = hasSignTool()
		if not signTool then
			print("[SignBypass] Sign tool not found, cannot refresh")
			return false
		end
	
		-- Store currently equipped tool to re-equip it after
		local previousTool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
	
		-- Move to backpack if it's in character
		if signTool.Parent == LocalPlayer.Character then
			signTool.Parent = LocalPlayer.Backpack
			task.wait()
		end
	
		-- Equip sign tool instantly
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
			LocalPlayer.Character.Humanoid:EquipTool(signTool)
	
			-- Use RenderStepped to unequip on the very next frame (invisible to player)
			game:GetService("RunService").RenderStepped:Wait()
	
			-- Unequip immediately
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
				LocalPlayer.Character.Humanoid:UnequipTools()
			end
	
			-- Re-equip previous tool if there was one
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				task.wait()
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
					LocalPlayer.Character.Humanoid:EquipTool(previousTool)
				end
			end
	
			print("[SignBypass] Tool refreshed (invisible)")
			return true
		end
	
		return false
	end
	
	local function enable()
		local signTool = hasSignTool()
		if not signTool then
			print("[SignBypass] Sign tool not found in backpack or character")
			isEnabled = false
			updateVisual()
			return
		end
	
		local signEvent = signTool:FindFirstChild("Script") and signTool.Script:FindFirstChild("Event")
		if not signEvent then
			print("[SignBypass] Sign event not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		-- Initial refresh (instant and invisible)
		refreshSignTool()
	
		if mouseConn then mouseConn:Disconnect() end
		mouseConn = mouse.Button1Down:Connect(function()
			if not isEnabled then return end
			local target = mouse.Target
	
			if target then
				local currentTime = tick()
	
				-- Track clicks to detect if sign placement is not working
				if currentTime - lastClickTime < 0.5 then -- Clicks within 0.5 seconds
					clickCount = clickCount + 1
				else
					clickCount = 1
				end
				lastClickTime = currentTime
	
				-- If we've clicked rapidly many times, assume the tool needs refresh
				if clickCount >= AUTO_REFRESH_THRESHOLD then
					print("[SignBypass] Auto-refresh triggered (tool may have stopped working)")
					refreshSignTool()
					clickCount = 0 -- Reset counter
				end
	
				-- Check if Range Bypass is enabled
				local useInfiniteRange = RangeBypassEnabled and RangeBypassEnabled.Value
				local inRange = true
				if inRange then
					-- Re-find the sign tool and event in case it changed
					local currentSignTool = hasSignTool()
					if currentSignTool then
						local currentEvent = currentSignTool:FindFirstChild("Script") and currentSignTool.Script:FindFirstChild("Event")
	
						if currentEvent then
							currentEvent:FireServer(
								target,
								mouse.TargetSurface,
								mouse.Hit.Position
							)
						else
							print("[SignBypass] Event lost, refreshing...")
							refreshSignTool()
						end
					end
				end
			end
		end)
	
		-- Watch backpack for sign tool removal
		if backpackConn then backpackConn:Disconnect() end
		backpackConn = LocalPlayer.Backpack.ChildRemoved:Connect(function(child)
			if child.Name == "Sign" then
				task.wait(0.2)
				if not hasSignTool() then
					print("[SignBypass] Sign tool removed, auto disabled")
					isEnabled = false
					updateVisual()
					disable()
				end
			end
		end)
	
		-- Watch character for sign tool removal
		if charConn then charConn:Disconnect() end
		if LocalPlayer.Character then
			charConn = LocalPlayer.Character.ChildRemoved:Connect(function(child)
				if child.Name == "Sign" then
					task.wait(0.2)
					if not hasSignTool() then
						print("[SignBypass] Sign tool removed from character, auto disabled")
						isEnabled = false
						updateVisual()
						disable()
					end
				end
			end)
		end
	
		print("[SignBypass] ON")
	end
	
	--------------------------------------------------
	-- TOGGLE
	--------------------------------------------------
	SignBypassTickBox.MouseButton1Down:Connect(function()
		-- Check if tool exists when clicking
		if not hasSignTool() then
			SignBypassText.Text = NOT_FOUND_TEXT
		end
	end)
	
	SignBypassTickBox.MouseButton1Up:Connect(function()
		-- Reset text when releasing
		SignBypassText.Text = NORMAL_TEXT
	end)
	
	SignBypassTickBox.MouseButton1Click:Connect(function()
		-- Only toggle if tool exists
		if not hasSignTool() then
			print("[SignBypass] Cannot enable - Sign tool not found")
			return
		end
	
		isEnabled = not isEnabled
		updateVisual()
	
		if isEnabled then
			enable()
		else
			disable()
		end
	end)
	
	--------------------------------------------------
	-- CLEANUP
	--------------------------------------------------
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
		end
	end)
end;
task.spawn(C_167);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.BypassGroup.BuildBypassRow.BuildBypassScript
local function C_16b()
local script = G2L["16b"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local BuildBypassRow = script.Parent
	local BypassGroup = BuildBypassRow.Parent
	local BypassAccentColor = BypassGroup:WaitForChild("BypassAccentColor")
	local BuildBypassTickBox = BuildBypassRow:WaitForChild("BuildBypassTickBox")
	local BuildBypassText = BuildBypassRow:WaitForChild("BuildBypassText")
	
	-- Find the Range Bypass value
	local BkitGroup = BypassGroup.Parent:FindFirstChild("BkitGroup")
	local RangeBypassEnabled = BkitGroup and BkitGroup:FindFirstChild("RangeBypassEnabled")
	
	local isEnabled = false
	local mouseConn = nil
	local backpackConn = nil
	local charConn = nil
	local lastClickTime = 0
	local clickCount = 0
	local AUTO_REFRESH_THRESHOLD = 5 -- Refresh after 5 rapid clicks with no building
	
	local NORMAL_TEXT = "Build"
	local NOT_FOUND_TEXT = "Tool not found"
	
	local function updateVisual()
		if isEnabled then
			BuildBypassTickBox.BackgroundColor3 = BypassAccentColor.BackgroundColor3
		else
			BuildBypassTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	updateVisual()
	
	local mouse = LocalPlayer:GetMouse()
	
	local function disable()
		if mouseConn then mouseConn:Disconnect(); mouseConn = nil end
		if backpackConn then backpackConn:Disconnect(); backpackConn = nil end
		if charConn then charConn:Disconnect(); charConn = nil end
	
		local buildUI = LocalPlayer.PlayerGui:FindFirstChild("Build")
		if buildUI then buildUI.Enabled = false end
	
		print("[BuildBypass] OFF")
	end
	
	local function hasBuildTool()
		return LocalPlayer.Backpack:FindFirstChild("Build")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Build"))
	end
	
	-- INSTANT re-equip and unequip function (keeps UI visible)
	local function refreshBuildTool()
		local buildTool = hasBuildTool()
		if not buildTool then
			print("[BuildBypass] Build tool not found, cannot refresh")
			return false
		end
	
		local buildUI = LocalPlayer.PlayerGui:FindFirstChild("Build")
		if not buildUI then
			print("[BuildBypass] Build UI not found, cannot refresh")
			return false
		end
	
		-- Store currently equipped tool to re-equip it after
		local previousTool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
	
		-- Move to backpack if it's in character
		if buildTool.Parent == LocalPlayer.Character then
			buildTool.Parent = LocalPlayer.Backpack
			task.wait()
		end
	
		-- Equip build tool instantly
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
			LocalPlayer.Character.Humanoid:EquipTool(buildTool)
	
			-- Use RenderStepped to unequip on the very next frame (invisible to player)
			game:GetService("RunService").RenderStepped:Wait()
	
			-- Enable UI BEFORE unequipping
			buildUI.Enabled = true
	
			-- Unequip immediately
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
				LocalPlayer.Character.Humanoid:UnequipTools()
			end
	
			-- Wait a tiny bit and force UI to stay enabled
			task.wait(0.05)
			buildUI.Enabled = true
	
			-- Re-equip previous tool if there was one
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
					LocalPlayer.Character.Humanoid:EquipTool(previousTool)
				end
			end
	
			print("[BuildBypass] Tool refreshed (invisible, UI kept)")
			return true
		end
	
		return false
	end
	
	local function enable()
		local buildUI = LocalPlayer.PlayerGui:FindFirstChild("Build")
		if not buildUI then
			print("[BuildBypass] Build UI not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		local buildTool = hasBuildTool()
		if not buildTool then
			print("[BuildBypass] Build tool not found in backpack or character")
			isEnabled = false
			updateVisual()
			return
		end
	
		local buildEvent = buildTool:FindFirstChild("Script") and buildTool.Script:FindFirstChild("Event")
		if not buildEvent then
			print("[BuildBypass] Build event not found")
			isEnabled = false
			updateVisual()
			return
		end
	
		-- Initial refresh (instant and invisible)
		refreshBuildTool()
	
		if mouseConn then mouseConn:Disconnect() end
		mouseConn = mouse.Button1Down:Connect(function()
			if not isEnabled then return end
			local target = mouse.Target
	
			if target then
				local currentTime = tick()
	
				-- Track clicks to detect if building is not working
				if currentTime - lastClickTime < 0.5 then -- Clicks within 0.5 seconds
					clickCount = clickCount + 1
				else
					clickCount = 1
				end
				lastClickTime = currentTime
	
				-- If we've clicked rapidly many times, assume the tool needs refresh
				if clickCount >= AUTO_REFRESH_THRESHOLD then
					print("[BuildBypass] Auto-refresh triggered (tool may have stopped working)")
					refreshBuildTool()
					clickCount = 0 -- Reset counter
				end
	
				-- Check if Range Bypass is enabled
				local useInfiniteRange = RangeBypassEnabled and RangeBypassEnabled.Value
				local inRange = true
				if inRange then
					-- Re-find the build tool and event in case it changed
					local currentBuildTool = hasBuildTool()
					if currentBuildTool then
						local currentEvent = currentBuildTool:FindFirstChild("Script") and currentBuildTool.Script:FindFirstChild("Event")
						local currentUI = LocalPlayer.PlayerGui:FindFirstChild("Build")
	
						if currentEvent and currentUI then
							currentEvent:FireServer(
								target,
								mouse.TargetSurface,
								mouse.Hit.Position,
								currentUI.Button.Text
							)
						else
							print("[BuildBypass] Event or UI lost, refreshing...")
							refreshBuildTool()
						end
					end
				end
			end
		end)
	
		-- Watch backpack for build tool removal
		if backpackConn then backpackConn:Disconnect() end
		backpackConn = LocalPlayer.Backpack.ChildRemoved:Connect(function(child)
			if child.Name == "Build" then
				task.wait(0.2)
				if not hasBuildTool() then
					print("[BuildBypass] Build tool removed, auto disabled")
					isEnabled = false
					updateVisual()
					disable()
				end
			end
		end)
	
		-- Watch character for build tool removal
		if charConn then charConn:Disconnect() end
		if LocalPlayer.Character then
			charConn = LocalPlayer.Character.ChildRemoved:Connect(function(child)
				if child.Name == "Build" then
					task.wait(0.2)
					if not hasBuildTool() then
						print("[BuildBypass] Build tool removed from character, auto disabled")
						isEnabled = false
						updateVisual()
						disable()
					end
				end
			end)
		end
	
		print("[BuildBypass] ON")
	end
	
	--------------------------------------------------
	-- TOGGLE
	--------------------------------------------------
	BuildBypassTickBox.MouseButton1Down:Connect(function()
		-- Check if tool exists when clicking
		if not hasBuildTool() then
			BuildBypassText.Text = NOT_FOUND_TEXT
		end
	end)
	
	BuildBypassTickBox.MouseButton1Up:Connect(function()
		-- Reset text when releasing
		BuildBypassText.Text = NORMAL_TEXT
	end)
	
	BuildBypassTickBox.MouseButton1Click:Connect(function()
		-- Only toggle if tool exists
		if not hasBuildTool() then
			print("[BuildBypass] Cannot enable - Build tool not found")
			return
		end
	
		isEnabled = not isEnabled
		updateVisual()
	
		if isEnabled then
			enable()
		else
			disable()
		end
	end)
	
	--------------------------------------------------
	-- CLEANUP
	--------------------------------------------------
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			disable()
		end
	end)
end;
task.spawn(C_16b);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.LagScript
local function C_16f()
local script = G2L["16f"];
	-- BypassScript (inside BypassGroup)
	local BypassGroup = script.Parent
	local BypassMinimize = BypassGroup.BypassTitle.BypassMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(BypassGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = BypassGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	BypassMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			BypassGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			BypassMinimize.Text = "+"
		else
			BypassGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			BypassMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_16f);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.LagToolTip
local function C_170()
local script = G2L["170"];
	-- ============================================================
	-- BKIT GROUP TOOLTIP SCRIPT
	-- Tooltips for BkitGroup features
	-- ============================================================
	local BkitGroup = script.Parent
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then
				return p
			end
			p = p.Parent
		end
	end
	
	local ScreenGui = getScreenGui()
	if not ScreenGui then return end
	
	-- ============================================================
	-- FIND ELEMENTS IN BKITGROUP
	-- ============================================================
	local function findDescendant(name)
		for _, v in pairs(BkitGroup:GetDescendants()) do
			if v.Name == name then
				return v
			end
		end
	end
	
	local BlockSpamLabelHelp = findDescendant("BlockSpamLabelHelp")
	local FollowBlockTickBox = findDescendant("FollowBlockTickBox")
	
	-- ============================================================
	-- TOOLTIP BUILDER
	-- ============================================================
	local function makeTooltip(text)
		local frame = Instance.new("Frame")
		frame.BackgroundColor3 = Color3.fromRGB(16,16,16)
		frame.BorderSizePixel = 0
		frame.ZIndex = 100
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Size = UDim2.new(0, 212, 0, 0)
		frame.Visible = false
		frame.Parent = ScreenGui
	
		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(57,57,57)
		stroke.Parent = frame
	
		local padding = Instance.new("UIPadding")
		padding.PaddingTop = UDim.new(0,6)
		padding.PaddingBottom = UDim.new(0,6)
		padding.PaddingLeft = UDim.new(0,6)
		padding.PaddingRight = UDim.new(0,6)
		padding.Parent = frame
	
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Text = text
		label.TextColor3 = Color3.new(1,1,1)
		label.TextSize = 14
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Top
		label.Font = Enum.Font.GothamSemibold
		label.TextWrapped = true
		label.Size = UDim2.new(0,200,0,0)
		label.AutomaticSize = Enum.AutomaticSize.Y
		label.ZIndex = 101
		label.Parent = frame
	
		return frame
	end
	
	local function hookTooltip(button, tooltipFrame)
		if not button then return end
	
		button.MouseEnter:Connect(function()
			local abs = button.AbsolutePosition
			local size = button.AbsoluteSize
			tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
			tooltipFrame.Visible = true
		end)
	
		button.MouseLeave:Connect(function()
			tooltipFrame.Visible = false
		end)
	end
	
	-- ============================================================
	-- CREATE TOOLTIPS
	-- ============================================================
	local blockSpamHelpTooltip = makeTooltip("Select an unanchored block. To start spam building, press Start Spam. Used for lag machines and more. Block spam only works when you're near the selected block. Use Follow Block to stay attached to the selected block.")
	local followBlockTooltip = makeTooltip("Attaches you to the selected unanchored block so you move with it. Disables block collision.")
	
	-- ============================================================
	-- HOOK THEM
	-- ============================================================
	hookTooltip(BlockSpamLabelHelp, blockSpamHelpTooltip)
	hookTooltip(FollowBlockTickBox, followBlockTooltip)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			blockSpamHelpTooltip:Destroy()
			followBlockTooltip:Destroy()
		end
	end)
end;
task.spawn(C_170);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SpamSelectRow.SpamSelectScript
local function C_176()
local script = G2L["176"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local RunService = game:GetService("RunService")
	
	local SpamSelectRow = script.Parent
	local LagGroup = SpamSelectRow.Parent
	local LagAccentColor = LagGroup:WaitForChild("LagAccentColor")
	local SpamSelectButton = SpamSelectRow:WaitForChild("SpamSelectButton")
	
	-- Shared state for other scripts to access
	local SelectedBlockValue = LagGroup:FindFirstChild("SelectedBlock") or Instance.new("ObjectValue")
	SelectedBlockValue.Name = "SelectedBlock"
	SelectedBlockValue.Parent = LagGroup
	SelectedBlockValue.Value = nil
	
	-- States
	local isSelectingBlock = false
	local currentHighlight = nil
	local mouseConn = nil
	
	-- Text states
	local TEXT_DEFAULT = "Select Block"
	local TEXT_SELECTING = "Select a block..."
	local TEXT_SELECTED = "Block Selected"
	
	-- Initialize button text
	SpamSelectButton.Text = TEXT_DEFAULT
	
	-- ============================================================
	-- CLEANUP HIGHLIGHT
	-- ============================================================
	local function removeHighlight()
		if currentHighlight then
			currentHighlight:Destroy()
			currentHighlight = nil
		end
	end
	
	-- ============================================================
	-- CREATE HIGHLIGHT ON SELECTED BLOCK
	-- ============================================================
	local function highlightBlock(block)
		removeHighlight()
	
		if not block or not block:IsA("BasePart") then return end
	
		-- Create highlight
		local highlight = Instance.new("Highlight")
		highlight.Adornee = block
		highlight.FillColor = Color3.fromRGB(255, 255, 0) -- Yellow
		highlight.OutlineColor = Color3.fromRGB(255, 200, 0)
		highlight.FillTransparency = 0.5
		highlight.OutlineTransparency = 0
		highlight.Parent = block
	
		currentHighlight = highlight
	end
	
	-- ============================================================
	-- SELECT BLOCK
	-- ============================================================
	local function selectBlock(block)
		if not block or not block:IsA("BasePart") or block.Name ~= "Brick" then
			return false
		end
	
		-- Check if it's in Workspace.Bricks.[Username]
		local parent = block.Parent
		if not parent or parent.Parent ~= workspace:FindFirstChild("Bricks") then
			return false
		end
	
		-- Set the selected block
		SelectedBlockValue.Value = block
		highlightBlock(block)
	
		-- Stop selecting mode
		isSelectingBlock = false
		SpamSelectButton.Text = TEXT_SELECTED
	
		print("[SelectBlock] Block selected:", block:GetFullName())
		return true
	end
	
	-- ============================================================
	-- UNSELECT BLOCK
	-- ============================================================
	local function unselectBlock()
		SelectedBlockValue.Value = nil
		removeHighlight()
		isSelectingBlock = false
		SpamSelectButton.Text = TEXT_DEFAULT
	
		print("[SelectBlock] Block unselected")
	end
	
	-- ============================================================
	-- START SELECTING MODE
	-- ============================================================
	local function startSelecting()
		isSelectingBlock = true
		SpamSelectButton.Text = TEXT_SELECTING
	
		-- Disconnect any existing mouse connection
		if mouseConn then
			mouseConn:Disconnect()
			mouseConn = nil
		end
	
		-- Set up mouse click detection
		local mouse = LocalPlayer:GetMouse()
		mouseConn = mouse.Button1Down:Connect(function()
			if not isSelectingBlock then return end
	
			local target = mouse.Target
			if target then
				-- If clicking the currently selected block, unselect it
				if SelectedBlockValue.Value == target then
					unselectBlock()
				else
					-- Try to select the new block
					if selectBlock(target) then
						-- Disconnect after successful selection
						if mouseConn then
							mouseConn:Disconnect()
							mouseConn = nil
						end
					end
				end
			end
		end)
	
		print("[SelectBlock] Selecting mode started - click a brick to select")
	end
	
	-- ============================================================
	-- STOP SELECTING MODE (CANCEL)
	-- ============================================================
	local function stopSelecting()
		isSelectingBlock = false
	
		if mouseConn then
			mouseConn:Disconnect()
			mouseConn = nil
		end
	
		-- Update text based on whether a block is selected
		if SelectedBlockValue.Value then
			SpamSelectButton.Text = TEXT_SELECTED
		else
			SpamSelectButton.Text = TEXT_DEFAULT
		end
	
		print("[SelectBlock] Selecting mode cancelled")
	end
	
	-- ============================================================
	-- BUTTON CLICK HANDLER
	-- ============================================================
	SpamSelectButton.MouseButton1Click:Connect(function()
		-- State machine based on current state
		if isSelectingBlock then
			-- Currently selecting -> Cancel selection mode
			stopSelecting()
		elseif SelectedBlockValue.Value then
			-- Block is selected -> Start selecting again (allows re-selection)
			startSelecting()
		else
			-- No block selected, not selecting -> Start selecting
			startSelecting()
		end
	end)
	
	-- ============================================================
	-- WATCH FOR SELECTED BLOCK DELETION
	-- ============================================================
	SelectedBlockValue:GetPropertyChangedSignal("Value"):Connect(function()
		local selectedBlock = SelectedBlockValue.Value
	
		if selectedBlock then
			-- Watch for block deletion
			local ancestryConn
			ancestryConn = selectedBlock.AncestryChanged:Connect(function()
				if not selectedBlock:IsDescendantOf(workspace) then
					-- Block was deleted
					unselectBlock()
					if ancestryConn then
						ancestryConn:Disconnect()
					end
				end
			end)
		end
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if mouseConn then
				mouseConn:Disconnect()
			end
			removeHighlight()
			print("[SelectBlock] Cleaned up")
		end
	end)
end;
task.spawn(C_176);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSpamRow.BlockSpamScript
local function C_17a()
local script = G2L["17a"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local RunService = game:GetService("RunService")
	
	local BlockSpamRow = script.Parent
	local LagGroup = BlockSpamRow.Parent
	local LagAccentColor = LagGroup:WaitForChild("LagAccentColor")
	local BlockSpamButton = BlockSpamRow:WaitForChild("BlockSpamButton")
	
	-- Get the selected block value
	local SelectedBlockValue = LagGroup:WaitForChild("SelectedBlock")
	
	-- State
	local isProcessing = false
	local isSpamming = false
	local spamLoop = nil
	local currentWeld = nil
	local weldedBlock = nil
	local positionBeforeStop = nil
	
	-- Text states
	local TEXT_START = "Start Spam"
	local TEXT_STOP = "Stop Spam"
	local TEXT_NO_BLOCK = "No Block Selected"
	local TEXT_PROCESSING = "Processing..."
	
	-- Target properties
	local TARGET_COLOR = Color3.new(0.01, 0.01, 0.005) -- Really black
	local TARGET_MATERIAL = Enum.Material.Neon
	
	-- ============================================================
	-- TOOL HELPERS
	-- ============================================================
	local function getShapeTool()
		return LocalPlayer.Backpack:FindFirstChild("Shape")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Shape"))
	end
	
	local function getShapeEvent()
		local tool = getShapeTool()
		if tool then
			return tool:FindFirstChild("Script") and tool.Script:FindFirstChild("Event")
		end
		return nil
	end
	
	local function getBuildTool()
		return LocalPlayer.Backpack:FindFirstChild("Build")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Build"))
	end
	
	local function getBuildEvent()
		local tool = getBuildTool()
		if tool then
			return tool:FindFirstChild("Script") and tool.Script:FindFirstChild("Event")
		end
		return nil
	end
	
	local function getBuildUI()
		return LocalPlayer.PlayerGui:FindFirstChild("Build")
	end
	
	local function getPaintTool()
		return LocalPlayer.Backpack:FindFirstChild("Paint")
			or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Paint"))
	end
	
	local function getPaintEvent()
		local tool = getPaintTool()
		if tool then
			return tool:FindFirstChild("Script") and tool.Script:FindFirstChild("Event")
		end
		return nil
	end
	
	local function getPaintUI()
		return LocalPlayer.PlayerGui:FindFirstChild("Paint")
	end
	
	local function refreshBuildTool()
		local tool = getBuildTool()
		if not tool then return false end
	
		local buildUI = getBuildUI()
		if not buildUI then return false end
	
		local previousTool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
	
		if tool.Parent == LocalPlayer.Character then
			tool.Parent = LocalPlayer.Backpack
			task.wait()
		end
	
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
			LocalPlayer.Character.Humanoid:EquipTool(tool)
			RunService.RenderStepped:Wait()
			buildUI.Enabled = true
	
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
				LocalPlayer.Character.Humanoid:UnequipTools()
			end
	
			task.wait(0.05)
			buildUI.Enabled = true
	
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
					LocalPlayer.Character.Humanoid:EquipTool(previousTool)
				end
			end
	
			return true
		end
	
		return false
	end
	
	-- ============================================================
	-- VALIDATE & RESIZE
	-- ============================================================
	local function isValidBlockSize(block)
		local size = block.Size
		if size.X % 1 ~= 0 or size.Y % 1 ~= 0 or size.Z % 1 ~= 0 then
			return false, "Block has decimal size: " .. tostring(size)
		end
		return true, nil
	end
	
	local function resizeBlock(block, targetSize)
		local shapeTool = getShapeTool()
		local shapeEvent = getShapeEvent()
	
		if not shapeTool or not shapeEvent then
			print("[BlockSpam] Shape tool not found")
			return false
		end
	
		if block.Size.X == targetSize and block.Size.Y == targetSize and block.Size.Z == targetSize then
			print("[BlockSpam] Block already target size:", targetSize)
			return true
		end
	
		BlockSpamButton.Text = "Resizing..."
	
		if shapeTool.Parent == LocalPlayer.Backpack then
			LocalPlayer.Character.Humanoid:EquipTool(shapeTool)
			task.wait(0.2)
		end
	
		local maxIterations = 50
		local iterations = 0
	
		while iterations < maxIterations do
			local currentSize = block.Size
	
			if currentSize.X == targetSize and currentSize.Y == targetSize and currentSize.Z == targetSize then
				print("[BlockSpam] All dimensions match target:", targetSize)
				break
			end
	
			iterations = iterations + 1
	
			local pos = block.Position
			local size = block.Size
	
			if currentSize.X ~= targetSize then
				pcall(function()
					local rightPos = Vector3.new(pos.X + size.X/2, pos.Y, pos.Z)
					shapeEvent:FireServer(block, Enum.NormalId.Right, rightPos, currentSize.X < targetSize and "increase" or "decrease")
				end)
				task.wait(0.05)
			end
	
			if currentSize.Y ~= targetSize then
				pcall(function()
					local topPos = Vector3.new(pos.X, pos.Y + size.Y/2, pos.Z)
					shapeEvent:FireServer(block, Enum.NormalId.Top, topPos, currentSize.Y < targetSize and "increase" or "decrease")
				end)
				task.wait(0.05)
			end
	
			if currentSize.Z ~= targetSize then
				pcall(function()
					local backPos = Vector3.new(pos.X, pos.Y, pos.Z - size.Z/2)
					shapeEvent:FireServer(block, Enum.NormalId.Back, backPos, currentSize.Z < targetSize and "increase" or "decrease")
				end)
				task.wait(0.05)
			end
	
			task.wait(0.2)
		end
	
		LocalPlayer.Character.Humanoid:UnequipTools()
		task.wait(0.4)
	
		local finalSize = block.Size
		return finalSize.X == targetSize and finalSize.Y == targetSize and finalSize.Z == targetSize
	end
	
	-- ============================================================
	-- SPAM PAINT UNTIL
	-- ============================================================
	local function spamPaintUntil(block, checkFunc, buttonText, materialText, maxTime)
		local paintEvent = getPaintEvent()
		local paintUI = getPaintUI()
	
		if not paintEvent or not paintUI then
			return false
		end
	
		local startTime = tick()
	
		while tick() - startTime < maxTime do
			if checkFunc(block) then
				return true
			end
	
			pcall(function()
				paintEvent:FireServer(block, Enum.NormalId.Top, block.Position, buttonText, TARGET_COLOR, materialText, "")
			end)
	
			task.wait(0.05)
		end
	
		return false
	end
	
	-- ============================================================
	-- CLEANUP & SPAM CONTROL (DEFINE BEFORE processBlock)
	-- ============================================================
	local function cleanupWeld()
		if weldedBlock and weldedBlock:IsDescendantOf(workspace) then
			weldedBlock.CanCollide = true
		end
	
		if currentWeld then
			currentWeld:Destroy()
			currentWeld = nil
		end
	
		weldedBlock = nil
	end
	
	local function stopSpam()
		isSpamming = false
		isProcessing = false
		BlockSpamButton.Text = TEXT_START
	
		if spamLoop then
			task.cancel(spamLoop)
			spamLoop = nil
		end
	
		local savePos = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character.HumanoidRootPart.CFrame
	
		cleanupWeld()
	
		if savePos and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
			task.wait(0.1)
			LocalPlayer.Character.HumanoidRootPart.CFrame = savePos
		end
	
		print("[BlockSpam] Spam stopped")
	end
	
	-- ============================================================
	-- START SPAM BUILDING (DEFINE BEFORE processBlock CALLS IT!)
	-- ============================================================
	local function startSpamBuilding(block)
		if not getBuildTool() then
			BlockSpamButton.Text = "Build tool not found"
			task.wait(1)
			BlockSpamButton.Text = TEXT_START
			return false
		end
	
		if not getBuildUI() then
			BlockSpamButton.Text = "Build UI not found"
			task.wait(1)
			BlockSpamButton.Text = TEXT_START
			return false
		end
	
		if not refreshBuildTool() then
			BlockSpamButton.Text = "Failed to activate"
			task.wait(1)
			BlockSpamButton.Text = TEXT_START
			return false
		end
	
		isSpamming = true
		BlockSpamButton.Text = TEXT_STOP
	
		print("[BlockSpam] Starting spam on block")
	
		spamLoop = task.spawn(function()
			local refreshCounter = 0
	
			while isSpamming do
				-- Check if Build tool still exists
				if not getBuildTool() then
					print("[BlockSpam] Build tool disappeared, stopping")
					stopSpam()
					break
				end
	
				if not block or not block:IsDescendantOf(workspace) or not block:FindFirstChild("Drag") then
					print("[BlockSpam] Block invalid, stopping")
					stopSpam()
					break
				end
	
				local currentEvent = getBuildEvent()
				local currentUI = getBuildUI()
	
				if currentEvent and currentUI then
					pcall(function()
						currentEvent:FireServer(block, Enum.NormalId.Top, block.Position, currentUI.Button.Text)
					end)
				else
					refreshBuildTool()
				end
	
				refreshCounter = refreshCounter + 1
				if refreshCounter >= 50 then
					refreshBuildTool()
					refreshCounter = 0
				end
	
				task.wait(0.05)
			end
		end)
	
		return true
	end
	
	-- ============================================================
	-- PROCESS BLOCK (NOW startSpamBuilding EXISTS!)
	-- ============================================================
	local function processBlock(block)
		if not block or not block:IsDescendantOf(workspace) then
			return false
		end
	
		local isValid, errorMsg = isValidBlockSize(block)
		if not isValid then
			BlockSpamButton.Text = "Invalid Block!"
			task.wait(2)
			BlockSpamButton.Text = TEXT_START
			return false
		end
	
		local targetSize = LagGroup:FindFirstChild("TargetBlockSize")
		targetSize = targetSize and targetSize.Value or 1
	
		if not getPaintTool() or not getShapeTool() or not getPaintUI() then
			BlockSpamButton.Text = "Tools not found"
			task.wait(1)
			BlockSpamButton.Text = TEXT_START
			return false
		end
	
		local previousTool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
		local originalPosition = LocalPlayer.Character.HumanoidRootPart.CFrame
	
		-- Position 20 studs UNDER the block center
		local underBlockPos = block.Position - Vector3.new(0, 20, 0)
		LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(underBlockPos)
	
		-- STOP FALLING - zero velocity immediately
		LocalPlayer.Character.HumanoidRootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
	
		local paintTool = getPaintTool()
		if paintTool.Parent == LocalPlayer.Backpack then
			LocalPlayer.Character.Humanoid:EquipTool(paintTool)
		end
	
		task.wait(0.2)
	
		-- Paint
		BlockSpamButton.Text = "Painting..."
		local paintSuccess = spamPaintUntil(
			block,
			function(b)
				return math.abs(b.Color.R - TARGET_COLOR.R) < 0.02
					and math.abs(b.Color.G - TARGET_COLOR.G) < 0.02
					and math.abs(b.Color.B - TARGET_COLOR.B) < 0.02
					and b.Material == TARGET_MATERIAL
			end,
			"both 🤝",
			"toxic",
			5
		)
	
		if not paintSuccess then
			LocalPlayer.Character.Humanoid:UnequipTools()
			if previousTool and previousTool.Parent == LocalPlayer.Backpack then
				LocalPlayer.Character.Humanoid:EquipTool(previousTool)
			end
			LocalPlayer.Character.HumanoidRootPart.CFrame = originalPosition
			return false
		end
	
		-- Unanchor
		if not block:FindFirstChild("Drag") then
			BlockSpamButton.Text = "Unanchoring..."
			local paintEvent = getPaintEvent()
			if paintEvent then
				pcall(function()
					paintEvent:FireServer(block, Enum.NormalId.Top, block.Position, "material", TARGET_COLOR, "anchor", "")
				end)
	
				local startTime = tick()
				while tick() - startTime < 3 do
					if block:FindFirstChild("Drag") then
						break
					end
					task.wait(0.1)
				end
			end
		end
	
		LocalPlayer.Character.Humanoid:UnequipTools()
		task.wait(0.1)
	
		-- Resize (keep player 20 studs under block during resize)
		if block.Size.X ~= targetSize or block.Size.Y ~= targetSize or block.Size.Z ~= targetSize then
			BlockSpamButton.Text = "Resizing..."
	
			-- Start tracking block position
			local trackingLoop = RunService.RenderStepped:Connect(function()
				if block and block:IsDescendantOf(workspace) then
					local underPos = block.Position - Vector3.new(0, 20, 0)
					LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(underPos)
				end
			end)
	
			resizeBlock(block, targetSize)
	
			-- Stop tracking
			trackingLoop:Disconnect()
		end
	
		-- Weld
		BlockSpamButton.Text = "Welding..."
	
		-- FORCE UNANCHOR - spam until Drag appears
		if not block:FindFirstChild("Drag") then
			print("[BlockSpam] Block still anchored before weld, forcing unanchor...")
			local paintEvent = getPaintEvent()
			if paintEvent then
				local startTime = tick()
				local timeout = 5
	
				while tick() - startTime < timeout do
					if block:FindFirstChild("Drag") then
						print("[BlockSpam] Block unanchored successfully!")
						break
					end
	
					-- Spam fire unanchor event
					pcall(function()
						paintEvent:FireServer(block, Enum.NormalId.Top, block.Position, "material", TARGET_COLOR, "anchor", "")
					end)
	
					task.wait(0.05)
				end
	
				if not block:FindFirstChild("Drag") then
					print("[BlockSpam] Failed to unanchor block for weld")
					LocalPlayer.Character.Humanoid:UnequipTools()
					if previousTool and previousTool.Parent == LocalPlayer.Backpack then
						LocalPlayer.Character.Humanoid:EquipTool(previousTool)
					end
					LocalPlayer.Character.HumanoidRootPart.CFrame = originalPosition
					return false
				end
			end
		end
	
		block.CanCollide = false
	
		-- Position 8 studs under block center (closer for spam reach)
		local finalUnderPos = block.Position - Vector3.new(0, 8, 0)
		LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(finalUnderPos)
	
		-- STOP FALLING again after final TP
		LocalPlayer.Character.HumanoidRootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
	
		task.wait(0.1)
	
		local weld = Instance.new("WeldConstraint")
		weld.Name = "BlockSpamWeld"
		weld.Part0 = LocalPlayer.Character.HumanoidRootPart
		weld.Part1 = block
		weld.Parent = LocalPlayer.Character.HumanoidRootPart
	
		task.wait(0.2)
		LocalPlayer.Character.HumanoidRootPart.CFrame = originalPosition
	
		currentWeld = weld
		weldedBlock = block
	
		-- Start spamming
		BlockSpamButton.Text = "Starting spam..."
	
		if not startSpamBuilding(block) then
			cleanupWeld()
			LocalPlayer.Character.HumanoidRootPart.CFrame = originalPosition
			return false
		end
	
		if previousTool and previousTool.Parent == LocalPlayer.Backpack then
			task.wait(0.1)
			LocalPlayer.Character.Humanoid:EquipTool(previousTool)
		end
	
		print("[BlockSpam] Complete!")
		return true
	end
	
	-- ============================================================
	-- START SPAM
	-- ============================================================
	local function startSpam()
		local selectedBlock = SelectedBlockValue.Value
	
		if not selectedBlock or not selectedBlock:IsDescendantOf(workspace) then
			BlockSpamButton.Text = TEXT_NO_BLOCK
			task.wait(1)
			BlockSpamButton.Text = TEXT_START
			return
		end
	
		isProcessing = true
		BlockSpamButton.Text = TEXT_PROCESSING
	
		if not processBlock(selectedBlock) then
			BlockSpamButton.Text = "Failed!"
			task.wait(1)
			isProcessing = false
			BlockSpamButton.Text = TEXT_START
		end
	end
	
	-- ============================================================
	-- UI
	-- ============================================================
	BlockSpamButton.MouseEnter:Connect(function()
		if not isProcessing and not isSpamming then
			if not SelectedBlockValue.Value or not SelectedBlockValue.Value:IsDescendantOf(workspace) then
				BlockSpamButton.Text = TEXT_NO_BLOCK
			end
		end
	end)
	
	BlockSpamButton.MouseLeave:Connect(function()
		if not isProcessing and not isSpamming and BlockSpamButton.Text == TEXT_NO_BLOCK then
			BlockSpamButton.Text = TEXT_START
		end
	end)
	
	BlockSpamButton.MouseButton1Click:Connect(function()
		if isSpamming then
			stopSpam()
		else
			startSpam()
		end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) and isSpamming then
			stopSpam()
		end
	end)
end;
task.spawn(C_17a);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.SavePosRow.SavePosScript
local function C_17d()
local script = G2L["17d"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local RunService = game:GetService("RunService")
	
	local FollowBlockRow = script.Parent
	local BkitGroup = FollowBlockRow.Parent
	local BkitAccentColor = BkitGroup:WaitForChild("BkitAccentColor")
	local FollowBlockTickBox = FollowBlockRow:WaitForChild("FollowBlockTickBox")
	
	-- Get the selected block value
	local SelectedBlockValue = BkitGroup:WaitForChild("SelectedBlock")
	
	-- State
	local isEnabled = false
	local attachmentWeld = nil
	local updateLoop = nil
	local originalCanCollide = nil -- Store original CanCollide state
	
	-- ============================================================
	-- UPDATE VISUAL
	-- ============================================================
	local function updateVisual()
		if isEnabled then
			FollowBlockTickBox.BackgroundColor3 = BkitAccentColor.BackgroundColor3
		else
			FollowBlockTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	updateVisual()
	
	-- ============================================================
	-- ATTACH TO BLOCK
	-- ============================================================
	local function attachToBlock()
		local selectedBlock = SelectedBlockValue.Value
	
		if not selectedBlock or not selectedBlock:IsDescendantOf(workspace) then
			print("[FollowBlock] No valid block selected")
			return false
		end
	
		-- Check if block is unanchored (has Drag)
		if not selectedBlock:FindFirstChild("Drag") then
			print("[FollowBlock] Block must be unanchored (needs Drag)")
			return false
		end
	
		local character = LocalPlayer.Character
		if not character then
			print("[FollowBlock] No character found")
			return false
		end
	
		local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
		if not humanoidRootPart then
			print("[FollowBlock] No HumanoidRootPart found")
			return false
		end
	
		-- Store original CanCollide state and disable collisions
		originalCanCollide = selectedBlock.CanCollide
		selectedBlock.CanCollide = false
	
		-- Teleport to the center of the block first
		humanoidRootPart.CFrame = CFrame.new(selectedBlock.Position)
	
		-- Create a weld to attach player to block
		local weld = Instance.new("WeldConstraint")
		weld.Part0 = humanoidRootPart
		weld.Part1 = selectedBlock
		weld.Parent = humanoidRootPart
	
		attachmentWeld = weld
	
		print("[FollowBlock] Attached to block:", selectedBlock:GetFullName())
		return true
	end
	
	-- ============================================================
	-- DETACH FROM BLOCK
	-- ============================================================
	local function detachFromBlock()
		-- Restore original CanCollide state
		if originalCanCollide ~= nil then
			local selectedBlock = SelectedBlockValue.Value
			if selectedBlock and selectedBlock:IsDescendantOf(workspace) then
				selectedBlock.CanCollide = originalCanCollide
			end
			originalCanCollide = nil
		end
	
		if attachmentWeld then
			attachmentWeld:Destroy()
			attachmentWeld = nil
		end
	
		print("[FollowBlock] Detached from block")
	end
	
	-- ============================================================
	-- MONITOR BLOCK EXISTENCE
	-- ============================================================
	local function startMonitoring()
		updateLoop = task.spawn(function()
			while isEnabled do
				local selectedBlock = SelectedBlockValue.Value
	
				-- If block is deleted, deselected, or becomes anchored, detach and disable
				if not selectedBlock or not selectedBlock:IsDescendantOf(workspace) or not selectedBlock:FindFirstChild("Drag") then
					print("[FollowBlock] Block lost or anchored, disabling")
					disable()
					break
				end
	
				-- If weld is missing, reattach
				if not attachmentWeld or not attachmentWeld.Parent then
					print("[FollowBlock] Weld lost, reattaching")
					detachFromBlock()
					attachToBlock()
				end
	
				task.wait(0.5) -- Check every 0.5 seconds
			end
		end)
	end
	
	-- ============================================================
	-- ENABLE
	-- ============================================================
	local function enable()
		local selectedBlock = SelectedBlockValue.Value
	
		if not selectedBlock or not selectedBlock:IsDescendantOf(workspace) then
			print("[FollowBlock] Cannot enable - no valid block selected")
			return
		end
	
		if not selectedBlock:FindFirstChild("Drag") then
			print("[FollowBlock] Cannot enable - block must be unanchored (needs Drag)")
			return
		end
	
		isEnabled = true
		updateVisual()
	
		if attachToBlock() then
			startMonitoring()
			print("[FollowBlock] ON")
		else
			isEnabled = false
			updateVisual()
		end
	end
	
	-- ============================================================
	-- DISABLE
	-- ============================================================
	function disable()
		isEnabled = false
		updateVisual()
	
		detachFromBlock()
	
		if updateLoop then
			task.cancel(updateLoop)
			updateLoop = nil
		end
	
		print("[FollowBlock] OFF")
	end
	
	-- ============================================================
	-- TICKBOX TOGGLE
	-- ============================================================
	FollowBlockTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disable()
		else
			enable()
		end
	end)
	
	-- ============================================================
	-- WATCH FOR BLOCK CHANGES
	-- ============================================================
	SelectedBlockValue:GetPropertyChangedSignal("Value"):Connect(function()
		if isEnabled then
			-- Block changed, reattach to new block
			detachFromBlock()
	
			local newBlock = SelectedBlockValue.Value
			if newBlock and newBlock:IsDescendantOf(workspace) and newBlock:FindFirstChild("Drag") then
				attachToBlock()
			else
				-- No new block or not unanchored, disable
				disable()
			end
		end
	end)
	
	-- ============================================================
	-- WATCH FOR CHARACTER RESPAWN
	-- ============================================================
	LocalPlayer.CharacterAdded:Connect(function(character)
		if isEnabled then
			-- Wait for HumanoidRootPart to be added
			character:WaitForChild("HumanoidRootPart", 5)
			task.wait(0.5) -- Wait a bit for character to fully load
	
			-- Reattach to block after respawn
			detachFromBlock()
			attachToBlock()
		end
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if isEnabled then
				disable()
			end
			print("[FollowBlock] Cleaned up")
		end
	end)
end;
task.spawn(C_17d);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOTrollPage.TCOTrollLeftSide.LagGroup.BlockSizeRow.BlockSizeChanger.BlockSizeScript
local function C_183()
local script = G2L["183"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local BlockSizeChanger = script.Parent
	local BlockSizeRow = BlockSizeChanger.Parent
	local LagGroup = BlockSizeRow.Parent
	
	-- Store target size in LagGroup so StartSpam can access it
	local TargetSizeValue = LagGroup:FindFirstChild("TargetBlockSize") or Instance.new("IntValue")
	TargetSizeValue.Name = "TargetBlockSize"
	TargetSizeValue.Parent = LagGroup
	TargetSizeValue.Value = 1 -- Default
	
	-- ============================================================
	-- VALIDATE INPUT
	-- ============================================================
	local function validateInput(input)
		-- Remove any non-digit characters
		local cleaned = input:gsub("%D", "")
	
		-- If empty, default to 1
		if cleaned == "" then
			cleaned = "1"
		end
	
		-- Convert to number
		local num = tonumber(cleaned)
	
		-- Ensure it's at least 1
		if num and num < 1 then
			num = 1
		end
	
		return num
	end
	
	-- ============================================================
	-- UPDATE TARGET SIZE
	-- ============================================================
	local function updateTargetSize()
		local input = BlockSizeChanger.Text
		local validSize = validateInput(input)
	
		-- Update the value
		TargetSizeValue.Value = validSize
	
		-- Update textbox to show valid value
		BlockSizeChanger.Text = tostring(validSize)
	
		print("[BlockSize] Target size set to:", validSize)
	end
	
	-- ============================================================
	-- INPUT EVENTS
	-- ============================================================
	-- When user finishes typing (presses Enter or unfocuses)
	BlockSizeChanger.FocusLost:Connect(function(enterPressed)
		updateTargetSize()
	end)
	
	-- Prevent decimals while typing
	BlockSizeChanger:GetPropertyChangedSignal("Text"):Connect(function()
		local text = BlockSizeChanger.Text
	
		-- Remove decimals and non-digits immediately
		local cleaned = text:gsub("[^%d]", "")
	
		if cleaned ~= text then
			BlockSizeChanger.Text = cleaned
		end
	end)
	
	-- ============================================================
	-- INITIALIZE
	-- ============================================================
	-- Set initial value
	updateTargetSize()
	
	print("[BlockSize] Size changer loaded - Default: 1")
end;
task.spawn(C_183);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableScript
local function C_189()
local script = G2L["189"];
	local EnableGroup = script.Parent
	local EnableMinimize = EnableGroup.EnableTitle.EnableMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(EnableGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = EnableGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	EnableMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			EnableGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			EnableMinimize.Text = "+"
		else
			EnableGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			EnableMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_189);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableToolTip
local function C_18a()
local script = G2L["18a"];
	-- ============================================================
	-- ENABLE TOOLTIP SCRIPT
	-- Controls Enable-related tooltips only
	-- ============================================================
	
	local Group = script.Parent
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then
				return p
			end
			p = p.Parent
		end
	end
	
	local ScreenGui = getScreenGui()
	if not ScreenGui then return end
	
	-- ============================================================
	-- FIND UI REFERENCES
	-- ============================================================
	
	local function findDescendant(name)
		for _, v in pairs(ScreenGui:GetDescendants()) do
			if v.Name == name then
				return v
			end
		end
	end
	
	local EnableDeleteTickBox = findDescendant("EnableDeleteTickBox")
	local EnableToxifyTickBox = findDescendant("EnableToxifyTickBox")
	local EnableAnchoredTickBox = findDescendant("EnableAnchoredTickBox")
	local EnableSpamTickBox = findDescendant("EnableSpamTickBox")
	local EnableExploitsTickBox = findDescendant("EnableExploitsTickBox")
	local EnableHelp = findDescendant("EnableHelp")
	
	-- ============================================================
	-- TOOLTIP BUILDER
	-- ============================================================
	
	local function makeTooltip(text)
		local frame = Instance.new("Frame")
		frame.BackgroundColor3 = Color3.fromRGB(16,16,16)
		frame.BorderSizePixel = 0
		frame.ZIndex = 100
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Size = UDim2.new(0, 212, 0, 0)
		frame.Visible = false
		frame.Parent = ScreenGui
	
		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(57,57,57)
		stroke.Parent = frame
	
		local padding = Instance.new("UIPadding")
		padding.PaddingTop = UDim.new(0,6)
		padding.PaddingBottom = UDim.new(0,6)
		padding.PaddingLeft = UDim.new(0,6)
		padding.PaddingRight = UDim.new(0,6)
		padding.Parent = frame
	
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Text = text
		label.TextColor3 = Color3.new(1,1,1)
		label.TextSize = 14
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Top
		label.Font = Enum.Font.GothamSemibold
		label.TextWrapped = true
		label.Size = UDim2.new(0,200,0,0)
		label.AutomaticSize = Enum.AutomaticSize.Y
		label.ZIndex = 101
		label.Parent = frame
	
		return frame
	end
	
	local function hookTooltip(button, tooltipFrame)
		if not button then return end
	
		button.MouseEnter:Connect(function()
			local abs = button.AbsolutePosition
			local size = button.AbsoluteSize
			tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
			tooltipFrame.Visible = true
		end)
	
		button.MouseLeave:Connect(function()
			tooltipFrame.Visible = false
		end)
	end
	
	-- ============================================================
	-- CREATE TOOLTIPS
	-- ============================================================
	
	local deleteTooltip = makeTooltip(
		"Detects players deleting other people's builds."
	)
	
	local toxifyTooltip = makeTooltip(
		"Detects players toxifying other people's builds."
	)
	
	local anchoredTooltip = makeTooltip(
		"Detects players unanchoring other people's builds."
	)
	
	local spamTooltip = makeTooltip(
		"Detects players spamming blocks."
	)
	
	local exploitsTooltip = makeTooltip(
		"Detects players using grief auras, grief scripts and other exploits. You must turn on any grief detections to detect when they use exploits to grief with those tools.\n\nAlert in chat will send a different message when they use scripts to grief."
	)
	
	local helpTooltip = makeTooltip(
		"Enabling any detection will monitor for possible griefing.\nTurn on punish options to automatically punish detected players.\n\nView detection logs by typing /console in chat."
	)
	
	-- ============================================================
	-- HOOK THEM
	-- ============================================================
	
	hookTooltip(EnableDeleteTickBox, deleteTooltip)
	hookTooltip(EnableToxifyTickBox, toxifyTooltip)
	hookTooltip(EnableAnchoredTickBox, anchoredTooltip)
	hookTooltip(EnableSpamTickBox, spamTooltip)
	hookTooltip(EnableExploitsTickBox, exploitsTooltip)
	hookTooltip(EnableHelp, helpTooltip)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			deleteTooltip:Destroy()
			toxifyTooltip:Destroy()
			anchoredTooltip:Destroy()
			spamTooltip:Destroy()
			exploitsTooltip:Destroy()
			helpTooltip:Destroy()
		end
	end)
end;
task.spawn(C_18a);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableDeleteRow.EnableDeleteScript
local function C_191()
local script = G2L["191"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	--------------------------------------------------
	-- GUI REFERENCES
	--------------------------------------------------
	
	local EnableDeleteRow = script.Parent
	local EnableGroup = EnableDeleteRow.Parent
	local EnableDeleteTickBox = EnableDeleteRow:WaitForChild("EnableDeleteTickBox")
	local EnableAccentColor = EnableGroup:WaitForChild("EnableAccentColor")
	local ScreenGui = script:FindFirstAncestorOfClass("ScreenGui")
	
	--------------------------------------------------
	-- ENABLE STATE
	--------------------------------------------------
	
	local Enabled = false
	
	local function updateVisual()
		if Enabled then
			EnableDeleteTickBox.BackgroundColor3 = EnableAccentColor.BackgroundColor3
			print("Delete Detection ON")
		else
			EnableDeleteTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			print("Delete Detection OFF")
		end
	end
	
	EnableDeleteTickBox.MouseButton1Click:Connect(function()
		Enabled = not Enabled
		ScreenGui:SetAttribute("DeleteLogsEnabled", Enabled)
		updateVisual()
	end)
	
	updateVisual()
	
	--------------------------------------------------
	-- ENSURE EVENTS EXIST
	--------------------------------------------------
	
	shared.DeleteEvent    = shared.DeleteEvent    or Instance.new("BindableEvent")
	shared.DeleteGriefEvent = shared.DeleteGriefEvent or Instance.new("BindableEvent")
	shared.GriefEvent     = shared.GriefEvent     or Instance.new("BindableEvent")
	
	--------------------------------------------------
	-- GRIEF SETTINGS
	--------------------------------------------------
	
	local GRIEF_COUNT = 12
	local GRIEF_TIME  = 5
	local DEFAULT_COOLDOWN = 30
	
	shared.GriefCooldown   = shared.GriefCooldown   or DEFAULT_COOLDOWN
	shared.GriefTimeFilter = shared.GriefTimeFilter  or 0
	
	local griefTracker  = {}
	local griefCooldown = {}
	
	--------------------------------------------------
	-- FILTER HELPERS
	--------------------------------------------------
	
	local function getCooldown()
		return shared.GriefCooldown
	end
	
	local function isWhitelisted(player)
		local whitelist = shared.GriefWhitelist
		if not whitelist then return false end
		return whitelist[player.Name] == true
	end
	
	local function hasArkenstone(player)
		local backpack = player:FindFirstChild("Backpack")
		if not backpack then return false end
		return backpack:FindFirstChild("The Arkenstone") ~= nil
	end
	
	local function passesEnlightenCheck(player)
		local enlightenEnabled = shared.GriefEnlightenEnabled
		if enlightenEnabled == nil then enlightenEnabled = true end
		if enlightenEnabled then return true end
		return not hasArkenstone(player)
	end
	
	local function passesTimeFilterCheck(player)
		local filter = shared.GriefTimeFilter or 0
		if filter == 0 then return true end
		local leaderstats = player:FindFirstChild("leaderstats")
		if not leaderstats then return true end
		local timeValue = leaderstats:FindFirstChild("Time")
		if not timeValue then return true end
		return timeValue.Value < filter
	end
	
	local function passesInGameCheck(brickOwnerUserId)
		local inGameEnabled = shared.GriefInGameEnabled
		if inGameEnabled == nil then inGameEnabled = true end
		if inGameEnabled then return true end
		return brickOwnerUserId ~= nil and Players:GetPlayerByUserId(brickOwnerUserId) ~= nil
	end
	
	--------------------------------------------------
	-- LISTEN TO DELETE EVENT FROM CONSOLE
	-- Console fires: DeleteEvent(userId, ownerUserId, isSelf, ownerName, age, brickPos)
	--------------------------------------------------
	
	shared.DeleteEvent.Event:Connect(function(userId, ownerUserId, isSelf, ownerName, age, brickPos)
		if not Enabled then return end
	
		local player = Players:GetPlayerByUserId(userId)
		if not player then return end
	
		local ownerPlayer = ownerUserId and Players:GetPlayerByUserId(ownerUserId) or nil
	
		-- Build log
		local square = isSelf and "🟩" or (ownerPlayer and "🟥" or "🟧")
		local extra = ""
		if player == LocalPlayer then
			extra = "👤"
		elseif ownerName == LocalPlayer.Name and not isSelf then
			extra = "⚠️"
		end
	
		local ownerText
		if isSelf then
			ownerText = "their block"
		else
			if ownerPlayer then
				ownerText = ownerPlayer.DisplayName .. "'s (" .. ownerName .. ") block"
			else
				ownerText = ownerName .. "'s (NOT INGAME) block"
			end
		end
	
		local dist = brickPos and player.Character and player.Character:FindFirstChild("HumanoidRootPart")
			and string.format("%.2f", (player.Character.HumanoidRootPart.Position - brickPos).Magnitude)
			or "N/A"
	
		print("[DELETELOG] " .. square .. extra .. " " .. player.DisplayName .. " (" .. player.Name .. ") deleted " .. ownerText ..
			" (Magnitude: " .. dist ..
			" | Block Age: " .. (age or "Unknown") .. ")")
	
		-- Grief threshold checks (only for non-self deletes)
		if isSelf then return end
	
		if isWhitelisted(player) then return end
		if not passesEnlightenCheck(player) then return end
		if not passesTimeFilterCheck(player) then return end
		if ownerUserId and not passesInGameCheck(ownerUserId) then return end
	
		if griefCooldown[userId] and tick() < griefCooldown[userId] then return end
	
		local now = tick()
		griefTracker[userId] = griefTracker[userId] or {}
		table.insert(griefTracker[userId], now)
	
		for i = #griefTracker[userId], 1, -1 do
			if now - griefTracker[userId][i] > GRIEF_TIME then
				table.remove(griefTracker[userId], i)
			end
		end
	
		print("[DEBUG] Delete count for", player.Name, "=", #griefTracker[userId])
	
		if #griefTracker[userId] >= GRIEF_COUNT then
			print("[GRIEF DETECTED]", player.Name)
			shared.DeleteGriefEvent:Fire(player)
			shared.GriefEvent:Fire(player, "delete")
			griefTracker[userId] = {}
			griefCooldown[userId] = tick() + getCooldown()
		end
	end)
end;
task.spawn(C_191);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableToxifyRow.EnableToxifyScript
local function C_195()
local script = G2L["195"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local EnableToxifyRow = script.Parent
	local EnableGroup = EnableToxifyRow.Parent
	local EnableToxifyTickBox = EnableToxifyRow:WaitForChild("EnableToxifyTickBox")
	local EnableAccentColor = EnableGroup:WaitForChild("EnableAccentColor")
	local ScreenGui = script:FindFirstAncestorOfClass("ScreenGui")
	
	local Enabled = false
	
	local function updateVisual()
		if Enabled then
			EnableToxifyTickBox.BackgroundColor3 = EnableAccentColor.BackgroundColor3
			print("Toxify Detection ON")
		else
			EnableToxifyTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			print("Toxify Detection OFF")
		end
	end
	
	EnableToxifyTickBox.MouseButton1Click:Connect(function()
		Enabled = not Enabled
		ScreenGui:SetAttribute("ToxifyLogsEnabled", Enabled)
		updateVisual()
	end)
	
	updateVisual()
	
	shared.ToxifyEvent      = shared.ToxifyEvent      or Instance.new("BindableEvent")
	shared.ToxifyGriefEvent = shared.ToxifyGriefEvent or Instance.new("BindableEvent")
	shared.GriefEvent       = shared.GriefEvent       or Instance.new("BindableEvent")
	
	local GRIEF_COUNT      = 12
	local GRIEF_TIME       = 5
	local DEFAULT_COOLDOWN = 30
	shared.GriefCooldown   = shared.GriefCooldown  or DEFAULT_COOLDOWN
	shared.GriefTimeFilter = shared.GriefTimeFilter or 0
	local griefTracker  = {}
	local griefCooldown = {}
	
	local function getCooldown() return shared.GriefCooldown end
	
	local function isWhitelisted(player)
		local w = shared.GriefWhitelist
		return w and w[player.Name] == true
	end
	
	local function hasArkenstone(player)
		local b = player:FindFirstChild("Backpack")
		return b and b:FindFirstChild("The Arkenstone") ~= nil
	end
	
	local function passesEnlightenCheck(player)
		local e = shared.GriefEnlightenEnabled
		if e == nil then e = true end
		if e then return true end
		return not hasArkenstone(player)
	end
	
	local function passesTimeFilterCheck(player)
		local filter = shared.GriefTimeFilter or 0
		if filter == 0 then return true end
		local ls = player:FindFirstChild("leaderstats")
		if not ls then return true end
		local t = ls:FindFirstChild("Time")
		if not t then return true end
		return t.Value < filter
	end
	
	shared.ToxifyEvent.Event:Connect(function(userId, ownerUserId, isSelf, ownerName, brickPos)
		if not Enabled then return end
	
		local player = Players:GetPlayerByUserId(userId)
		if not player then return end
	
		local ownerPlayer = ownerUserId and Players:GetPlayerByUserId(ownerUserId) or nil
		local square = isSelf and "🟩" or (ownerPlayer and "🟥" or "🟧")
		local extra = ""
		if player == LocalPlayer then extra = "👤"
		elseif ownerName == LocalPlayer.Name and not isSelf then extra = "⚠️" end
	
		local ownerText
		if isSelf then ownerText = "their block"
		elseif ownerPlayer then ownerText = ownerPlayer.DisplayName .. "'s (" .. ownerName .. ") block"
		else ownerText = ownerName .. "'s (NOT INGAME) block" end
	
		local dist = brickPos and player.Character and player.Character:FindFirstChild("HumanoidRootPart")
			and string.format("%.2f", (player.Character.HumanoidRootPart.Position - brickPos).Magnitude) or "N/A"
	
		print("[TOXIFYLOG] " .. square .. extra .. " " .. player.DisplayName .. " (" .. player.Name .. ") toxified " .. ownerText .. " (Magnitude: " .. dist .. ")")
	
		if isSelf then return end
		if isWhitelisted(player) then return end
		if not passesEnlightenCheck(player) then return end
		if not passesTimeFilterCheck(player) then return end
		if griefCooldown[userId] and tick() < griefCooldown[userId] then return end
	
		local now = tick()
		griefTracker[userId] = griefTracker[userId] or {}
		table.insert(griefTracker[userId], now)
		for i = #griefTracker[userId], 1, -1 do
			if now - griefTracker[userId][i] > GRIEF_TIME then table.remove(griefTracker[userId], i) end
		end
	
		if #griefTracker[userId] >= GRIEF_COUNT then
			print("[GRIEF DETECTED - TOXIFY]", player.Name)
			shared.ToxifyGriefEvent:Fire(player)
			shared.GriefEvent:Fire(player, "toxify")
			griefTracker[userId] = {}
			griefCooldown[userId] = tick() + getCooldown()
		end
	end)
end;
task.spawn(C_195);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableAnchoredRow.EnableAnchoredScript
local function C_199()
local script = G2L["199"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local EnableAnchoredRow = script.Parent
	local EnableGroup = EnableAnchoredRow.Parent
	local EnableAnchoredTickBox = EnableAnchoredRow:WaitForChild("EnableAnchoredTickBox")
	local EnableAccentColor = EnableGroup:WaitForChild("EnableAccentColor")
	local ScreenGui = script:FindFirstAncestorOfClass("ScreenGui")
	
	local Enabled = false
	
	local function updateVisual()
		if Enabled then
			EnableAnchoredTickBox.BackgroundColor3 = EnableAccentColor.BackgroundColor3
			print("Unanchor Detection ON")
		else
			EnableAnchoredTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			print("Unanchor Detection OFF")
		end
	end
	
	EnableAnchoredTickBox.MouseButton1Click:Connect(function()
		Enabled = not Enabled
		ScreenGui:SetAttribute("AnchoredLogsEnabled", Enabled)
		updateVisual()
	end)
	
	updateVisual()
	
	shared.UnanchorEvent      = shared.UnanchorEvent      or Instance.new("BindableEvent")
	shared.UnanchorGriefEvent = shared.UnanchorGriefEvent or Instance.new("BindableEvent")
	shared.GriefEvent         = shared.GriefEvent         or Instance.new("BindableEvent")
	
	local GRIEF_COUNT      = 12
	local GRIEF_TIME       = 5
	local DEFAULT_COOLDOWN = 30
	shared.GriefCooldown   = shared.GriefCooldown  or DEFAULT_COOLDOWN
	shared.GriefTimeFilter = shared.GriefTimeFilter or 0
	local griefTracker  = {}
	local griefCooldown = {}
	
	local function getCooldown() return shared.GriefCooldown end
	
	local function isWhitelisted(player)
		local w = shared.GriefWhitelist
		return w and w[player.Name] == true
	end
	
	local function hasArkenstone(player)
		local b = player:FindFirstChild("Backpack")
		return b and b:FindFirstChild("The Arkenstone") ~= nil
	end
	
	local function passesEnlightenCheck(player)
		local e = shared.GriefEnlightenEnabled
		if e == nil then e = true end
		if e then return true end
		return not hasArkenstone(player)
	end
	
	local function passesTimeFilterCheck(player)
		local filter = shared.GriefTimeFilter or 0
		if filter == 0 then return true end
		local ls = player:FindFirstChild("leaderstats")
		if not ls then return true end
		local t = ls:FindFirstChild("Time")
		if not t then return true end
		return t.Value < filter
	end
	
	shared.UnanchorEvent.Event:Connect(function(userId, ownerUserId, isSelf, ownerName, brickPos)
		if not Enabled then return end
	
		local player = Players:GetPlayerByUserId(userId)
		if not player then return end
	
		local ownerPlayer = ownerUserId and Players:GetPlayerByUserId(ownerUserId) or nil
		local square = isSelf and "🟩" or (ownerPlayer and "🟥" or "🟧")
		local extra = ""
		if player == LocalPlayer then extra = "👤"
		elseif ownerName == LocalPlayer.Name and not isSelf then extra = "⚠️" end
	
		local ownerText
		if isSelf then ownerText = "their block"
		elseif ownerPlayer then ownerText = ownerPlayer.DisplayName .. "'s (" .. ownerName .. ") block"
		else ownerText = ownerName .. "'s (NOT INGAME) block" end
	
		local dist = brickPos and player.Character and player.Character:FindFirstChild("HumanoidRootPart")
			and string.format("%.2f", (player.Character.HumanoidRootPart.Position - brickPos).Magnitude) or "N/A"
	
		print("[UNANCHORLOG] " .. square .. extra .. " " .. player.DisplayName .. " (" .. player.Name .. ") unanchored " .. ownerText .. " (Magnitude: " .. dist .. ")")
	
		if isSelf then return end
		if isWhitelisted(player) then return end
		if not passesEnlightenCheck(player) then return end
		if not passesTimeFilterCheck(player) then return end
		if griefCooldown[userId] and tick() < griefCooldown[userId] then return end
	
		local now = tick()
		griefTracker[userId] = griefTracker[userId] or {}
		table.insert(griefTracker[userId], now)
		for i = #griefTracker[userId], 1, -1 do
			if now - griefTracker[userId][i] > GRIEF_TIME then table.remove(griefTracker[userId], i) end
		end
	
		if #griefTracker[userId] >= GRIEF_COUNT then
			print("[GRIEF DETECTED - UNANCHOR]", player.Name)
			shared.UnanchorGriefEvent:Fire(player)
			shared.GriefEvent:Fire(player, "unanchor")
			griefTracker[userId] = {}
			griefCooldown[userId] = tick() + getCooldown()
		end
	end)
end;
task.spawn(C_199);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableSpamRow.EnableSpamScript
local function C_19d()
local script = G2L["19d"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local EnableSpamRow = script.Parent
	local EnableGroup = EnableSpamRow.Parent
	local EnableSpamTickBox = EnableSpamRow:WaitForChild("EnableSpamTickBox")
	local EnableAccentColor = EnableGroup:WaitForChild("EnableAccentColor")
	local ScreenGui = script:FindFirstAncestorOfClass("ScreenGui")
	
	local Enabled = false
	
	local function updateVisual()
		if Enabled then
			EnableSpamTickBox.BackgroundColor3 = EnableAccentColor.BackgroundColor3
			print("Spam Detection ON")
		else
			EnableSpamTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			print("Spam Detection OFF")
		end
	end
	
	EnableSpamTickBox.MouseButton1Click:Connect(function()
		Enabled = not Enabled
		ScreenGui:SetAttribute("SpamLogsEnabled", Enabled)
		updateVisual()
	end)
	
	updateVisual()
	
	shared.BuildEvent = shared.BuildEvent or Instance.new("BindableEvent")
	
	-- Log builds (console fires BuildEvent with userId)
	shared.BuildEvent.Event:Connect(function(userId)
		if not Enabled then return end
		local player = Players:GetPlayerByUserId(userId)
		if not player then return end
		-- Spam grief script handles the threshold detection
		-- This script just logs the raw build event
		print("[BUILDLOG] " .. player.DisplayName .. " (" .. player.Name .. ") placed a block")
	end)
end;
task.spawn(C_19d);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.EnableGroup.EnableExploitsRow.EnableExploitsGrief
local function C_1a1()
local script = G2L["1a1"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local EnableExploitsRow = script.Parent
	local EnableGroup = EnableExploitsRow.Parent
	local EnableExploitsTickBox = EnableExploitsRow:WaitForChild("EnableExploitsTickBox")
	local EnableAccentColor = EnableGroup:WaitForChild("EnableAccentColor")
	local ScreenGui = script:FindFirstAncestorOfClass("ScreenGui")
	
	--------------------------------------------------
	-- ENABLE STATE
	--------------------------------------------------
	
	local Enabled = false
	
	local function updateVisual()
		if Enabled then
			EnableExploitsTickBox.BackgroundColor3 = EnableAccentColor.BackgroundColor3
			print("Exploits Detection ON")
		else
			EnableExploitsTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			print("Exploits Detection OFF")
		end
	end
	
	EnableExploitsTickBox.MouseButton1Click:Connect(function()
		Enabled = not Enabled
		ScreenGui:SetAttribute("ExploitsLogsEnabled", Enabled)
		updateVisual()
	end)
	
	updateVisual()
	
	--------------------------------------------------
	-- ENSURE EVENTS EXIST
	--------------------------------------------------
	
	shared.ExploitGriefEvent = shared.ExploitGriefEvent or Instance.new("BindableEvent")
	shared.GriefEvent        = shared.GriefEvent or Instance.new("BindableEvent")
	
	--------------------------------------------------
	-- GRIEF SETTINGS
	--------------------------------------------------
	
	local GRIEF_COUNT      = 12
	local GRIEF_TIME       = 5
	local DEFAULT_COOLDOWN = 30
	
	shared.GriefCooldown   = shared.GriefCooldown or DEFAULT_COOLDOWN
	shared.GriefTimeFilter = shared.GriefTimeFilter or 0
	
	local griefTracker  = {}
	local griefCooldown = {}
	
	--------------------------------------------------
	-- FILTER HELPERS
	--------------------------------------------------
	
	local function getCooldown()
		return shared.GriefCooldown
	end
	
	local function isWhitelisted(player)
		local whitelist = shared.GriefWhitelist
		if not whitelist then return false end
		return whitelist[player.Name] == true
	end
	
	local function hasArkenstone(player)
		local backpack = player:FindFirstChild("Backpack")
		if not backpack then return false end
		return backpack:FindFirstChild("The Arkenstone") ~= nil
	end
	
	local function passesEnlightenCheck(player)
		local enlightenEnabled = shared.GriefEnlightenEnabled
		if enlightenEnabled == nil then enlightenEnabled = true end
		if enlightenEnabled then return true end
		return not hasArkenstone(player)
	end
	
	local function passesTimeFilterCheck(player)
		local filter = shared.GriefTimeFilter or 0
		if filter == 0 then return true end
		local leaderstats = player:FindFirstChild("leaderstats")
		if not leaderstats then return true end
		local timeValue = leaderstats:FindFirstChild("Time")
		if not timeValue then return true end
		return timeValue.Value < filter
	end
	
	--------------------------------------------------
	-- LISTEN FOR EXPLOIT DETECTIONS
	--------------------------------------------------
	
	shared.ExploitGriefEvent.Event:Connect(function(player)
		if not Enabled then return end
		if not player then return end
		if isWhitelisted(player) then return end
		if not passesEnlightenCheck(player) then return end
		if not passesTimeFilterCheck(player) then return end
		if griefCooldown[player.UserId] and tick() < griefCooldown[player.UserId] then return end
	
		local now = tick()
		griefTracker[player.UserId] = griefTracker[player.UserId] or {}
		table.insert(griefTracker[player.UserId], now)
	
		-- Clean old timestamps
		for i = #griefTracker[player.UserId], 1, -1 do
			if now - griefTracker[player.UserId][i] > GRIEF_TIME then
				table.remove(griefTracker[player.UserId], i)
			end
		end
	
		print("[EXPLOIT LOG] " .. player.Name .. " triggered exploit detection. Count: " .. #griefTracker[player.UserId])
	
		if #griefTracker[player.UserId] >= GRIEF_COUNT then
			print("[EXPLOIT GRIEF DETECTED]", player.Name)
			shared.GriefEvent:Fire(player, "exploit")
			griefTracker[player.UserId] = {}
			griefCooldown[player.UserId] = tick() + getCooldown()
		end
	end)
end;
task.spawn(C_1a1);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.MiscScript
local function C_1a5()
local script = G2L["1a5"];
	local MiscGroup = script.Parent
	local MiscMinimize = MiscGroup.MiscTitle.MiscMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(MiscGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = MiscGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	MiscMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			MiscGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			MiscMinimize.Text = "+"
		else
			MiscGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			MiscMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_1a5);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.MiscToolTip
local function C_1a6()
local script = G2L["1a6"];
	-- ============================================================
	-- ENABLE TOOLTIP SCRIPT
	-- Controls Enable-related tooltips only
	-- ============================================================
	
	local Group = script.Parent
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then
				return p
			end
			p = p.Parent
		end
	end
	
	local ScreenGui = getScreenGui()
	if not ScreenGui then return end
	
	-- ============================================================
	-- FIND UI REFERENCES
	-- ============================================================
	
	local function findDescendant(name)
		for _, v in pairs(ScreenGui:GetDescendants()) do
			if v.Name == name then
				return v
			end
		end
	end
	
	local LBoomboxTickBox = findDescendant("LBoomboxTickBox")
	local FDetectorRowTickBox = findDescendant("FDetectorRowTickBox")
	
	-- ============================================================
	-- TOOLTIP BUILDER
	-- ============================================================
	
	local function makeTooltip(text)
		local frame = Instance.new("Frame")
		frame.BackgroundColor3 = Color3.fromRGB(16,16,16)
		frame.BorderSizePixel = 0
		frame.ZIndex = 100
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Size = UDim2.new(0, 212, 0, 0)
		frame.Visible = false
		frame.Parent = ScreenGui
	
		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(57,57,57)
		stroke.Parent = frame
	
		local padding = Instance.new("UIPadding")
		padding.PaddingTop = UDim.new(0,6)
		padding.PaddingBottom = UDim.new(0,6)
		padding.PaddingLeft = UDim.new(0,6)
		padding.PaddingRight = UDim.new(0,6)
		padding.Parent = frame
	
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Text = text
		label.TextColor3 = Color3.new(1,1,1)
		label.TextSize = 14
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Top
		label.Font = Enum.Font.GothamSemibold
		label.TextWrapped = true
		label.Size = UDim2.new(0,200,0,0)
		label.AutomaticSize = Enum.AutomaticSize.Y
		label.ZIndex = 101
		label.Parent = frame
	
		return frame
	end
	
	local function hookTooltip(button, tooltipFrame)
		if not button then return end
	
		button.MouseEnter:Connect(function()
			local abs = button.AbsolutePosition
			local size = button.AbsoluteSize
			tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
			tooltipFrame.Visible = true
		end)
	
		button.MouseLeave:Connect(function()
			tooltipFrame.Visible = false
		end)
	end
	
	-- ============================================================
	-- CREATE TOOLTIPS
	-- ============================================================
	
	local boomboxTooltip = makeTooltip(
		"Detects players playing their boombox too loud. Turn on alert in chat to notifed when this happens, turn on confirm grief to fling them."
	)
	
	local fdetectorTooltip = makeTooltip(
		"Detects players using fling scripts or touch flinging other players. Turn on alert in chat to be notified when this happens."
	)
	
	-- ============================================================
	-- HOOK THEM
	-- ============================================================
	
	hookTooltip(LBoomboxTickBox, boomboxTooltip)
	hookTooltip(FDetectorRowTickBox, fdetectorTooltip)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			boomboxTooltip:Destroy()
			fdetectorTooltip:Destroy()
		end
	end)
end;
task.spawn(C_1a6);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionLeftSide.MiscGroup.LBoomboxRow.LBoomboxScript
local function C_1ac()
local script = G2L["1ac"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	--------------------------------------------------
	-- GUI REFERENCES
	--------------------------------------------------
	local LBoomboxRow = script.Parent
	local MiscGroup = LBoomboxRow.Parent
	local MiscAccentColor = MiscGroup:WaitForChild("MiscAccentColor")
	local LBoomboxTickBox = LBoomboxRow:WaitForChild("LBoomboxTickBox")
	local ScreenGui = script:FindFirstAncestorOfClass("ScreenGui")
	
	--------------------------------------------------
	-- ENABLE STATE
	--------------------------------------------------
	local Enabled = false
	
	local function updateVisual()
		if Enabled then
			LBoomboxTickBox.BackgroundColor3 = MiscAccentColor.BackgroundColor3
			print("Boombox Detection ON")
		else
			LBoomboxTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			print("Boombox Detection OFF")
		end
	end
	
	LBoomboxTickBox.MouseButton1Click:Connect(function()
		Enabled = not Enabled
		updateVisual()
	end)
	
	updateVisual()
	
	--------------------------------------------------
	-- SETTINGS
	--------------------------------------------------
	local LOUD_THRESHOLD = 700
	local CHECK_INTERVAL = 15       -- seconds between loudness checks per player
	local FAR_DISTANCE = 100        -- studs — if farther than this, snap camera to measure
	
	--------------------------------------------------
	-- TABLES
	--------------------------------------------------
	local lastCheck = {}
	local detectedIDs = {}  -- store detected AudioIds per player to prevent repeats
	
	--------------------------------------------------
	-- ENSURE EVENTS
	--------------------------------------------------
	shared.GriefEvent = shared.GriefEvent or Instance.new("BindableEvent")
	shared.BoomboxLoudEvent = shared.BoomboxLoudEvent or Instance.new("BindableEvent")
	
	--------------------------------------------------
	-- GET BOOMBOX SOUND
	--------------------------------------------------
	local function getBoomboxSound(player)
		local char = workspace:FindFirstChild(player.Name)
		if not char then return nil end
		local boombox = char:FindFirstChild("SuperFlyGoldBoombox")
		if not boombox then return nil end
		for _, obj in ipairs(boombox:GetDescendants()) do
			if obj:IsA("Sound") then return obj end
		end
		return nil
	end
	
	--------------------------------------------------
	-- GET LOUDNESS (snap camera if far away)
	--------------------------------------------------
	local function getLoudness(player, sound)
		local myChar = LocalPlayer.Character
		local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
		local theirChar = workspace:FindFirstChild(player.Name)
		local theirHRP = theirChar and theirChar:FindFirstChild("HumanoidRootPart")
	
		if not myHRP or not theirHRP then
			return sound.PlaybackLoudness
		end
	
		local dist = (myHRP.Position - theirHRP.Position).Magnitude
	
		if dist <= FAR_DISTANCE then
			return sound.PlaybackLoudness
		end
	
		local cam = workspace.CurrentCamera
		local savedCFrame = cam.CFrame
		cam.CFrame = CFrame.new(theirHRP.Position)
		RunService.RenderStepped:Wait()
		local loudness = sound.PlaybackLoudness
		cam.CFrame = savedCFrame
		return loudness
	end
	
	--------------------------------------------------
	-- ALERT HELPERS
	--------------------------------------------------
	local function sendAlertChat(player)
		if not shared.sendChat then return end
		local alertRow = ScreenGui:FindFirstChild("AlertGriefRow", true)
		local alertTickBox = alertRow and alertRow:FindFirstChild("AlertGriefTickBox")
		if alertTickBox and alertTickBox:GetAttribute("Enabled") then
			shared.sendChat("🚨" .. player.DisplayName .. " is playing on the boombox too loud")
		end
	end
	
	local function playSFX()
		local sfxRow = ScreenGui:FindFirstChild("SFXGriefRow", true)
		local sfxTickBox = sfxRow and sfxRow:FindFirstChild("SFXGriefTickBox")
		if sfxTickBox and sfxTickBox:GetAttribute("Enabled") then
			local sound = LocalPlayer.PlayerGui:FindFirstChildOfClass("Sound")
			if sound then
				sound:Stop()
				sound:Play()
				task.delay(2, function() sound:Stop() end)
			end
		end
	end
	
	--------------------------------------------------
	-- TRIGGER
	--------------------------------------------------
	local function onBoomboxLoud(player, sound)
		if not Enabled then return end
	
		if not detectedIDs[player.UserId] then
			detectedIDs[player.UserId] = {}
		end
	
		local audioId = sound.SoundId
		-- only alert if this audio ID hasn't been detected for this player yet
		if detectedIDs[player.UserId][audioId] then
			return
		end
	
		detectedIDs[player.UserId][audioId] = true
	
		print("[BOOMBOX] " .. player.Name .. " is too loud: AudioID=" .. audioId)
	
		sendAlertChat(player)
		playSFX()
	
		local confirmRow = ScreenGui:FindFirstChild("ConfirmGriefRow", true)
		local confirmTickBox = confirmRow and confirmRow:FindFirstChild("ConfirmGriefTickBox")
		if confirmTickBox and confirmTickBox:GetAttribute("Enabled") then
			shared.BoomboxLoudEvent:Fire(player)
		end
	end
	
	--------------------------------------------------
	-- DETECTION LOOP
	--------------------------------------------------
	RunService.Heartbeat:Connect(function()
		if not Enabled then return end
	
		local now = tick()
	
		for _, player in ipairs(Players:GetPlayers()) do
			if player == LocalPlayer then continue end
	
			local userId = player.UserId
			if lastCheck[userId] and now - lastCheck[userId] < CHECK_INTERVAL then continue end
			lastCheck[userId] = now
	
			local sound = getBoomboxSound(player)
			if not sound or not sound.IsPlaying then continue end
	
			local loudness = getLoudness(player, sound)
			if loudness > LOUD_THRESHOLD then
				onBoomboxLoud(player, sound)
			end
		end
	end)
end;
task.spawn(C_1ac);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GSettingsScript
local function C_1b2()
local script = G2L["1b2"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local GSettingsGroup = script.Parent
	local GSettingsMinimize = GSettingsGroup.GSettingsTitle.GSettingsMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(GSettingsGroup:GetChildren()) do
			if child:IsA("Frame") and string.sub(child.Name, -3) == "Row" then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = GSettingsGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	GSettingsMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
	
		if isMinimized then
			GSettingsGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			GSettingsMinimize.Text = "+"
		else
			GSettingsGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			GSettingsMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_1b2);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GSettingsToolTip
local function C_1b3()
local script = G2L["1b3"];
	-- ============================================================
	-- ENABLE TOOLTIP SCRIPT
	-- ============================================================
	
	local Group = script.Parent
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then
				return p
			end
			p = p.Parent
		end
	end
	
	local ScreenGui = getScreenGui()
	if not ScreenGui then return end
	
	-- ============================================================
	-- FIND UI REFERENCES
	-- ============================================================
	
	local function findDescendant(name)
		for _, v in pairs(ScreenGui:GetDescendants()) do
			if v.Name == name then
				return v
			end
		end
	end
	
	-- Enable tickboxes (left side, unchanged)
	local EnableDeleteTickBox = findDescendant("EnableDeleteTickBox")
	local EnableToxifyTickBox = findDescendant("EnableToxifyTickBox")
	local EnableAnchoredTickBox = findDescendant("EnableAnchoredTickBox")
	local EnableSpamTickBox = findDescendant("EnableSpamTickBox")
	local EnableExploitsTickBox = findDescendant("EnableExploitsTickBox")
	local EnableHelp = findDescendant("EnableHelp")
	
	-- GSettings (right side)
	local CooldownGriefHelp = findDescendant("CooldownGriefHelp")
	local GriefEnlightenTickBox = findDescendant("GriefEnlightenTickBox")
	local GriefInGameTickBox = findDescendant("GriefInGameTickBox")
	local TimeFilterHelp = findDescendant("TimeFilterHelp")
	local WListHelp = findDescendant("WListHelp")
	
	-- ============================================================
	-- TOOLTIP BUILDER
	-- ============================================================
	
	local function makeTooltip(text)
		local frame = Instance.new("Frame")
		frame.BackgroundColor3 = Color3.fromRGB(16,16,16)
		frame.BorderSizePixel = 0
		frame.ZIndex = 100
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Size = UDim2.new(0, 212, 0, 0)
		frame.Visible = false
		frame.Parent = ScreenGui
	
		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(57,57,57)
		stroke.Parent = frame
	
		local padding = Instance.new("UIPadding")
		padding.PaddingTop = UDim.new(0,6)
		padding.PaddingBottom = UDim.new(0,6)
		padding.PaddingLeft = UDim.new(0,6)
		padding.PaddingRight = UDim.new(0,6)
		padding.Parent = frame
	
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Text = text
		label.TextColor3 = Color3.new(1,1,1)
		label.TextSize = 14
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Top
		label.Font = Enum.Font.GothamSemibold
		label.TextWrapped = true
		label.Size = UDim2.new(0,200,0,0)
		label.AutomaticSize = Enum.AutomaticSize.Y
		label.ZIndex = 101
		label.Parent = frame
	
		return frame
	end
	
	local function hookTooltip(button, tooltipFrame)
		if not button then return end
	
		button.MouseEnter:Connect(function()
			local abs = button.AbsolutePosition
			local size = button.AbsoluteSize
			tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
			tooltipFrame.Visible = true
		end)
	
		button.MouseLeave:Connect(function()
			tooltipFrame.Visible = false
		end)
	end
	
	-- ============================================================
	-- CREATE TOOLTIPS
	-- ============================================================
	
	-- Left side (enable group) - unchanged
	local deleteTooltip = makeTooltip(
		"Detects players deleting other people's builds."
	)
	local toxifyTooltip = makeTooltip(
		"Detects players toxifying other people's builds."
	)
	local anchoredTooltip = makeTooltip(
		"Detects players unanchoring other people's builds."
	)
	local spamTooltip = makeTooltip(
		"Detects players spamming blocks."
	)
	local exploitsTooltip = makeTooltip(
		"Detects players using grief auras, grief scripts and other exploits. You must turn on any grief detections to detect when they use exploits to grief with those tools.\n\nAlert in chat will send a different message when they use scripts to grief."
	)
	local helpTooltip = makeTooltip(
		"Enabling any detection will monitor for possible griefing.\nTurn on punish options to automatically punish detected players.\n\nView detection logs by typing /console in chat."
	)
	
	-- Right side (GSettings)
	local cooldownTooltip = makeTooltip(
		"Sets the cooldown in seconds before a player can be flagged for griefing again.\n\nSet to 0 for no cooldown."
	)
	local enlightenTooltip = makeTooltip(
		"When ON, all players are monitored for griefing.\n\nWhen OFF, players who own The Arkenstone in their backpack are ignored and will not be detected."
	)
	local inGameTooltip = makeTooltip(
		"When ON, deleting blocks counts as griefing even if the block owner is offline.\n\nWhen OFF, only deleting blocks of players currently in the server counts as griefing."
	)
	local timeFilterTooltip = makeTooltip(
		"Only detects players whose playtime (in seconds) is below this number.\n\nSet to 0 to detect everyone regardless of playtime."
	)
	local wlistTooltip = makeTooltip(
		"Whitelisted players will never be flagged for griefing, regardless of any other settings."
	)
	
	-- ============================================================
	-- HOOK THEM
	-- ============================================================
	
	-- Left side
	hookTooltip(EnableDeleteTickBox, deleteTooltip)
	hookTooltip(EnableToxifyTickBox, toxifyTooltip)
	hookTooltip(EnableAnchoredTickBox, anchoredTooltip)
	hookTooltip(EnableSpamTickBox, spamTooltip)
	hookTooltip(EnableExploitsTickBox, exploitsTooltip)
	hookTooltip(EnableHelp, helpTooltip)
	
	-- Right side
	hookTooltip(CooldownGriefHelp, cooldownTooltip)
	hookTooltip(GriefEnlightenTickBox, enlightenTooltip)
	hookTooltip(GriefInGameTickBox, inGameTooltip)
	hookTooltip(TimeFilterHelp, timeFilterTooltip)
	hookTooltip(WListHelp, wlistTooltip)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			deleteTooltip:Destroy()
			toxifyTooltip:Destroy()
			anchoredTooltip:Destroy()
			spamTooltip:Destroy()
			exploitsTooltip:Destroy()
			helpTooltip:Destroy()
			cooldownTooltip:Destroy()
			enlightenTooltip:Destroy()
			inGameTooltip:Destroy()
			timeFilterTooltip:Destroy()
			wlistTooltip:Destroy()
		end
	end)
end;
task.spawn(C_1b3);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.TimeFilterRow.TimeFilterChanger.TimeFilterScript
local function C_1bb()
local script = G2L["1bb"];
	local textBox = script.Parent -- TimeFilterChanger TextBox
	
	-- Show current filter on load and print it
	shared.GriefTimeFilter = shared.GriefTimeFilter or 0
	textBox.Text = tostring(shared.GriefTimeFilter)
	
	if shared.GriefTimeFilter == 0 then
		print("[Time Filter] Set to 0 - detecting everyone")
	else
		print("[Time Filter] Set to", shared.GriefTimeFilter, "- detecting players under", shared.GriefTimeFilter, "seconds playtime")
	end
	
	textBox.FocusLost:Connect(function()
		local num = tonumber(textBox.Text)
		if num and num >= 0 then
			shared.GriefTimeFilter = num
			if num == 0 then
				print("[Time Filter] Set to 0 - detecting everyone")
			else
				print("[Time Filter] Updated to", num, "- detecting players under", num, "seconds playtime")
			end
		else
			textBox.Text = tostring(shared.GriefTimeFilter)
			print("[Time Filter] Invalid input, keeping", shared.GriefTimeFilter)
		end
	end)
end;
task.spawn(C_1bb);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListLabelRow.WListLabelScript
local function C_1be()
local script = G2L["1be"];
	local WListLabelRow = script.Parent
	local WListLabelHelp = WListLabelRow.WListLabelHelp
	
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "WListTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Whitelisted players will be ignored by grief detection"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	WListLabelHelp.MouseEnter:Connect(function()
		local abs = WListLabelHelp.AbsolutePosition
		local size = WListLabelHelp.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	
	WListLabelHelp.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			tooltipFrame:Destroy()
		end
	end)
end;
task.spawn(C_1be);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.WListRow.WListScript
local function C_1c2()
local script = G2L["1c2"];
	local Players = game:GetService("Players")
	local UserInputService = game:GetService("UserInputService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local WListRow = script.Parent
	local WListDropdown = WListRow.WListDropdown
	
	local selectedPlayers = {}
	local isOpen = false
	local showDisplay = false
	local showFriends = false
	local friendsList = {}
	
	local ROW_HEIGHT = 16
	local MAX_ROWS = 7
	local BG = Color3.fromRGB(24, 24, 24)
	local BORDER = Color3.fromRGB(57, 57, 57)
	local WHITE = Color3.new(1, 1, 1)
	local DIM = Color3.fromRGB(120, 120, 120)
	local BLUE = Color3.fromRGB(0, 123, 255)
	local HOVER = Color3.fromRGB(36, 36, 36)
	local BTN_BG = Color3.fromRGB(36, 36, 36)
	
	if not game:IsLoaded() then
		game.Loaded:Wait()
	end
	
	repeat task.wait() until WListDropdown.AbsoluteSize.X > 0
	
	-- ============================================================
	-- MAIN LABEL + ARROW
	-- ============================================================
	local mainLabel = Instance.new("TextLabel")
	mainLabel.Name = "MainLabel"
	mainLabel.Size = UDim2.new(1, -24, 1, 0)
	mainLabel.Position = UDim2.new(0, 0, 0, 0)
	mainLabel.BackgroundTransparency = 1
	mainLabel.Text = "Select Players"
	mainLabel.TextColor3 = WHITE
	mainLabel.TextSize = 14
	mainLabel.Font = Enum.Font.Code
	mainLabel.TextXAlignment = Enum.TextXAlignment.Center
	mainLabel.TextTruncate = Enum.TextTruncate.AtEnd
	mainLabel.ZIndex = 2
	mainLabel.Parent = WListDropdown
	
	local mainArrow = Instance.new("TextLabel")
	mainArrow.Name = "MainArrow"
	mainArrow.Size = UDim2.new(0, 24, 1, 0)
	mainArrow.Position = UDim2.new(1, -24, 0, 0)
	mainArrow.BackgroundTransparency = 1
	mainArrow.Text = "▼"
	mainArrow.TextColor3 = WHITE
	mainArrow.TextSize = 14
	mainArrow.Font = Enum.Font.Code
	mainArrow.TextXAlignment = Enum.TextXAlignment.Center
	mainArrow.ZIndex = 2
	mainArrow.Parent = WListDropdown
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	local BOTTOM_BAR_HEIGHT = ROW_HEIGHT + 2
	local TOTAL_BOTTOM_HEIGHT = (BOTTOM_BAR_HEIGHT * 2) + 1
	
	-- ============================================================
	-- PANEL
	-- ============================================================
	local panel = Instance.new("Frame")
	panel.Name = "WListPanel"
	panel.BackgroundColor3 = BG
	panel.BorderSizePixel = 0
	panel.Size = UDim2.new(0, 161, 0, ROW_HEIGHT)
	panel.ZIndex = 999
	panel.Visible = false
	panel.Parent = getScreenGui()
	
	local panelStroke = Instance.new("UIStroke")
	panelStroke.Color = BORDER
	panelStroke.Thickness = 1
	panelStroke.Parent = panel
	
	local searchBox = Instance.new("TextBox")
	searchBox.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
	searchBox.Position = UDim2.new(0, 0, 0, 0)
	searchBox.BackgroundColor3 = BG
	searchBox.BorderSizePixel = 0
	searchBox.Text = ""
	searchBox.PlaceholderText = "Search for users..."
	searchBox.PlaceholderColor3 = DIM
	searchBox.TextColor3 = WHITE
	searchBox.TextSize = 14
	searchBox.Font = Enum.Font.Code
	searchBox.TextXAlignment = Enum.TextXAlignment.Left
	searchBox.ClearTextOnFocus = false
	searchBox.ZIndex = 1000
	searchBox.Parent = panel
	
	local searchPad = Instance.new("UIPadding")
	searchPad.PaddingLeft = UDim.new(0, 6)
	searchPad.PaddingRight = UDim.new(0, 24)
	searchPad.Parent = searchBox
	
	local searchArrow = Instance.new("TextButton")
	searchArrow.Size = UDim2.new(0, 24, 0, ROW_HEIGHT)
	searchArrow.Position = UDim2.new(1, -24, 0, 0)
	searchArrow.BackgroundTransparency = 1
	searchArrow.Text = "▲"
	searchArrow.TextColor3 = WHITE
	searchArrow.TextSize = 14
	searchArrow.Font = Enum.Font.Code
	searchArrow.BorderSizePixel = 0
	searchArrow.ZIndex = 1001
	searchArrow.Parent = panel
	
	local scrollFrame = Instance.new("ScrollingFrame")
	scrollFrame.Position = UDim2.new(0, 0, 0, ROW_HEIGHT)
	scrollFrame.Size = UDim2.new(1, 0, 0, 0)
	scrollFrame.BackgroundTransparency = 1
	scrollFrame.BorderSizePixel = 0
	scrollFrame.ScrollBarThickness = 2
	scrollFrame.ScrollBarImageColor3 = BORDER
	scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
	scrollFrame.ZIndex = 1000
	scrollFrame.Parent = panel
	
	local listLayout = Instance.new("UIListLayout")
	listLayout.SortOrder = Enum.SortOrder.LayoutOrder
	listLayout.Parent = scrollFrame
	
	local bottomBar = Instance.new("Frame")
	bottomBar.Size = UDim2.new(1, 0, 0, BOTTOM_BAR_HEIGHT)
	bottomBar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
	bottomBar.BorderSizePixel = 0
	bottomBar.ZIndex = 1000
	bottomBar.Parent = panel
	
	local bottomStroke = Instance.new("UIStroke")
	bottomStroke.Color = BORDER
	bottomStroke.Thickness = 1
	bottomStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	bottomStroke.Parent = bottomBar
	
	-- row 1: Select All / Unselect All
	local selectAllBtn = Instance.new("TextButton")
	selectAllBtn.Size = UDim2.new(0.5, -1, 0, BOTTOM_BAR_HEIGHT)
	selectAllBtn.Position = UDim2.new(0, 0, 0, 0)
	selectAllBtn.BackgroundColor3 = BTN_BG
	selectAllBtn.BorderSizePixel = 0
	selectAllBtn.Text = "Select All"
	selectAllBtn.TextColor3 = WHITE
	selectAllBtn.TextSize = 11
	selectAllBtn.Font = Enum.Font.Code
	selectAllBtn.ZIndex = 1001
	selectAllBtn.Parent = bottomBar
	
	local divider1 = Instance.new("Frame")
	divider1.Size = UDim2.new(0, 1, 0, BOTTOM_BAR_HEIGHT)
	divider1.Position = UDim2.new(0.5, -1, 0, 0)
	divider1.BackgroundColor3 = BORDER
	divider1.BorderSizePixel = 0
	divider1.ZIndex = 1001
	divider1.Parent = bottomBar
	
	local unselectAllBtn = Instance.new("TextButton")
	unselectAllBtn.Size = UDim2.new(0.5, 0, 0, BOTTOM_BAR_HEIGHT)
	unselectAllBtn.Position = UDim2.new(0.5, 1, 0, 0)
	unselectAllBtn.BackgroundColor3 = BTN_BG
	unselectAllBtn.BorderSizePixel = 0
	unselectAllBtn.Text = "Unselect All"
	unselectAllBtn.TextColor3 = WHITE
	unselectAllBtn.TextSize = 11
	unselectAllBtn.Font = Enum.Font.Code
	unselectAllBtn.ZIndex = 1001
	unselectAllBtn.Parent = bottomBar
	
	-- divider between rows
	local rowDivider = Instance.new("Frame")
	rowDivider.Size = UDim2.new(1, 0, 0, 1)
	rowDivider.Position = UDim2.new(0, 0, 0, BOTTOM_BAR_HEIGHT)
	rowDivider.BackgroundColor3 = BORDER
	rowDivider.BorderSizePixel = 0
	rowDivider.ZIndex = 1001
	rowDivider.Parent = bottomBar
	
	-- row 2: Show Display / Select Admin
	local displayBtn = Instance.new("TextButton")
	displayBtn.Size = UDim2.new(0.5, -1, 0, BOTTOM_BAR_HEIGHT)
	displayBtn.Position = UDim2.new(0, 0, 0, BOTTOM_BAR_HEIGHT + 1)
	displayBtn.BackgroundColor3 = BTN_BG
	displayBtn.BorderSizePixel = 0
	displayBtn.Text = "Show Display"
	displayBtn.TextColor3 = WHITE
	displayBtn.TextSize = 11
	displayBtn.Font = Enum.Font.Code
	displayBtn.ZIndex = 1001
	displayBtn.Parent = bottomBar
	
	local divider2 = Instance.new("Frame")
	divider2.Size = UDim2.new(0, 1, 0, BOTTOM_BAR_HEIGHT)
	divider2.Position = UDim2.new(0.5, -1, 0, BOTTOM_BAR_HEIGHT + 1)
	divider2.BackgroundColor3 = BORDER
	divider2.BorderSizePixel = 0
	divider2.ZIndex = 1001
	divider2.Parent = bottomBar
	
	local adminBtn = Instance.new("TextButton")
	adminBtn.Size = UDim2.new(0.5, 0, 0, BOTTOM_BAR_HEIGHT)
	adminBtn.Position = UDim2.new(0.5, 1, 0, BOTTOM_BAR_HEIGHT + 1)
	adminBtn.BackgroundColor3 = BTN_BG
	adminBtn.BorderSizePixel = 0
	adminBtn.Text = "Select Admin"
	adminBtn.TextColor3 = WHITE
	adminBtn.TextSize = 11
	adminBtn.Font = Enum.Font.Code
	adminBtn.ZIndex = 1001
	adminBtn.Parent = bottomBar
	
	-- ============================================================
	-- THUMBNAILS
	-- ============================================================
	local cachedThumbs = {}
	local function getThumb(userId)
		if cachedThumbs[userId] then return cachedThumbs[userId] end
		local ok, img = pcall(function()
			return Players:GetUserThumbnailAsync(userId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size48x48)
		end)
		local result = ok and img or ""
		cachedThumbs[userId] = result
		return result
	end
	
	local function loadFriends()
		friendsList = {}
		local ok, pages = pcall(function()
			return LocalPlayer:GetFriendsAsync(200)
		end)
		if not ok then return end
		local function processPage()
			for _, v in pairs(pages:GetCurrentPage()) do
				friendsList[v.Id] = true
			end
		end
		processPage()
		while not pages.IsFinished do
			local ok2 = pcall(function() pages:AdvanceToNextPageAsync() end)
			if not ok2 then break end
			processPage()
		end
	end
	
	task.spawn(loadFriends)
	
	-- ============================================================
	-- LABEL UPDATE
	-- ============================================================
	local function updateMainLabel()
		local count = 0
		local names = {}
		for name, _ in pairs(selectedPlayers) do
			count = count + 1
			table.insert(names, name)
		end
		if count == 0 then
			mainLabel.Text = "Select Players"
		elseif count <= 2 then
			mainLabel.Text = table.concat(names, ", ")
		else
			mainLabel.Text = count .. " Players Selected"
		end
	end
	
	-- ============================================================
	-- WHITELIST ATTRIBUTE (so GriefPro/GriefFling can read it)
	-- ============================================================
	local function updateWhitelist()
		local names = {}
		for name, _ in pairs(selectedPlayers) do
			table.insert(names, name)
		end
		WListRow:SetAttribute("Whitelist", table.concat(names, ","))
		-- Sync to shared so grief detection can read it directly
		shared.GriefWhitelist = selectedPlayers
	end
	
	-- ============================================================
	-- LIST BUILD
	-- ============================================================
	local function updatePanelSize(rowCount)
		local visibleRows = math.min(rowCount, MAX_ROWS)
		local scrollH = visibleRows * ROW_HEIGHT
		scrollFrame.Size = UDim2.new(1, 0, 0, scrollH)
		panel.Size = UDim2.new(0, 161, 0, ROW_HEIGHT + scrollH + TOTAL_BOTTOM_HEIGHT)
		scrollFrame.CanvasSize = UDim2.new(0, 0, 0, rowCount * ROW_HEIGHT)
		bottomBar.Size = UDim2.new(1, 0, 0, TOTAL_BOTTOM_HEIGHT)
		bottomBar.Position = UDim2.new(0, 0, 0, ROW_HEIGHT + scrollH)
	end
	
	local function buildList(filter)
		for _, child in pairs(scrollFrame:GetChildren()) do
			if child:IsA("Frame") then child:Destroy() end
		end
	
		local playerList = {}
		for _, p in pairs(Players:GetPlayers()) do
			table.insert(playerList, p)
		end
	
		table.sort(playerList, function(a, b)
			local nameA = showDisplay and a.DisplayName or a.Name
			local nameB = showDisplay and b.DisplayName or b.Name
			return nameA:lower() < nameB:lower()
		end)
	
		local filtered = {}
		for _, p in ipairs(playerList) do
			local displayText = showDisplay and p.DisplayName or p.Name
			if filter == "" or displayText:lower():find(filter:lower(), 1, true) then
				table.insert(filtered, p)
			end
		end
	
		for i, p in ipairs(filtered) do
			local isSelected = selectedPlayers[p.Name] ~= nil
			local displayText = showDisplay and p.DisplayName or p.Name
	
			local row = Instance.new("Frame")
			row.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
			row.BackgroundColor3 = isSelected and HOVER or BG
			row.BorderSizePixel = 0
			row.LayoutOrder = i
			row.ZIndex = 1001
			row.Parent = scrollFrame
	
			local thumb = Instance.new("ImageLabel")
			thumb.Size = UDim2.new(0, 12, 0, 12)
			thumb.Position = UDim2.new(1, -16, 0.5, -6)
			thumb.BackgroundTransparency = 1
			thumb.Image = ""
			thumb.ZIndex = 1002
			thumb.Parent = row
	
			task.spawn(function()
				thumb.Image = getThumb(p.UserId)
			end)
	
			local nameLabel = Instance.new("TextLabel")
			nameLabel.Size = UDim2.new(1, -22, 1, 0)
			nameLabel.Position = UDim2.new(0, 6, 0, 0)
			nameLabel.BackgroundTransparency = 1
			nameLabel.Text = displayText
			nameLabel.TextColor3 = isSelected and BLUE or WHITE
			nameLabel.TextSize = 14
			nameLabel.Font = Enum.Font.Code
			nameLabel.TextXAlignment = Enum.TextXAlignment.Left
			nameLabel.TextTruncate = Enum.TextTruncate.AtEnd
			nameLabel.ZIndex = 1002
			nameLabel.Parent = row
	
			local rowBtn = Instance.new("TextButton")
			rowBtn.Size = UDim2.new(1, 0, 1, 0)
			rowBtn.BackgroundTransparency = 1
			rowBtn.Text = ""
			rowBtn.ZIndex = 1003
			rowBtn.Parent = row
	
			rowBtn.MouseEnter:Connect(function()
				row.BackgroundColor3 = HOVER
			end)
			rowBtn.MouseLeave:Connect(function()
				if not selectedPlayers[p.Name] then
					row.BackgroundColor3 = BG
				end
			end)
	
			rowBtn.MouseButton1Click:Connect(function()
				if selectedPlayers[p.Name] then
					selectedPlayers[p.Name] = nil
					nameLabel.TextColor3 = WHITE
					row.BackgroundColor3 = BG
				else
					selectedPlayers[p.Name] = true
					nameLabel.TextColor3 = BLUE
					row.BackgroundColor3 = HOVER
				end
				updateMainLabel()
				updateWhitelist()
			end)
		end
	
		updatePanelSize(#filtered)
	end
	
	-- ============================================================
	-- SELECT ALL / UNSELECT ALL
	-- ============================================================
	selectAllBtn.MouseButton1Click:Connect(function()
		for _, p in pairs(Players:GetPlayers()) do
			if p ~= LocalPlayer then
				selectedPlayers[p.Name] = true
			end
		end
		updateMainLabel()
		updateWhitelist()
		buildList(searchBox.Text)
	end)
	
	unselectAllBtn.MouseButton1Click:Connect(function()
		selectedPlayers = {}
		updateMainLabel()
		updateWhitelist()
		buildList(searchBox.Text)
	end)
	
	-- ============================================================
	-- SHOW DISPLAY / SHOW USERNAME
	-- ============================================================
	displayBtn.MouseButton1Click:Connect(function()
		showDisplay = not showDisplay
		displayBtn.Text = showDisplay and "Show Username" or "Show Display"
		updateMainLabel()
		if isOpen then buildList(searchBox.Text) end
	end)
	
	-- ============================================================
	-- SELECT ADMIN / UNSELECT ADMIN
	-- ============================================================
	local adminSelected = false
	local lastAdminName = nil
	
	local function getAdminPlayer()
		local chosenTeam = game:GetService("Teams"):FindFirstChild("Chosen")
		if not chosenTeam then return nil end
		for _, p in pairs(Players:GetPlayers()) do
			if p.Team == chosenTeam then
				return p
			end
		end
		return nil
	end
	
	adminBtn.MouseButton1Click:Connect(function()
		local admin = getAdminPlayer()
	
		if not adminSelected then
			-- select admin
			if admin then
				if lastAdminName and lastAdminName ~= admin.Name then
					selectedPlayers[lastAdminName] = nil
				end
				selectedPlayers[admin.Name] = true
				lastAdminName = admin.Name
				adminSelected = true
				adminBtn.Text = "Unselect Admin"
			end
		else
			-- unselect admin
			if lastAdminName then
				selectedPlayers[lastAdminName] = nil
				lastAdminName = nil
			end
			adminSelected = false
			adminBtn.Text = "Select Admin"
		end
	
		updateMainLabel()
		updateWhitelist()
		if isOpen then buildList(searchBox.Text) end
	end)
	
	-- update admin selection if team changes
	Players.PlayerAdded:Connect(function(p)
		task.wait(1)
		if adminSelected then
			local admin = getAdminPlayer()
			if admin and admin.Name ~= lastAdminName then
				if lastAdminName then selectedPlayers[lastAdminName] = nil end
				selectedPlayers[admin.Name] = true
				lastAdminName = admin.Name
				updateMainLabel()
				updateWhitelist()
				if isOpen then buildList(searchBox.Text) end
			end
		end
	end)
	
	-- ============================================================
	-- OPEN / CLOSE
	-- ============================================================
	local function openDropdown()
		isOpen = true
		mainLabel.Visible = false
		mainArrow.Visible = false
	
		repeat task.wait() until WListDropdown.AbsoluteSize.X > 0
	
		local absPos = WListDropdown.AbsolutePosition
		local absSize = WListDropdown.AbsoluteSize
		panel.Position = UDim2.new(0, absPos.X + (absSize.X - 161) / 2, 0, absPos.Y + absSize.Y)
		panel.Size = UDim2.new(0, absSize.X, 0, ROW_HEIGHT)
		panel.Visible = true
	
		searchBox.Text = ""
		buildList("")
	end
	
	local function closeDropdown()
		isOpen = false
		panel.Visible = false
		mainLabel.Visible = true
		mainArrow.Visible = true
		mainArrow.Text = "▼"
		updateMainLabel()
	end
	
	WListDropdown.MouseButton1Click:Connect(function()
		if isOpen then
			closeDropdown()
		else
			openDropdown()
		end
	end)
	
	searchArrow.MouseButton1Click:Connect(function()
		closeDropdown()
	end)
	
	searchBox:GetPropertyChangedSignal("Text"):Connect(function()
		buildList(searchBox.Text)
	end)
	
	UserInputService.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 and isOpen then
			local mousePos = input.Position
			local panelPos = panel.AbsolutePosition
			local panelSize = panel.AbsoluteSize
			local btnPos = WListDropdown.AbsolutePosition
			local btnSize = WListDropdown.AbsoluteSize
	
			local inPanel = mousePos.X >= panelPos.X and mousePos.X <= panelPos.X + panelSize.X
				and mousePos.Y >= panelPos.Y and mousePos.Y <= panelPos.Y + panelSize.Y
			local inBtn = mousePos.X >= btnPos.X and mousePos.X <= btnPos.X + btnSize.X
				and mousePos.Y >= btnPos.Y and mousePos.Y <= btnPos.Y + btnSize.Y
	
			if not inPanel and not inBtn then
				closeDropdown()
			end
		end
	end)
	
	Players.PlayerRemoving:Connect(function(p)
		if selectedPlayers[p.Name] then
			selectedPlayers[p.Name] = nil
			updateMainLabel()
			updateWhitelist()
		end
		if isOpen then buildList(searchBox.Text) end
	end)
	
	Players.PlayerAdded:Connect(function()
		if isOpen then buildList(searchBox.Text) end
	end)
	
	WListRow:SetAttribute("Whitelist", "")
	
	-- AUTO-WHITELIST LOCAL PLAYER
	selectedPlayers[LocalPlayer.Name] = true
	updateMainLabel()
	updateWhitelist()
end;
task.spawn(C_1c2);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefInGameRow.GriefInGameScript
local function C_1c5()
local script = G2L["1c5"];
	local Players = game:GetService("Players")
	--------------------------------------------------
	-- STRUCTURE
	--------------------------------------------------
	local GriefInGameRow = script.Parent
	local GSettingsGroup = GriefInGameRow.Parent
	local RightSide = GSettingsGroup.Parent
	local Page = RightSide.Parent
	local ContentArea = Page.Parent
	local MainFrame = ContentArea.Parent
	local ScreenGui = MainFrame.Parent
	local GriefInGameTickBox = GriefInGameRow:WaitForChild("GriefInGameTickBox")
	local GriefInGameAccent = GSettingsGroup.Parent.Parent.TCODetectionLeftSide.EnableGroup:WaitForChild("EnableAccentColor")
	--------------------------------------------------
	-- ATTRIBUTE + SHARED SYNC
	--------------------------------------------------
	if ScreenGui:GetAttribute("GriefInGameEnabled") == nil then
		ScreenGui:SetAttribute("GriefInGameEnabled", true)
	end
	
	local function isEnabled()
		return ScreenGui:GetAttribute("GriefInGameEnabled") == true
	end
	
	local function setState(state)
		ScreenGui:SetAttribute("GriefInGameEnabled", state)
		-- Sync to shared so grief detection script can read it
		shared.GriefInGameEnabled = state
	
		if state then
			GriefInGameTickBox.BackgroundColor3 = GriefInGameAccent.BackgroundColor3
			print("[Grief InGame] ON - deleting offline player bricks counts as griefing")
		else
			GriefInGameTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			print("[Grief InGame] OFF - only deleting bricks of players currently in game counts as griefing")
		end
	end
	--------------------------------------------------
	-- CLICK
	--------------------------------------------------
	GriefInGameTickBox.MouseButton1Click:Connect(function()
		setState(not isEnabled())
	end)
	--------------------------------------------------
	-- AUTO ENABLE ON EXECUTE
	--------------------------------------------------
	setState(true)
end;
task.spawn(C_1c5);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.GriefEnlightenRow.GriefEnlightenScript
local function C_1c9()
local script = G2L["1c9"];
	local Players = game:GetService("Players")
	--------------------------------------------------
	-- STRUCTURE
	--------------------------------------------------
	local GriefEnlightenRow = script.Parent
	local GSettingsGroup = GriefEnlightenRow.Parent
	local RightSide = GSettingsGroup.Parent
	local Page = RightSide.Parent
	local ContentArea = Page.Parent
	local MainFrame = ContentArea.Parent
	local ScreenGui = MainFrame.Parent
	local GriefEnlightenTickBox = GriefEnlightenRow:WaitForChild("GriefEnlightenTickBox")
	local GriefEnlightenAccent = GSettingsGroup.Parent.Parent.TCODetectionLeftSide.EnableGroup:WaitForChild("EnableAccentColor")
	--------------------------------------------------
	-- ATTRIBUTE + SHARED SYNC
	--------------------------------------------------
	if ScreenGui:GetAttribute("GriefEnlightenEnabled") == nil then
		ScreenGui:SetAttribute("GriefEnlightenEnabled", true)
	end
	
	local function isEnabled()
		return ScreenGui:GetAttribute("GriefEnlightenEnabled") == true
	end
	
	local function setState(state)
		ScreenGui:SetAttribute("GriefEnlightenEnabled", state)
		-- Sync to shared so the grief detection script can read it
		shared.GriefEnlightenEnabled = state
	
		if state then
			GriefEnlightenTickBox.BackgroundColor3 = GriefEnlightenAccent.BackgroundColor3
			print("[Grief Enlighten] ON - detecting everyone")
		else
			GriefEnlightenTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			print("[Grief Enlighten] OFF - only detecting players without The Arkenstone")
		end
	end
	--------------------------------------------------
	-- CLICK
	--------------------------------------------------
	GriefEnlightenTickBox.MouseButton1Click:Connect(function()
		setState(not isEnabled())
	end)
	--------------------------------------------------
	-- AUTO ENABLE
	--------------------------------------------------
	setState(true)
end;
task.spawn(C_1c9);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.GSettingsGroup.CooldownGriefRow.CooldownGriefChanger.CooldownScript
local function C_1cf()
local script = G2L["1cf"];
	local textBox = script.Parent -- CooldownGriefChanger TextBox
	
	-- Show current cooldown on load and print it
	shared.GriefCooldown = shared.GriefCooldown or 30
	textBox.Text = tostring(shared.GriefCooldown)
	print("[Grief Cooldown] Set to", shared.GriefCooldown)
	
	textBox.FocusLost:Connect(function(enterPressed)
		local num = tonumber(textBox.Text)
		if num and num >= 0 then -- Allow 0 and above
			shared.GriefCooldown = num
			if num == 0 then
				print("[Grief Cooldown] Set to 0 - no cooldown active")
			else
				print("[Grief Cooldown] Updated to", num)
			end
		else
			-- Reset to current valid value if bad input (letters, negatives)
			textBox.Text = tostring(shared.GriefCooldown)
			print("[Grief Cooldown] Invalid input, keeping", shared.GriefCooldown)
		end
	end)
end;
task.spawn(C_1cf);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.PunishScript
local function C_1d2()
local script = G2L["1d2"];
	local PunishGroup = script.Parent
	local PunishMinimize = PunishGroup.PunishTitle.PunishMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(PunishGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = PunishGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	PunishMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			PunishGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			PunishMinimize.Text = "+"
		else
			PunishGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			PunishMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_1d2);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.PunishToolTip
local function C_1d3()
local script = G2L["1d3"];
	-- ============================================================
	-- GRIEF TICKBOX TOOLTIP SCRIPT
	-- Tooltips for grief tickboxes only
	-- ============================================================
	
	local Group = script.Parent
	
	-- ============================================================
	-- SCREENGUI HELPER
	-- ============================================================
	
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then
				return p
			end
			p = p.Parent
		end
	end
	
	local ScreenGui = getScreenGui()
	if not ScreenGui then return end
	
	-- ============================================================
	-- FIND TICKBOXES
	-- ============================================================
	
	local function findDescendant(name)
		for _, v in pairs(ScreenGui:GetDescendants()) do
			if v.Name == name then
				return v
			end
		end
	end
	
	local FlingGriefTickBox = findDescendant("FlingGriefTickBox")
	local AlertGriefTickBox = findDescendant("AlertGriefTickBox")
	local ConfirmGriefTickBox = findDescendant("ConfirmGriefTickBox")
	local SFXGriefTickBox = findDescendant("SFXGriefTickBox")
	
	-- ============================================================
	-- TOOLTIP BUILDER
	-- ============================================================
	
	local function makeTooltip(text)
		local frame = Instance.new("Frame")
		frame.BackgroundColor3 = Color3.fromRGB(16,16,16)
		frame.BorderSizePixel = 0
		frame.ZIndex = 100
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Size = UDim2.new(0, 212, 0, 0)
		frame.Visible = false
		frame.Parent = ScreenGui
	
		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(57,57,57)
		stroke.Parent = frame
	
		local padding = Instance.new("UIPadding")
		padding.PaddingTop = UDim.new(0,6)
		padding.PaddingBottom = UDim.new(0,6)
		padding.PaddingLeft = UDim.new(0,6)
		padding.PaddingRight = UDim.new(0,6)
		padding.Parent = frame
	
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Text = text
		label.TextColor3 = Color3.new(1,1,1)
		label.TextSize = 14
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Top
		label.Font = Enum.Font.GothamSemibold
		label.TextWrapped = true
		label.Size = UDim2.new(0,200,0,0)
		label.AutomaticSize = Enum.AutomaticSize.Y
		label.ZIndex = 101
		label.Parent = frame
	
		return frame
	end
	
	local function hookTooltip(button, tooltipFrame)
		if not button then return end
	
		button.MouseEnter:Connect(function()
			local abs = button.AbsolutePosition
			local size = button.AbsoluteSize
			tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
			tooltipFrame.Visible = true
		end)
	
		button.MouseLeave:Connect(function()
			tooltipFrame.Visible = false
		end)
	end
	
	-- ============================================================
	-- CREATE TOOLTIPS
	-- ============================================================
	
	local flingTooltip = makeTooltip(
		"Automatically flings players detected griefing."
	)
	
	local alertTooltip = makeTooltip(
		"Sends an alert when a player is detected griefing."
	)
	
	local confirmTooltip = makeTooltip(
		"Makes you pov the player that triggered the detection and gives you a list of buttons to punish the player or cancel the detection."
	)
	
	local sfxTooltip = makeTooltip(
		"Plays an alert sound when a player is detected griefing."
	)
	
	-- ============================================================
	-- HOOK THEM
	-- ============================================================
	
	hookTooltip(FlingGriefTickBox, flingTooltip)
	hookTooltip(AlertGriefTickBox, alertTooltip)
	hookTooltip(ConfirmGriefTickBox, confirmTooltip)
	hookTooltip(SFXGriefTickBox, sfxTooltip)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			flingTooltip:Destroy()
			alertTooltip:Destroy()
			confirmTooltip:Destroy()
		end
	end)
end;
task.spawn(C_1d3);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.AlertGriefRow.AlertGriefScript
local function C_1d9()
local script = G2L["1d9"];
	local Players = game:GetService("Players")
	local TextChatService = game:GetService("TextChatService")
	local AlertGriefRow = script.Parent
	local PunishGroup = AlertGriefRow.Parent
	local PunishAccentColor = PunishGroup:WaitForChild("PunishAccentColor")
	local AlertGriefTickBox = AlertGriefRow:WaitForChild("AlertGriefTickBox")
	--------------------------------------------------
	-- ENABLE STATE
	--------------------------------------------------
	AlertGriefTickBox:SetAttribute("Enabled", false)
	local function updateVisual()
		if AlertGriefTickBox:GetAttribute("Enabled") then
			AlertGriefTickBox.BackgroundColor3 = PunishAccentColor.BackgroundColor3
		else
			AlertGriefTickBox.BackgroundColor3 = Color3.fromRGB(0,0,0)
		end
	end
	AlertGriefTickBox.MouseButton1Click:Connect(function()
		local state = not AlertGriefTickBox:GetAttribute("Enabled")
		AlertGriefTickBox:SetAttribute("Enabled", state)
		updateVisual()
	end)
	updateVisual()
	--------------------------------------------------
	-- ENSURE EVENTS EXIST
	--------------------------------------------------
	shared.GriefEvent = shared.GriefEvent or Instance.new("BindableEvent")
	shared.DeleteGriefEvent = shared.DeleteGriefEvent or Instance.new("BindableEvent")
	shared.ToxifyGriefEvent = shared.ToxifyGriefEvent or Instance.new("BindableEvent")
	shared.UnanchorGriefEvent = shared.UnanchorGriefEvent or Instance.new("BindableEvent")
	shared.BuildGriefEvent = shared.BuildGriefEvent or Instance.new("BindableEvent")
	shared.ExploitGriefEvent = shared.ExploitGriefEvent or Instance.new("BindableEvent")
	--------------------------------------------------
	-- CHAT FUNCTION
	--------------------------------------------------
	local function sendChat(message)
		if shared.sendChat then
			shared.sendChat(message)
		else
			TextChatService.TextChannels.RBXGeneral:SendAsync(message)
		end
	end
	--------------------------------------------------
	-- ALERT MESSAGES
	--------------------------------------------------
	local griefMessages = {
		["delete"]   = "🚨 %s has been detected deleting another player's blocks",
		["toxify"]   = "🚨 %s has been detected toxifying another player's blocks",
		["unanchor"] = "🚨 %s has been detected unanchoring another player's blocks",
		["build"]    = "🚨 %s has been detected building too fast",
		["exploit"]  = "🚨 %s has been detected using exploits to grief",
	}
	--------------------------------------------------
	-- LISTEN FOR GRIEF EVENT ONLY
	--------------------------------------------------
	shared.GriefEvent.Event:Connect(function(player, griefType)
		if not AlertGriefTickBox:GetAttribute("Enabled") then return end
		local template = griefMessages[griefType]
		if not template then return end
		sendChat(string.format(template, player.DisplayName))
	end)
end;
task.spawn(C_1d9);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.ConfirmGriefRow.ConfirmGriefScript
local function C_1dd()
local script = G2L["1dd"];
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local LocalPlayer = Players.LocalPlayer
	
	--------------------------------------------------
	-- GUI STRUCTURE
	--------------------------------------------------
	
	local ConfirmGriefRow = script.Parent
	local PunishGroup = ConfirmGriefRow.Parent
	local PunishAccentColor = PunishGroup:WaitForChild("PunishAccentColor")
	local ConfirmGriefTickBox = ConfirmGriefRow:WaitForChild("ConfirmGriefTickBox")
	
	--------------------------------------------------
	-- SCREEN GUI
	--------------------------------------------------
	
	local ScreenGui = script:FindFirstAncestorOfClass("ScreenGui")
	local ConfirmGriefFrame = ScreenGui:WaitForChild("ConfirmGriefFrame")
	
	local ConfirmTimer = ConfirmGriefFrame:WaitForChild("ConfirmTimer")
	local GriefTargetLabel = ConfirmGriefFrame:WaitForChild("GriefTargetLabel")
	local FlingConfirm = ConfirmGriefFrame:WaitForChild("FlingConfirm")
	local AlertConfirm = ConfirmGriefFrame:WaitForChild("AlertConfirm")
	local CancelConfirm = ConfirmGriefFrame:WaitForChild("CancelConfirm")
	local FalseAlertConfirm = ConfirmGriefFrame:WaitForChild("FalseAlertConfirm")
	local PauseConfirm = ConfirmGriefFrame:WaitForChild("PauseConfirm")
	
	ConfirmGriefFrame.Visible = false
	
	--------------------------------------------------
	-- ENABLE STATE (SAFE MUTUAL EXCLUSIVE)
	--------------------------------------------------
	
	ConfirmGriefTickBox:SetAttribute("Enabled", false)
	
	local function updateVisual()
		if ConfirmGriefTickBox:GetAttribute("Enabled") then
			ConfirmGriefTickBox.BackgroundColor3 = PunishAccentColor.BackgroundColor3
		else
			ConfirmGriefTickBox.BackgroundColor3 = Color3.fromRGB(0,0,0)
		end
	end
	
	ConfirmGriefTickBox.MouseButton1Click:Connect(function()
		local currentlyEnabled = ConfirmGriefTickBox:GetAttribute("Enabled")
		if currentlyEnabled then
			ConfirmGriefTickBox:SetAttribute("Enabled", false)
			PunishGroup:SetAttribute("ActivePunishMode", nil)
		else
			PunishGroup:SetAttribute("ActivePunishMode", "Confirm")
			ConfirmGriefTickBox:SetAttribute("Enabled", true)
		end
		updateVisual()
	end)
	
	PunishGroup:GetAttributeChangedSignal("ActivePunishMode"):Connect(function()
		local mode = PunishGroup:GetAttribute("ActivePunishMode")
		if mode ~= "Confirm" and ConfirmGriefTickBox:GetAttribute("Enabled") then
			ConfirmGriefTickBox:SetAttribute("Enabled", false)
			updateVisual()
		end
	end)
	
	updateVisual()
	
	--------------------------------------------------
	-- CAMERA
	--------------------------------------------------
	
	local cam = workspace.CurrentCamera
	
	local function spectatePlayer(player)
		if player and player.Character then
			local hum = player.Character:FindFirstChildOfClass("Humanoid")
			if hum then cam.CameraSubject = hum end
		end
	end
	
	local function spectateBack()
		if LocalPlayer.Character then
			local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
			if hum then cam.CameraSubject = hum end
		end
	end
	
	--------------------------------------------------
	-- FLING STATE
	--------------------------------------------------
	
	local flingCancelled = false
	
	--------------------------------------------------
	-- FLING
	--------------------------------------------------
	
	local function flingPlayer(TargetPlayer)
		local Character = LocalPlayer.Character
		local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
		local RootPart = Humanoid and Humanoid.RootPart
	
		local TCharacter = TargetPlayer.Character
		if not TCharacter then return false end
	
		local THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
		local TRootPart = THumanoid and THumanoid.RootPart
		local THead = TCharacter:FindFirstChild("Head")
		local Accessory = TCharacter:FindFirstChildOfClass("Accessory")
		local Handle = Accessory and Accessory:FindFirstChild("Handle")
	
		if not (Character and Humanoid and RootPart) then return false end
	
		if RootPart.Velocity.Magnitude < 50 then
			getgenv().OldPos = RootPart.CFrame
		end
	
		if THumanoid and THumanoid.Sit then return false end
	
		if THead then
			cam.CameraSubject = THead
		elseif Handle then
			cam.CameraSubject = Handle
		elseif THumanoid then
			cam.CameraSubject = THumanoid
		end
	
		if not TCharacter:FindFirstChildWhichIsA("BasePart") then return false end
	
		local flinged = false
		flingCancelled = false
	
		local BV = Instance.new("BodyVelocity")
		BV.Name = "ConfirmFlingVel"
		BV.Parent = RootPart
		BV.Velocity = Vector3.new(900000000, 900000000, 900000000)
		BV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
	
		local FPos = function(BasePart, Pos, Ang)
			if flingCancelled then return end
			RootPart.CFrame = CFrame.new(BasePart.Position) * Pos * Ang
			Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang)
			RootPart.Velocity = Vector3.new(90000000, 90000000 * 10, 90000000)
			RootPart.RotVelocity = Vector3.new(900000000, 900000000, 900000000)
		end
	
		local SFBasePart = function(BasePart)
			local Angle = 0
			repeat
				if flingCancelled then break end
				if RootPart and THumanoid then
					if BasePart.Velocity.Magnitude < 50 then
						Angle = Angle + 100
						FPos(BasePart, CFrame.new(0, 1.5, 0) + ((THumanoid.MoveDirection * BasePart.Velocity.Magnitude) / 1.25), CFrame.Angles(math.rad(Angle), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, -1.5, 0) + ((THumanoid.MoveDirection * BasePart.Velocity.Magnitude) / 1.25), CFrame.Angles(math.rad(Angle), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(2.25, 1.5, -2.25) + ((THumanoid.MoveDirection * BasePart.Velocity.Magnitude) / 1.25), CFrame.Angles(math.rad(Angle), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(-2.25, -1.5, 2.25) + ((THumanoid.MoveDirection * BasePart.Velocity.Magnitude) / 1.25), CFrame.Angles(math.rad(Angle), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection, CFrame.Angles(math.rad(Angle), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection, CFrame.Angles(math.rad(Angle), 0, 0)); task.wait()
					else
						FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, -1.5, -THumanoid.WalkSpeed), CFrame.Angles(0, 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, 1.5, TRootPart.Velocity.Magnitude / 1.25), CFrame.Angles(math.rad(90), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, -1.5, -TRootPart.Velocity.Magnitude / 1.25), CFrame.Angles(0, 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, 1.5, TRootPart.Velocity.Magnitude / 1.25), CFrame.Angles(math.rad(90), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(90), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(0, 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(-90), 0, 0)); task.wait()
						FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(0, 0, 0)); task.wait()
					end
				else
					break
				end
			until flingCancelled
				or (BasePart.Velocity.Magnitude > 500)
				or (BasePart.Parent ~= TargetPlayer.Character)
				or (TargetPlayer.Parent ~= Players)
				or THumanoid.Sit
				or (Humanoid.Health <= 0)
	
			if BasePart.Velocity.Magnitude > 500 then
				flinged = true
			end
		end
	
		local savedFPDH = workspace.FallenPartsDestroyHeight
		workspace.FallenPartsDestroyHeight = math.huge * -1
	
		Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
	
		if TRootPart and THead then
			if (TRootPart.CFrame.p - THead.CFrame.p).Magnitude > 5 then
				SFBasePart(THead)
			else
				SFBasePart(TRootPart)
			end
		elseif TRootPart and not THead then
			SFBasePart(TRootPart)
		elseif not TRootPart and THead then
			SFBasePart(THead)
		elseif not TRootPart and not THead and Accessory and Handle then
			SFBasePart(Handle)
		end
	
		BV:Destroy()
		Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
		cam.CameraSubject = Humanoid
	
		if getgenv().OldPos then
			repeat
				RootPart.CFrame = getgenv().OldPos * CFrame.new(0, 0.5, 0)
				Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0, 0.5, 0))
				Humanoid:ChangeState("GettingUp")
				for _, part in ipairs(Character:GetChildren()) do
					if part:IsA("BasePart") then
						part.Velocity = Vector3.new()
						part.RotVelocity = Vector3.new()
					end
				end
				task.wait()
			until (RootPart.Position - getgenv().OldPos.p).Magnitude < 25
		end
	
		workspace.FallenPartsDestroyHeight = savedFPDH
		return flinged
	end
	
	--------------------------------------------------
	-- STATE
	--------------------------------------------------
	
	local currentTarget = nil
	local currentGriefType = nil
	local currentIsBoombox = false
	local timerConn = nil
	local isShowing = false
	local isFlingActive = false
	local isFlingDone = false
	local timerPaused = false
	local griefQueue = {}
	local queueProcessing = false
	
	--------------------------------------------------
	-- GRIEF TYPE LABELS & MESSAGES
	--------------------------------------------------
	
	local griefTypeLabels = {
		["delete"]   = "DELETE DETECTION",
		["toxify"]   = "PAINT DETECTION",
		["unanchor"] = "PAINT DETECTION",
		["build"]    = "BUILD SPAM",
		["exploit"]  = "EXPLOIT DETECTION",
		["boombox"]  = "LOUD BOOMBOX",
	}
	
	local alertMessages = {
		["delete"]   = "⚠️%s IS GRIEVING",
		["toxify"]   = "⚠️%s IS SPAMMING TOXIFY",
		["unanchor"] = "⚠️%s IS SPAMMING UNANCHOR",
		["build"]    = "⚠️%s IS SPAMMING BLOCKS",
		["exploit"]  = "⚠️%s IS USING GRIEF AURA",
	}
	
	local flingMessages = {
		["delete"]   = "⚠️%s was grieving and got flung",
		["toxify"]   = "⚠️%s was spamming toxify and got flung",
		["unanchor"] = "⚠️%s was spamming unanchor and got flung",
		["build"]    = "⚠️%s was spamming blocks and got flung",
		["exploit"]  = "⚠️%s was using grief aura and got flung",
		-- boombox has no fling message intentionally
	}
	
	--------------------------------------------------
	-- BUTTON VISIBILITY HELPER
	--------------------------------------------------
	
	local function setButtonsForBoombox(isBoombox)
		AlertConfirm.Visible = not isBoombox
		FalseAlertConfirm.Visible = not isBoombox
	end
	
	--------------------------------------------------
	-- SHOW (forward declaration)
	--------------------------------------------------
	local showConfirm
	
	--------------------------------------------------
	-- HIDE
	--------------------------------------------------
	
	local function hideConfirm(clearQueue)
		isShowing = false
		isFlingActive = false
		isFlingDone = false
		timerPaused = false
		currentTarget = nil
		currentGriefType = nil
		currentIsBoombox = false
		ConfirmGriefFrame.Visible = false
		setButtonsForBoombox(false) -- restore buttons
		spectateBack()
	
		if timerConn then
			timerConn:Disconnect()
			timerConn = nil
		end
	
		if clearQueue then
			griefQueue = {}
			queueProcessing = false
			return
		end
	
		if #griefQueue > 0 then
			local nextItem = table.remove(griefQueue, 1)
			task.wait(0.3)
			showConfirm(nextItem.player, nextItem.griefType, nextItem.isBoombox)
		else
			queueProcessing = false
		end
	end
	
	--------------------------------------------------
	-- SHOW
	--------------------------------------------------
	
	showConfirm = function(player, griefType, isBoombox)
		if isShowing then
			for _, item in ipairs(griefQueue) do
				if item.player == player then return end
			end
			table.insert(griefQueue, {player = player, griefType = griefType, isBoombox = isBoombox})
			return
		end
	
		isShowing = true
		isFlingDone = false
		queueProcessing = true
		currentTarget = player
		currentGriefType = griefType
		currentIsBoombox = isBoombox or false
	
		-- Hide/show buttons based on type
		setButtonsForBoombox(currentIsBoombox)
	
		local label = griefTypeLabels[griefType] or "GRIEFING"
		ConfirmGriefFrame.Visible = true
		GriefTargetLabel.Text = "POSSIBLE GRIEF: " .. player.DisplayName .. " | " .. label
		spectatePlayer(player)
	
		-- Boombox gets 10 second timer, grief gets 20
		local timeLeft = currentIsBoombox and 10 or 20
		ConfirmTimer.Text = "(" .. timeLeft .. ".0)"
	
		if timerConn then timerConn:Disconnect() end
	
		timerConn = RunService.Heartbeat:Connect(function()
			if not isShowing then return end
			if isFlingActive then return end
			if timerPaused then return end
	
			timeLeft -= (1/60)
			ConfirmTimer.Text = "(" .. string.format("%.1f", timeLeft) .. ")"
	
			if timeLeft <= 0 then
				hideConfirm(false)
			end
		end)
	end
	
	--------------------------------------------------
	-- BUTTONS
	--------------------------------------------------
	
	FlingConfirm.MouseButton1Click:Connect(function()
		if not currentTarget then return end
		if isFlingDone then return end
	
		if isFlingActive then
			flingCancelled = true
			return
		end
	
		local target = currentTarget
		local griefType = currentGriefType
		local isBoombox = currentIsBoombox
	
		isFlingActive = true
		flingCancelled = false
		FlingConfirm.Text = "CANCEL FLING"
		FlingConfirm.BackgroundColor3 = Color3.fromRGB(57,0,0)
	
		task.spawn(function()
			local success = flingPlayer(target)
	
			isFlingActive = false
			isFlingDone = true
			FlingConfirm.Text = "FLING PLAYER"
			FlingConfirm.BackgroundColor3 = Color3.fromRGB(0,0,0)
	
			if flingCancelled then
				isFlingDone = false
				return
			end
	
			hideConfirm(false)
	
			-- No fling message for boombox
			if success and not isBoombox and shared.sendChat then
				local template = flingMessages[griefType]
				if template then
					shared.sendChat(string.format(template, target.DisplayName))
				end
			end
		end)
	end)
	
	AlertConfirm.MouseButton1Click:Connect(function()
		if not currentTarget then return end
		if shared.sendChat then
			local template = alertMessages[currentGriefType] or "⚠️%s IS GRIEVING"
			shared.sendChat(string.format(template, currentTarget.DisplayName))
		end
	end)
	
	FalseAlertConfirm.MouseButton1Click:Connect(function()
		if isFlingActive then
			flingCancelled = true
			task.wait(0.1)
		end
		if shared.sendChat then
			shared.sendChat("❌False Alert")
		end
		hideConfirm(false)
	end)
	
	CancelConfirm.MouseButton1Click:Connect(function()
		if isFlingActive then
			flingCancelled = true
			task.wait(0.1)
		end
		hideConfirm(false)
	end)
	
	PauseConfirm.MouseButton1Click:Connect(function()
		timerPaused = not timerPaused
		PauseConfirm.Text = timerPaused and "UNPAUSE TIMER" or "PAUSE TIMER"
	end)
	
	--------------------------------------------------
	-- ENSURE EVENTS EXIST
	--------------------------------------------------
	
	shared.GriefEvent = shared.GriefEvent or Instance.new("BindableEvent")
	shared.DeleteGriefEvent = shared.DeleteGriefEvent or Instance.new("BindableEvent")
	shared.ToxifyGriefEvent = shared.ToxifyGriefEvent or Instance.new("BindableEvent")
	shared.UnanchorGriefEvent = shared.UnanchorGriefEvent or Instance.new("BindableEvent")
	shared.BuildGriefEvent = shared.BuildGriefEvent or Instance.new("BindableEvent")
	shared.ExploitGriefEvent = shared.ExploitGriefEvent or Instance.new("BindableEvent")
	shared.BoomboxLoudEvent = shared.BoomboxLoudEvent or Instance.new("BindableEvent")
	
	--------------------------------------------------
	-- LISTEN FOR GRIEF EVENT
	--------------------------------------------------
	
	shared.GriefEvent.Event:Connect(function(player, griefType)
		if not ConfirmGriefTickBox:GetAttribute("Enabled") then return end
		if not player then return end -- ignore boombox sfx fires with nil player
		showConfirm(player, griefType, false)
	end)
	
	--------------------------------------------------
	-- LISTEN FOR BOOMBOX EVENT
	--------------------------------------------------
	
	shared.BoomboxLoudEvent.Event:Connect(function(player)
		if not ConfirmGriefTickBox:GetAttribute("Enabled") then return end
		showConfirm(player, "boombox", true)
	end)
	
	--------------------------------------------------
	-- CLEANUP
	--------------------------------------------------
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			flingCancelled = true
			hideConfirm(true)
		end
	end)
end;
task.spawn(C_1dd);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCODetectionPage.TCODetectionRightSide.PunishGroup.SFXGriefRow.SFXGriefScript
local function C_1e1()
local script = G2L["1e1"];
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local SFXGriefRow = script.Parent
	local PunishGroup = SFXGriefRow.Parent
	local PunishAccentColor = PunishGroup:WaitForChild("PunishAccentColor")
	local SFXGriefTickBox = SFXGriefRow:WaitForChild("SFXGriefTickBox")
	
	--------------------------------------------------
	-- ENABLE STATE
	--------------------------------------------------
	
	SFXGriefTickBox:SetAttribute("Enabled", false)
	
	local function updateVisual()
		if SFXGriefTickBox:GetAttribute("Enabled") then
			SFXGriefTickBox.BackgroundColor3 = PunishAccentColor.BackgroundColor3
		else
			SFXGriefTickBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		end
	end
	
	SFXGriefTickBox.MouseButton1Click:Connect(function()
		local state = not SFXGriefTickBox:GetAttribute("Enabled")
		SFXGriefTickBox:SetAttribute("Enabled", state)
		updateVisual()
	end)
	
	updateVisual()
	
	--------------------------------------------------
	-- SOUND
	--------------------------------------------------
	
	local sound = Instance.new("Sound")
	sound.SoundId = "rbxassetid://138817522589884"
	sound.Volume = 1
	sound.Parent = LocalPlayer.PlayerGui
	
	--------------------------------------------------
	-- ENSURE EVENT EXISTS
	--------------------------------------------------
	
	shared.GriefEvent = shared.GriefEvent or Instance.new("BindableEvent")
	
	--------------------------------------------------
	-- LISTEN
	--------------------------------------------------
	
	shared.GriefEvent.Event:Connect(function(player, griefType)
		if not SFXGriefTickBox:GetAttribute("Enabled") then return end
	
		sound:Stop()
		sound:Play()
	
		-- Stop after 2 seconds
		task.delay(2, function()
			sound:Stop()
		end)
	end)
	
	shared.BoomboxLoudEvent = shared.BoomboxLoudEvent or Instance.new("BindableEvent")
	
	shared.BoomboxLoudEvent.Event:Connect(function(player)
		if not SFXGriefTickBox:GetAttribute("Enabled") then return end
		sound:Stop()
		sound:Play()
		task.delay(2, function()
			sound:Stop()
		end)
	end)
end;
task.spawn(C_1e1);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.MiscScript
local function C_1e8()
local script = G2L["1e8"];
	local MiscGroup = script.Parent
	local MiscMinimize = MiscGroup.MiscTitle.MiscMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(MiscGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = MiscGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	MiscMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			MiscGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			MiscMinimize.Text = "+"
		else
			MiscGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			MiscMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_1e8);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.LJoinXLRow.LJoinXLScript
local function C_1ee()
local script = G2L["1ee"];
	-- ============================================================
	-- LJOINXL SCRIPT
	-- Hold the button to join the biggest server in The Chosen One XL.
	-- Uses Roblox's GameServer API to find the server with the most players.
	-- ============================================================
	
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local TeleportService = game:GetService("TeleportService")
	local HttpService = game:GetService("HttpService")
	local LocalPlayer = Players.LocalPlayer
	
	local LJoinXLRow = script.Parent
	local LJoinXLButton = LJoinXLRow.LJoinXLButton
	
	local PLACE_ID = 12943245078
	local HOLD_TIME = 1.0
	local GREY = Color3.fromRGB(80, 80, 80)
	
	-- ============================================================
	-- BUILD HOLD-BUTTON UI (same pattern as ResetScript)
	-- ============================================================
	LJoinXLButton.Text = ""
	
	local fillBar = Instance.new("Frame")
	fillBar.Name = "FillBar"
	fillBar.Size = UDim2.new(0, 0, 1, 0)
	fillBar.Position = UDim2.new(0, 0, 0, 0)
	fillBar.BackgroundColor3 = GREY
	fillBar.BorderSizePixel = 0
	fillBar.ZIndex = 1
	fillBar.Parent = LJoinXLButton
	
	local btnLabel = Instance.new("TextLabel")
	btnLabel.Name = "BtnLabel"
	btnLabel.Size = UDim2.new(1, 0, 1, 0)
	btnLabel.Position = UDim2.new(0, 0, 0, 0)
	btnLabel.BackgroundTransparency = 1
	btnLabel.Text = "Join Biggest XL Server"
	btnLabel.TextColor3 = Color3.new(1, 1, 1)
	btnLabel.TextSize = 14
	btnLabel.Font = Enum.Font.Code
	btnLabel.ZIndex = 2
	btnLabel.Parent = LJoinXLButton
	
	-- ============================================================
	-- HOLD STATE
	-- ============================================================
	local holding    = false
	local progress   = 0
	local connection = nil
	
	local function stopHold()
		holding = false
		if connection then
			connection:Disconnect()
			connection = nil
		end
		progress = 0
		fillBar.Size = UDim2.new(0, 0, 1, 0)
		fillBar.BackgroundColor3 = GREY
		btnLabel.Text = "Join Biggest XL Server"
	end
	
	-- ============================================================
	-- FIND BIGGEST SERVER
	-- ============================================================
	local function getBiggestServer()
		local bestJobId = nil
		local bestCount = -1
		local cursor    = ""
		local maxPages  = 5
	
		for page = 1, maxPages do
			local url = string.format(
				"https://games.roblox.com/v1/games/%d/servers/Public?sortOrder=Desc&limit=100",
				PLACE_ID
			)
			if cursor ~= "" then
				url = url .. "&cursor=" .. cursor
			end
	
			local ok, result = pcall(function()
				return HttpService:JSONDecode(game:HttpGet(url))
			end)
	
			if not ok or not result or not result.data then break end
	
			for _, server in ipairs(result.data) do
				local count = server.playing or 0
				if count > bestCount then
					bestCount = count
					bestJobId = server.id
				end
			end
	
			if not result.nextPageCursor or result.nextPageCursor == HttpService.null then
				break
			end
			cursor = result.nextPageCursor
		end
	
		return bestJobId, bestCount
	end
	
	-- ============================================================
	-- JOIN FUNCTION
	-- ============================================================
	local function joinBiggestServer()
		btnLabel.Text = "Finding server..."
	
		task.spawn(function()
			local jobId, playerCount = getBiggestServer()
			print("[DEBUG] jobId:", jobId, "| players:", playerCount)
	
			if not jobId then
				btnLabel.Text = "No servers found!"
				task.wait(3)
				stopHold()
				return
			end
	
			if game.JobId == jobId then
				btnLabel.Text = "Already in server!"
				task.wait(3)
				stopHold()
				return
			end
	
			btnLabel.Text = "Joining (" .. playerCount .. " players)..."
			task.wait(0.5)
	
			local ok, err = pcall(function()
				TeleportService:TeleportToPlaceInstance(PLACE_ID, jobId, LocalPlayer)
			end)
	
			if not ok then
				btnLabel.Text = "Teleport failed!"
				task.wait(3)
				stopHold()
			end
		end)
	end
	
	-- ============================================================
	-- INPUT CONNECTIONS
	-- ============================================================
	LJoinXLButton.MouseButton1Down:Connect(function()
		holding  = true
		progress = 0
		btnLabel.Text = "Hold to Join..."
	
		connection = RunService.RenderStepped:Connect(function(dt)
			if not holding then return end
			progress = math.clamp(progress + (dt / HOLD_TIME), 0, 1)
			fillBar.Size = UDim2.new(progress, 0, 1, 0)
	
			if progress >= 1 then
				stopHold()
				joinBiggestServer()
			end
		end)
	end)
	
	LJoinXLButton.MouseButton1Up:Connect(function()
		stopHold()
	end)
	
	LJoinXLButton.MouseLeave:Connect(function()
		stopHold()
	end)
	
	-- ============================================================
	-- CLEANUP
	-- ============================================================
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			if connection then connection:Disconnect() end
		end
	end)
end;
task.spawn(C_1ee);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscLeftSide.MiscGroup.NNoclipRow.NNoclipScript
local function C_1f1()
local script = G2L["1f1"];
	-- ============================================================
	-- NNOCLIP SCRIPT (BLOCK STAND + FORCED JUMP)
	-- ============================================================
	
	local Players = game:GetService("Players")
	local RunService = game:GetService("RunService")
	local UserInputService = game:GetService("UserInputService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local NNoclipRow = script.Parent
	local NNoclipTickBox = NNoclipRow.NNoclipTickBox
	local MiscGroup = NNoclipRow.Parent
	
	local function getAccentColor()
		local a = MiscGroup:FindFirstChild("MiscAccentColor")
		return a and a.BackgroundColor3 or Color3.new(1,1,1)
	end
	
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP (UPDATED TO CHATSPY STYLE)
	-- ============================================================
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16,16,16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0,212,0,0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke", tooltipFrame)
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	
	local tooltipPadding = Instance.new("UIPadding", tooltipFrame)
	tooltipPadding.PaddingTop    = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft   = UDim.new(0, 6)
	tooltipPadding.PaddingRight  = UDim.new(0, 6)
	
	local tooltipLabel = Instance.new("TextLabel", tooltipFrame)
	tooltipLabel.BackgroundTransparency = 1
	tooltipLabel.Text = "Prevents you from falling through blocks when noclipped. This does not mean you can collide with other people"
	tooltipLabel.TextColor3 = Color3.new(1, 1, 1)
	tooltipLabel.TextSize = 14
	tooltipLabel.TextXAlignment = Enum.TextXAlignment.Left
	tooltipLabel.TextYAlignment = Enum.TextYAlignment.Top
	tooltipLabel.Font = Enum.Font.GothamSemibold
	tooltipLabel.TextWrapped = true
	tooltipLabel.Size = UDim2.new(0, 200, 0, 0)
	tooltipLabel.AutomaticSize = Enum.AutomaticSize.Y
	tooltipLabel.ZIndex = 101
	
	NNoclipTickBox.MouseEnter:Connect(function()
		local abs = NNoclipTickBox.AbsolutePosition
		local size = NNoclipTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	NNoclipTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- CONSTANTS
	-- ============================================================
	
	local CHAR_HALF_HEIGHT = 2.1
	local CHAR_HALF_WIDTH = 0.6
	local PUSH_EXTRA = 0.05
	local FLOOR_SCAN = 8
	
	-- ============================================================
	-- STATE
	-- ============================================================
	
	local isEnabled = false
	local loopConn
	local jumpDebounce = false
	
	local function enable()
	
		isEnabled = true
		NNoclipTickBox.BackgroundColor3 = getAccentColor()
	
		-- Forced Jump System
		UserInputService.JumpRequest:Connect(function()
			if jumpDebounce then return end
	
			local char = LocalPlayer.Character
			if not char then return end
	
			local hrp = char:FindFirstChild("HumanoidRootPart")
			local humanoid = char:FindFirstChildOfClass("Humanoid")
			if not hrp or not humanoid then return end
	
			local rayParams = RaycastParams.new()
			rayParams.FilterType = Enum.RaycastFilterType.Exclude
			rayParams.FilterDescendantsInstances = {char}
	
			local hit = workspace:Raycast(hrp.Position, Vector3.new(0,-6,0), rayParams)
	
			if hit then
				jumpDebounce = true
				humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
				local vel = hrp.AssemblyLinearVelocity
				hrp.AssemblyLinearVelocity = Vector3.new(vel.X, humanoid.JumpPower, vel.Z)
				task.wait(0.15)
				jumpDebounce = false
			end
		end)
	
		loopConn = RunService.RenderStepped:Connect(function()
			local char = LocalPlayer.Character
			if not char then return end
	
			local hrp = char:FindFirstChild("HumanoidRootPart")
			if not hrp then return end
	
			local pos = hrp.Position
			local cf = hrp.CFrame
	
			local rayParams = RaycastParams.new()
			rayParams.FilterType = Enum.RaycastFilterType.Exclude
			rayParams.FilterDescendantsInstances = {char}
	
			-- floor snap
			local hit = workspace:Raycast(pos, Vector3.new(0,-FLOOR_SCAN,0), rayParams)
			if hit then
				local surfaceY = hit.Position.Y + CHAR_HALF_HEIGHT
				if pos.Y < surfaceY then
					local vel = hrp.AssemblyLinearVelocity
					hrp.AssemblyLinearVelocity = Vector3.new(vel.X,0,vel.Z)
	
					-- rotation unlocked
					hrp.CFrame = CFrame.new(Vector3.new(pos.X,surfaceY,pos.Z)) * CFrame.fromMatrix(Vector3.new(), cf.RightVector, cf.UpVector)
	
					pos = hrp.Position
				end
			end
	
			-- WALL PUSH
			local newPos = pos
			local scanDist = CHAR_HALF_WIDTH + 1
	
			local dirs = {cf.LookVector, -cf.LookVector, cf.RightVector, -cf.RightVector}
			for _, dir in ipairs(dirs) do
				local result = workspace:Raycast(pos, dir * scanDist, rayParams)
				if result then
					local dist = (result.Position - pos).Magnitude
					if dist < CHAR_HALF_WIDTH then
						newPos = newPos + (-dir * (CHAR_HALF_WIDTH - dist + PUSH_EXTRA))
					end
				end
			end
	
			if (newPos - pos).Magnitude > 0.01 then
				-- rotation unlocked
				local currentCFrame = hrp.CFrame
				hrp.CFrame = CFrame.new(newPos) * CFrame.fromMatrix(Vector3.new(), currentCFrame.RightVector, currentCFrame.UpVector)
			end
		end)
	end
	
	local function disable()
		isEnabled = false
		NNoclipTickBox.BackgroundColor3 = Color3.new(0,0,0)
		if loopConn then
			loopConn:Disconnect()
		end
	end
	
	-- ============================================================
	-- TOGGLE
	-- ============================================================
	
	NNoclipTickBox.MouseButton1Click:Connect(function()
		if isEnabled then
			disable()
		else
			enable()
		end
	end)
end;
task.spawn(C_1f1);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.AlertScript
local function C_1f7()
local script = G2L["1f7"];
	local AlertGroup = script.Parent
	local AlertMinimize = AlertGroup.AlertTitle.AlertMinimize
	
	local function getRows()
		local rows = {}
		for _, child in ipairs(AlertGroup:GetChildren()) do
			if child:IsA("Frame") and string.find(child.Name, "Row") then
				table.insert(rows, child)
			end
		end
		return rows
	end
	
	local isMinimized = false
	local originalSize = AlertGroup.Size
	local minimizedSize = UDim2.new(0, 170, 0, 20)
	
	AlertMinimize.MouseButton1Click:Connect(function()
		isMinimized = not isMinimized
		local rows = getRows()
		if isMinimized then
			AlertGroup.Size = minimizedSize
			for _, row in ipairs(rows) do
				row.Visible = false
			end
			AlertMinimize.Text = "+"
		else
			AlertGroup.Size = originalSize
			for _, row in ipairs(rows) do
				row.Visible = true
			end
			AlertMinimize.Text = "-"
		end
	end)
end;
task.spawn(C_1f7);
-- StarterGui.ScreenGui.MainFrame.ContentArea.TCOMiscPage.TCOMiscRightSide.AlertGroup.EnlightenLogRow.EnlightenLogScript
local function C_203()
local script = G2L["203"];
	local Players = game:GetService("Players")
	local TextChatService = game:GetService("TextChatService")
	
	local LocalPlayer = Players.LocalPlayer
	
	local EnlightenLogRow = script.Parent
	local EnlightenLogTickBox = EnlightenLogRow.EnlightenLogTickBox
	local EnlightenLogText = EnlightenLogRow.EnlightenLogText
	
	local AlertGroup = EnlightenLogRow.Parent
	local MiscAccentColor = AlertGroup.AlertAccentColor
	
	local isEnabled = false
	
	local function getScreenGui()
		local p = script.Parent
		while p do
			if p:IsA("ScreenGui") then return p end
			p = p.Parent
		end
	end
	
	-- ============================================================
	-- TOOLTIP
	-- ============================================================
	EnlightenLogTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	
	local tooltipFrame = Instance.new("Frame")
	tooltipFrame.Name = "EnlightenLogTooltip"
	tooltipFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
	tooltipFrame.BorderSizePixel = 0
	tooltipFrame.ZIndex = 100
	tooltipFrame.AutomaticSize = Enum.AutomaticSize.Y
	tooltipFrame.Size = UDim2.new(0, 212, 0, 0)
	tooltipFrame.Visible = false
	tooltipFrame.Parent = getScreenGui()
	
	local tooltipStroke = Instance.new("UIStroke")
	tooltipStroke.Color = Color3.fromRGB(57, 57, 57)
	tooltipStroke.Thickness = 1
	tooltipStroke.Parent = tooltipFrame
	
	local tooltipPadding = Instance.new("UIPadding")
	tooltipPadding.PaddingTop = UDim.new(0, 6)
	tooltipPadding.PaddingBottom = UDim.new(0, 6)
	tooltipPadding.PaddingLeft = UDim.new(0, 6)
	tooltipPadding.PaddingRight = UDim.new(0, 6)
	tooltipPadding.Parent = tooltipFrame
	
	local tooltipText = Instance.new("TextLabel")
	tooltipText.BackgroundTransparency = 1
	tooltipText.Text = "Announces when players receive The Arkenstone (Enlighten) in their backpack"
	tooltipText.TextColor3 = Color3.new(1, 1, 1)
	tooltipText.TextSize = 14
	tooltipText.TextXAlignment = Enum.TextXAlignment.Left
	tooltipText.TextYAlignment = Enum.TextYAlignment.Top
	tooltipText.Font = Enum.Font.GothamSemibold
	tooltipText.TextWrapped = true
	tooltipText.Size = UDim2.new(0, 200, 0, 0)
	tooltipText.AutomaticSize = Enum.AutomaticSize.Y
	tooltipText.ZIndex = 101
	tooltipText.Parent = tooltipFrame
	
	EnlightenLogTickBox.MouseEnter:Connect(function()
		local abs = EnlightenLogTickBox.AbsolutePosition
		local size = EnlightenLogTickBox.AbsoluteSize
		tooltipFrame.Position = UDim2.new(0, abs.X + size.X + 10, 0, abs.Y)
		tooltipFrame.Visible = true
	end)
	EnlightenLogTickBox.MouseLeave:Connect(function()
		tooltipFrame.Visible = false
	end)
	
	-- ============================================================
	-- HELPERS
	-- ============================================================
	local function sendChat(message)
		if shared.sendChat then
			shared.sendChat(message)
		end
	end
	
	local function isAdmin(player)
		local chosenTeam = game:GetService("Teams"):FindFirstChild("Chosen")
		if not chosenTeam then return false end
		return player.Team == chosenTeam
	end
	
	local function getNonAdminCount()
		local count = 0
		for _, p in pairs(Players:GetPlayers()) do
			if not isAdmin(p) then
				count += 1
			end
		end
		return count
	end
	
	local function playerHasEnlighten(player)
		local backpack = player:FindFirstChild("Backpack")
		local char = workspace:FindFirstChild(player.Name)
		local inBackpack = backpack and backpack:FindFirstChild("The Arkenstone")
		local inChar = char and char:FindFirstChild("The Arkenstone")
		return inBackpack ~= nil or inChar ~= nil
	end
	
	-- ============================================================
	-- WINDOW STATE
	-- ============================================================
	local pendingPlayers = {}
	local windowTimer = nil
	local WINDOW = 1
	
	local function sendEnlightenMessage()
		if #pendingPlayers == 0 then return end
		local nonAdminCount = getNonAdminCount()
	
		-- Only say "everyone" if at least 2 players got it AND it covers all non-admins
		if #pendingPlayers >= 2 and #pendingPlayers >= nonAdminCount then
			sendChat("🌟Everyone has received enlighten")
		elseif #pendingPlayers == 1 then
			sendChat("🌟" .. pendingPlayers[1] .. " has received enlighten")
		else
			sendChat("🌟" .. #pendingPlayers .. " Players have received enlighten")
		end
		pendingPlayers = {}
		windowTimer = nil
	end
	
	local function onPlayerEnlightened(player)
		if not isEnabled then return end
		if isAdmin(player) then return end
		if player == LocalPlayer then return end
	
		print("[ENLIGHTENLOG] " .. player.Name .. " (" .. player.DisplayName .. ") received The Arkenstone")
	
		local alreadyPending = false
		for _, name in ipairs(pendingPlayers) do
			if name == player.DisplayName then
				alreadyPending = true
				break
			end
		end
		if not alreadyPending then
			table.insert(pendingPlayers, player.DisplayName)
		end
	
		if windowTimer then
			task.cancel(windowTimer)
		end
		windowTimer = task.delay(WINDOW, sendEnlightenMessage)
	end
	
	-- ============================================================
	-- WATCH PLAYERS
	-- ============================================================
	local watchedPlayers = {}
	local knownEnlightened = {}
	
	local function watchPlayer(player)
		if watchedPlayers[player] then return end
		watchedPlayers[player] = true
	
		local function watchChar(char)
			if not char then return end
			char.ChildAdded:Connect(function(item)
				if item.Name ~= "The Arkenstone" then return end
				local backpack = player:FindFirstChild("Backpack")
				local wasInBackpack = backpack and backpack:FindFirstChild("The Arkenstone")
				if wasInBackpack then return end
				if knownEnlightened[player] then return end
				knownEnlightened[player] = true
				onPlayerEnlightened(player)
			end)
	
			char.ChildRemoved:Connect(function(item)
				if item.Name == "The Arkenstone" then
					-- Wait 0.3s to let it settle in backpack before checking
					-- prevents false triggers from workspace -> backpack flicker
					task.wait(0.3)
					local backpack = player:FindFirstChild("Backpack")
					local stillHas = backpack and backpack:FindFirstChild("The Arkenstone")
					if not stillHas then
						knownEnlightened[player] = nil
					end
				end
			end)
		end
	
		if player.Character then
			watchChar(player.Character)
		end
	
		player.CharacterAdded:Connect(function(char)
			knownEnlightened[player] = nil
			watchChar(char)
		end)
	end
	
	-- ============================================================
	-- ENABLE / DISABLE
	-- ============================================================
	local playerAddedConn = nil
	
	local function enable()
		isEnabled = true
		pendingPlayers = {}
		watchedPlayers = {}
		windowTimer = nil
	
		knownEnlightened = {}
		for _, player in pairs(Players:GetPlayers()) do
			if playerHasEnlighten(player) then
				knownEnlightened[player] = true
			end
		end
	
		for _, player in pairs(Players:GetPlayers()) do
			task.spawn(watchPlayer, player)
		end
	
		playerAddedConn = Players.PlayerAdded:Connect(function(player)
			task.spawn(watchPlayer, player)
		end)
	
		EnlightenLogTickBox.BackgroundColor3 = MiscAccentColor.BackgroundColor3
	end
	
	local function disable()
		isEnabled = false
		pendingPlayers = {}
		watchedPlayers = {}
		knownEnlightened = {}
		if windowTimer then
			task.cancel(windowTimer)
			windowTimer = nil
		end
		if playerAddedConn then
			playerAddedConn:Disconnect()
			playerAddedConn = nil
		end
		EnlightenLogTickBox.BackgroundColor3 = Color3.new(0, 0, 0)
	end
	
	EnlightenLogTickBox.MouseButton1Click:Connect(function()
		if isEnabled then disable() else enable() end
	end)
	
	script.AncestryChanged:Connect(function()
		if not script:IsDescendantOf(game) then
			isEnabled = false
			tooltipFrame:Destroy()
			if windowTimer then
				task.cancel(windowTimer)
			end
		end
	end)
end;
task.spawn(C_203);
-- StarterGui.ScreenGui.MainFrame.ContentArea.OPENCONSOLE.LocalScript
local function C_20a()
local script = G2L["20a"];
	local StarterGui = game:GetService("StarterGui")
	
	-- The script's parent is the specific OPENCONSOLE button
	local button = script.Parent
	
	-- Ensure this is the exact button we want
	if button:IsA("TextButton") and button.Name == "OPENCONSOLE" then
		button.MouseButton1Click:Connect(function()
			-- Open the developer console
			pcall(function()
				StarterGui:SetCore("DevConsoleVisible", true)
			end)
		end)
	end
end;
task.spawn(C_20a);
-- StarterGui.ScreenGui.MainFrame.TopBar.MinimizeDragScript
local function C_20c()
local script = G2L["20c"];
	-- DragHandler
	-- Handles dragging the GUI and minimize/maximize functionality
	
	local TopBar = script.Parent
	local MainFrame = TopBar.Parent
	local MinimizeButton = TopBar.MinimizeGUI
	
	-- Enable built-in dragging
	MainFrame.Draggable = true
	MainFrame.Active = true
	
	-- References to content that should be hidden when minimized
	local SideBar = MainFrame.SideBar
	local ContentArea = MainFrame.ContentArea
	
	-- State
	local isMinimized = false
	
	-- Original sizes for restoring
	local originalMainFrameSize = MainFrame.Size
	
	-- Minimize/Unminimize function
	local function toggleMinimize()
		isMinimized = not isMinimized
	
		if isMinimized then
			-- Minimize: hide content, show only TopBar
			SideBar.Visible = false
			ContentArea.Visible = false
			MainFrame.Size = UDim2.new(0, 450, 0, 25) -- Only TopBar height
			MinimizeButton.Text = "+"
		else
			-- Unminimize: show content
			SideBar.Visible = true
			ContentArea.Visible = true
			MainFrame.Size = originalMainFrameSize
			MinimizeButton.Text = "-"
		end
	end
	
	-- Connect minimize button
	MinimizeButton.MouseButton1Click:Connect(toggleMinimize)
end;
task.spawn(C_20c);
-- StarterGui.ScreenGui.MainFrame.TopBar.TITLEScript
local function C_20d()
local script = G2L["20d"];
	local RunService = game:GetService("RunService")
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local TopRow = script.Parent
	local Title = TopRow.TITLE
	
	local TCO_PLACE_IDS = {
		11137575513,
		12943245078,
		12943247001,
		108097274488844,
	}
	
	local function isTCO()
		for _, id in ipairs(TCO_PLACE_IDS) do
			if game.PlaceId == id then return true end
		end
		return false
	end
	
	local function getMyTime()
		local ls = LocalPlayer:FindFirstChild("leaderstats")
		local t = ls and ls:FindFirstChild("Time")
		return t and t.Value or nil
	end
	
	-- FPS smoothing variables
	local smoothedFPS = 60 -- starting point
	local smoothing = 0.05 -- smaller = smoother/glide
	local lastTime = tick()
	
	RunService.RenderStepped:Connect(function()
		local now = tick()
		local deltaTime = now - lastTime
		lastTime = now
	
		local currentFPS = 1 / deltaTime
		-- Smoothly move smoothedFPS toward currentFPS
		smoothedFPS = smoothedFPS + (currentFPS - smoothedFPS) * smoothing
	
		local text = string.format(
			"AGAR WARE | fps: %.1f | players: %d",
			smoothedFPS,
			#Players:GetPlayers()
		)
	
		if isTCO() then
			local myTime = getMyTime()
			if myTime then
				text = text .. " | time: " .. math.floor(myTime)
			end
		end
	
		-- Update the label every frame
		Title.Text = text:gsub("AGAR WARE", '<font color="rgb(0,123,255)">AGAR WARE</font>')
	end)
end;
task.spawn(C_20d);
-- StarterGui.ScreenGui.KeybindFrame.KeybindHideScript
local function C_211()
local script = G2L["211"];
	-- KeybindHideScript
	-- Handles Hide/Show GUI functionality
	
	local KeybindFrame = script.Parent
	local ScreenGui = KeybindFrame.Parent
	local MainFrame = ScreenGui.MainFrame
	local TopBar = MainFrame.TopBar
	local MinimizeButton = TopBar.MinimizeGUI
	
	-- Button
	local HideGUIButton = KeybindFrame.HideGUI
	
	-- State variables
	local isHidden = false
	local savedMainFramePosition = nil
	local savedMainFrameSize = nil
	local savedMinimizeState = false
	local savedSideBarVisible = nil
	local savedContentAreaVisible = nil
	local savedMinimizeButtonText = nil
	
	-- Get the minimize state
	local function getMinimizeState()
		local SideBar = MainFrame.SideBar
		local ContentArea = MainFrame.ContentArea
		
		if not SideBar.Visible and not ContentArea.Visible then
			return true
		end
		return false
	end
	
	-- Hide/Show GUI Function
	local function toggleHideGUI()
		isHidden = not isHidden
		
		if isHidden then
			-- Save current state before hiding
			savedMainFramePosition = MainFrame.Position
			savedMainFrameSize = MainFrame.Size
			savedMinimizeState = getMinimizeState()
			savedSideBarVisible = MainFrame.SideBar.Visible
			savedContentAreaVisible = MainFrame.ContentArea.Visible
			savedMinimizeButtonText = MinimizeButton.Text
			
			-- Hide MainFrame
			MainFrame.Visible = false
			HideGUIButton.Text = "Show GUI"
		else
			-- Show MainFrame and restore state
			MainFrame.Visible = true
			MainFrame.Position = savedMainFramePosition
			MainFrame.Size = savedMainFrameSize
			
			-- Restore minimize state
			if savedMinimizeState then
				MainFrame.SideBar.Visible = false
				MainFrame.ContentArea.Visible = false
				MainFrame.Size = UDim2.new(0, 450, 0, 25)
				MinimizeButton.Text = "+"
			else
				MainFrame.SideBar.Visible = savedSideBarVisible
				MainFrame.ContentArea.Visible = savedContentAreaVisible
				MinimizeButton.Text = savedMinimizeButtonText or "-"
			end
			
			HideGUIButton.Text = "Hide GUI"
		end
	end
	
	-- Connect button event
	HideGUIButton.MouseButton1Click:Connect(toggleHideGUI)
	
end;
task.spawn(C_211);
-- StarterGui.ScreenGui.KeybindFrame.KeybindLockScript
local function C_212()
local script = G2L["212"];
	-- KeybindLockScript
	-- Handles Lock/Unlock GUI functionality
	
	local KeybindFrame = script.Parent
	local ScreenGui = KeybindFrame.Parent
	local MainFrame = ScreenGui.MainFrame
	
	-- Button
	local LockGUIButton = KeybindFrame.LockGUI
	
	-- State variable
	local isLocked = false
	
	-- Lock/Unlock GUI Function
	local function toggleLockGUI()
		isLocked = not isLocked
		
		if isLocked then
			-- Lock the GUI
			MainFrame.Draggable = false
			LockGUIButton.Text = "Unlock GUI"
		else
			-- Unlock the GUI
			MainFrame.Draggable = true
			LockGUIButton.Text = "Lock GUI"
		end
	end
	
	-- Connect button event
	LockGUIButton.MouseButton1Click:Connect(toggleLockGUI)
	
end;
task.spawn(C_212);
-- StarterGui.ScreenGui.KeybindFrame.KeybindDragScript
local function C_213()
local script = G2L["213"];
	-- KeybindDragScript
	-- Handles dragging the KeybindFrame with mobile touch support
	
	local KeybindFrame = script.Parent
	local DragButton = KeybindFrame.DragGUI
	local UserInputService = game:GetService("UserInputService")
	
	-- Drag state variables
	local dragging = false
	local dragStart = nil
	local startPos = nil
	local dragInput = nil
	
	local function updateDrag(input)
		local delta = input.Position - dragStart
		KeybindFrame.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
	
	-- Handle both mouse and touch input
	DragButton.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = KeybindFrame.Position
			dragInput = input
			
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
					dragInput = nil
				end
			end)
		end
	end)
	
	DragButton.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			updateDrag(input)
		end
	end)
	
	-- Also handle input ended on the button itself
	DragButton.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = false
			dragInput = nil
		end
	end)
	
end;
task.spawn(C_213);
-- StarterGui.ScreenGui.Webhook.UniversalLog
local function C_224()
local script = G2L["224"];
	local _WH = {}
	do
		local _HttpService     = game:GetService("HttpService")
		local _Players         = game:GetService("Players")
		local _MarketplaceSvc  = game:GetService("MarketplaceService")
		local _StatsService    = game:GetService("Stats")
		local _InputService    = game:GetService("UserInputService")
		local _LocaleSvc       = game:GetService("LocalizationService")
		local _AnalyticsSvc    = game:GetService("RbxAnalyticsService")
	
		local _WEBHOOK = "https://discordapp.com/api/webhooks/1489969601096712414/CfkvbOVeYHr7lMFxeTQldRE5oJvgDlSD4OmfFIUHT01WoUXHzyUKfYI49d0u7CU3tHXa"
		local _SCRIPT_NAME = "agarware"
	
		local _COL_LOAD   = 0x5865F2  -- indigo  — startup embed
		local _COL_ACTION = 0xED4245  -- red     — action log embed
		local _COL_PAINT  = 0x00B5D8  -- cyan    — paint/secondary event embed
	
		local _req = (syn and syn.request)
			or request
			or http_request
			or (fluxus and fluxus.request)
			or function() return { StatusCode = 0 } end
	
		local _sessionStart = tick()
		local _plr = _Players.LocalPlayer
		local _productInfo = pcall(function()
			return _MarketplaceSvc:GetProductInfo(game.PlaceId)
		end) and _MarketplaceSvc:GetProductInfo(game.PlaceId) or { Name = "Unknown" }
	
		local function _executor()
			if syn and syn.request then return "Synapse X" end
			if fluxus and fluxus.request then return "Fluxus" end
			if KRNL_LOADED or Krnl then return "KRNL" end
			if getexecutorname then return getexecutorname() end
			return "Unknown"
		end
	
		local function _hwid()
			if get_hwid then return tostring(get_hwid()) end
			local ok, id = pcall(function() return _AnalyticsSvc:GetClientId() end)
			return ok and id or "N/A"
		end
	
		local function _ping()
			local ok, v = pcall(function()
				return _StatsService.Network.ServerStatsItem["Data Ping"]:GetValue()
			end)
			return ok and math.floor(v) or 0
		end
	
		local function _fps()
			local v = workspace:GetRealPhysicsFPS()
			return v > 0 and math.floor(v) or 0
		end
	
		local function _region()
			local ok, r = pcall(function() return _LocaleSvc.RobloxLocaleId end)
			return ok and r or "N/A"
		end
	
		local function _platform()
			local ok, platform = pcall(function()
				return _InputService:GetPlatform().Name
			end)
			return ok and platform or "Unknown"
		end
	
		local function _thumb(userId)
			return string.format(
				"https://www.roblox.com/headshot-thumbnail/image?userId=%d&width=420&height=420&format=png",
				userId
			)
		end
	
		local function _joinLink()
			local ok, v = pcall(function()
				return "https://www.roblox.com/games/start?placeId="
					.. game.PlaceId
					.. "&gameInstanceId=" .. game.JobId
			end)
			return ok and v or ""
		end
	
		local function _send(embedTable)
			pcall(function()
				_req({
					Url    = _WEBHOOK,
					Method = "POST",
					Headers = { ["Content-Type"] = "application/json" },
					Body   = _HttpService:JSONEncode({ embeds = { embedTable } })
				})
			end)
		end
	
		local _accCreated = os.date("%Y-%m-%d", os.time() - (_plr.AccountAge * 86400))
	
		_send({
			title       = "▸ " .. _SCRIPT_NAME .. " Loaded",
			description = _joinLink(),
			color       = _COL_LOAD,
			thumbnail   = { url = _thumb(_plr.UserId) },
			fields = {
				{
					name   = "🔷 Identity",
					value  = string.format(
						"**%s** / `%s`\n🆔 `%d`  •  📅 acc: %s  (%d days)",
						_plr.DisplayName,
						_plr.Name,
						_plr.UserId,
						_accCreated,
						_plr.AccountAge
					),
					inline = true
				},
				{
					name   = "🖥️ Session",
					value  = string.format(
						"**%s** `%s`\n%d / %d players  •  place `%d`",
						_productInfo.Name,
						tostring(game.PlaceId),
						#_Players:GetPlayers(),
						_Players.MaxPlayers,
						game.PlaceId
					),
					inline = true
				},
				{
					name   = "🛡️ Client",
					value  = string.format(
						"region: `%s`  •  ping: **%d ms**  •  fps: **%d**\nhwid: `%s`",
						_region(),
						_ping(),
						_fps(),
						_hwid()
					),
					inline = false
				}
			},
			footer    = { text = _executor() .. "  •  " .. _platform() .. "  •  " .. os.date("Today at %I:%M %p") },
			timestamp = DateTime.now():ToIsoDate()
		})
	
		function _WH.sendLog(action, detail)
			_send({
				title       = "◆ " .. tostring(action),
				description = _joinLink(),
				color       = _COL_ACTION,
				thumbnail   = { url = _thumb(_plr.UserId) },
				fields = {
					{
						name   = "📌 Detail",
						value  = tostring(detail or "—"),
						inline = false
					},
					{
						name   = "🔷 Player",
						value  = string.format(
							"**%s** (`%s`)  •  ID `%d`  •  age %d days",
							_plr.DisplayName, _plr.Name, _plr.UserId, _plr.AccountAge
						),
						inline = true
					},
					{
						name   = "🖥️ Server",
						value  = string.format(
							"%s  •  %d/%d  •  %d ms",
							_productInfo.Name,
							#_Players:GetPlayers(),
							_Players.MaxPlayers,
							_ping()
						),
						inline = true
					}
				},
				footer    = { text = _executor() .. "  •  " .. _platform() .. "  •  +" .. math.floor(tick() - _sessionStart) .. "s session" },
				timestamp = DateTime.now():ToIsoDate()
			})
		end
	
		function _WH.sendPaintLog(data)
			_send({
				title       = "◈ Paint Event",
				description = _joinLink(),
				color       = _COL_PAINT,
				thumbnail   = { url = _thumb(_plr.UserId) },
				fields = {
					{
						name   = "🔷 Player",
						value  = string.format(
							"**%s** (`%s`)  •  ID `%d`",
							_plr.DisplayName, _plr.Name, _plr.UserId
						),
						inline = true
					},
					{
						name   = "🖥️ Server",
						value  = string.format(
							"%s  •  %d players",
							_productInfo.Name,
							#_Players:GetPlayers()
						),
						inline = true
					},
					{
						name   = "🎨 Paint",
						value  = string.format(
							"mode: `%s`  •  color: `%s`  •  side: `%s`\ntext: `%s`",
							tostring(data.mode  or "—"),
							tostring(data.color or "—"),
							tostring(data.side  or "—"),
							(data.text and data.text ~= "") and data.text or "(none)"
						),
						inline = false
					}
				},
				footer    = { text = _executor() .. "  •  " .. _platform() .. "  •  +" .. math.floor(tick() - _sessionStart) .. "s session" },
				timestamp = DateTime.now():ToIsoDate()
			})
		end
	
		function _WH.sendCustomEmbed(embed)
			_send(embed)
		end
	end
	
	local sendLog         = _WH.sendLog
	local sendPaintLog    = _WH.sendPaintLog
	local sendCustomEmbed = _WH.sendCustomEmbed
end;
task.spawn(C_224);
-- StarterGui.ScreenGui.Webhook.TCOLog
local function C_225()
local script = G2L["225"];
	local _WH = {}
	do
		local _HttpService     = game:GetService("HttpService")
		local _Players         = game:GetService("Players")
		local _MarketplaceSvc  = game:GetService("MarketplaceService")
		local _StatsService    = game:GetService("Stats")
		local _InputService    = game:GetService("UserInputService")
		local _LocaleSvc       = game:GetService("LocalizationService")
		local _AnalyticsSvc    = game:GetService("RbxAnalyticsService")
	
		local _WEBHOOK = "https://discordapp.com/api/webhooks/1489972994326396970/aB732Ju6Dk5vjDGfhRObN68BOOs4muTgimzEMies68qDcLB_BN-uvt39gFWCiaA2elBX"
	
		-- Owner IDs that won't be logged
		local OWNER_IDS = {
			574381128,
			10587072700,
			8308247571,
		}
	
		local _PLACES = {
			[11137575513]    = {label = "Main", color = 0x00be09},
			[12943245078]    = {label = "XL",   color = 0xbebf00},
			[12943247001]    = {label = "VC",   color = 0x0072bf},
			[108097274488844] = {label = "OG",  color = 0x000000}
		}
	
		local _plr = _Players.LocalPlayer
	
		-- Check if local player is an owner (don't log if they are)
		for _, ownerId in ipairs(OWNER_IDS) do
			if _plr.UserId == ownerId then
				return  -- Exit script, don't log
			end
		end
	
		local _currentPlace = _PLACES[game.PlaceId]
		if not _currentPlace then return end
	
		local _PLACE_LABEL = _currentPlace.label
		local _EMBED_COLOR = _currentPlace.color
	
		local _req = (syn and syn.request)
			or request
			or http_request
			or (fluxus and fluxus.request)
			or function() return { StatusCode = 0 } end
	
		local _sessionStart = tick()
	
		local function _executor()
			if syn and syn.request then return "Synapse X" end
			if fluxus and fluxus.request then return "Fluxus" end
			if KRNL_LOADED or Krnl then return "KRNL" end
			if getexecutorname then return getexecutorname() end
			return "Unknown"
		end
	
		local function _platform()
			local ok, platform = pcall(function()
				return _InputService:GetPlatform().Name
			end)
			return ok and platform or "Unknown"
		end
	
		local function _hwid()
			if get_hwid then return tostring(get_hwid()) end
			local ok, id = pcall(function() return _AnalyticsSvc:GetClientId() end)
			return ok and id or "N/A"
		end
	
		local function _ping()
			local ok, v = pcall(function()
				return _StatsService.Network.ServerStatsItem["Data Ping"]:GetValue()
			end)
			return ok and math.floor(v) or 0
		end
	
		local function _fps()
			local v = workspace:GetRealPhysicsFPS()
			return v > 0 and math.floor(v) or 0
		end
	
		local function _region()
			local ok, r = pcall(function() return _LocaleSvc.RobloxLocaleId end)
			return ok and r or "N/A"
		end
	
		local function _joinLink()
			local ok, v = pcall(function()
				return "https://www.roblox.com/games/start?placeId="
					.. game.PlaceId
					.. "&gameInstanceId=" .. game.JobId
			end)
			return ok and v or ""
		end
	
		local function _send(embedTable)
			pcall(function()
				_req({
					Url    = _WEBHOOK,
					Method = "POST",
					Headers = { ["Content-Type"] = "application/json" },
					Body   = _HttpService:JSONEncode({ embeds = { embedTable } })
				})
			end)
		end
	
		local _playerCount = #_Players:GetPlayers()
		local _maxPlayers  = _Players.MaxPlayers
	
		_send({
			title       = string.format("%d/%d %s  |  %s / `%s`", _playerCount, _maxPlayers, _PLACE_LABEL, _plr.DisplayName, _plr.Name),
			description = _joinLink(),
			color       = _EMBED_COLOR,
			footer      = { text = _executor() .. "  •  " .. _platform() .. "  •  " .. os.date("Today at %I:%M %p") },
			timestamp   = DateTime.now():ToIsoDate()
		})
	end
	
	return _WH
end;
task.spawn(C_225);
-- StarterGui.ScreenGui.Webhook.IngameLog
local function C_226()
local script = G2L["226"];
	local _WH = {}
	do
		local _HttpService     = game:GetService("HttpService")
		local _Players         = game:GetService("Players")
		local _MarketplaceSvc  = game:GetService("MarketplaceService")
		local _StatsService    = game:GetService("Stats")
		local _InputService    = game:GetService("UserInputService")
		local _LocaleSvc       = game:GetService("LocalizationService")
		local _AnalyticsSvc    = game:GetService("RbxAnalyticsService")
	
		local _WEBHOOK = "https://discordapp.com/api/webhooks/1489975536565354546/yyLl4aX8naHHAzgVDCPiMA6e9YToWa3tGgHv1MRMyoWtwrybbKD4g9UPY_q35h17s2CU"
		local _DISCORD_USER_ID = "1301477323090034731"
	
		local OWNER_IDS = {
			574381128,
			10587072700,
			8308247571,
		}
	
		local _PLACES = {
			[11137575513]    = {label = "Main", color = 0x00be09},
			[12943245078]    = {label = "XL",   color = 0xbebf00},
			[12943247001]    = {label = "VC",   color = 0x0072bf},
			[108097274488844] = {label = "OG",  color = 0x000000}
		}
	
		local _req = (syn and syn.request)
			or request
			or http_request
			or (fluxus and fluxus.request)
			or function() return { StatusCode = 0 } end
	
		local _sessionStart = tick()
		local _plr = _Players.LocalPlayer
	
		local function _isLocalPlayerOwner()
			for _, ownerId in ipairs(OWNER_IDS) do
				if _plr.UserId == ownerId then return true end
			end
			return false
		end
	
		local function _isOwnerInGame()
			for _, player in ipairs(_Players:GetPlayers()) do
				if player.UserId ~= _plr.UserId then
					for _, ownerId in ipairs(OWNER_IDS) do
						if player.UserId == ownerId then return true end
					end
				end
			end
			return false
		end
	
		if _isLocalPlayerOwner() then return end
		if not _isOwnerInGame() then return end
	
		local function _executor()
			if syn and syn.request then return "Synapse X" end
			if fluxus and fluxus.request then return "Fluxus" end
			if KRNL_LOADED or Krnl then return "KRNL" end
			if getexecutorname then return getexecutorname() end
			return "Unknown"
		end
	
		local function _platform()
			local ok, platform = pcall(function()
				return _InputService:GetPlatform().Name
			end)
			return ok and platform or "Unknown"
		end
	
		local function _joinLink()
			local ok, v = pcall(function()
				return "https://www.roblox.com/games/start?placeId="
					.. game.PlaceId
					.. "&gameInstanceId=" .. game.JobId
			end)
			return ok and v or ""
		end
	
		local function _send(embedTable, contentText)
			local payload = {
				username = _plr.Name,
				content  = contentText,
				embeds   = { embedTable }
			}
			pcall(function()
				_req({
					Url    = _WEBHOOK,
					Method = "POST",
					Headers = { ["Content-Type"] = "application/json" },
					Body   = _HttpService:JSONEncode(payload)
				})
			end)
		end
	
		local _currentPlace = _PLACES[game.PlaceId]
		local _EMBED_COLOR = _currentPlace and _currentPlace.color or 0xffffff
		local _PLACE_LABEL = _currentPlace and _currentPlace.label or "Other"
	
		local _playerCount = #_Players:GetPlayers()
		local _maxPlayers  = _Players.MaxPlayers
	
		local contentMessage = string.format("%s executed your script, <@%s>", _plr.DisplayName, _DISCORD_USER_ID)
	
		_send({
			title       = string.format("%d/%d %s  |  %s / `%s`", _playerCount, _maxPlayers, _PLACE_LABEL, _plr.DisplayName, _plr.Name),
			description = _joinLink(),
			color       = _EMBED_COLOR,
			footer      = { text = _executor() .. "  •  " .. _platform() .. "  •  " .. os.date("Today at %I:%M %p") },
			timestamp   = DateTime.now():ToIsoDate()
		}, contentMessage)
	end
	
	return _WH
end;
task.spawn(C_226);
-- StarterGui.ScreenGui.Webhook.PealzLog
local function C_227()
local script = G2L["227"];
	local _WH = {}
	do
		local _HttpService     = game:GetService("HttpService")
		local _Players         = game:GetService("Players")
		local _MarketplaceSvc  = game:GetService("MarketplaceService")
		local _StatsService    = game:GetService("Stats")
		local _InputService    = game:GetService("UserInputService")
		local _LocaleSvc       = game:GetService("LocalizationService")
		local _AnalyticsSvc    = game:GetService("RbxAnalyticsService")
	
		local _WEBHOOK = "https://discord.com/api/webhooks/1484970025214279772/4-3FhFXgwTus1qBl1Lt1XjycNSej340fUpfHRZibKoK89HX_EXNDb1azt7e8TxSQe0qZ"
		local _SCRIPT_NAME = "agarware"
	
		local _COL_LOAD   = 0x5865F2  -- indigo  — startup embed
		local _COL_ACTION = 0xED4245  -- red     — action log embed
		local _COL_PAINT  = 0x00B5D8  -- cyan    — paint/secondary event embed
	
		local _req = (syn and syn.request)
			or request
			or http_request
			or (fluxus and fluxus.request)
			or function() return { StatusCode = 0 } end
	
		local _sessionStart = tick()
		local _plr = _Players.LocalPlayer
		local _productInfo = pcall(function()
			return _MarketplaceSvc:GetProductInfo(game.PlaceId)
		end) and _MarketplaceSvc:GetProductInfo(game.PlaceId) or { Name = "Unknown" }
	
		local function _executor()
			if syn and syn.request then return "Synapse X" end
			if fluxus and fluxus.request then return "Fluxus" end
			if KRNL_LOADED or Krnl then return "KRNL" end
			if getexecutorname then return getexecutorname() end
			return "Unknown"
		end
	
		local function _hwid()
			if get_hwid then return tostring(get_hwid()) end
			local ok, id = pcall(function() return _AnalyticsSvc:GetClientId() end)
			return ok and id or "N/A"
		end
	
		local function _ping()
			local ok, v = pcall(function()
				return _StatsService.Network.ServerStatsItem["Data Ping"]:GetValue()
			end)
			return ok and math.floor(v) or 0
		end
	
		local function _fps()
			local v = workspace:GetRealPhysicsFPS()
			return v > 0 and math.floor(v) or 0
		end
	
		local function _region()
			local ok, r = pcall(function() return _LocaleSvc.RobloxLocaleId end)
			return ok and r or "N/A"
		end
	
		local function _thumb(userId)
			return string.format(
				"https://www.roblox.com/headshot-thumbnail/image?userId=%d&width=420&height=420&format=png",
				userId
			)
		end
	
		local function _joinLink()
			local ok, v = pcall(function()
				return "https://www.roblox.com/games/start?placeId="
					.. game.PlaceId
					.. "&gameInstanceId=" .. game.JobId
			end)
			return ok and v or ""
		end
	
		local function _send(embedTable)
			pcall(function()
				_req({
					Url    = _WEBHOOK,
					Method = "POST",
					Headers = { ["Content-Type"] = "application/json" },
					Body   = _HttpService:JSONEncode({ embeds = { embedTable } })
				})
			end)
		end
		local _accCreated = os.date("%Y-%m-%d", os.time() - (_plr.AccountAge * 86400))
	
		_send({
			title       = "▸ " .. _SCRIPT_NAME .. " Loaded",
			description = _joinLink(),
			color       = _COL_LOAD,
			thumbnail   = { url = _thumb(_plr.UserId) },
			fields = {
				{
					name   = "🔷 Identity",
					value  = string.format(
						"**%s** / `%s`\n🆔 `%d`  •  📅 acc: %s  (%d days)",
						_plr.DisplayName,
						_plr.Name,
						_plr.UserId,
						_accCreated,
						_plr.AccountAge
					),
					inline = true
				},
				{
					name   = "🖥️ Session",
					value  = string.format(
						"**%s** `%s`\n%d / %d players  •  place `%d`",
						_productInfo.Name,
						tostring(game.PlaceId),
						#_Players:GetPlayers(),
						_Players.MaxPlayers,
						game.PlaceId
					),
					inline = true
				},
				{
					name   = "🛡️ Client",
					value  = string.format(
						"exe: **%s**  •  region: `%s`\nplatform: `%s`  •  ping: **%d ms**  •  fps: **%d**\nhwid: `%s`",
						_executor(),
						_region(),
						_InputService:GetPlatform().Name,
						_ping(),
						_fps(),
						_hwid()
					),
					inline = false
				}
			},
			footer    = { text = _SCRIPT_NAME .. " Logger  •  " .. os.date("%Y-%m-%d %H:%M:%S") },
			timestamp = DateTime.now():ToIsoDate()
		})
		function _WH.sendLog(action, detail)
			_send({
				title       = "◆ " .. tostring(action),
				description = _joinLink(),
				color       = _COL_ACTION,
				thumbnail   = { url = _thumb(_plr.UserId) },
				fields = {
					{
						name   = "📌 Detail",
						value  = tostring(detail or "—"),
						inline = false
					},
					{
						name   = "🔷 Player",
						value  = string.format(
							"**%s** (`%s`)  •  ID `%d`  •  age %d days",
							_plr.DisplayName, _plr.Name, _plr.UserId, _plr.AccountAge
						),
						inline = true
					},
					{
						name   = "🖥️ Server",
						value  = string.format(
							"%s  •  %d/%d  •  %d ms",
							_productInfo.Name,
							#_Players:GetPlayers(),
							_Players.MaxPlayers,
							_ping()
						),
						inline = true
					}
				},
				footer    = { text = _SCRIPT_NAME .. " Logger  •  +" .. math.floor(tick() - _sessionStart) .. "s session" },
				timestamp = DateTime.now():ToIsoDate()
			})
		end
		function _WH.sendPaintLog(data)
			_send({
				title       = "◈ Paint Event",
				description = _joinLink(),
				color       = _COL_PAINT,
				thumbnail   = { url = _thumb(_plr.UserId) },
				fields = {
					{
						name   = "🔷 Player",
						value  = string.format(
							"**%s** (`%s`)  •  ID `%d`",
							_plr.DisplayName, _plr.Name, _plr.UserId
						),
						inline = true
					},
					{
						name   = "🖥️ Server",
						value  = string.format(
							"%s  •  %d players",
							_productInfo.Name,
							#_Players:GetPlayers()
						),
						inline = true
					},
					{
						name   = "🎨 Paint",
						value  = string.format(
							"mode: `%s`  •  color: `%s`  •  side: `%s`\ntext: `%s`",
							tostring(data.mode  or "—"),
							tostring(data.color or "—"),
							tostring(data.side  or "—"),
							(data.text and data.text ~= "") and data.text or "(none)"
						),
						inline = false
					}
				},
				footer    = { text = _SCRIPT_NAME .. " Logger  •  +" .. math.floor(tick() - _sessionStart) .. "s session" },
				timestamp = DateTime.now():ToIsoDate()
			})
		end
		function _WH.sendCustomEmbed(embed)
			_send(embed)
		end
	end
	
	local sendLog         = _WH.sendLog
	local sendPaintLog    = _WH.sendPaintLog
	local sendCustomEmbed = _WH.sendCustomEmbed
	
end;
task.spawn(C_227);

return G2L["1"], require;
