--[=[
 d888b  db    db d888888b      .d888b.      db      db    db  .d8b.  
88' Y8b 88    88   `88'        VP  `8D      88      88    88 d8' `8b 
88      88    88    88            odD'      88      88    88 88ooo88 
88  ooo 88    88    88          .88'        88      88    88 88~~~88 
88. ~8~ 88b  d88   .88.        j88.         88booo. 88b  d88 88   88    @uniquadev
 Y888P  ~Y8888P' Y888888P      888888D      Y88888P ~Y8888P' YP   YP  CONVERTER 
]=]

-- Instances: 18 | Scripts: 6 | Modules: 0 | Tags: 0
local G2L = {};

-- StarterGui.AutoBuildbyagarv
G2L["1"] = Instance.new("ScreenGui", game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui"));
G2L["1"]["Name"] = [[AutoBuildbyagarv]];
G2L["1"]["ZIndexBehavior"] = Enum.ZIndexBehavior.Sibling;
G2L["1"]["ResetOnSpawn"] = false;


-- StarterGui.AutoBuildbyagarv.autobuildmain
G2L["2"] = Instance.new("LocalScript", G2L["1"]);
G2L["2"]["Name"] = [[autobuildmain]];


-- StarterGui.AutoBuildbyagarv.Frame
G2L["3"] = Instance.new("Frame", G2L["1"]);
G2L["3"]["BorderSizePixel"] = 0;
G2L["3"]["BackgroundColor3"] = Color3.fromRGB(132, 132, 132);
G2L["3"]["Size"] = UDim2.new(0, 300, 0, 170);
G2L["3"]["Position"] = UDim2.new(0.41865, 0, 0.38137, 0);
G2L["3"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);


-- StarterGui.AutoBuildbyagarv.Frame.hi
G2L["4"] = Instance.new("Frame", G2L["3"]);
G2L["4"]["BorderSizePixel"] = 0;
G2L["4"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["4"]["Size"] = UDim2.new(1, 0, 1, 0);
G2L["4"]["Position"] = UDim2.new(0, 0, 0, 20);
G2L["4"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["4"]["Name"] = [[hi]];
G2L["4"]["BackgroundTransparency"] = 1;


-- StarterGui.AutoBuildbyagarv.Frame.hi.main
G2L["5"] = Instance.new("Frame", G2L["4"]);
G2L["5"]["BorderSizePixel"] = 0;
G2L["5"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["5"]["Size"] = UDim2.new(1, 0, 0, 50);
G2L["5"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5"]["Name"] = [[main]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.main.build
G2L["6"] = Instance.new("TextButton", G2L["5"]);
G2L["6"]["BorderSizePixel"] = 0;
G2L["6"]["TextSize"] = 14;
G2L["6"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 9);
G2L["6"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["6"]["Size"] = UDim2.new(1, -220, 1, 0);
G2L["6"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6"]["Text"] = [[Start Build]];
G2L["6"]["Name"] = [[build]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.main.build.build
G2L["7"] = Instance.new("LocalScript", G2L["6"]);
G2L["7"]["Name"] = [[build]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.main.import
G2L["8"] = Instance.new("TextButton", G2L["5"]);
G2L["8"]["TextWrapped"] = true;
G2L["8"]["BorderSizePixel"] = 0;
G2L["8"]["TextSize"] = 14;
G2L["8"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["8"]["BackgroundColor3"] = Color3.fromRGB(255, 239, 0);
G2L["8"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["8"]["Size"] = UDim2.new(0.93333, -220, 1, 0);
G2L["8"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["8"]["Text"] = [[Import Build]];
G2L["8"]["Name"] = [[import]];
G2L["8"]["Position"] = UDim2.new(0.96667, -50, 0, 0);


-- StarterGui.AutoBuildbyagarv.Frame.hi.main.import.import
G2L["9"] = Instance.new("LocalScript", G2L["8"]);
G2L["9"]["Name"] = [[import]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.main.string
G2L["a"] = Instance.new("TextButton", G2L["5"]);
G2L["a"]["TextWrapped"] = true;
G2L["a"]["BorderSizePixel"] = 0;
G2L["a"]["TextSize"] = 14;
G2L["a"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a"]["BackgroundColor3"] = Color3.fromRGB(143, 255, 166);
G2L["a"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["a"]["Size"] = UDim2.new(1.26667, -220, 1, 0);
G2L["a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a"]["Text"] = [[Paste json file (click here, no textbox needed)]];
G2L["a"]["Name"] = [[string]];
G2L["a"]["Position"] = UDim2.new(0.26667, 0, 0, 0);


-- StarterGui.AutoBuildbyagarv.Frame.hi.main.string.string
G2L["b"] = Instance.new("LocalScript", G2L["a"]);
G2L["b"]["Name"] = [[string]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.second
G2L["c"] = Instance.new("Frame", G2L["4"]);
G2L["c"]["BorderSizePixel"] = 0;
G2L["c"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["c"]["Size"] = UDim2.new(1, 0, 0, 50);
G2L["c"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["c"]["Name"] = [[second]];
G2L["c"]["LayoutOrder"] = 1;


-- StarterGui.AutoBuildbyagarv.Frame.hi.second.copybuilds
G2L["d"] = Instance.new("TextButton", G2L["c"]);
G2L["d"]["BorderSizePixel"] = 0;
G2L["d"]["TextSize"] = 14;
G2L["d"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["d"]["BackgroundColor3"] = Color3.fromRGB(0, 174, 4);
G2L["d"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["d"]["Size"] = UDim2.new(1.73333, -220, 1, 0);
G2L["d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["d"]["Text"] = [[Copy Server Builds to .json]];
G2L["d"]["Name"] = [[copybuilds]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.second.copybuilds.copybuilds
G2L["e"] = Instance.new("LocalScript", G2L["d"]);
G2L["e"]["Name"] = [[copybuilds]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.third
G2L["f"] = Instance.new("Frame", G2L["4"]);
G2L["f"]["BorderSizePixel"] = 0;
G2L["f"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["f"]["Size"] = UDim2.new(1, 0, -0.14, 50);
G2L["f"]["Position"] = UDim2.new(0, 0, 0.66667, 0);
G2L["f"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["f"]["Name"] = [[third]];
G2L["f"]["LayoutOrder"] = 2;
G2L["f"]["BackgroundTransparency"] = 1;


-- StarterGui.AutoBuildbyagarv.Frame.hi.third.copybuildsYOURS
G2L["10"] = Instance.new("TextButton", G2L["f"]);
G2L["10"]["BorderSizePixel"] = 0;
G2L["10"]["TextSize"] = 14;
G2L["10"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10"]["BackgroundColor3"] = Color3.fromRGB(0, 125, 3);
G2L["10"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["10"]["Size"] = UDim2.new(1, 0, 0, 30);
G2L["10"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10"]["Text"] = [[Copy Your Blocks to .json (PRIV SERVER NOT SUPPORTED)]];
G2L["10"]["Name"] = [[copybuildsYOURS]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.third.copybuildsYOURS.copybuildsYOURS
G2L["11"] = Instance.new("LocalScript", G2L["10"]);
G2L["11"]["Name"] = [[copybuildsYOURS]];


-- StarterGui.AutoBuildbyagarv.Frame.hi.UIListLayout
G2L["12"] = Instance.new("UIListLayout", G2L["4"]);



-- StarterGui.AutoBuildbyagarv.autobuildmain
local function C_2()
local script = G2L["2"];
	local frame = script.Parent.Frame
	
	-- Center the frame
	frame.Position = UDim2.new(0.5, -frame.AbsoluteSize.X / 2, 0.5, -frame.AbsoluteSize.Y / 2)
	frame.AnchorPoint = Vector2.new(0.5, 0.5)
	
	-- Make it draggable
	frame.Draggable = true
	frame.Active = true
end;
task.spawn(C_2);
-- StarterGui.AutoBuildbyagarv.Frame.hi.main.build.build
local function C_7()
local script = G2L["7"];
	-- PUT THIS SCRIPT UNDER THE "build" BUTTON
	local button = script.Parent
	local textbox = script.Parent.Parent.json
	
	local isBuilding = false
	
	button.MouseButton1Click:Connect(function()
		if not isBuilding then
			-- Check if visuals are enabled
			if not _G.visualsEnabled then
				return
			end
	
			-- START BUILD
			local buildString = textbox.Text
	
			if buildString == "" or buildString == "Put your build file..." then
				return
			end
	
			isBuilding = true
			_G.autobuildRunning = true
			button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
			button.Text = "Stop Build"
	
			-- Destroy import visuals AND reset import button state
			local visualFolder = workspace:FindFirstChild("BuildVisuals")
			if visualFolder then
				visualFolder:Destroy()
			end
	
			local handles = game.CoreGui:FindFirstChild("Handles")
			if handles then
				handles:Destroy()
			end
	
			-- Reset import button text AND state
			local importButton = script.Parent.Parent:FindFirstChild("second")
			if importButton then
				importButton.Text = "Import Build"
				-- Reset the import script's internal state by calling its cleanup
				_G.visualsEnabled = false
				_G.forceImportReset = true  -- Signal to import script to reset
			end
	
			-- Set the build string globally
			_G.buildString = buildString
	
			-- Run the autobuild script
			task.spawn(function()
				loadstring(game:HttpGet("https://raw.githubusercontent.com/agarv69/Open-Source-Scripts/refs/heads/main/autobuild.lua"))()
	
				-- Reset after build finishes
				isBuilding = false
				_G.autobuildRunning = false
				button.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
				button.Text = "Start Build"
			end)
		else
			-- STOP BUILD
			_G.stopautobuild = true
			isBuilding = false
			_G.autobuildRunning = false
			button.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
			button.Text = "Start Build"
		end
	end)
end;
task.spawn(C_7);
-- StarterGui.AutoBuildbyagarv.Frame.hi.main.import.import
local function C_9()
local script = G2L["9"];
	-- PUT THIS SCRIPT UNDER THE IMPORT BUTTON
	local button = script.Parent
	local textbox = script.Parent.Parent.json
	
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local RunService = game:GetService("RunService")
	
	-- ============================================================================
	-- PARSE BUILD STRING
	-- ============================================================================
	local function hexToColor(hex)
		local r = tonumber(hex:sub(1, 2), 16) / 255
		local g = tonumber(hex:sub(3, 4), 16) / 255
		local b = tonumber(hex:sub(5, 6), 16) / 255
		return Color3.new(r, g, b)
	end
	
	local SymbolToMaterial = {
		["!"] = Enum.Material.SmoothPlastic,
		["@"] = Enum.Material.Plastic,
		["#"] = Enum.Material.CeramicTiles,
		["$"] = Enum.Material.Brick,
		["%"] = Enum.Material.WoodPlanks,
		["^"] = Enum.Material.Ice,
		["&"] = Enum.Material.Grass,
		["*"] = Enum.Material.Sand,
		["("] = Enum.Material.Snow,
		[")"] = Enum.Material.Glass,
		["-"] = Enum.Material.Wood,
		["_"] = Enum.Material.Slate,
		["="] = Enum.Material.Pebble,
		["+"] = Enum.Material.Marble,
		["["] = Enum.Material.Granite,
		["]"] = Enum.Material.DiamondPlate,
		["{"] = Enum.Material.Metal,
		["}"] = Enum.Material.Asphalt,
		["~"] = Enum.Material.Concrete,
		["`"] = Enum.Material.Pavement,
		["?"] = Enum.Material.Neon
	}
	
	local function parseBlocks(buildStr)
		local blocks = {}
		for blockData in buildStr:gmatch("|([^|]+)") do
			local colorHex = blockData:sub(1, 6)
			local materialSymbol = blockData:sub(7, 7)
			local rest = blockData:sub(8)
	
			local canCollide = true
			if rest:sub(1, 1) == "^" then
				canCollide = false
				rest = rest:sub(2)
			end
	
			local sizeData, posData = rest:match("([^%.]+)%.(.+)")
			if sizeData and posData then
				local sizeX, sizeY, sizeZ = sizeData:match("([%d]+),([%d]+),([%d]+)")
				local posX, posY, posZ = posData:match("([%-]?%d+%.?%d*),([%-]?%d+%.?%d*),([%-]?%d+%.?%d*)")
	
				if sizeX and posX then
					local size = Vector3.new(tonumber(sizeX), tonumber(sizeY), tonumber(sizeZ))
					local centerPos = Vector3.new(tonumber(posX), tonumber(posY), tonumber(posZ))
	
					table.insert(blocks, {
						size = size,
						centerPos = centerPos,
						color = hexToColor(colorHex),
						material = SymbolToMaterial[materialSymbol] or Enum.Material.SmoothPlastic,
						canCollide = canCollide
					})
				end
			end
		end
		return blocks
	end
	
	-- ============================================================================
	-- VISUAL CREATION WITH DRAG HANDLES
	-- ============================================================================
	local visualFolder = nil
	local centerPart = nil
	local handles = nil
	local currentString = ""
	local visualsShowing = false
	local originalButtonText = button.Text
	local originalCenterPos = Vector3.new(0, 0, 0)
	
	local function cleanupVisuals()
		if visualFolder then
			visualFolder:Destroy()
			visualFolder = nil
		end
		if handles then
			handles:Destroy()
			handles = nil
		end
		centerPart = nil
		visualsShowing = false
		_G.visualsEnabled = false
		_G.buildOffset = Vector3.new(0, 0, 0)
		button.Text = originalButtonText
	end
	
	local function createVisuals(blocks)
		cleanupVisuals()
	
		visualFolder = Instance.new("Folder")
		visualFolder.Name = "BuildVisuals"
		visualFolder.Parent = workspace
	
		local totalPos = Vector3.new(0, 0, 0)
		for i, blockData in ipairs(blocks) do
			totalPos = totalPos + blockData.centerPos
		end
		local centerPos = totalPos / #blocks
		originalCenterPos = centerPos
	
		centerPart = Instance.new("Part")
		centerPart.Name = "CenterHandle"
		centerPart.Size = Vector3.new(2, 2, 2)
		centerPart.Position = centerPos
		centerPart.Anchored = true
		centerPart.CanCollide = false
		centerPart.Transparency = 1
		centerPart.Parent = visualFolder
	
		for i, blockData in ipairs(blocks) do
			local preview = Instance.new("Part")
			preview.Name = "Visual_" .. i
			preview.Size = blockData.size
			preview.CFrame = CFrame.new(blockData.centerPos)
			preview.Anchored = true
			preview.CanCollide = false
			preview.Material = blockData.material
			preview.Color = blockData.color
			preview.Transparency = 0.5
			preview.Parent = visualFolder
		end
	
		visualsShowing = true
		_G.visualsEnabled = true
		_G.buildOffset = Vector3.new(0, 0, 0)
		button.Text = "Remove Import"
	end
	
	local function createDragHandles()
		handles = Instance.new("Handles")
		handles.Parent = game.CoreGui
		handles.Style = Enum.HandlesStyle.Movement
		handles.Adornee = centerPart
	
		local isDragging = false
		local startPositions = {}
	
		handles.MouseButton1Down:Connect(function(face)
			isDragging = true
			workspace.CurrentCamera.CameraType = Enum.CameraType.Scriptable
	
			startPositions = {}
			for _, child in pairs(visualFolder:GetChildren()) do
				if child:IsA("BasePart") then
					startPositions[child] = child.Position
				end
			end
		end)
	
		handles.MouseDrag:Connect(function(face, distance)
			if not isDragging then return end
	
			local direction = Vector3.new()
	
			if face == Enum.NormalId.Right then
				direction = Vector3.new(1, 0, 0)
			elseif face == Enum.NormalId.Left then
				direction = Vector3.new(-1, 0, 0)
			elseif face == Enum.NormalId.Top then
				direction = Vector3.new(0, 1, 0)
			elseif face == Enum.NormalId.Bottom then
				direction = Vector3.new(0, -1, 0)
			elseif face == Enum.NormalId.Front then
				direction = Vector3.new(0, 0, -1)
			elseif face == Enum.NormalId.Back then
				direction = Vector3.new(0, 0, 1)
			end
	
			local snappedDistance = math.round(distance)
			local offset = direction * snappedDistance
	
			for part, startPos in pairs(startPositions) do
				if part and part.Parent then
					part.Position = startPos + offset
				end
			end
	
			-- Update build offset
			_G.buildOffset = centerPart.Position - originalCenterPos
		end)
	
		handles.MouseButton1Up:Connect(function()
			isDragging = false
			workspace.CurrentCamera.CameraType = Enum.CameraType.Custom
			startPositions = {}
		end)
	end
	
	textbox:GetPropertyChangedSignal("Text"):Connect(function()
		if textbox.Text ~= currentString and visualsShowing then
			cleanupVisuals()
		end
	end)
	
	-- ============================================================================
	-- BUTTON CLICK
	-- ============================================================================
	button.MouseButton1Click:Connect(function()
		-- Check if autobuild is running
		if _G.autobuildRunning then
			return
		end
	
		-- Check if we need to force reset
		if _G.forceImportReset then
			visualsShowing = false
			currentString = ""
			_G.forceImportReset = false
		end
	
		local buildString = textbox.Text
	
		if buildString == "" or buildString == "Put your build file..." then
			return
		end
	
		if visualsShowing and buildString == currentString then
			cleanupVisuals()
			return
		end
	
		local blocks = parseBlocks(buildString)
	
		if #blocks == 0 then
			return
		end
	
		currentString = buildString
		createVisuals(blocks)
		createDragHandles()
	end)
end;
task.spawn(C_9);
-- StarterGui.AutoBuildbyagarv.Frame.hi.main.string.string
local function C_b()
local script = G2L["b"];
	-- PUT THIS SCRIPT UNDER A TEXTBUTTON
	local button = script.Parent
	
	button.MouseButton1Click:Connect(function()
		local clipboard = getclipboard()
	
		if clipboard and clipboard ~= "" then
			local success, err = pcall(function()
				loadstring(clipboard)()
			end)
	
			if success then
				button.Text = "Executed!"
				task.wait(1)
				button.Text = "Execute Clipboard"
			else
				button.Text = "Failed to execute"
				warn("Error:", err)
				task.wait(1)
				button.Text = "Execute Clipboard"
			end
		else
			button.Text = "Clipboard is empty"
			task.wait(1)
			button.Text = "Execute Clipboard"
		end
	end)
end;
task.spawn(C_b);
-- StarterGui.AutoBuildbyagarv.Frame.hi.second.copybuilds.copybuilds
local function C_e()
local script = G2L["e"];
	-- PUT THIS SCRIPT UNDER THE "copybuilds" BUTTON
	local button = script.Parent
	
	button.MouseButton1Click:Connect(function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/agarv69/UNIVERSAL-SCRIPT-BY-AGARV/refs/heads/main/!%20autobuildclipboard"))()
	
		button.Text = "Copied to clipboard!"
		task.wait(1)
		button.Text = "Copy Server Builds to .json"
	end)
end;
task.spawn(C_e);
-- StarterGui.AutoBuildbyagarv.Frame.hi.third.copybuildsYOURS.copybuildsYOURS
local function C_11()
local script = G2L["11"];
	-- PUT THIS SCRIPT UNDER THE "copybuilds" BUTTON
	local button = script.Parent
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local function hexFromColor(color)
		return string.format("%02X%02X%02X", 
			math.floor(color.R * 255), 
			math.floor(color.G * 255), 
			math.floor(color.B * 255))
	end
	
	local MaterialToSymbol = {
		[Enum.Material.SmoothPlastic] = "!",
		[Enum.Material.Plastic] = "@",
		[Enum.Material.CeramicTiles] = "#",
		[Enum.Material.Brick] = "$",
		[Enum.Material.WoodPlanks] = "%",
		[Enum.Material.Ice] = "^",
		[Enum.Material.Grass] = "&",
		[Enum.Material.Sand] = "*",
		[Enum.Material.Snow] = "(",
		[Enum.Material.Glass] = ")",
		[Enum.Material.Wood] = "-",
		[Enum.Material.Slate] = "_",
		[Enum.Material.Pebble] = "=",
		[Enum.Material.Marble] = "+",
		[Enum.Material.Granite] = "[",
		[Enum.Material.DiamondPlate] = "]",
		[Enum.Material.Metal] = "{",
		[Enum.Material.Asphalt] = "}",
		[Enum.Material.Concrete] = "~",
		[Enum.Material.Pavement] = "`",
		[Enum.Material.Neon] = "?"
	}
	
	button.MouseButton1Click:Connect(function()
		local success = pcall(function()
			local bricksFolder = workspace:FindFirstChild("Bricks")
			if not bricksFolder then error("No bricks") end
	
			local playerFolder = bricksFolder:FindFirstChild(LocalPlayer.Name)
			if not playerFolder then error("No player folder") end
	
			local buildString = ""
	
			for _, brick in pairs(playerFolder:GetChildren()) do
				if brick:IsA("BasePart") and brick.Name == "Brick" then
					local colorHex = hexFromColor(brick.Color)
					local mat = MaterialToSymbol[brick.Material] or "@"
					local collide = brick.CanCollide and "" or "^"
					local size = brick.Size
					local pos = brick.Position
	
					buildString = buildString .. string.format("|%s%s%s%d,%d,%d.%.1f,%.1f,%.1f",
						colorHex, mat, collide,
						math.floor(size.X + 0.5), math.floor(size.Y + 0.5), math.floor(size.Z + 0.5),
						pos.X, pos.Y, pos.Z)
				end
			end
	
			buildString = buildString .. "|"
			setclipboard(buildString)
	
			button.Text = "Copied to clipboard!"
			task.wait(1)
			button.Text = "Copy Your Blocks to .json (PRIV SERVER NOT SUPPORTED)"
		end)
	
		if not success then
			button.Text = "Failed to copy"
			task.wait(1)
			button.Text = "Copy Your Blocks to .json (PRIV SERVER NOT SUPPORTED)"
		end
	end)
end;
task.spawn(C_11);

return G2L["1"], require;
