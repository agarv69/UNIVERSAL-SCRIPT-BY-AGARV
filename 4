--[=[
 d888b  db    db d888888b      .d888b.      db      db    db  .d8b.  
88' Y8b 88    88   `88'        VP  `8D      88      88    88 d8' `8b 
88      88    88    88            odD'      88      88    88 88ooo88 
88  ooo 88    88    88          .88'        88      88    88 88~~~88 
88. ~8~ 88b  d88   .88.        j88.         88booo. 88b  d88 88   88    @uniquadev
 Y888P  ~Y8888P' Y888888P      888888D      Y88888P ~Y8888P' YP   YP  CONVERTER 
]=]

-- Instances: 15 | Scripts: 4 | Modules: 0 | Tags: 0
local G2L = {};

-- StarterGui.ScreenGui
G2L["1"] = Instance.new("ScreenGui", game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui"));
G2L["1"]["ZIndexBehavior"] = Enum.ZIndexBehavior.Sibling;


-- StarterGui.ScreenGui.Frame
G2L["2"] = Instance.new("Frame", G2L["1"]);
G2L["2"]["BorderSizePixel"] = 0;
G2L["2"]["BackgroundColor3"] = Color3.fromRGB(132, 132, 132);
G2L["2"]["Size"] = UDim2.new(0, 300, 0, 150);
G2L["2"]["Position"] = UDim2.new(0.41865, 0, 0.38137, 0);
G2L["2"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);


-- StarterGui.ScreenGui.Frame.UIListLayout
G2L["3"] = Instance.new("UIListLayout", G2L["2"]);



-- StarterGui.ScreenGui.Frame.main
G2L["4"] = Instance.new("Frame", G2L["2"]);
G2L["4"]["BorderSizePixel"] = 0;
G2L["4"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["4"]["Size"] = UDim2.new(1, 0, 0, 50);
G2L["4"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["4"]["Name"] = [[main]];


-- StarterGui.ScreenGui.Frame.main.build
G2L["5"] = Instance.new("TextButton", G2L["4"]);
G2L["5"]["BorderSizePixel"] = 0;
G2L["5"]["TextSize"] = 14;
G2L["5"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5"]["BackgroundColor3"] = Color3.fromRGB(0, 255, 9);
G2L["5"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["5"]["Size"] = UDim2.new(1, -220, 1, 0);
G2L["5"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["5"]["Text"] = [[Start Build]];
G2L["5"]["Name"] = [[build]];


-- StarterGui.ScreenGui.Frame.main.build.build
G2L["6"] = Instance.new("LocalScript", G2L["5"]);
G2L["6"]["Name"] = [[build]];


-- StarterGui.ScreenGui.Frame.main.json
G2L["7"] = Instance.new("TextBox", G2L["4"]);
G2L["7"]["CursorPosition"] = -1;
G2L["7"]["Name"] = [[json]];
G2L["7"]["BorderSizePixel"] = 0;
G2L["7"]["TextWrapped"] = true;
G2L["7"]["TextSize"] = 14;
G2L["7"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["7"]["PlaceholderText"] = [[Put your build file...]];
G2L["7"]["Size"] = UDim2.new(0.8, -80, 1, 0);
G2L["7"]["Position"] = UDim2.new(0, 80, 0, 0);
G2L["7"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7"]["Text"] = [[]];


-- StarterGui.ScreenGui.Frame.main.import
G2L["8"] = Instance.new("TextButton", G2L["4"]);
G2L["8"]["TextWrapped"] = true;
G2L["8"]["BorderSizePixel"] = 0;
G2L["8"]["TextSize"] = 14;
G2L["8"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["8"]["BackgroundColor3"] = Color3.fromRGB(255, 239, 0);
G2L["8"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["8"]["Size"] = UDim2.new(0.93333, -220, 1, 0);
G2L["8"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["8"]["Text"] = [[Import Build]];
G2L["8"]["Name"] = [[import]];
G2L["8"]["Position"] = UDim2.new(0.96667, -50, 0, 0);


-- StarterGui.ScreenGui.Frame.main.import.import
G2L["9"] = Instance.new("LocalScript", G2L["8"]);
G2L["9"]["Name"] = [[import]];


-- StarterGui.ScreenGui.Frame.second
G2L["a"] = Instance.new("Frame", G2L["2"]);
G2L["a"]["BorderSizePixel"] = 0;
G2L["a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a"]["Size"] = UDim2.new(1, 0, 0, 50);
G2L["a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a"]["Name"] = [[second]];


-- StarterGui.ScreenGui.Frame.second.copybuilds
G2L["b"] = Instance.new("TextButton", G2L["a"]);
G2L["b"]["BorderSizePixel"] = 0;
G2L["b"]["TextSize"] = 14;
G2L["b"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b"]["BackgroundColor3"] = Color3.fromRGB(0, 174, 4);
G2L["b"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["b"]["Size"] = UDim2.new(1.73333, -220, 1, 0);
G2L["b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b"]["Text"] = [[Copy Server Builds to Json]];
G2L["b"]["Name"] = [[copybuilds]];


-- StarterGui.ScreenGui.Frame.second.copybuilds.copybuilds
G2L["c"] = Instance.new("LocalScript", G2L["b"]);
G2L["c"]["Name"] = [[copybuilds]];


-- StarterGui.ScreenGui.Frame.third
G2L["d"] = Instance.new("Frame", G2L["2"]);
G2L["d"]["BorderSizePixel"] = 0;
G2L["d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["d"]["Size"] = UDim2.new(1, 0, -0.14, 50);
G2L["d"]["Position"] = UDim2.new(0, 0, 0.66667, 0);
G2L["d"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["d"]["Name"] = [[third]];


-- StarterGui.ScreenGui.Frame.third.copybuildsYOURS
G2L["e"] = Instance.new("TextButton", G2L["d"]);
G2L["e"]["BorderSizePixel"] = 0;
G2L["e"]["TextSize"] = 14;
G2L["e"]["TextColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e"]["BackgroundColor3"] = Color3.fromRGB(0, 125, 3);
G2L["e"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["e"]["Size"] = UDim2.new(1, 0, 0, 30);
G2L["e"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["e"]["Text"] = [[Copy Your Blocks to .json (PRIV SERVER NOT SUPPORTED)]];
G2L["e"]["Name"] = [[copybuildsYOURS]];


-- StarterGui.ScreenGui.Frame.third.copybuildsYOURS.copybuildsYOURS
G2L["f"] = Instance.new("LocalScript", G2L["e"]);
G2L["f"]["Name"] = [[copybuildsYOURS]];


-- StarterGui.ScreenGui.Frame.main.build.build
local function C_6()
local script = G2L["6"];
	-- PUT THIS SCRIPT UNDER THE "build" BUTTON
	local button = script.Parent
	local textbox = script.Parent.Parent.json
	
	local isBuilding = false
	
	button.MouseButton1Click:Connect(function()
		if not isBuilding then
			-- START BUILD
			local buildString = textbox.Text
	
			if buildString == "" or buildString == "Put your build file..." then
				return
			end
	
			isBuilding = true
			button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
			button.Text = "Stop Build"
	
			-- Set the build string globally
			_G.buildString = buildString
	
			-- Run the autobuild script
			task.spawn(function()
				loadstring(game:HttpGet("https://raw.githubusercontent.com/agarv69/Open-Source-Scripts/refs/heads/main/autobuild.lua"))()
			end)
		else
			-- STOP BUILD
			_G.stopautobuild = true
			isBuilding = false
			button.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
			button.Text = "Start Build"
		end
	end)
end;
task.spawn(C_6);
-- StarterGui.ScreenGui.Frame.main.import.import
local function C_9()
local script = G2L["9"];
	-- PUT THIS SCRIPT UNDER A BUTTON
	local button = script.Parent
	local textbox = script.Parent.Parent.json
	
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local RunService = game:GetService("RunService")
	local UserInputService = game:GetService("UserInputService")
	
	-- ============================================================================
	-- PARSE BUILD STRING
	-- ============================================================================
	local function hexToColor(hex)
		local r = tonumber(hex:sub(1, 2), 16) / 255
		local g = tonumber(hex:sub(3, 4), 16) / 255
		local b = tonumber(hex:sub(5, 6), 16) / 255
		return Color3.new(r, g, b)
	end
	
	local SymbolToMaterial = {
		["!"] = Enum.Material.SmoothPlastic,
		["@"] = Enum.Material.Plastic,
		["#"] = Enum.Material.CeramicTiles,
		["$"] = Enum.Material.Brick,
		["%"] = Enum.Material.WoodPlanks,
		["^"] = Enum.Material.Ice,
		["&"] = Enum.Material.Grass,
		["*"] = Enum.Material.Sand,
		["("] = Enum.Material.Snow,
		[")"] = Enum.Material.Glass,
		["-"] = Enum.Material.Wood,
		["_"] = Enum.Material.Slate,
		["="] = Enum.Material.Pebble,
		["+"] = Enum.Material.Marble,
		["["] = Enum.Material.Granite,
		["]"] = Enum.Material.DiamondPlate,
		["{"] = Enum.Material.Metal,
		["}"] = Enum.Material.Asphalt,
		["~"] = Enum.Material.Concrete,
		["`"] = Enum.Material.Pavement,
		["?"] = Enum.Material.Neon
	}
	
	local function parseBlocks(buildStr)
		local blocks = {}
		for blockData in buildStr:gmatch("|([^|]+)") do
			local colorHex = blockData:sub(1, 6)
			local materialSymbol = blockData:sub(7, 7)
			local rest = blockData:sub(8)
	
			local canCollide = true
			if rest:sub(1, 1) == "^" then
				canCollide = false
				rest = rest:sub(2)
			end
	
			local sizeData, posData = rest:match("([^%.]+)%.(.+)")
			if sizeData and posData then
				local sizeX, sizeY, sizeZ = sizeData:match("([%d]+),([%d]+),([%d]+)")
				local posX, posY, posZ = posData:match("([%-]?%d+%.?%d*),([%-]?%d+%.?%d*),([%-]?%d+%.?%d*)")
	
				if sizeX and posX then
					local size = Vector3.new(tonumber(sizeX), tonumber(sizeY), tonumber(sizeZ))
					local centerPos = Vector3.new(tonumber(posX), tonumber(posY), tonumber(posZ))
	
					table.insert(blocks, {
						size = size,
						centerPos = centerPos,
						color = hexToColor(colorHex),
						material = SymbolToMaterial[materialSymbol] or Enum.Material.SmoothPlastic,
						canCollide = canCollide
					})
				end
			end
		end
		return blocks
	end
	
	-- ============================================================================
	-- VISUAL CREATION WITH DRAG HANDLES (IN-GAME)
	-- ============================================================================
	local visualFolder = nil
	local selectedPart = nil
	local arcHandles = nil
	local selectionBox = nil
	
	local function createVisuals(blocks)
		-- Cleanup old visuals
		if visualFolder then
			visualFolder:Destroy()
		end
	
		visualFolder = Instance.new("Folder")
		visualFolder.Name = "BuildVisuals"
		visualFolder.Parent = workspace
	
		for i, blockData in ipairs(blocks) do
			local preview = Instance.new("Part")
			preview.Name = "Visual_" .. i
			preview.Size = blockData.size
			preview.CFrame = CFrame.new(blockData.centerPos)
			preview.Anchored = true
			preview.CanCollide = false
			preview.Material = blockData.material
			preview.Color = blockData.color
			preview.Transparency = 0.5
			preview.Parent = visualFolder
		end
	
		print("Created " .. #blocks .. " visual blocks")
	end
	
	local function createDragHandles()
		-- Create ArcHandles (works in-game)
		arcHandles = Instance.new("ArcHandles")
		arcHandles.Parent = game.CoreGui
		arcHandles.Adornee = nil
	
		-- Create SelectionBox
		selectionBox = Instance.new("SelectionBox")
		selectionBox.LineThickness = 0.05
		selectionBox.Color3 = Color3.fromRGB(0, 255, 0)
		selectionBox.Parent = game.CoreGui
		selectionBox.Adornee = nil
	
		local mouse = LocalPlayer:GetMouse()
		local dragStart = nil
		local startPos = nil
	
		-- Select part on click
		mouse.Button1Down:Connect(function()
			local target = mouse.Target
			if target and target.Parent == visualFolder then
				selectedPart = target
				arcHandles.Adornee = target
				selectionBox.Adornee = target
			else
				selectedPart = nil
				arcHandles.Adornee = nil
				selectionBox.Adornee = nil
			end
		end)
	
		-- Mouse drag for 1 stud snap movement
		arcHandles.MouseButton1Down:Connect(function(axis)
			if selectedPart then
				dragStart = axis
				startPos = selectedPart.Position
			end
		end)
	
		arcHandles.MouseDrag:Connect(function(axis, relativeAngle, deltaRadius)
			if selectedPart and startPos then
				local direction = Vector3.new()
	
				if axis == Enum.Axis.X then
					direction = Vector3.new(1, 0, 0)
				elseif axis == Enum.Axis.Y then
					direction = Vector3.new(0, 1, 0)
				elseif axis == Enum.Axis.Z then
					direction = Vector3.new(0, 0, 1)
				end
	
				-- Calculate movement distance and snap to 1 stud
				local distance = math.deg(relativeAngle) / 15 -- Convert angle to distance approximation
				local snappedDistance = math.round(distance)
	
				selectedPart.Position = startPos + (direction * snappedDistance)
			end
		end)
	
		arcHandles.MouseButton1Up:Connect(function()
			dragStart = nil
			startPos = nil
		end)
	end
	
	-- ============================================================================
	-- BUTTON CLICK
	-- ============================================================================
	button.MouseButton1Click:Connect(function()
		local buildString = textbox.Text
	
		if buildString == "" or buildString == "Put your build file..." then
			warn("No build string!")
			return
		end
	
		local blocks = parseBlocks(buildString)
	
		if #blocks == 0 then
			warn("Failed to parse blocks!")
			return
		end
	
		createVisuals(blocks)
	
		if not arcHandles then
			createDragHandles()
		end
	
		button.Text = "Visuals Created!"
		task.wait(1)
		button.Text = button.Name
	end)
end;
task.spawn(C_9);
-- StarterGui.ScreenGui.Frame.second.copybuilds.copybuilds
local function C_c()
local script = G2L["c"];
	-- PUT THIS SCRIPT UNDER THE "copybuilds" BUTTON
	local button = script.Parent
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local function hexFromColor(color)
		return string.format("%02X%02X%02X", 
			math.floor(color.R * 255), 
			math.floor(color.G * 255), 
			math.floor(color.B * 255))
	end
	
	local MaterialToSymbol = {
		[Enum.Material.SmoothPlastic] = "!",
		[Enum.Material.Plastic] = "@",
		[Enum.Material.CeramicTiles] = "#",
		[Enum.Material.Brick] = "$",
		[Enum.Material.WoodPlanks] = "%",
		[Enum.Material.Ice] = "^",
		[Enum.Material.Grass] = "&",
		[Enum.Material.Sand] = "*",
		[Enum.Material.Snow] = "(",
		[Enum.Material.Glass] = ")",
		[Enum.Material.Wood] = "-",
		[Enum.Material.Slate] = "_",
		[Enum.Material.Pebble] = "=",
		[Enum.Material.Marble] = "+",
		[Enum.Material.Granite] = "[",
		[Enum.Material.DiamondPlate] = "]",
		[Enum.Material.Metal] = "{",
		[Enum.Material.Asphalt] = "}",
		[Enum.Material.Concrete] = "~",
		[Enum.Material.Pavement] = "`",
		[Enum.Material.Neon] = "?"
	}
	
	button.MouseButton1Click:Connect(function()
		local success = pcall(function()
			local bricksFolder = workspace:FindFirstChild("Bricks")
			if not bricksFolder then error("No bricks") end
	
			local buildString = ""
	
			for _, playerFolder in pairs(bricksFolder:GetChildren()) do
				for _, brick in pairs(playerFolder:GetChildren()) do
					if brick:IsA("BasePart") and brick.Name == "Brick" then
						local colorHex = hexFromColor(brick.Color)
						local mat = MaterialToSymbol[brick.Material] or "@"
						local collide = brick.CanCollide and "" or "^"
						local size = brick.Size
						local pos = brick.Position
	
						buildString = buildString .. string.format("|%s%s%s%d,%d,%d.%.1f,%.1f,%.1f",
							colorHex, mat, collide,
							math.floor(size.X + 0.5), math.floor(size.Y + 0.5), math.floor(size.Z + 0.5),
							pos.X, pos.Y, pos.Z)
					end
				end
			end
	
			buildString = buildString .. "|"
			setclipboard(buildString)
	
			button.Text = "Copied to clipboard!"
			task.wait(1)
			button.Text = "Copy Server Builds to .json"
		end)
	
		if not success then
			button.Text = "Failed to copy"
			task.wait(1)
			button.Text = "Copy Server Builds to .json"
		end
	end)
end;
task.spawn(C_c);
-- StarterGui.ScreenGui.Frame.third.copybuildsYOURS.copybuildsYOURS
local function C_f()
local script = G2L["f"];
	-- PUT THIS SCRIPT UNDER THE "copybuilds" BUTTON
	local button = script.Parent
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	
	local function hexFromColor(color)
		return string.format("%02X%02X%02X", 
			math.floor(color.R * 255), 
			math.floor(color.G * 255), 
			math.floor(color.B * 255))
	end
	
	local MaterialToSymbol = {
		[Enum.Material.SmoothPlastic] = "!",
		[Enum.Material.Plastic] = "@",
		[Enum.Material.CeramicTiles] = "#",
		[Enum.Material.Brick] = "$",
		[Enum.Material.WoodPlanks] = "%",
		[Enum.Material.Ice] = "^",
		[Enum.Material.Grass] = "&",
		[Enum.Material.Sand] = "*",
		[Enum.Material.Snow] = "(",
		[Enum.Material.Glass] = ")",
		[Enum.Material.Wood] = "-",
		[Enum.Material.Slate] = "_",
		[Enum.Material.Pebble] = "=",
		[Enum.Material.Marble] = "+",
		[Enum.Material.Granite] = "[",
		[Enum.Material.DiamondPlate] = "]",
		[Enum.Material.Metal] = "{",
		[Enum.Material.Asphalt] = "}",
		[Enum.Material.Concrete] = "~",
		[Enum.Material.Pavement] = "`",
		[Enum.Material.Neon] = "?"
	}
	
	button.MouseButton1Click:Connect(function()
		local success = pcall(function()
			local bricksFolder = workspace:FindFirstChild("Bricks")
			if not bricksFolder then error("No bricks") end
	
			local playerFolder = bricksFolder:FindFirstChild(LocalPlayer.Name)
			if not playerFolder then error("No player folder") end
	
			local buildString = ""
	
			for _, brick in pairs(playerFolder:GetChildren()) do
				if brick:IsA("BasePart") and brick.Name == "Brick" then
					local colorHex = hexFromColor(brick.Color)
					local mat = MaterialToSymbol[brick.Material] or "@"
					local collide = brick.CanCollide and "" or "^"
					local size = brick.Size
					local pos = brick.Position
	
					buildString = buildString .. string.format("|%s%s%s%d,%d,%d.%.1f,%.1f,%.1f",
						colorHex, mat, collide,
						math.floor(size.X + 0.5), math.floor(size.Y + 0.5), math.floor(size.Z + 0.5),
						pos.X, pos.Y, pos.Z)
				end
			end
	
			buildString = buildString .. "|"
			setclipboard(buildString)
	
			button.Text = "Copied to clipboard!"
			task.wait(1)
			button.Text = "Copy Your Blocks to .json (PRIV SERVER NOT SUPPORTED)"
		end)
	
		if not success then
			button.Text = "Failed to copy"
			task.wait(1)
			button.Text = "Copy Your Blocks to .json (PRIV SERVER NOT SUPPORTED)"
		end
	end)
end;
task.spawn(C_f);

return G2L["1"], require;
