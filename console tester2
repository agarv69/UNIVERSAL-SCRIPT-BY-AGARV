local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TextChatService = game:GetService("TextChatService")

local LocalPlayer = Players.LocalPlayer

local isRunning = false
local stopped = false
local jumpConn = nil
local hasSentStop = false

-- ============================================================
-- SYSTEM MESSAGE HELPER
-- ============================================================
local function systemMessage(text)
	local key = "SystemMsg_" .. tostring(text):sub(1, 50)
	local existing = LocalPlayer.PlayerGui:FindFirstChild(key)
	if existing then return end

	local screenGui = Instance.new("ScreenGui")
	screenGui.Name = key
	screenGui.ResetOnSpawn = false
	screenGui.Parent = LocalPlayer.PlayerGui

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, 0, 0, 30)
	label.Position = UDim2.new(0, 0, 0.85, 0)
	label.BackgroundTransparency = 1
	label.Text = "[System]: " .. text
	label.TextColor3 = Color3.fromRGB(255, 255, 0)
	label.TextSize = 16
	label.Font = Enum.Font.GothamSemibold
	label.ZIndex = 999
	label.Parent = screenGui

	task.delay(5, function()
		screenGui:Destroy()
	end)
end

-- ============================================================
-- WIPE VELOCITY — no anchor, no delay, just zeroes
-- Fast so it doesn't cause input lag
-- ============================================================
local function wipeVelocity(Character)
	if not Character then return end
	for _, x in pairs(Character:GetChildren()) do
		if x:IsA("BasePart") then
			x.Velocity = Vector3.new()
			x.RotVelocity = Vector3.new()
			pcall(function() x.AssemblyLinearVelocity = Vector3.new() end)
			pcall(function() x.AssemblyAngularVelocity = Vector3.new() end)
		end
	end
end

-- ============================================================
-- STOP
-- ============================================================
local function stop()
	if stopped then return end
	stopped = true
	isRunning = false
	if jumpConn then
		jumpConn:Disconnect()
		jumpConn = nil
	end
	systemMessage("Stopped")
end

-- ============================================================
-- SEND CHAT (one-shot, no spam)
-- ============================================================
local function sendChat(msg)
	if hasSentStop then return end
	hasSentStop = true
	pcall(function()
		local channels = TextChatService:FindFirstChild("TextChannels")
		if channels then
			local general = channels:FindFirstChild("RBXGeneral")
			if general then
				general:SendAsync(msg)
			end
		end
	end)
end

-- ============================================================
-- FLUNG DETECTION
-- ============================================================
local FLUNG_SPEED = 120

local function isPlayerFlung(TargetPlayer)
	local TChar = TargetPlayer and TargetPlayer.Character
	if not TChar then return true end
	local THum = TChar:FindFirstChildOfClass("Humanoid")
	local TRoot = TChar:FindFirstChild("HumanoidRootPart")
	if THum and THum.Health <= 0 then return true end
	if not TRoot then return true end
	local speed = TRoot.Velocity.Magnitude
	local walkSpeed = THum and THum.WalkSpeed or 16
	if speed > FLUNG_SPEED and speed > walkSpeed * 4 then return true end
	if TRoot.RotVelocity.Magnitude > 25 then return true end
	return false
end

-- ============================================================
-- FLING
-- v7 base — hard teleport each frame directly into target torso,
-- standing upright (no spin angle), halfway into their torso.
-- Returns instantly to OldPos with velocity wipe — no lag.
-- ============================================================
local function flingPlayer(TargetPlayer)
	if not isRunning then return end

	local Character = LocalPlayer.Character
	local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
	local RootPart = Humanoid and Humanoid.RootPart
	if not (Character and Humanoid and RootPart) then return end

	local TCharacter = TargetPlayer.Character
	if not TCharacter then return end

	local THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
	local TRootPart = THumanoid and THumanoid.RootPart
	local THead = TCharacter:FindFirstChild("Head")
	local Accessory = TCharacter:FindFirstChildOfClass("Accessory")
	local Handle = Accessory and Accessory:FindFirstChild("Handle")

	if not TCharacter:FindFirstChildWhichIsA("BasePart") then return end

	-- Save position before moving (only when we're not already flying)
	if RootPart.Velocity.Magnitude < 50 then
		getgenv().OldPos = RootPart.CFrame
	end

	local TargetBase = TRootPart or THead or Handle
	if not TargetBase then return end

	-- Camera
	if THead then
		workspace.CurrentCamera.CameraSubject = THead
	elseif Handle then
		workspace.CurrentCamera.CameraSubject = Handle
	elseif THumanoid then
		workspace.CurrentCamera.CameraSubject = THumanoid
	end

	local savedFPDH = workspace.FallenPartsDestroyHeight
	workspace.FallenPartsDestroyHeight = math.huge

	local BV = Instance.new("BodyVelocity")
	BV.Name = "EpixVel"
	BV.Parent = RootPart
	BV.Velocity = Vector3.new(900000000, 900000000, 900000000)
	BV.MaxForce = Vector3.new(1/0, 1/0, 1/0)

	Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)

	local TimeToWait = 2
	local Time = tick()
	local flingDetected = false

	-- Upright orientations — standing straight, no angle increment.
	-- Cycles through: forward, backward, upside-down fwd, upside-down bwd.
	-- All keep character vertical like a default standing pose.
	-- Offset Y=-0.5 so our torso sits halfway inside theirs, not on their head.
	local orientIndex = 0
	local orientList = {
		CFrame.new(0, -0.5, 0),                                            -- upright, forward
		CFrame.new(0, -0.5, 0) * CFrame.Angles(0, math.rad(180), 0),      -- upright, backward
		CFrame.new(0, -0.5, 0) * CFrame.Angles(math.rad(180), 0, 0),      -- upside-down, forward
		CFrame.new(0, -0.5, 0) * CFrame.Angles(math.rad(180), math.rad(180), 0), -- upside-down, backward
	}

	repeat
		if not isRunning then break end
		if not (RootPart and THumanoid and TargetBase) then break end

		orientIndex = (orientIndex % #orientList) + 1

		-- Pure hard teleport: grab fresh target CFrame every frame, snap to it
		local destCF = TargetBase.CFrame * orientList[orientIndex]
		RootPart.CFrame = destCF
		Character:SetPrimaryPartCFrame(destCF)

		-- Blast fling velocity
		RootPart.Velocity = Vector3.new(90000000, 90000000 * 10, 90000000)
		RootPart.RotVelocity = Vector3.new(900000000, 900000000, 900000000)

		task.wait()

		if isPlayerFlung(TargetPlayer) then
			flingDetected = true
		end

	until flingDetected
		or (TargetBase.Parent ~= TargetPlayer.Character)
		or (TargetPlayer.Parent ~= Players)
		or THumanoid.Sit
		or (Humanoid.Health <= 0)
		or (tick() > (Time + TimeToWait))

	-- Clean up immediately
	BV:Destroy()
	Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
	workspace.CurrentCamera.CameraSubject = Humanoid
	workspace.FallenPartsDestroyHeight = savedFPDH

	-- Return to saved position INSTANTLY with velocity wipe
	-- No anchoring delay so movement input responds right away
	if getgenv().OldPos then
		wipeVelocity(Character)
		RootPart.CFrame = getgenv().OldPos
		Character:SetPrimaryPartCFrame(getgenv().OldPos)
		wipeVelocity(Character)
	end

	-- Auto stop + chat if flung confirmed
	if flingDetected and isRunning then
		stop()
		sendChat(";flung user")
	end
end

-- ============================================================
-- RESPAWN WATCHER
-- ============================================================
local respawnConn = nil

local function watchForRespawn(target)
	if respawnConn then respawnConn:Disconnect(); respawnConn = nil end
	if not target then return end
	respawnConn = target.CharacterAdded:Connect(function()
		if not isRunning then return end
		task.wait(1.5)
		if isRunning then
			hasSentStop = false
			systemMessage("Target respawned - flinging again")
		end
	end)
end

-- ============================================================
-- FIND PLAYER
-- ============================================================
local function findPlayer(name)
	name = name:lower()
	for _, p in pairs(Players:GetPlayers()) do
		if p ~= LocalPlayer then
			if p.Name:lower():sub(1, #name) == name or p.DisplayName:lower():sub(1, #name) == name then
				return p
			end
		end
	end
	return nil
end

local function getRandomTarget()
	local others = {}
	for _, p in pairs(Players:GetPlayers()) do
		if p ~= LocalPlayer then table.insert(others, p) end
	end
	if #others == 0 then return nil end
	return others[math.random(1, #others)]
end

local lockedTarget = nil

-- ============================================================
-- START
-- ============================================================
local function start(target)
	if isRunning then return end
	stopped = false
	isRunning = true
	hasSentStop = false
	lockedTarget = target

	watchForRespawn(target)

	jumpConn = RunService.Heartbeat:Connect(function()
		if not isRunning then return end
		local t = lockedTarget or getRandomTarget()
		if not t or not t.Parent then return end
		task.spawn(function()
			flingPlayer(t)
		end)
	end)
end

-- ============================================================
-- CHAT COMMANDS
-- ;on            = start, random target
-- ;on username   = start, specific target
-- ;stop          = stop
-- ============================================================
local function handleCmd(text)
	text = text:lower()
	if text == ";on" then
		if not isRunning then start(nil) end
	elseif text:sub(1, 4) == ";on " then
		if not isRunning then
			local name = text:sub(5)
			local target = findPlayer(name)
			if target then
				start(target)
			else
				systemMessage("User not found: " .. name .. " - flinging random player")
				start(nil)
			end
		end
	elseif text == ";stop" then
		stop()
	end
end

local ok = pcall(function()
	TextChatService.OnIncomingMessage = function(msg)
		if msg.TextSource and msg.TextSource.UserId == LocalPlayer.UserId then
			handleCmd(msg.Text)
		end
		return nil
	end
end)

if not ok then
	LocalPlayer.Chatted:Connect(function(msg)
		handleCmd(msg)
	end)
end

-- ============================================================
-- ON EXECUTE
-- ============================================================
systemMessage("Executed (11)")
